module code;

review code.CredentialAttributeLogExposure {
  category: secret
  expected: [core.SecretRedaction]
  kind: attention
  text: "review diagnostic logging for credential attributes such as access keys or secret keys"
}
