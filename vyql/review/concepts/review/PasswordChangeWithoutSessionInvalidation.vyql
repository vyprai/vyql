module code;

review code.PasswordChangeWithoutSessionInvalidation {
  category: auth
  kind: attention
  text: "review password-change flows for session destruction, token revocation, or a token-version bump that stops credentials issued before the change from authenticating"
}
