module code;

review code.MiddlewarePathMatchNormalizationMismatch {
  category: access_control
  expected: [core.AuthorizationCheck]
  kind: attention
  text: "review the path a middleware matcher tests against a route pattern so it carries the same duplicate-slash, trailing-slash, case and parameter-delimiter canonicalization the router applies before it dispatches the request"
}
