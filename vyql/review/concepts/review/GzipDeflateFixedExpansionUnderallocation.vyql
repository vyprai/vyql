module code;

review code.GzipDeflateFixedExpansionUnderallocation {
  category: memory
  kind: attention
  text: "review gzip/deflate output buffers sized as input length plus a fixed constant instead of worst-case expansion"
}
