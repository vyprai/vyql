module code;

review code.UnsanitizedFrameDocumentWrite {
  category: xss
  expected: [core.HtmlEscape]
  kind: attention
  text: "review markup that fills a frame's own document, whether written into its contentDocument or assigned to its srcdoc, so the frame carries a sandbox restriction and the markup passes through a sanitizer before it reaches the frame's HTML parser"
}
