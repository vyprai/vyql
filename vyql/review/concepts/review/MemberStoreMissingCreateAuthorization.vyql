module code;

review code.MemberStoreMissingCreateAuthorization {
  category: access_control
  expected: [core.AuthorizationCheck]
  kind: target
  text: "verify member storage endpoints enforce create permissions on the POST store path, not only on the form-rendering route"
}
