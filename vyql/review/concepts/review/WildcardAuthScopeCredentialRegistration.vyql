module code;

review code.WildcardAuthScopeCredentialRegistration {
  category: secret
  expected: [core.SecretRedaction]
  kind: attention
  text: "review the HTTP credential registration for a host-specific auth scope when credentials are configured for one endpoint but registered with AuthScope's wildcard host or port"
}
