module code;

review code.JenkinsCredentialEndpointMissingPostPermission {
  category: auth
  expected: [core.AuthorizationCheck, core.CsrfProtection]
  kind: attention
  text: "review Jenkins descriptor credential creation and remote-test endpoints for @POST plus Overall/Administer permission checks"
}
