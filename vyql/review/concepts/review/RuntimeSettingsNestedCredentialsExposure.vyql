module code;

review code.RuntimeSettingsNestedCredentialsExposure {
  category: secret
  expected: [core.SecretRedaction]
  kind: target
  text: "verify settings responses remove every nested credentials object, not only the first known password field"
}
