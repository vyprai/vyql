module code;

review code.UnauthenticatedCredentialEnrollmentPath {
  category: auth
  expected: [core.AuthenticationCheck]
  kind: attention
  text: "review why the credential-enrollment path is exempt from authentication: an enrollment ceremony binds a new authenticator to an account, so it belongs behind the session the authentication paths establish, not beside them on the allowlist"
}
