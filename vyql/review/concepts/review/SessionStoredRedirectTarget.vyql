module code;

review code.SessionStoredRedirectTarget {
  category: request_forgery
  expected: [core.RedirectAllowlist]
  kind: attention
  text: "review session-stored login redirect targets for relative-URL or same-origin validation before redirecting"
}
