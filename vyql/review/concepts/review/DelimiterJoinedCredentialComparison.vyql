module code;

review code.DelimiterJoinedCredentialComparison {
  category: auth
  expected: [core.AuthenticationCheck, core.InputValidation]
  kind: target
  text: "verify the authentication check compares the fields of a delimited composite individually instead of the whole joined string"
}
