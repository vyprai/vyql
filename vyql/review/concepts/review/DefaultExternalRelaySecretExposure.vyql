module code;

review code.DefaultExternalRelaySecretExposure {
  category: secret
  expected: [core.AllowlistValidation]
  kind: attention
  text: "review default relay/proxy/websocket hosts for product-owned trust boundaries before embedding tokens or upstream URLs"
}
