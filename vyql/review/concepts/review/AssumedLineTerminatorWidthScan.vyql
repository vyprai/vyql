module code;

review code.AssumedLineTerminatorWidthScan {
  category: memory
  expected: [core.InputValidation]
  kind: attention
  text: "measure the delimiter by inspecting the bytes before deriving an offset or scan bound from the received length"
}
