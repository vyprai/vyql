module code;

review code.CredentialHeadersForwardedAcrossRedirect {
  category: secret
  expected: [core.SecretRedaction]
  kind: attention
  text: "review the redirect follow path for removal of Authorization, Cookie, Proxy-Authorization and WWW-Authenticate whenever the redirect target's origin differs from the current one"
}
