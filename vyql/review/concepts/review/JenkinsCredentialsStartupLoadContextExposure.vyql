module code;

review code.JenkinsCredentialsStartupLoadContextExposure {
  category: secret
  expected: [core.AuthorizationCheck]
  kind: attention
  text: "review Jenkins credentials providers for SYSTEM-context startup loading before persisted credential migration"
}
