module code;

review code.RedeployPinnedHostSchedulerBypass {
  category: auth
  expected: [core.AuthorizationCheck]
  kind: attention
  text: "review redeploy dispatch that pins the workload's existing host so the placement scheduler re-admits the replacement image"
}
