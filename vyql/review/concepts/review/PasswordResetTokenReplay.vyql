module code;

review code.PasswordResetTokenReplay {
  category: auth
  expected: [core.AuthenticationCheck]
  kind: attention
  text: "review password reset finalisation so the reset spends a single-use recovery record the server issued and deletes, rather than a token the handler recomputes from a static salt"
}
