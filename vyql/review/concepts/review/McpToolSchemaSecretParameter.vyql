module code;

review code.McpToolSchemaSecretParameter {
  category: secret
  expected: [core.EncryptionAtRest]
  kind: attention
  text: "review MCP tool schemas for key material accepted as a tool argument, and replace it with an opaque handle the server resolves against a secret it already holds, because tool arguments travel through the model context, the MCP transport and the host's logs"
}
