module code;

review code.NuxtPublicRuntimeConfigSecretExposure {
  category: secret
  expected: [core.SecretRedaction]
  kind: target
  text: "keep tokens and credentials out of Nuxt public runtime config; merge them only into private runtimeConfig"
}
