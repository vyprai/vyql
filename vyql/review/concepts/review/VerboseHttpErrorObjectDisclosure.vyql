module code;

review code.VerboseHttpErrorObjectDisclosure {
  category: secret
  expected: [core.SecretRedaction]
  kind: attention
  text: "review HTTP error messages for verbose request, response-body, or TLS object formatting before the error can reach logs or callers"
}
