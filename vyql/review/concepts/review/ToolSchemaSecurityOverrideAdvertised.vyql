module code;

review code.ToolSchemaSecurityOverrideAdvertised {
  category: access_control
  expected: [core.AuthorizationCheck]
  kind: attention
  text: "verify a tool input parameter that turns a security boundary off is not accepted from the model: keep it out of the advertised schema by omitting it, and require a caller-side approval field the schema does not expose"
}
