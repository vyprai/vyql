module code;

review code.RemoteUrlObjectDebugLog {
  category: secret
  expected: [core.SecretRedaction]
  kind: target
  text: "verify remote URL objects and request URIs are not logged before credentials, query strings, and fragments are stripped"
}
