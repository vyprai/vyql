module code;

review code.OverbroadRolePermissionGrant {
  category: auth
  expected: [core.AuthorizationCheck]
  kind: target
  text: "review non-admin or read-only role permission tables for configuration, backup, restore, or other privileged operation grants"
}
