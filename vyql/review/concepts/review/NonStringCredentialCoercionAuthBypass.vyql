module code;

review code.NonStringCredentialCoercionAuthBypass {
  category: auth
  expected: [core.AuthenticationCheck]
  kind: attention
  text: "pin the credential's type where it enters this login handler, since a non-string value reaching a database equality lookup is coerced by the column rather than rejected, and a spurious match here authorises a login that never checks a password"
}
