module code;

review code.OptionalSecretComparisonFailOpen {
  category: auth
  expected: [core.ConfiguredSecretPresenceCheck]
  kind: attention
  text: "review the credential comparison's expected side: it is built from a configuration read that can be unset, so reject before comparing when that configuration is absent rather than comparing against its degenerate value"
}
