module code;

review code.GatedSecretVerificationBypass {
  category: auth
  expected: [core.AuthenticationCheck]
  kind: attention
  text: "review the credential release's verification guard: the secret verification is negated behind another conjunct of the guard, so an input that falsifies that conjunct skips the verification -- make the verification the whole guard or reject the inputs that bypass it"
}
