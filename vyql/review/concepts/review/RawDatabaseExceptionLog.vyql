module code;

review code.RawDatabaseExceptionLog {
  category: secret
  expected: [core.SecretRedaction]
  kind: target
  text: "verify database exception messages and traces are redacted before being written to error logs"
}
