module code;

review code.CommandAllowlistBeforeParsing {
  category: auth
  expected: [core.AuthorizationCheck]
  kind: target
  text: "verify command allowlists and execution permission checks run on the canonical parsed command that is later executed"
}
