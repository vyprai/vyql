module code;

review code.ClosureUnwindStaleLength {
  category: memory
  kind: attention
  text: "review whether a panic in the caller-supplied closure or trait method leaves this buffer with a length claiming unwritten elements; zero the length before the loop, advance it per element, or restore it from a drop guard"
}
