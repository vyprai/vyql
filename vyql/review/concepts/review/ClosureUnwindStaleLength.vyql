module code;

review code.ClosureUnwindStaleLength {
  category: memory
  kind: attention
  text: "review whether a panic in the caller-supplied closure leaves this buffer with a stale length; zero the length before the loop or restore it from a drop guard"
}
