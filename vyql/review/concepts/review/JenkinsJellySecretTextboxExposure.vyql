module code;

review code.JenkinsJellySecretTextboxExposure {
  category: secret
  expected: [core.SecretRedaction]
  kind: target
  text: "render Jenkins secret-like configuration fields with f:password so stored values are masked and round-tripped encrypted"
}
