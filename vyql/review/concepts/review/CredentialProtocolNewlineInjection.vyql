module code;

review code.CredentialProtocolNewlineInjection {
  category: injection
  expected: [core.InputValidation]
  kind: target
  text: "reject embedded newlines before writing line-oriented credential protocol key/value records"
}
