# Rank 112 / CVE-2021-26028: Joomla Archive entry names flow into archive
# extraction file paths without canonical containment.

test "rank112: archive entry name reaches file path access"
  expect VYQL-PATH-001
  graph
  ```
  node src core.UserControlledData
  node sink code.FilePathAccess
  edge FLOWS src -> sink
  ```

