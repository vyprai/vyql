# Rank 552 / CVE-2023-3893: csi-proxy interpolated volume/path values into
# PowerShell command strings before RunPowershellCmd. Agentic prep supplies the
# repo-local exact context marks; shipped rules classify the shared concept as
# both command injection and CWE-20 input validation.

test "rank552: PowerShell command-string wrapper maps to validation and command injection"
  expect VYQL-VAL-005
  expect VYQL-INJ-119
  expect_review code.CommandStringWrapperExecution
  graph
  ```
  node sink code.CommandStringWrapperExecution { loc = api.go:130 }
  ```
