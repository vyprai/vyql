# Rank 13 / CVE-2021-4250: ActiveAttr boolean typecasting used a
# backtracking-prone regular expression on user-controlled input.

test "rank13: boolean typecaster regex match is ReDoS-prone"
  expect VYQL-DOS-002
  graph
  ```
  node src core.UserControlledData
  node sink code.RegexMatchSink
  edge FLOWS src -> sink
  ```

