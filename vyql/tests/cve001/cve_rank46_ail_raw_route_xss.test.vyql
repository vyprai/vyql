# Rank 46 / CVE-2026-39416: AIL returned object item content directly from
# Flask routes as HTML before the fix marked those responses text/plain.

test "rank46: raw route content reaches html rendering"
  expect VYQL-INJ-004
  graph
  ```
  node src core.UserControlledData
  node sink code.HtmlRender
  edge FLOWS src -> sink
  ```

