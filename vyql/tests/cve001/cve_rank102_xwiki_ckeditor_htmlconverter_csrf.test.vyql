# Rank 102 / CVE-2023-22457: XWiki CKEditor HTMLConverter performed a
# state-changing conversion without CSRF protection.

test "rank102: HTMLConverter state-changing operation lacks CSRF guard"
  expect VYQL-RF-003
  graph
  ```
  node src core.UserControlledData
  node sink code.StateChangingOpSink
  edge FLOWS src -> sink
  ```

