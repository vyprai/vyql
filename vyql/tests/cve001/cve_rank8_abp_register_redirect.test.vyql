# Rank 8 / CVE-2025-65581: ABP registration redirected with ReturnUrl without
# the ReturnUrlHash validation before the fix restored the hash argument.

test "rank8: return url reaches redirect target"
  expect VYQL-RF-002
  graph
  ```
  node src core.UserControlledData
  node sink code.RedirectTargetSink
  edge FLOWS src -> sink
  ```

