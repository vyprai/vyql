# Rank 100 / CVE-2025-52477: octo-sts let token-controlled OIDC issuer
# metadata drive provider URL fetching before the validation fix.

test "rank100: OIDC issuer reaches outbound fetch"
  expect VYQL-RF-001
  graph
  ```
  node src core.UserControlledData
  node sink code.UrlFetch
  edge FLOWS src -> sink
  ```

