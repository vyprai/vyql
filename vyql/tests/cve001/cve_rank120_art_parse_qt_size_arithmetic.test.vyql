# Rank 120 / CVE-2026-4731: ART/dcraw parse_qt used attacker-controlled
# QuickTime atom sizes in recursive offsets before the overflow guards.

test "rank120: quicktime atom size reaches size arithmetic"
  expect VYQL-NUM-003
  graph
  ```
  node src core.UserControlledData
  node sink code.IntegerSizeArithmeticSink
  edge FLOWS src -> sink
  ```

