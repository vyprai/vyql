# Rank 4 / CVE-2026-5323: a11y-mcp caller-controlled URL reaches browser
# navigation/fetch behavior.

test "rank4: MCP caller URL reaches outbound fetch"
  expect VYQL-RF-001
  graph
  ```
  node src core.UserControlledData
  node sink code.UrlFetch
  edge FLOWS src -> sink
  ```

