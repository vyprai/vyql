# Rank 5 / CVE-2024-8418: aardvark-dns accepted unbounded TCP connection
# handling without the timeout/resource guard added by the fix.

test "rank5: DNS TCP handler performs unbounded resource operation"
  expect VYQL-DOS-042
  graph
  ```
  node op code.UnboundedResourceOperation
  ```

