# Rank 79 / CVE-2023-0743: Answer avatar upload/sniffing path can render
# attacker-controlled content as active HTML.

test "rank79: avatar content reaches HTML rendering"
  expect VYQL-INJ-004
  graph
  ```
  node src core.UserControlledData
  node sink code.HtmlRender
  edge FLOWS src -> sink
  ```

