# Rank 75 / CVE-2016-8614: Ansible truncated apt-key identifiers to the short
# 8-character form before trusting the received key.

test "rank75: short apt key id reaches trust operation"
  expect VYQL-CRY-005
  graph
  ```
  node src code.ShortSecurityIdentifier
  node sink code.SecurityKeyTrustOperation
  edge FLOWS src -> sink
  ```

