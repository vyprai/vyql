# Rank 11 / CVE-2018-1999028: Jenkins AccuRev validation endpoint used
# configured credentials against caller-supplied connection details without
# the required permission/POST protection.

test "rank11: AccuRev credentialed validation endpoint lacks authorization"
  expect VYQL-AUTH-078
  graph
  ```
  node op code.CredentialedEndpointReview
  ```

