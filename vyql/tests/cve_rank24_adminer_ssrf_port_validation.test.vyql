# Rank 24 / CVE-2018-7667: Adminer validated only that the supplied port was
# numeric before connecting, allowing SSRF to privileged/internal ports.

test "rank24: Adminer server port validation is incomplete"
  expect VYQL-RF-005
  graph
  ```
  node guard code.IncompleteServerPortValidation
  ```

