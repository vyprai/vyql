test "cve rank565 cvat: remote URL downloader needs URL allowlist validation"
  lang python
  expect VYQL-RF-001
  code
  ```
  import requests

  def _download_data(urls, upload_dir):
      local_files = {}
      for url in urls:
          response = requests.get(url, stream=True)
          if response.status_code == 200:
              local_files[url] = True
      return list(local_files.keys())
  ```

test "cve rank565 cvat: URL validation suppresses remote URL downloader SSRF"
  lang python
  reject VYQL-RF-001
  code
  ```
  import requests

  def _validate_url(url):
      return True

  def _download_data(urls, upload_dir):
      local_files = {}
      for url in urls:
          _validate_url(url)
          response = requests.get(url, stream=True)
          if response.status_code == 200:
              local_files[url] = True
      return list(local_files.keys())
  ```
