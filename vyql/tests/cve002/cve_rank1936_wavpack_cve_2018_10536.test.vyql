# Rank 1936 / CVE-2018-10536: Security review for CVE-2018-10536 in dbry/wavpack.
# Fix commit 26cb47f99d in dbry/wavpack.
# ATTENTION, vocabulary/engine gap.

test "rank1936: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1936: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
