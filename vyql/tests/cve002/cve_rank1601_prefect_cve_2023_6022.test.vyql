# Rank 1601 / CVE-2023-6022: Security review for CVE-2023-6022 in prefecthq/prefect.
# Fix commit 227dfcc7e3 in prefecthq/prefect.
# ATTENTION, vocabulary/engine gap.

test "rank1601: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1601: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
