# Rank 1764 / CVE-2023-51662: Security review for CVE-2023-51662 in snowflakedb/snowflake-connector-net.
# Fix commit 49cb77ddd6 in snowflakedb/snowflake-connector-net.
# ATTENTION, vocabulary/engine gap.

test "rank1764: vulnerable code pattern before fix"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        return input;
    }
}
  ```

test "rank1764: validated or patched form"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        if (input == null) return string.Empty;
        return input;
    }
}
  ```
