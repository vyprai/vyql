# Rank 1656 / CVE-2022-4724: Security review for CVE-2022-4724 in ikus060/rdiffweb.
# Fix commit c4a19cf67d in ikus060/rdiffweb.
# ATTENTION, vocabulary/engine gap.

test "rank1656: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1656: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
