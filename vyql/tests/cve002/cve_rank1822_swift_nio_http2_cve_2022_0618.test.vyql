# Rank 1822 / CVE-2022-0618: Security review for CVE-2022-0618 in apple/swift-nio-http2.
# Fix commit ac2a5afce3 in apple/swift-nio-http2.
# ATTENTION, vocabulary/engine gap.

test "rank1822: vulnerable code pattern before fix"
  lang swift
  file main.swift
  reject VYQL-PATH-001
  code
  ```
  func handleRequest(input: String) -> String {
    return input
}
  ```

test "rank1822: validated or patched form"
  lang swift
  file main.swift
  reject VYQL-PATH-001
  code
  ```
  func handleRequest(input: String) -> String {
    if input.isEmpty { return "" }
    return input
}
  ```
