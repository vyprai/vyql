# Rank 1909 / CVE-2020-15230: Security review for CVE-2020-15230 in vapor/vapor.
# Fix commit cf1651f7ff in vapor/vapor.
# ATTENTION, vocabulary/engine gap.

test "rank1909: vulnerable code pattern before fix"
  lang swift
  file main.swift
  reject VYQL-PATH-001
  code
  ```
  func handleRequest(input: String) -> String {
    return input
}
  ```

test "rank1909: validated or patched form"
  lang swift
  file main.swift
  reject VYQL-PATH-001
  code
  ```
  func handleRequest(input: String) -> String {
    if input.isEmpty { return "" }
    return input
}
  ```
