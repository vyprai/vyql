# Rank 1928 / CVE-2026-39918: Security review for CVE-2026-39918 in givanz/vvveb.
# Fix commit 5162c16391 in givanz/vvveb.
# ATTENTION, vocabulary/engine gap.

test "rank1928: vulnerable code pattern before fix"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    return $input;
}
  ```

test "rank1928: validated or patched form"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    if (!$input) return '';
    return $input;
}
  ```
