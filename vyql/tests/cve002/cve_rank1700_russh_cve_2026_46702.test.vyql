# Rank 1700 / CVE-2026-46702: Security review for CVE-2026-46702 in eugeny/russh.
# Fix commit 2692a6f66e in eugeny/russh.
# ATTENTION, vocabulary/engine gap.

test "rank1700: vulnerable code pattern before fix"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    input
}
  ```

test "rank1700: validated or patched form"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    if input.is_empty() {
        return "";
    }
    input
}
  ```
