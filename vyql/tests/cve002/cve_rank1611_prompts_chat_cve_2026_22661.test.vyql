# Rank 1611 / CVE-2026-22661: Security review for CVE-2026-22661 in f/prompts.chat.
# Fix commit 7707781f0f in f/prompts.chat.
# ATTENTION, vocabulary/engine gap.

test "rank1611: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1611: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
