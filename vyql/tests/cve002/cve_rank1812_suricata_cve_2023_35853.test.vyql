# Rank 1812 / CVE-2023-35853: Security review for CVE-2023-35853 in oisf/suricata.
# Fix commit b95bbcc66d in oisf/suricata.
# ATTENTION, vocabulary/engine gap.

test "rank1812: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1812: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
