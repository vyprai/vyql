# Rank 1827 / CVE-2018-21029: Security review for CVE-2018-21029 in systemd/systemd.
# Fix commit b7a4129ca9 in systemd/systemd.
# ATTENTION, vocabulary/engine gap.

test "rank1827: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1827: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
