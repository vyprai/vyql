# Rank 1858 / CVE-2026-41293: Security review for CVE-2026-41293 in apache/tomcat.
# Fix commit 2a2476460e in apache/tomcat.
# ATTENTION, vocabulary/engine gap.

test "rank1858: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1858: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
