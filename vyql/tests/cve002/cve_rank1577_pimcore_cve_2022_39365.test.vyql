# Rank 1577 / CVE-2022-39365: Security review for CVE-2022-39365 in pimcore/pimcore.
# Fix commit 43aa34e018 in pimcore/pimcore.
# ATTENTION, vocabulary/engine gap.

test "rank1577: vulnerable code pattern before fix"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    return $input;
}
  ```

test "rank1577: validated or patched form"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    if (!$input) return '';
    return $input;
}
  ```
