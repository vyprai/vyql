# Rank 1655 / CVE-2018-20177: Security review for CVE-2018-20177 in rdesktop/rdesktop.
# Fix commit 4dca546d04 in rdesktop/rdesktop.
# ATTENTION, vocabulary/engine gap.

test "rank1655: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1655: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
