# Rank 1791 / CVE-2026-54283: Security review for CVE-2026-54283 in kludex/starlette.
# Fix commit 25b8e179d8 in kludex/starlette.
# ATTENTION, vocabulary/engine gap.

test "rank1791: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1791: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
