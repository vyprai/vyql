# Rank 1658 / CVE-2025-55182: Security review for CVE-2025-55182 in facebook/react.
# Fix commit 7dc903cd29 in facebook/react.
# ATTENTION, vocabulary/engine gap.

test "rank1658: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1658: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
