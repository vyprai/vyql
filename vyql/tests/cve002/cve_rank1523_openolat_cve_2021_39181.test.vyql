# Rank 1523 / CVE-2021-39181: Security review for CVE-2021-39181 in openolat/openolat.
# Fix commit 3f219ac457 in openolat/openolat.
# ATTENTION, vocabulary/engine gap.

test "rank1523: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1523: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
