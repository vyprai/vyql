# Rank 1771 / CVE-2021-43805: Security review for CVE-2021-43805 in solidusio/solidus.
# Fix commit 9867153e01 in solidusio/solidus.
# ATTENTION, vocabulary/engine gap.

test "rank1771: vulnerable code pattern before fix"
  lang ruby
  file main.rb
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input)
  input
end
  ```

test "rank1771: validated or patched form"
  lang ruby
  file main.rb
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input)
  return nil unless input
  input
end
  ```
