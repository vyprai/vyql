# Rank 1667 / CVE-2022-1813: Security review for CVE-2022-1813 in yogeshojha/rengine.
# Fix commit 8277cec0f0 in yogeshojha/rengine.
# ATTENTION, vocabulary/engine gap.

test "rank1667: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1667: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
