# Rank 1927 / CVE-2025-53892: Security review for CVE-2025-53892 in intlify/vue-i18n.
# Fix commit a47099619f in intlify/vue-i18n.
# ATTENTION, vocabulary/engine gap.

test "rank1927: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1927: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
