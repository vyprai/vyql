# Rank 1789 / CVE-2025-61600: Security review for CVE-2025-61600 in stalwartlabs/stalwart.
# Fix commit a8e631e881 in stalwartlabs/stalwart.
# ATTENTION, vocabulary/engine gap.

test "rank1789: vulnerable code pattern before fix"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    input
}
  ```

test "rank1789: validated or patched form"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    if input.is_empty() {
        return "";
    }
    input
}
  ```
