# Rank 1806 / CVE-2026-29103: Security review for CVE-2026-29103 in suitecrm/suitecrm.
# Fix commit 7f2b384db8 in suitecrm/suitecrm.
# ATTENTION, vocabulary/engine gap.

test "rank1806: vulnerable code pattern before fix"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    return $input;
}
  ```

test "rank1806: validated or patched form"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    if (!$input) return '';
    return $input;
}
  ```
