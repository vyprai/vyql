# Rank 1516 / CVE-2025-54874: Security review for CVE-2025-54874 in uclouvain/openjpeg.
# Fix commit 44119c2db5 in uclouvain/openjpeg.
# ATTENTION, vocabulary/engine gap.

test "rank1516: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1516: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
