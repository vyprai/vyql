# Rank 1563 / CVE-2025-2945: Security review for CVE-2025-2945 in pgadmin-org/pgadmin4.
# Fix commit 75be0bc22d in pgadmin-org/pgadmin4.
# ATTENTION, vocabulary/engine gap.

test "rank1563: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1563: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
