# Rank 1725 / CVE-2026-23737: Security review for CVE-2026-23737 in lxsmnsyc/seroval.
# Fix commit ce9408ebc8 in lxsmnsyc/seroval.
# ATTENTION, vocabulary/engine gap.

test "rank1725: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1725: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
