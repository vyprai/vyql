# Rank 1644 / CVE-2019-14745: Security review for CVE-2019-14745 in radare/radare2.
# Fix commit 7d30ff52fc in radare/radare2.
# ATTENTION, vocabulary/engine gap.

test "rank1644: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1644: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
