# Rank 1612 / CVE-2026-0994: Security review for CVE-2026-0994 in protocolbuffers/protobuf.
# Fix commit 5ebddcb1bc in protocolbuffers/protobuf.
# ATTENTION, vocabulary/engine gap.

test "rank1612: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1612: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
