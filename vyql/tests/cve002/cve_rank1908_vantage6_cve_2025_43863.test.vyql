# Rank 1908 / CVE-2025-43863: Security review for CVE-2025-43863 in vantage6/vantage6.
# Fix commit e0f1841b31 in vantage6/vantage6.
# ATTENTION, vocabulary/engine gap.

test "rank1908: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1908: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
