# Rank 1558 / CVE-2024-4367: Security review for CVE-2024-4367 in mozilla/pdf.js.
# Fix commit 85e64b5c16 in mozilla/pdf.js.
# ATTENTION, vocabulary/engine gap.

test "rank1558: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1558: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
