# Rank 1559 / CVE-2016-5426: Security review for CVE-2016-5426 in powerdns/pdns.
# Fix commit 881b5b03a5 in powerdns/pdns.
# ATTENTION, vocabulary/engine gap.

test "rank1559: vulnerable code pattern before fix"
  lang cpp
  file main.cpp
  reject VYQL-PATH-001
  code
  ```
  const char* handle_request(const char* input) {
    return input;
}
  ```

test "rank1559: validated or patched form"
  lang cpp
  file main.cpp
  reject VYQL-PATH-001
  code
  ```
  const char* handle_request(const char* input) {
    if (!input) return nullptr;
    return input;
}
  ```
