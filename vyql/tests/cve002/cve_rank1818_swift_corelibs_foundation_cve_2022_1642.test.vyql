# Rank 1818 / CVE-2022-1642: Security review for CVE-2022-1642 in apple/swift-corelibs-foundation.
# Fix commit b541491f73 in apple/swift-corelibs-foundation.
# ATTENTION, vocabulary/engine gap.

test "rank1818: vulnerable code pattern before fix"
  lang swift
  file main.swift
  reject VYQL-PATH-001
  code
  ```
  func handleRequest(input: String) -> String {
    return input
}
  ```

test "rank1818: validated or patched form"
  lang swift
  file main.swift
  reject VYQL-PATH-001
  code
  ```
  func handleRequest(input: String) -> String {
    if input.isEmpty { return "" }
    return input
}
  ```
