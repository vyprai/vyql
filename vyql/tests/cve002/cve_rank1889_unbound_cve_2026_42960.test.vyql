# Rank 1889 / CVE-2026-42960: Security review for CVE-2026-42960 in nlnetlabs/unbound.
# Fix commit 8ae4b4545d in nlnetlabs/unbound.
# ATTENTION, vocabulary/engine gap.

test "rank1889: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1889: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
