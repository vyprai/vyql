# Rank 1932 / CVE-2023-4455: Security review for CVE-2023-4455 in wallabag/wallabag.
# Fix commit ffcc5c9062 in wallabag/wallabag.
# ATTENTION, vocabulary/engine gap.

test "rank1932: vulnerable code pattern before fix"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    return $input;
}
  ```

test "rank1932: validated or patched form"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    if (!$input) return '';
    return $input;
}
  ```
