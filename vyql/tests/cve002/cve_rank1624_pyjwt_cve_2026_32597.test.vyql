# Rank 1624 / CVE-2026-32597: Security review for CVE-2026-32597 in jpadilla/pyjwt.
# Fix commit bd9700cca7 in jpadilla/pyjwt.
# ATTENTION, vocabulary/engine gap.

test "rank1624: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1624: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
