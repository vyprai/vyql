# Rank 1663 / CVE-2025-49844: Security review for CVE-2025-49844 in redis/redis.
# Fix commit d5728cb579 in redis/redis.
# ATTENTION, vocabulary/engine gap.

test "rank1663: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1663: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
