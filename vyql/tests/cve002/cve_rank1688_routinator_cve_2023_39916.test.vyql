# Rank 1688 / CVE-2023-39916: Security review for CVE-2023-39916 in nlnetlabs/routinator.
# Fix commit 0ff795d501 in nlnetlabs/routinator.
# ATTENTION, vocabulary/engine gap.

test "rank1688: vulnerable code pattern before fix"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    input
}
  ```

test "rank1688: validated or patched form"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    if input.is_empty() {
        return "";
    }
    input
}
  ```
