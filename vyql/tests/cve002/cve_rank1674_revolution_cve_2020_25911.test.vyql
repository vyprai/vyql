# Rank 1674 / CVE-2020-25911: Security review for CVE-2020-25911 in modxcms/revolution.
# Fix commit c532dae6cf in modxcms/revolution.
# ATTENTION, vocabulary/engine gap.

test "rank1674: vulnerable code pattern before fix"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    return $input;
}
  ```

test "rank1674: validated or patched form"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    if (!$input) return '';
    return $input;
}
  ```
