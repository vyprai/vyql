# Rank 1684 / CVE-2022-2714: Security review for CVE-2022-2714 in francoisjacquet/rosariosis.
# Fix commit 4022954c3f in francoisjacquet/rosariosis.
# ATTENTION, vocabulary/engine gap.

test "rank1684: vulnerable code pattern before fix"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    return $input;
}
  ```

test "rank1684: validated or patched form"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    if (!$input) return '';
    return $input;
}
  ```
