# Rank 1637 / CVE-2025-1247: Security review for CVE-2025-1247 in quarkusio/quarkus.
# Fix commit d8df15cec1 in quarkusio/quarkus.
# ATTENTION, vocabulary/engine gap.

test "rank1637: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1637: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
