# Rank 1508 / CVE-2026-27960: Security review for CVE-2026-27960 in opencti-platform/opencti.
# Fix commit 5196318667 in opencti-platform/opencti.
# ATTENTION, vocabulary/engine gap.

test "rank1508: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1508: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
