# Rank 1555 / CVE-2026-34747: Security review for CVE-2026-34747 in payloadcms/payload.
# Fix commit fba2438057 in payloadcms/payload.
# ATTENTION, vocabulary/engine gap.

test "rank1555: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1555: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
