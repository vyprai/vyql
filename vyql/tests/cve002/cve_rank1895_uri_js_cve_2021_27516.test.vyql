# Rank 1895 / CVE-2021-27516: Security review for CVE-2021-27516 in medialize/URI.js.
# Fix commit a1ad8bcbc3 in medialize/URI.js.
# ATTENTION, vocabulary/engine gap.

test "rank1895: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1895: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
