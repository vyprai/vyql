# Rank 1547 / CVE-2021-38383: Security review for CVE-2021-38383 in owntone/owntone-server.
# Fix commit 246d8ae0ce in owntone/owntone-server.
# ATTENTION, vocabulary/engine gap.

test "rank1547: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1547: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
