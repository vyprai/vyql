# Rank 1692 / CVE-2023-22493: Security review for CVE-2023-22493 in diygod/rsshub.
# Fix commit a66cbcf6ee in diygod/rsshub.
# ATTENTION, vocabulary/engine gap.

test "rank1692: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1692: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
