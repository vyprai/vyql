# Rank 1817 / CVE-2025-0343: Security review for CVE-2025-0343 in apple/swift-asn1.
# Fix commit ae33e5941b in apple/swift-asn1.
# ATTENTION, vocabulary/engine gap.

test "rank1817: vulnerable code pattern before fix"
  lang swift
  file main.swift
  reject VYQL-PATH-001
  code
  ```
  func handleRequest(input: String) -> String {
    return input
}
  ```

test "rank1817: validated or patched form"
  lang swift
  file main.swift
  reject VYQL-PATH-001
  code
  ```
  func handleRequest(input: String) -> String {
    if input.isEmpty { return "" }
    return input
}
  ```
