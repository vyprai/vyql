# Rank 1660 / CVE-2025-23211: Security review for CVE-2025-23211 in tandoorrecipes/recipes.
# Fix commit e6087d5129 in tandoorrecipes/recipes.
# ATTENTION, vocabulary/engine gap.

test "rank1660: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1660: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
