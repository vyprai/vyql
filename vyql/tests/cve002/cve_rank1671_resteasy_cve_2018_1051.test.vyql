# Rank 1671 / CVE-2018-1051: Security review for CVE-2018-1051 in resteasy/Resteasy.
# Fix commit cb780ddaa2 in resteasy/Resteasy.
# ATTENTION, vocabulary/engine gap.

test "rank1671: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1671: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
