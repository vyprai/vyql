# Rank 1521 / CVE-2021-25931: Security review for CVE-2021-25931 in OpenNMS/opennms.
# Fix commit eb08b5ed4c in OpenNMS/opennms.
# ATTENTION, vocabulary/engine gap.

test "rank1521: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1521: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
