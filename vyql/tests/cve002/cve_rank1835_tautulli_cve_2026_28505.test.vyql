# Rank 1835 / CVE-2026-28505: Security review for CVE-2026-28505 in tautulli/tautulli.
# Fix commit 5610c167a1 in tautulli/tautulli.
# ATTENTION, vocabulary/engine gap.

test "rank1835: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1835: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
