# Rank 1842 / CVE-2020-12274: Security review for CVE-2020-12274 in testlinkopensourcetrms/testlink-code.
# Fix commit 2d17cd00f9 in testlinkopensourcetrms/testlink-code.
# ATTENTION, vocabulary/engine gap.

test "rank1842: vulnerable code pattern before fix"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    return $input;
}
  ```

test "rank1842: validated or patched form"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    if (!$input) return '';
    return $input;
}
  ```
