# Rank 1715 / CVE-2026-43898: Security review for CVE-2026-43898 in nyariv/SandboxJS.
# Fix commit 8268652512 in nyariv/SandboxJS.
# ATTENTION, vocabulary/engine gap.

test "rank1715: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1715: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
