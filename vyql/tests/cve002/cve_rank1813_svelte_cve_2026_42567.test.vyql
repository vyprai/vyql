# Rank 1813 / CVE-2026-42567: Security review for CVE-2026-42567 in sveltejs/svelte.
# Fix commit a16ebc67bb in sveltejs/svelte.
# ATTENTION, vocabulary/engine gap.

test "rank1813: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1813: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
