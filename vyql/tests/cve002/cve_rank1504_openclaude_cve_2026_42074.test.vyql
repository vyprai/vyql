# Rank 1504 / CVE-2026-42074: Security review for CVE-2026-42074 in gitlawb/openclaude.
# Fix commit aab489055c in gitlawb/openclaude.
# ATTENTION, vocabulary/engine gap.

test "rank1504: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1504: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
