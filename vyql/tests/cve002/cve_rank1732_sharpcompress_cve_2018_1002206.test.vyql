# Rank 1732 / CVE-2018-1002206: Security review for CVE-2018-1002206 in adamhathcock/sharpcompress.
# Fix commit 42b1205fb4 in adamhathcock/sharpcompress.
# ATTENTION, vocabulary/engine gap.

test "rank1732: vulnerable code pattern before fix"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        return input;
    }
}
  ```

test "rank1732: validated or patched form"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        if (input == null) return string.Empty;
        return input;
    }
}
  ```
