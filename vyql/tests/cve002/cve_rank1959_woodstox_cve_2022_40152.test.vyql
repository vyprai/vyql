# Rank 1959 / CVE-2022-40152: Security review for CVE-2022-40152 in FasterXML/woodstox.
# Fix commit 7e93907e9c in FasterXML/woodstox.
# ATTENTION, vocabulary/engine gap.

test "rank1959: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1959: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
