# Rank 1535 / CVE-2026-22207: Security review for CVE-2026-22207 in volcengine/openviking.
# Fix commit 9e69113ad0 in volcengine/openviking.
# ATTENTION, vocabulary/engine gap.

test "rank1535: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1535: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
