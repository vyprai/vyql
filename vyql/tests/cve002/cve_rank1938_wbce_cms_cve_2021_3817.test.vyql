# Rank 1938 / CVE-2021-3817: Security review for CVE-2021-3817 in wbce/wbce_cms.
# Fix commit 6ca63f0cad in wbce/wbce_cms.
# ATTENTION, vocabulary/engine gap.

test "rank1938: vulnerable code pattern before fix"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    return $input;
}
  ```

test "rank1938: validated or patched form"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    if (!$input) return '';
    return $input;
}
  ```
