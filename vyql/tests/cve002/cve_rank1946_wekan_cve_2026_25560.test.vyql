# Rank 1946 / CVE-2026-25560: Security review for CVE-2026-25560 in wekan/wekan.
# Fix commit 0b0e16c3ea in wekan/wekan.
# ATTENTION, vocabulary/engine gap.

test "rank1946: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1946: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
