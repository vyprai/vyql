# Rank 1731 / CVE-2026-5760: Security review for CVE-2026-5760 in sgl-project/sglang.
# Fix commit 1e14bd6f36 in sgl-project/sglang.
# ATTENTION, vocabulary/engine gap.

test "rank1731: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1731: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
