# Rank 1929 / CVE-2022-24845: Security review for CVE-2022-24845 in vyperlang/vyper.
# Fix commit 049dbdc647 in vyperlang/vyper.
# ATTENTION, vocabulary/engine gap.

test "rank1929: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1929: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
