# Rank 1683 / CVE-2025-65946: Security review for CVE-2025-65946 in RooCodeInc/Roo-Code.
# Fix commit b50104cc59 in RooCodeInc/Roo-Code.
# ATTENTION, vocabulary/engine gap.

test "rank1683: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1683: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
