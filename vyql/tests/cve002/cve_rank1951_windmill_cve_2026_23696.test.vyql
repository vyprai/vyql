# Rank 1951 / CVE-2026-23696: Security review for CVE-2026-23696 in windmill-labs/windmill.
# Fix commit 942fb62921 in windmill-labs/windmill.
# ATTENTION, vocabulary/engine gap.

test "rank1951: vulnerable code pattern before fix"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    input
}
  ```

test "rank1951: validated or patched form"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    if input.is_empty() {
        return "";
    }
    input
}
  ```
