# Rank 1863 / CVE-2021-41149: Security review for CVE-2021-41149 in awslabs/tough.
# Fix commit 1809b9bd11 in awslabs/tough.
# ATTENTION, vocabulary/engine gap.

test "rank1863: vulnerable code pattern before fix"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    input
}
  ```

test "rank1863: validated or patched form"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    if input.is_empty() {
        return "";
    }
    input
}
  ```
