# Rank 1780 / CVE-2023-20873: Security review for CVE-2023-20873 in spring-projects/spring-boot.
# Fix commit 32444fed4b in spring-projects/spring-boot.
# ATTENTION, vocabulary/engine gap.

test "rank1780: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1780: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
