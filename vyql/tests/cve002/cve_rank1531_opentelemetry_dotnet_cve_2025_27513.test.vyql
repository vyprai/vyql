# Rank 1531 / CVE-2025-27513: Security review for CVE-2025-27513 in open-telemetry/opentelemetry-dotnet.
# Fix commit 1b555c1201 in open-telemetry/opentelemetry-dotnet.
# ATTENTION, vocabulary/engine gap.

test "rank1531: vulnerable code pattern before fix"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        return input;
    }
}
  ```

test "rank1531: validated or patched form"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        if (input == null) return string.Empty;
        return input;
    }
}
  ```
