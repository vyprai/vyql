# Rank 1881 / CVE-2021-27292: Security review for CVE-2021-27292 in faisalman/ua-parser-js.
# Fix commit 809439e20e in faisalman/ua-parser-js.
# ATTENTION, vocabulary/engine gap.

test "rank1881: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1881: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
