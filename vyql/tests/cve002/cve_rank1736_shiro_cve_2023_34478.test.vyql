# Rank 1736 / CVE-2023-34478: Security review for CVE-2023-34478 in apache/shiro.
# Fix commit c3ede3f94e in apache/shiro.
# ATTENTION, vocabulary/engine gap.

test "rank1736: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1736: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
