# Rank 1947 / CVE-2023-46136: Security review for CVE-2023-46136 in pallets/werkzeug.
# Fix commit f3c803b3ad in pallets/werkzeug.
# ATTENTION, vocabulary/engine gap.

test "rank1947: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1947: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
