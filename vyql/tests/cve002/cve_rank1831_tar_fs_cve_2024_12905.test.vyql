# Rank 1831 / CVE-2024-12905: Security review for CVE-2024-12905 in mafintosh/tar-fs.
# Fix commit a1dd7e7c7f in mafintosh/tar-fs.
# ATTENTION, vocabulary/engine gap.

test "rank1831: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1831: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
