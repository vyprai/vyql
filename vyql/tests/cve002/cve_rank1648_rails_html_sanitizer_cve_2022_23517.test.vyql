# Rank 1648 / CVE-2022-23517: Security review for CVE-2022-23517 in rails/rails-html-sanitizer.
# Fix commit 56c61c0ceb in rails/rails-html-sanitizer.
# ATTENTION, vocabulary/engine gap.

test "rank1648: vulnerable code pattern before fix"
  lang ruby
  file main.rb
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input)
  input
end
  ```

test "rank1648: validated or patched form"
  lang ruby
  file main.rb
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input)
  return nil unless input
  input
end
  ```
