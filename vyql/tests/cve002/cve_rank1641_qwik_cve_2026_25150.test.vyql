# Rank 1641 / CVE-2026-25150: Security review for CVE-2026-25150 in qwikdev/qwik.
# Fix commit 5f65bae2bc in qwikdev/qwik.
# ATTENTION, vocabulary/engine gap.

test "rank1641: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1641: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
