# Rank 1954 / CVE-2024-58103: Security review for CVE-2024-58103 in square/wire.
# Fix commit b90e60c09b in square/wire.
# ATTENTION, vocabulary/engine gap.

test "rank1954: vulnerable code pattern before fix"
  lang kotlin
  file Main.kt
  reject VYQL-PATH-001
  code
  ```
  fun handleRequest(input: String): String {
    return input
}
  ```

test "rank1954: validated or patched form"
  lang kotlin
  file Main.kt
  reject VYQL-PATH-001
  code
  ```
  fun handleRequest(input: String): String {
    if (input.isEmpty()) return ""
    return input
}
  ```
