# Rank 1554 / CVE-2026-4926: Security review for CVE-2026-4926 in pillarjs/path-to-regexp.
# Fix commit 22a967901a in pillarjs/path-to-regexp.
# ATTENTION, vocabulary/engine gap.

test "rank1554: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1554: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
