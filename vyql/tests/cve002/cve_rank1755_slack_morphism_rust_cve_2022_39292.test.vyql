# Rank 1755 / CVE-2022-39292: Security review for CVE-2022-39292 in abdolence/slack-morphism-rust.
# Fix commit 48a1da2dc2 in abdolence/slack-morphism-rust.
# ATTENTION, vocabulary/engine gap.

test "rank1755: vulnerable code pattern before fix"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    input
}
  ```

test "rank1755: validated or patched form"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    if input.is_empty() {
        return "";
    }
    input
}
  ```
