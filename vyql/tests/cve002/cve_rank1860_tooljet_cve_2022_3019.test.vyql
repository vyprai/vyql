# Rank 1860 / CVE-2022-3019: Security review for CVE-2022-3019 in tooljet/tooljet.
# Fix commit 45e0d3302d in tooljet/tooljet.
# ATTENTION, vocabulary/engine gap.

test "rank1860: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1860: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
