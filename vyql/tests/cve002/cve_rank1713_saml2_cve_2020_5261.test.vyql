# Rank 1713 / CVE-2020-5261: Security review for CVE-2020-5261 in Sustainsys/Saml2.
# Fix commit e58e0a1aff in Sustainsys/Saml2.
# ATTENTION, vocabulary/engine gap.

test "rank1713: vulnerable code pattern before fix"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        return input;
    }
}
  ```

test "rank1713: validated or patched form"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        if (input == null) return string.Empty;
        return input;
    }
}
  ```
