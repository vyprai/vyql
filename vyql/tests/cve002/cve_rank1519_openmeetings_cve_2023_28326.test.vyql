# Rank 1519 / CVE-2023-28326: Security review for CVE-2023-28326 in apache/openmeetings.
# Fix commit 1fb71af36 in apache/openmeetings.
# ATTENTION, vocabulary/engine gap.

test "rank1519: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1519: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
