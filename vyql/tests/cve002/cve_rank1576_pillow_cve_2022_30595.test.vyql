# Rank 1576 / CVE-2022-30595: Security review for CVE-2022-30595 in python-pillow/pillow.
# Fix commit c846cc881e in python-pillow/pillow.
# ATTENTION, vocabulary/engine gap.

test "rank1576: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1576: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
