# Rank 1980 / CVE-2017-9438: Security review for CVE-2017-9438 in virustotal/yara.
# Fix commit 10e8bd3071 in virustotal/yara.
# ATTENTION, vocabulary/engine gap.

test "rank1980: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1980: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
