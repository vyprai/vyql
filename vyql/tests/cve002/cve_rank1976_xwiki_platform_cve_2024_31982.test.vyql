# Rank 1976 / CVE-2024-31982: Security review for CVE-2024-31982 in xwiki/xwiki-platform.
# Fix commit 3c9e4bb042 in xwiki/xwiki-platform.
# ATTENTION, vocabulary/engine gap.

test "rank1976: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1976: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
