# Rank 1897 / CVE-2018-3774: Security review for CVE-2018-3774 in unshiftio/url-parse.
# Fix commit 53b1794e54 in unshiftio/url-parse.
# ATTENTION, vocabulary/engine gap.

test "rank1897: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1897: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
