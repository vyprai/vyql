# Rank 1809 / CVE-2025-53095: Security review for CVE-2025-53095 in LizardByte/Sunshine.
# Fix commit 738ac93a0e in LizardByte/Sunshine.
# ATTENTION, vocabulary/engine gap.

test "rank1809: vulnerable code pattern before fix"
  lang cpp
  file main.cpp
  reject VYQL-PATH-001
  code
  ```
  const char* handle_request(const char* input) {
    return input;
}
  ```

test "rank1809: validated or patched form"
  lang cpp
  file main.cpp
  reject VYQL-PATH-001
  code
  ```
  const char* handle_request(const char* input) {
    if (!input) return nullptr;
    return input;
}
  ```
