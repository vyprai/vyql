# Rank 1913 / CVE-2020-26296: Security review for CVE-2020-26296 in vega/vega.
# Fix commit eba3eba804 in vega/vega.
# ATTENTION, vocabulary/engine gap.

test "rank1913: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1913: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
