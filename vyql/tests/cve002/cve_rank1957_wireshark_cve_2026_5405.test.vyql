# Rank 1957 / CVE-2026-5405: Security review for CVE-2026-5405 in wireshark/wireshark.
# Fix commit 45eb952b12 in wireshark/wireshark.
# ATTENTION, vocabulary/engine gap.

test "rank1957: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1957: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
