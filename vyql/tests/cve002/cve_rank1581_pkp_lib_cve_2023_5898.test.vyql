# Rank 1581 / CVE-2023-5898: Security review for CVE-2023-5898 in pkp/pkp-lib.
# Fix commit 01feef7a93 in pkp/pkp-lib.
# ATTENTION, vocabulary/engine gap.

test "rank1581: vulnerable code pattern before fix"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    return $input;
}
  ```

test "rank1581: validated or patched form"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    if (!$input) return '';
    return $input;
}
  ```
