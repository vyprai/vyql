# Rank 1975 / CVE-2024-31996: Security review for CVE-2024-31996 in xwiki/xwiki-commons.
# Fix commit b94142e2a6 in xwiki/xwiki-commons.
# ATTENTION, vocabulary/engine gap.

test "rank1975: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1975: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
