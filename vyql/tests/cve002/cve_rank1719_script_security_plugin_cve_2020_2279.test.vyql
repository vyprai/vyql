# Rank 1719 / CVE-2020-2279: Security review for CVE-2020-2279 in jenkinsci/script-security-plugin.
# Fix commit 79d1e9207c in jenkinsci/script-security-plugin.
# ATTENTION, vocabulary/engine gap.

test "rank1719: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1719: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
