# Rank 1922 / CVE-2026-53633: Security review for CVE-2026-53633 in vitest-dev/vitest.
# Fix commit 385a1aefd4 in vitest-dev/vitest.
# ATTENTION, vocabulary/engine gap.

test "rank1922: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1922: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
