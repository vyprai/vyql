# Rank 1682 / CVE-2026-29198: Security review for CVE-2026-29198 in rocketchat/rocket.chat.
# Fix commit f4dcad44d5 in rocketchat/rocket.chat.
# ATTENTION, vocabulary/engine gap.

test "rank1682: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1682: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
