# Rank 1867 / CVE-2026-48491: Security review for CVE-2026-48491 in traefik/traefik.
# Fix commit a664812e9c in traefik/traefik.
# ATTENTION, vocabulary/engine gap.

test "rank1867: vulnerable code pattern before fix"
  lang go
  file main.go
  reject VYQL-PATH-001
  code
  ```
  package main

func HandleRequest(input string) string {
	return input
}
  ```

test "rank1867: validated or patched form"
  lang go
  file main.go
  reject VYQL-PATH-001
  code
  ```
  package main

func HandleRequest(input string) string {
	if input == "" {
		return ""
	}
	return input
}
  ```
