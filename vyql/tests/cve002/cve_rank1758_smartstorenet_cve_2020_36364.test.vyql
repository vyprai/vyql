# Rank 1758 / CVE-2020-36364: Security review for CVE-2020-36364 in smartstore/smartstorenet.
# Fix commit b1f535a040 in smartstore/smartstorenet.
# ATTENTION, vocabulary/engine gap.

test "rank1758: vulnerable code pattern before fix"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        return input;
    }
}
  ```

test "rank1758: validated or patched form"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        if (input == null) return string.Empty;
        return input;
    }
}
  ```
