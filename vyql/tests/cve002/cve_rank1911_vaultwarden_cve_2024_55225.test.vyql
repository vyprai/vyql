# Rank 1911 / CVE-2024-55225: Security review for CVE-2024-55225 in dani-garcia/vaultwarden.
# Fix commit 37c14c3c69 in dani-garcia/vaultwarden.
# ATTENTION, vocabulary/engine gap.

test "rank1911: vulnerable code pattern before fix"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    input
}
  ```

test "rank1911: validated or patched form"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    if input.is_empty() {
        return "";
    }
    input
}
  ```
