# Rank 1862 / CVE-2026-49853: Security review for CVE-2026-49853 in tornadoweb/tornado.
# Fix commit e8fc7edb23 in tornadoweb/tornado.
# ATTENTION, vocabulary/engine gap.

test "rank1862: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1862: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
