# Rank 1567 / CVE-2019-16993: Security review for CVE-2019-16993 in phpbb/phpbb.
# Fix commit 18abef716e in phpbb/phpbb.
# ATTENTION, vocabulary/engine gap.

test "rank1567: vulnerable code pattern before fix"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    return $input;
}
  ```

test "rank1567: validated or patched form"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    if (!$input) return '';
    return $input;
}
  ```
