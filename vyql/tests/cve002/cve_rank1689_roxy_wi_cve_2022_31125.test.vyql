# Rank 1689 / CVE-2022-31125: Security review for CVE-2022-31125 in hap-wi/roxy-wi.
# Fix commit 2e060eb801 in hap-wi/roxy-wi.
# ATTENTION, vocabulary/engine gap.

test "rank1689: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1689: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
