# Rank 1629 / CVE-2026-24486: Security review for CVE-2026-24486 in kludex/python-multipart.
# Fix commit 9433f4bbc9 in kludex/python-multipart.
# ATTENTION, vocabulary/engine gap.

test "rank1629: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1629: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
