# Rank 1894 / CVE-2025-61594: Security review for CVE-2025-61594 in ruby/uri.
# Fix commit d3116ca66a in ruby/uri.
# ATTENTION, vocabulary/engine gap.

test "rank1894: vulnerable code pattern before fix"
  lang ruby
  file main.rb
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input)
  input
end
  ```

test "rank1894: validated or patched form"
  lang ruby
  file main.rb
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input)
  return nil unless input
  input
end
  ```
