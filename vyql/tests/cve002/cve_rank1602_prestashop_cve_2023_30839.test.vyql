# Rank 1602 / CVE-2023-30839: Security review for CVE-2023-30839 in PrestaShop/PrestaShop.
# Fix commit d1d27dc371 in PrestaShop/PrestaShop.
# ATTENTION, vocabulary/engine gap.

test "rank1602: vulnerable code pattern before fix"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    return $input;
}
  ```

test "rank1602: validated or patched form"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    if (!$input) return '';
    return $input;
}
  ```
