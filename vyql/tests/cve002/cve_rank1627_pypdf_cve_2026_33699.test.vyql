# Rank 1627 / CVE-2026-33699: Security review for CVE-2026-33699 in py-pdf/pypdf.
# Fix commit da867f4961 in py-pdf/pypdf.
# ATTENTION, vocabulary/engine gap.

test "rank1627: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1627: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
