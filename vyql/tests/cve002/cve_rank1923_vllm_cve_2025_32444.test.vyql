# Rank 1923 / CVE-2025-32444: Security review for CVE-2025-32444 in vllm-project/vllm.
# Fix commit a5450f11c9 in vllm-project/vllm.
# ATTENTION, vocabulary/engine gap.

test "rank1923: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1923: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
