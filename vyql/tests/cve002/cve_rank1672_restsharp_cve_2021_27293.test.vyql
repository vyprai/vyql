# Rank 1672 / CVE-2021-27293: Security review for CVE-2021-27293 in restsharp/RestSharp.
# Fix commit be39346784 in restsharp/RestSharp.
# ATTENTION, vocabulary/engine gap.

test "rank1672: vulnerable code pattern before fix"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        return input;
    }
}
  ```

test "rank1672: validated or patched form"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        if (input == null) return string.Empty;
        return input;
    }
}
  ```
