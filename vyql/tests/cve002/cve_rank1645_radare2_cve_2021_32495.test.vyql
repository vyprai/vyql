# Rank 1645 / CVE-2021-32495: Security review for CVE-2021-32495 in radareorg/radare2.
# Fix commit 5e16e2d1c9 in radareorg/radare2.
# ATTENTION, vocabulary/engine gap.

test "rank1645: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1645: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
