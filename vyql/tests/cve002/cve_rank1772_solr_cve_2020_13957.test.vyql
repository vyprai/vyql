# Rank 1772 / CVE-2020-13957: Security review for CVE-2020-13957 in apache/solr.
# Fix commit e001c22218 in apache/solr.
# ATTENTION, vocabulary/engine gap.

test "rank1772: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1772: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
