# Rank 1608 / CVE-2017-1000481: Security review for CVE-2017-1000481 in plone/Products.CMFPlone.
# Fix commit 236b62b756 in plone/Products.CMFPlone.
# ATTENTION, vocabulary/engine gap.

test "rank1608: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1608: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
