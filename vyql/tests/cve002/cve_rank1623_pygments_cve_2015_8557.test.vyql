# Rank 1623 / CVE-2015-8557: Security review for CVE-2015-8557 in pygments/pygments.
# Fix commit db6dd826f8 in pygments/pygments.
# ATTENTION, vocabulary/engine gap.

test "rank1623: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1623: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
