# Rank 1799 / CVE-2026-35523: Security review for CVE-2026-35523 in strawberry-graphql/strawberry.
# Fix commit 0977a4e6b4 in strawberry-graphql/strawberry.
# ATTENTION, vocabulary/engine gap.

test "rank1799: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1799: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
