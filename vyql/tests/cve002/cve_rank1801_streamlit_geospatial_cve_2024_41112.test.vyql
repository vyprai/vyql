# Rank 1801 / CVE-2024-41112: Security review for CVE-2024-41112 in opengeos/streamlit-geospatial.
# Fix commit c4f81d9616 in opengeos/streamlit-geospatial.
# ATTENTION, vocabulary/engine gap.

test "rank1801: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1801: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
