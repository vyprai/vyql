# Rank 1854 / CVE-2026-47759: Security review for CVE-2026-47759 in tinymce/tinymce.
# Fix commit 9dac5bb3c8 in tinymce/tinymce.
# ATTENTION, vocabulary/engine gap.

test "rank1854: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1854: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
