# Rank 1552 / CVE-2022-2216: Security review for CVE-2022-2216 in ionicabizau/parse-url.
# Fix commit 21c72ab941 in ionicabizau/parse-url.
# ATTENTION, vocabulary/engine gap.

test "rank1552: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1552: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
