# Rank 1659 / CVE-2025-43865: Security review for CVE-2025-43865 in remix-run/react-router.
# Fix commit c84302972a in remix-run/react-router.
# ATTENTION, vocabulary/engine gap.

test "rank1659: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1659: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
