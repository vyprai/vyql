# Rank 1906 / CVE-2026-42555: Security review for CVE-2026-42555 in valtimo-platform/valtimo.
# Fix commit f9d56f0798 in valtimo-platform/valtimo.
# ATTENTION, vocabulary/engine gap.

test "rank1906: vulnerable code pattern before fix"
  lang kotlin
  file Main.kt
  reject VYQL-PATH-001
  code
  ```
  fun handleRequest(input: String): String {
    return input
}
  ```

test "rank1906: validated or patched form"
  lang kotlin
  file Main.kt
  reject VYQL-PATH-001
  code
  ```
  fun handleRequest(input: String): String {
    if (input.isEmpty()) return ""
    return input
}
  ```
