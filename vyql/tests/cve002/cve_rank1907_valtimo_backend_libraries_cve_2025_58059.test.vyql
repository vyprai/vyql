# Rank 1907 / CVE-2025-58059: Security review for CVE-2025-58059 in valtimo-platform/valtimo-backend-libraries.
# Fix commit 3c3335f1ac in valtimo-platform/valtimo-backend-libraries.
# ATTENTION, vocabulary/engine gap.

test "rank1907: vulnerable code pattern before fix"
  lang kotlin
  file Main.kt
  reject VYQL-PATH-001
  code
  ```
  fun handleRequest(input: String): String {
    return input
}
  ```

test "rank1907: validated or patched form"
  lang kotlin
  file Main.kt
  reject VYQL-PATH-001
  code
  ```
  fun handleRequest(input: String): String {
    if (input.isEmpty()) return ""
    return input
}
  ```
