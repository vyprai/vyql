# Rank 1748 / CVE-2025-15099: Security review for CVE-2025-15099 in simstudioai/sim.
# Fix commit e359dc2946 in simstudioai/sim.
# ATTENTION, vocabulary/engine gap.

test "rank1748: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1748: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
