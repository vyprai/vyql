# Rank 1687 / CVE-2024-32971: Security review for CVE-2024-32971 in apollographql/router.
# Fix commit ff9f666598 in apollographql/router.
# ATTENTION, vocabulary/engine gap.

test "rank1687: vulnerable code pattern before fix"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    input
}
  ```

test "rank1687: validated or patched form"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    if input.is_empty() {
        return "";
    }
    input
}
  ```
