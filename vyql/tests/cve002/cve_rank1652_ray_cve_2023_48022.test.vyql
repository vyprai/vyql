# Rank 1652 / CVE-2023-48022: Security review for CVE-2023-48022 in ray-project/ray.
# Fix commit 978947083b in ray-project/ray.
# ATTENTION, vocabulary/engine gap.

test "rank1652: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1652: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
