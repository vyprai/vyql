# Rank 1551 / CVE-2024-27298: Security review for CVE-2024-27298 in parse-community/parse-server.
# Fix commit cbefe770a7 in parse-community/parse-server.
# ATTENTION, vocabulary/engine gap.

test "rank1551: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1551: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
