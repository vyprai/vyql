# Rank 1509 / CVE-2025-53644: Security review for CVE-2025-53644 in opencv/opencv.
# Fix commit a39db41390 in opencv/opencv.
# ATTENTION, vocabulary/engine gap.

test "rank1509: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1509: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
