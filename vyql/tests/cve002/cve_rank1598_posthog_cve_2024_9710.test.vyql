# Rank 1598 / CVE-2024-9710: Security review for CVE-2024-9710 in PostHog/posthog.
# Fix commit 3732c0fd95 in PostHog/posthog.
# ATTENTION, vocabulary/engine gap.

test "rank1598: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1598: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
