# Rank 1919 / CVE-2022-3520: Security review for CVE-2022-3520 in vim/vim.
# Fix commit 36343ae0fb in vim/vim.
# ATTENTION, vocabulary/engine gap.

test "rank1919: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1919: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
