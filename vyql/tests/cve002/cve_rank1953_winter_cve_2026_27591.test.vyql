# Rank 1953 / CVE-2026-27591: Security review for CVE-2026-27591 in wintercms/winter.
# Fix commit b6ac66c5e8 in wintercms/winter.
# ATTENTION, vocabulary/engine gap.

test "rank1953: vulnerable code pattern before fix"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    return $input;
}
  ```

test "rank1953: validated or patched form"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    if (!$input) return '';
    return $input;
}
  ```
