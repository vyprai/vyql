# Rank 1541 / CVE-2022-0821: Security review for CVE-2022-0821 in orchardcms/orchardcore.
# Fix commit b7096af102 in orchardcms/orchardcore.
# ATTENTION, vocabulary/engine gap.

test "rank1541: vulnerable code pattern before fix"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        return input;
    }
}
  ```

test "rank1541: validated or patched form"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        if (input == null) return string.Empty;
        return input;
    }
}
  ```
