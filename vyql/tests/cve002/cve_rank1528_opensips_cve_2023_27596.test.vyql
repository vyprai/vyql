# Rank 1528 / CVE-2023-27596: Security review for CVE-2023-27596 in OpenSIPS/opensips.
# Fix commit dd051f8ed5 in OpenSIPS/opensips.
# ATTENTION, vocabulary/engine gap.

test "rank1528: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1528: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
