# Rank 1768 / CVE-2026-33151: Security review for CVE-2026-33151 in socketio/socket.io.
# Fix commit 719f9ebab0 in socketio/socket.io.
# ATTENTION, vocabulary/engine gap.

test "rank1768: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1768: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
