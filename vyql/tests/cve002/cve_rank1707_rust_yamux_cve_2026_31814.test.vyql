# Rank 1707 / CVE-2026-31814: Security review for CVE-2026-31814 in libp2p/rust-yamux.
# Fix commit b1aae09d60 in libp2p/rust-yamux.
# ATTENTION, vocabulary/engine gap.

test "rank1707: vulnerable code pattern before fix"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    input
}
  ```

test "rank1707: validated or patched form"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    if input.is_empty() {
        return "";
    }
    input
}
  ```
