# Rank 1586 / CVE-2022-31018: Security review for CVE-2022-31018 in playframework/playframework.
# Fix commit f6722f1e57 in playframework/playframework.
# ATTENTION, vocabulary/engine gap.

test "rank1586: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1586: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
