# Rank 1675 / CVE-2024-49761: Security review for CVE-2024-49761 in ruby/rexml.
# Fix commit ce59f2eb1a in ruby/rexml.
# ATTENTION, vocabulary/engine gap.

test "rank1675: vulnerable code pattern before fix"
  lang ruby
  file main.rb
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input)
  input
end
  ```

test "rank1675: validated or patched form"
  lang ruby
  file main.rb
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input)
  return nil unless input
  input
end
  ```
