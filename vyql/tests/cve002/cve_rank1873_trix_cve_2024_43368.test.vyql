# Rank 1873 / CVE-2024-43368: Security review for CVE-2024-43368 in basecamp/trix.
# Fix commit 7656f578af in basecamp/trix.
# ATTENTION, vocabulary/engine gap.

test "rank1873: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1873: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
