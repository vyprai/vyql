# Rank 1997 / CVE-2021-32674: Security review for CVE-2021-32674 in zopefoundation/Zope.
# Fix commit 1d89791013 in zopefoundation/Zope.
# ATTENTION, vocabulary/engine gap.

test "rank1997: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1997: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
