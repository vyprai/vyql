# Rank 1892 / CVE-2023-36821: Security review for CVE-2023-36821 in louislam/uptime-kuma.
# Fix commit a0736e04b2 in louislam/uptime-kuma.
# ATTENTION, vocabulary/engine gap.

test "rank1892: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1892: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
