# Rank 1767 / CVE-2022-24441: Security review for CVE-2022-24441 in snyk/snyk-intellij-plugin.
# Fix commit 56682f4ba6 in snyk/snyk-intellij-plugin.
# ATTENTION, vocabulary/engine gap.

test "rank1767: vulnerable code pattern before fix"
  lang kotlin
  file Main.kt
  reject VYQL-PATH-001
  code
  ```
  fun handleRequest(input: String): String {
    return input
}
  ```

test "rank1767: validated or patched form"
  lang kotlin
  file Main.kt
  reject VYQL-PATH-001
  code
  ```
  fun handleRequest(input: String): String {
    if (input.isEmpty()) return ""
    return input
}
  ```
