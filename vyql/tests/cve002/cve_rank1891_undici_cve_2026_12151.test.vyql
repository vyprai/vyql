# Rank 1891 / CVE-2026-12151: Security review for CVE-2026-12151 in nodejs/undici.
# Fix commit a027a4a04c in nodejs/undici.
# ATTENTION, vocabulary/engine gap.

test "rank1891: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1891: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
