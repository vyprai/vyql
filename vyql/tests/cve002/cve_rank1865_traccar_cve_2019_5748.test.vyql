# Rank 1865 / CVE-2019-5748: Security review for CVE-2019-5748 in traccar/traccar.
# Fix commit d7f6c53fd8 in traccar/traccar.
# ATTENTION, vocabulary/engine gap.

test "rank1865: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1865: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
