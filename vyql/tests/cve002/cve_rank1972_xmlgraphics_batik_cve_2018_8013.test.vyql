# Rank 1972 / CVE-2018-8013: Security review for CVE-2018-8013 in apache/xmlgraphics-batik.
# Fix commit f91125b26a in apache/xmlgraphics-batik.
# ATTENTION, vocabulary/engine gap.

test "rank1972: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1972: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
