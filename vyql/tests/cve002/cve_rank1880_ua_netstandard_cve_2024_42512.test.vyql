# Rank 1880 / CVE-2024-42512: Security review for CVE-2024-42512 in OPCFoundation/UA-.NETStandard.
# Fix commit 3543d02925 in OPCFoundation/UA-.NETStandard.
# ATTENTION, vocabulary/engine gap.

test "rank1880: vulnerable code pattern before fix"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        return input;
    }
}
  ```

test "rank1880: validated or patched form"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        if (input == null) return string.Empty;
        return input;
    }
}
  ```
