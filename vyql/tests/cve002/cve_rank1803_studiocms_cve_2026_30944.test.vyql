# Rank 1803 / CVE-2026-30944: Security review for CVE-2026-30944 in withstudiocms/studiocms.
# Fix commit 9eec9c3b45 in withstudiocms/studiocms.
# ATTENTION, vocabulary/engine gap.

test "rank1803: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1803: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
