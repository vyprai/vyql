# Rank 1985 / CVE-2026-26331: Security review for CVE-2026-26331 in yt-dlp/yt-dlp.
# Fix commit 1fbbe29b99 in yt-dlp/yt-dlp.
# ATTENTION, vocabulary/engine gap.

test "rank1985: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1985: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
