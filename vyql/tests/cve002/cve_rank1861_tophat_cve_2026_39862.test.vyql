# Rank 1861 / CVE-2026-39862: Security review for CVE-2026-39862 in shopify/tophat.
# Fix commit 60f7765942 in shopify/tophat.
# ATTENTION, vocabulary/engine gap.

test "rank1861: vulnerable code pattern before fix"
  lang swift
  file main.swift
  reject VYQL-PATH-001
  code
  ```
  func handleRequest(input: String) -> String {
    return input
}
  ```

test "rank1861: validated or patched form"
  lang swift
  file main.swift
  reject VYQL-PATH-001
  code
  ```
  func handleRequest(input: String) -> String {
    if input.isEmpty { return "" }
    return input
}
  ```
