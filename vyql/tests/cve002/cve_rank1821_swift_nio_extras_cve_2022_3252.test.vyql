# Rank 1821 / CVE-2022-3252: Security review for CVE-2022-3252 in apple/swift-nio-extras.
# Fix commit e8000754be in apple/swift-nio-extras.
# ATTENTION, vocabulary/engine gap.

test "rank1821: vulnerable code pattern before fix"
  lang swift
  file main.swift
  reject VYQL-PATH-001
  code
  ```
  func handleRequest(input: String) -> String {
    return input
}
  ```

test "rank1821: validated or patched form"
  lang swift
  file main.swift
  reject VYQL-PATH-001
  code
  ```
  func handleRequest(input: String) -> String {
    if input.isEmpty { return "" }
    return input
}
  ```
