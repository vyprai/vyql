# Rank 1616 / CVE-2023-0299: Security review for CVE-2023-0299 in publify/publify_core.
# Fix commit 34f6e9c98e in publify/publify_core.
# ATTENTION, vocabulary/engine gap.

test "rank1616: vulnerable code pattern before fix"
  lang ruby
  file main.rb
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input)
  input
end
  ```

test "rank1616: validated or patched form"
  lang ruby
  file main.rb
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input)
  return nil unless input
  input
end
  ```
