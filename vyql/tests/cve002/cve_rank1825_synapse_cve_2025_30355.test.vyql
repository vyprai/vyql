# Rank 1825 / CVE-2025-30355: Security review for CVE-2025-30355 in element-hq/synapse.
# Fix commit 2277df2a1e in element-hq/synapse.
# ATTENTION, vocabulary/engine gap.

test "rank1825: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1825: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
