# Rank 1948 / CVE-2022-2650: Security review for CVE-2022-2650 in wger-project/wger.
# Fix commit 5e3167e3a2 in wger-project/wger.
# ATTENTION, vocabulary/engine gap.

test "rank1948: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1948: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
