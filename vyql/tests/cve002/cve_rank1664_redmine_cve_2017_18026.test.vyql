# Rank 1664 / CVE-2017-18026: Security review for CVE-2017-18026 in redmine/redmine.
# Fix commit 9d797400ea in redmine/redmine.
# ATTENTION, vocabulary/engine gap.

test "rank1664: vulnerable code pattern before fix"
  lang ruby
  file main.rb
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input)
  input
end
  ```

test "rank1664: validated or patched form"
  lang ruby
  file main.rb
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input)
  return nil unless input
  input
end
  ```
