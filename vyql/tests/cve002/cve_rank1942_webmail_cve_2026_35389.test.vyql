# Rank 1942 / CVE-2026-35389: Security review for CVE-2026-35389 in bulwarkmail/webmail.
# Fix commit 7102add194 in bulwarkmail/webmail.
# ATTENTION, vocabulary/engine gap.

test "rank1942: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1942: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
