# Rank 1990 / CVE-2024-2083: Security review for CVE-2024-2083 in zenml-io/zenml.
# Fix commit 00e934f33a in zenml-io/zenml.
# ATTENTION, vocabulary/engine gap.

test "rank1990: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1990: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
