# Rank 1939 / CVE-2026-33765: Security review for CVE-2026-33765 in pi-hole/web.
# Fix commit a66d859aec in pi-hole/web.
# ATTENTION, vocabulary/engine gap.

test "rank1939: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1939: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
