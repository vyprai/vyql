# Rank 1514 / CVE-2026-7551: Security review for CVE-2026-7551 in hkuds/openharness.
# Fix commit 438e373097 in hkuds/openharness.
# ATTENTION, vocabulary/engine gap.

test "rank1514: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1514: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
