# Rank 1778 / CVE-2021-32689: Security review for CVE-2021-32689 in nextcloud/spreed.
# Fix commit 75661785fa in nextcloud/spreed.
# ATTENTION, vocabulary/engine gap.

test "rank1778: vulnerable code pattern before fix"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    return $input;
}
  ```

test "rank1778: validated or patched form"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    if (!$input) return '';
    return $input;
}
  ```
