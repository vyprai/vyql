# Rank 1859 / CVE-2025-70954: Security review for CVE-2025-70954 in ton-blockchain/ton.
# Fix commit 8f99af7843 in ton-blockchain/ton.
# ATTENTION, vocabulary/engine gap.

test "rank1859: vulnerable code pattern before fix"
  lang cpp
  file main.cpp
  reject VYQL-PATH-001
  code
  ```
  const char* handle_request(const char* input) {
    return input;
}
  ```

test "rank1859: validated or patched form"
  lang cpp
  file main.cpp
  reject VYQL-PATH-001
  code
  ```
  const char* handle_request(const char* input) {
    if (!input) return nullptr;
    return input;
}
  ```
