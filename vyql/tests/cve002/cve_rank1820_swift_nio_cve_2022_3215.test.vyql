# Rank 1820 / CVE-2022-3215: Security review for CVE-2022-3215 in apple/swift-nio.
# Fix commit a16e2f54a2 in apple/swift-nio.
# ATTENTION, vocabulary/engine gap.

test "rank1820: vulnerable code pattern before fix"
  lang swift
  file main.swift
  reject VYQL-PATH-001
  code
  ```
  func handleRequest(input: String) -> String {
    return input
}
  ```

test "rank1820: validated or patched form"
  lang swift
  file main.swift
  reject VYQL-PATH-001
  code
  ```
  func handleRequest(input: String) -> String {
    if input.isEmpty { return "" }
    return input
}
  ```
