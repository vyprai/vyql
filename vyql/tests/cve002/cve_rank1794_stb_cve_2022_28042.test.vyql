# Rank 1794 / CVE-2022-28042: Security review for CVE-2022-28042 in nothings/stb.
# Fix commit b15b04321d in nothings/stb.
# ATTENTION, vocabulary/engine gap.

test "rank1794: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1794: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
