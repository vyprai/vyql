# Rank 1520 / CVE-2017-7990: Security review for CVE-2017-7990 in openmrs/openmrs-module-reporting.
# Fix commit 079977276f in openmrs/openmrs-module-reporting.
# ATTENTION, vocabulary/engine gap.

test "rank1520: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1520: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
