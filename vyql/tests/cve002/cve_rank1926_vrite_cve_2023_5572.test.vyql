# Rank 1926 / CVE-2023-5572: Security review for CVE-2023-5572 in vriteio/vrite.
# Fix commit 1877683b93 in vriteio/vrite.
# ATTENTION, vocabulary/engine gap.

test "rank1926: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1926: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
