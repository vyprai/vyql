# Rank 1726 / CVE-2026-43640: Security review for CVE-2026-43640 in bitwarden/server.
# Fix commit eb251d9bf8 in bitwarden/server.
# ATTENTION, vocabulary/engine gap.

test "rank1726: vulnerable code pattern before fix"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        return input;
    }
}
  ```

test "rank1726: validated or patched form"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        if (input == null) return string.Empty;
        return input;
    }
}
  ```
