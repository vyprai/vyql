# Rank 1893 / CVE-2021-30500: Security review for CVE-2021-30500 in upx/upx.
# Fix commit 90279abdfc in upx/upx.
# ATTENTION, vocabulary/engine gap.

test "rank1893: vulnerable code pattern before fix"
  lang cpp
  file main.cpp
  reject VYQL-PATH-001
  code
  ```
  const char* handle_request(const char* input) {
    return input;
}
  ```

test "rank1893: validated or patched form"
  lang cpp
  file main.cpp
  reject VYQL-PATH-001
  code
  ```
  const char* handle_request(const char* input) {
    if (!input) return nullptr;
    return input;
}
  ```
