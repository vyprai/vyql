# Rank 1839 / CVE-2015-7564: Security review for CVE-2015-7564 in nilsteampassnet/teampass.
# Fix commit 3c2a39760c in nilsteampassnet/teampass.
# ATTENTION, vocabulary/engine gap.

test "rank1839: vulnerable code pattern before fix"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    return $input;
}
  ```

test "rank1839: validated or patched form"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    if (!$input) return '';
    return $input;
}
  ```
