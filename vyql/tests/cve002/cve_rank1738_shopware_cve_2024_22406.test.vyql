# Rank 1738 / CVE-2024-22406: Security review for CVE-2024-22406 in shopware/shopware.
# Fix commit 5005213e60 in shopware/shopware.
# ATTENTION, vocabulary/engine gap.

test "rank1738: vulnerable code pattern before fix"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    return $input;
}
  ```

test "rank1738: validated or patched form"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    if (!$input) return '';
    return $input;
}
  ```
