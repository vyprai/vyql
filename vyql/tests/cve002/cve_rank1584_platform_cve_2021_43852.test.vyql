# Rank 1584 / CVE-2021-43852: Security review for CVE-2021-43852 in oroinc/platform.
# Fix commit 62c26936b3 in oroinc/platform.
# ATTENTION, vocabulary/engine gap.

test "rank1584: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1584: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
