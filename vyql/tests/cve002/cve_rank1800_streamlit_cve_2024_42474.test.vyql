# Rank 1800 / CVE-2024-42474: Security review for CVE-2024-42474 in streamlit/streamlit.
# Fix commit 3a639859cf in streamlit/streamlit.
# ATTENTION, vocabulary/engine gap.

test "rank1800: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1800: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
