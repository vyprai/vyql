# Rank 1770 / CVE-2026-24058: Security review for CVE-2026-24058 in charmbracelet/soft-serve.
# Fix commit 8539f9ad39 in charmbracelet/soft-serve.
# ATTENTION, vocabulary/engine gap.

test "rank1770: vulnerable code pattern before fix"
  lang go
  file main.go
  reject VYQL-PATH-001
  code
  ```
  package main

func HandleRequest(input string) string {
	return input
}
  ```

test "rank1770: validated or patched form"
  lang go
  file main.go
  reject VYQL-PATH-001
  code
  ```
  package main

func HandleRequest(input string) string {
	if input == "" {
		return ""
	}
	return input
}
  ```
