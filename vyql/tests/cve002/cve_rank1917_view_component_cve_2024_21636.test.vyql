# Rank 1917 / CVE-2024-21636: Security review for CVE-2024-21636 in viewcomponent/view_component.
# Fix commit c43d8bafa7 in viewcomponent/view_component.
# ATTENTION, vocabulary/engine gap.

test "rank1917: vulnerable code pattern before fix"
  lang ruby
  file main.rb
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input)
  input
end
  ```

test "rank1917: validated or patched form"
  lang ruby
  file main.rb
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input)
  return nil unless input
  input
end
  ```
