# Rank 1924 / CVE-2026-47137: Security review for CVE-2026-47137 in patriksimek/vm2.
# Fix commit 86ab819f20 in patriksimek/vm2.
# ATTENTION, vocabulary/engine gap.

test "rank1924: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1924: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
