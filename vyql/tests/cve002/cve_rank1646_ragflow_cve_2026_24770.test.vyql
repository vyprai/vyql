# Rank 1646 / CVE-2026-24770: Security review for CVE-2026-24770 in infiniflow/ragflow.
# Fix commit 64c75d558e in infiniflow/ragflow.
# ATTENTION, vocabulary/engine gap.

test "rank1646: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1646: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
