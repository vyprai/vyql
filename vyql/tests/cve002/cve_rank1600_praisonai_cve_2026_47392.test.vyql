# Rank 1600 / CVE-2026-47392: Security review for CVE-2026-47392 in mervinpraison/praisonai.
# Fix commit a72e156c4d in mervinpraison/praisonai.
# ATTENTION, vocabulary/engine gap.

test "rank1600: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1600: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
