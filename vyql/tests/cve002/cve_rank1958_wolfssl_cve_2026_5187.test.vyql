# Rank 1958 / CVE-2026-5187: Security review for CVE-2026-5187 in wolfssl/wolfssl.
# Fix commit 1d363f3adc in wolfssl/wolfssl.
# ATTENTION, vocabulary/engine gap.

test "rank1958: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1958: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
