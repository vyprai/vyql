# Rank 1733 / CVE-2021-37580: Security review for CVE-2021-37580 in apache/shenyu.
# Fix commit f78adb2692 in apache/shenyu.
# ATTENTION, vocabulary/engine gap.

test "rank1733: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1733: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
