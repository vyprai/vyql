# Rank 1568 / CVE-2018-1000869: Security review for CVE-2018-1000869 in phpipam/phpipam.
# Fix commit 856b10ca85 in phpipam/phpipam.
# ATTENTION, vocabulary/engine gap.

test "rank1568: vulnerable code pattern before fix"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    return $input;
}
  ```

test "rank1568: validated or patched form"
  lang php
  file main.php
  reject VYQL-PATH-001
  code
  ```
  <?php
function handleRequest($input) {
    if (!$input) return '';
    return $input;
}
  ```
