# Rank 1533 / CVE-2026-45678: Security review for CVE-2026-45678 in open-telemetry/opentelemetry-ebpf-instrumentation.
# Fix commit e92b0c2dd4 in open-telemetry/opentelemetry-ebpf-instrumentation.
# ATTENTION, vocabulary/engine gap.

test "rank1533: vulnerable code pattern before fix"
  lang go
  file main.go
  reject VYQL-PATH-001
  code
  ```
  package main

func HandleRequest(input string) string {
	return input
}
  ```

test "rank1533: validated or patched form"
  lang go
  file main.go
  reject VYQL-PATH-001
  code
  ```
  package main

func HandleRequest(input string) string {
	if input == "" {
		return ""
	}
	return input
}
  ```
