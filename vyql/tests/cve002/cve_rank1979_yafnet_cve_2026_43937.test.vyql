# Rank 1979 / CVE-2026-43937: Security review for CVE-2026-43937 in YAFNET/YAFNET.
# Fix commit 27f7e671f9 in YAFNET/YAFNET.
# ATTENTION, vocabulary/engine gap.

test "rank1979: vulnerable code pattern before fix"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        return input;
    }
}
  ```

test "rank1979: validated or patched form"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        if (input == null) return string.Empty;
        return input;
    }
}
  ```
