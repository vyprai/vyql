# Rank 1888 / CVE-2025-32017: Security review for CVE-2025-32017 in umbraco/Umbraco-CMS.
# Fix commit d3c1443b14 in umbraco/Umbraco-CMS.
# ATTENTION, vocabulary/engine gap.

test "rank1888: vulnerable code pattern before fix"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        return input;
    }
}
  ```

test "rank1888: validated or patched form"
  lang csharp
  file Main.cs
  reject VYQL-PATH-001
  code
  ```
  class Handler {
    public static string HandleRequest(string input) {
        if (input == null) return string.Empty;
        return input;
    }
}
  ```
