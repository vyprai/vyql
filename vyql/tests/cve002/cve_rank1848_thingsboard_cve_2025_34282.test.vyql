# Rank 1848 / CVE-2025-34282: Security review for CVE-2025-34282 in thingsboard/thingsboard.
# Fix commit 8e400fa2b4 in thingsboard/thingsboard.
# ATTENTION, vocabulary/engine gap.

test "rank1848: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1848: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
