# Rank 1575 / CVE-2026-53874: Security review for CVE-2026-53874 in mmaitre314/picklescan.
# Fix commit 173c8f2a86 in mmaitre314/picklescan.
# ATTENTION, vocabulary/engine gap.

test "rank1575: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1575: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
