# Rank 1640 / CVE-2024-45311: Security review for CVE-2024-45311 in quinn-rs/quinn.
# Fix commit e01609ccd8 in quinn-rs/quinn.
# ATTENTION, vocabulary/engine gap.

test "rank1640: vulnerable code pattern before fix"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    input
}
  ```

test "rank1640: validated or patched form"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    if input.is_empty() {
        return "";
    }
    input
}
  ```
