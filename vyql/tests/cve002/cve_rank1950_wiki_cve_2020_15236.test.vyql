# Rank 1950 / CVE-2020-15236: Security review for CVE-2020-15236 in requarks/wiki.
# Fix commit 084dcd69d1 in requarks/wiki.
# ATTENTION, vocabulary/engine gap.

test "rank1950: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1950: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
