# Rank 1815 / CVE-2022-27818: Security review for CVE-2022-27818 in waycrate/swhkd.
# Fix commit f70b99dd57 in waycrate/swhkd.
# ATTENTION, vocabulary/engine gap.

test "rank1815: vulnerable code pattern before fix"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    input
}
  ```

test "rank1815: validated or patched form"
  lang rust
  file main.rs
  reject VYQL-PATH-001
  code
  ```
  pub fn handle_request(input: &str) -> &str {
    if input.is_empty() {
        return "";
    }
    input
}
  ```
