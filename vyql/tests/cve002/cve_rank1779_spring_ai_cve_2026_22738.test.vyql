# Rank 1779 / CVE-2026-22738: Security review for CVE-2026-22738 in spring-projects/spring-ai.
# Fix commit ba9220b223 in spring-projects/spring-ai.
# ATTENTION, vocabulary/engine gap.

test "rank1779: vulnerable code pattern before fix"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        return input;
    }
}
  ```

test "rank1779: validated or patched form"
  lang java
  file Main.java
  reject VYQL-PATH-001
  code
  ```
  class App {
    public static String handleRequest(String input) {
        if (input == null) return "";
        return input;
    }
}
  ```
