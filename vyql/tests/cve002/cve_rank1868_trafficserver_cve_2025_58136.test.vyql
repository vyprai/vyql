# Rank 1868 / CVE-2025-58136: Security review for CVE-2025-58136 in apache/trafficserver.
# Fix commit c735ae73bc in apache/trafficserver.
# ATTENTION, vocabulary/engine gap.

test "rank1868: vulnerable code pattern before fix"
  lang cpp
  file main.cpp
  reject VYQL-PATH-001
  code
  ```
  const char* handle_request(const char* input) {
    return input;
}
  ```

test "rank1868: validated or patched form"
  lang cpp
  file main.cpp
  reject VYQL-PATH-001
  code
  ```
  const char* handle_request(const char* input) {
    if (!input) return nullptr;
    return input;
}
  ```
