# Rank 1857 / CVE-2024-52297: Security review for CVE-2024-52297 in tolgee/tolgee-platform.
# Fix commit 6ddba66d06 in tolgee/tolgee-platform.
# ATTENTION, vocabulary/engine gap.

test "rank1857: vulnerable code pattern before fix"
  lang kotlin
  file Main.kt
  reject VYQL-PATH-001
  code
  ```
  fun handleRequest(input: String): String {
    return input
}
  ```

test "rank1857: validated or patched form"
  lang kotlin
  file Main.kt
  reject VYQL-PATH-001
  code
  ```
  fun handleRequest(input: String): String {
    if (input.isEmpty()) return ""
    return input
}
  ```
