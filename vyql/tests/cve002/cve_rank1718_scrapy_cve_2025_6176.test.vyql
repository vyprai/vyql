# Rank 1718 / CVE-2025-6176: Security review for CVE-2025-6176 in scrapy/scrapy.
# Fix commit 14737e91ed in scrapy/scrapy.
# ATTENTION, vocabulary/engine gap.

test "rank1718: vulnerable code pattern before fix"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    data = input_val
    return data
  ```

test "rank1718: validated or patched form"
  lang python
  file main.py
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input_val):
    if not input_val:
        return None
    data = input_val
    return data
  ```
