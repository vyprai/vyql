# Rank 1792 / CVE-2017-5887: Security review for CVE-2017-5887 in daltoniam/starscream.
# Fix commit dbeb1190b8 in daltoniam/starscream.
# ATTENTION, vocabulary/engine gap.

test "rank1792: vulnerable code pattern before fix"
  lang swift
  file main.swift
  reject VYQL-PATH-001
  code
  ```
  func handleRequest(input: String) -> String {
    return input
}
  ```

test "rank1792: validated or patched form"
  lang swift
  file main.swift
  reject VYQL-PATH-001
  code
  ```
  func handleRequest(input: String) -> String {
    if input.isEmpty { return "" }
    return input
}
  ```
