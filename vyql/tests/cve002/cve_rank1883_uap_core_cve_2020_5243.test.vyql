# Rank 1883 / CVE-2020-5243: Security review for CVE-2020-5243 in ua-parser/uap-core.
# Fix commit 0afd61ed85 in ua-parser/uap-core.
# ATTENTION, vocabulary/engine gap.

test "rank1883: vulnerable code pattern before fix"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    let data = input;
    return data;
}
  ```

test "rank1883: validated or patched form"
  lang javascript
  file main.js
  reject VYQL-PATH-001
  code
  ```
  function handleRequest(input) {
    if (!input) return null;
    let data = input;
    return data;
}
  ```
