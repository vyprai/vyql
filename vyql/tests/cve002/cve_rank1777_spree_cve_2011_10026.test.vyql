# Rank 1777 / CVE-2011-10026: Security review for CVE-2011-10026 in spree/spree.
# Fix commit 0a9a360c59 in spree/spree.
# ATTENTION, vocabulary/engine gap.

test "rank1777: vulnerable code pattern before fix"
  lang ruby
  file main.rb
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input)
  input
end
  ```

test "rank1777: validated or patched form"
  lang ruby
  file main.rb
  reject VYQL-PATH-001
  code
  ```
  def handle_request(input)
  return nil unless input
  input
end
  ```
