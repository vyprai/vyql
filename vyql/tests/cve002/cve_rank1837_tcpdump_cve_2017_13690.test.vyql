# Rank 1837 / CVE-2017-13690: Security review for CVE-2017-13690 in the-tcpdump-group/tcpdump.
# Fix commit 8dca25d26c in the-tcpdump-group/tcpdump.
# ATTENTION, vocabulary/engine gap.

test "rank1837: vulnerable code pattern before fix"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    return input;
}
  ```

test "rank1837: validated or patched form"
  lang c
  file main.c
  reject VYQL-PATH-001
  code
  ```
  char* handle_request(char* input) {
    if (!input) return 0;
    return input;
}
  ```
