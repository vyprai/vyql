# Rank 25 / CVE-2020-8816: Pi-hole AdminLTE DHCP static-lease settings reach
# shell command construction.

test "rank25: DHCP static lease setting reaches shell command"
  expect VYQL-INJ-002
  graph
  ```
  node src core.UserControlledData
  node sink code.CommandExecution
  edge FLOWS src -> sink
  ```

