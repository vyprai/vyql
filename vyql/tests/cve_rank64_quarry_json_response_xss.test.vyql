# Rank 64 / CVE-2020-36324: Quarry returned JSON through a Flask Response
# without the safe JSON content type, making reflected XSS reachable.

test "rank64: response data reaches HTML-capable render sink"
  expect VYQL-INJ-004
  graph
  ```
  node src core.UserControlledData
  node sink code.HtmlRender
  edge FLOWS src -> sink
  ```

