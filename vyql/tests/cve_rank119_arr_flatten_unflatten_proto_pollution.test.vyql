# Rank 119 / CVE-2020-7713: arr-flatten-unflatten wrote caller-controlled path
# segments into object structure, enabling prototype pollution.

test "rank119: unflatten writes user path into prototype-polluting sink"
  expect VYQL-INJ-013
  graph
  ```
  node src core.UserControlledData
  node sink code.ProtoPollute
  edge FLOWS src -> sink
  ```

