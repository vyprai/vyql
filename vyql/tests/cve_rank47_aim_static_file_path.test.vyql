# Rank 47 / CVE-2021-43775: Aim served caller-controlled static file names
# without proving the resolved path stayed under the static-file root.

test "rank47: static file name reaches file path access"
  expect VYQL-PATH-001
  graph
  ```
  node src core.UserControlledData
  node sink code.FilePathAccess
  edge FLOWS src -> sink
  ```

