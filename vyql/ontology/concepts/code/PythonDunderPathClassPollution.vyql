module code;

concept PythonDunderPathClassPollution : issue {
  cwe: [CWE_915]
  review_assumption: "a remote component or API request can provide the property path and value"
  reviewCategory: mass_assignment
  reviewCondition: "caller-controlled dotted attribute paths are traversed and written without rejecting Python magic attributes"
  reviewConfidence: high
  reviewEvidence: "property-path setter context {pattern} splits a property name, traverses attributes, and writes setattr without a __...__ segment guard"
  vulnerableTo: [injection.MassAssignment]
}
