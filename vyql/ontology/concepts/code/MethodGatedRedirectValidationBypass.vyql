module code;

concept MethodGatedRedirectValidationBypass : issue {
  cwe: [CWE_601]
  reviewAssumption: "a GET or other non-matching method can carry attacker-controlled redirect/callback URL parameters"
  reviewCategory: request_forgery
  reviewCondition: "redirect or callback URL validation is skipped for non-matching HTTP methods"
  reviewConfidence: medium
  reviewEvidence: "URL validation context {pattern} returns before checking callback or redirect URL parameters"
  vulnerableTo: [request_forgery.OpenRedirect]
}
