module code;

concept CkeditorRawElementLiveDocumentRenderXss : issue {
  cwe: [CWE_79]
  reviewAssumption: "attacker-controlled RawElement content can include active markup such as image onerror handlers in affected editor configurations"
  reviewCategory: xss
  reviewCondition: "CKEditor clipboard plain-text conversion renders a RawElement into the live document before reading text"
  reviewConfidence: high
  reviewEvidence: "clipboard conversion context {pattern} creates a live document element, renders a view RawElement into it, and reads DOM text"
  vulnerableTo: [injection.Xss]
}
