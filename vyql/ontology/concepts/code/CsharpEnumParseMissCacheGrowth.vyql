module code;

concept CsharpEnumParseMissCacheGrowth : issue {
  cwe: [CWE_400, CWE_770]
  review_assumption: "callers can submit high-cardinality invalid enum strings to the parser"
  reviewCategory: resource
  reviewCondition: "C# enum string parsing caches failed lookup keys in process-global state"
  reviewConfidence: high
  reviewEvidence: "enum parse cache context {pattern} inserts arbitrary miss keys into a ConcurrentDictionary"
  vulnerableTo: [dos.UncontrolledResourceConsumption]
}
