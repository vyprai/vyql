module bindings.php.generated.mfbe84119;

binding shopwareStoreApiManyToManyCriteriaBypass171 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.context.name contains "processCriteria" and node.token contains "SalesChannelDefinitionInterface" and node.token contains "$definition->processCriteria" and node.token contains "$criteria->getAssociations" and node.token contains "$definition->getField" and node.token contains "AssociationField" and node.token contains "$field->getReferenceDefinition" and node.token contains "$processed[$definition::class]" and not (node.token contains "ManyToManyAssociationField") and not (node.token contains "$field->getToManyReferenceDefinition") and not (node.token contains "getToManyReferenceDefinition") and not (node.token contains "$processedKey")
  emit issue code.ShopwareStoreApiManyToManyCriteriaBypass at node
}
