module bindings.php.generated.mfbe84119;

binding secretFilePermissionReview434 {
  query pattern presenceNode where node.scope == "function" and node.token contains "generateKey" and node.token contains "keyfile" and not (node.token contains "chmod")
  emit issue code.SecretFilePermissionReview at node
}
