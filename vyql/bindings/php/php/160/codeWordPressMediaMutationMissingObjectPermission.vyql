module bindings.php.generated.mfbe84119;

binding wordPressMediaMutationMissingObjectPermission160 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.token contains "$_REQUEST['attachment_id']" and node.token contains "get_post($attachment_id)" and node.token contains "ReplaceController($attachment_id)" and node.token contains "$this->loadView('prepare-remove-background')" and not (node.token contains "checkImagePermission") and not (node.token contains "current_user_can('edit_post'") and not (node.token contains "current_user_can(\"edit_post\"")
  emit issue code.WordPressMediaMutationMissingObjectPermission at node
}
