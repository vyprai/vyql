module bindings.php.generated.mfbe84119;

binding ezPublishSearchNumericParamXss106 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "php" and node.context.callPath contains "eZSearch.search" and node.context.callPath contains "$tpl.setVariable" and node.context.assignCall contains "$searchSectionID:$http.variable" and node.context.assignLiteral contains "$searchSectionID:SectionID" and node.context.callArg contains "eZSearch.search:array" and node.context.callArg contains "$tpl.setVariable:$searchSectionID" and not (node.context.castCallLiteral contains "int:$http.variable:SectionID")
  emit issue code.EzPublishSearchNumericParamXss at node
}
