module bindings.php.generated.mfbe84119;

binding idorPolicyReview153 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.token contains "fileId" and node.token contains "getRules(" and node.token contains "return$userRules;" and not (node.token contains "userHasAccessTo")
  emit issue code.IdorPolicyReview at node
}
