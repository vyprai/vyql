module bindings.php.generated.mfbe84119;

binding codeIgniterActiveRecordOffsetSqlInjection193 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.context.name contains "offset" and node.token contains "$this->ar_offset=$offset" and not (node.token contains "$this->ar_offset=(int)$offset")
  emit issue code.CodeIgniterActiveRecordOffsetSqlInjection at node
}
