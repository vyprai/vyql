module bindings.php.generated.mfbe84119;

binding uncheckedUploadedImageProcessing387 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.token contains "imagecopyresampled" and node.token contains "$imgt($new_image,$thumb_path)" and node.token contains "$imgt($old_image,$source_path)" and node.token contains "returntrue" and not (node.token contains "===false") and not (node.token contains "return$imgt")
  emit issue code.UncheckedUploadedImageProcessing at node
}
