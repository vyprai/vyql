module bindings.php.generated.mfbe84119;

binding archiveManifestDeserialization67 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.token contains "Tar" and node.token contains "extract(" and node.token contains "manifest" and node.token contains "file_get_contents" and node.token contains "unserialize" and not (node.token contains "allowed_classes")
  emit issue code.ArchiveManifestDeserialization at node
}
