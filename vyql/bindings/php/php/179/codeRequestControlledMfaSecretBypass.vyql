module bindings.php.generated.mfbe84119;

binding requestControlledMfaSecretBypass179 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.context.name contains "getAuthService" and node.token contains "MfaHelper" and node.token contains "$this->Session->get('mfa_secret')" and node.token contains "$this->Request->request->get('mfa_secret')" and not (node.token contains "newMfaHelper($this->Session->get('mfa_secret'))")
  emit issue code.RequestControlledMfaSecretBypass at node
}
