module bindings.php.generated.mfbe84119;

binding incompleteIpDenylist185 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "php" and node.token contains "base64Url" and node.token contains "get_ffmpeg()" and node.token contains "-i" and not (node.token contains "parse_url($url)") and not (node.token contains "ip_is_private")
  emit issue code.IncompleteIpDenylist at node
}
