module bindings.php.generated.mfbe84119;

binding templatePathComponentTraversal138 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.token contains "templates/{$type}/{$language}" and node.token contains "{$name}.tpl" and not (node.token contains "basename($name)")
  emit issue code.TemplatePathComponentTraversal at node
}
