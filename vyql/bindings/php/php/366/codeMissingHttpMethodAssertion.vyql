module bindings.php.generated.mfbe84119;

binding missingHttpMethodAssertion366 {
  query pattern presenceNode where node.scope == "class" and node.token contains "Action" and node.token contains "$this->addToCompareList" and node.token contains "$this->removeFromCompareList" and not (node.token contains "$this->assertAllowedHttpMethod")
  emit issue code.MissingHttpMethodAssertion at node
}
