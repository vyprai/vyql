module bindings.javascript.generated.mfb63017a;

binding insecureCookie663 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "res.cookie" and not (node.token contains "secure=true")
  emit issue code.InsecureCookie at node
}
