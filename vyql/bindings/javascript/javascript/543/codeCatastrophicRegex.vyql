module bindings.javascript.generated.mfb63017a;

binding catastrophicRegex543 {
  query pattern presenceNode where node.kind == "any" and node.method == "match" and node.token contains "^\\{(,+(?:(\\{,+\\})*),*|,*(?:(\\{,+\\})*),+)\\}" and not (node.token contains "^\\{((?:,|\\{,+\\})+)\\}")
  emit issue code.CatastrophicRegex at node
  emit sink code.CatastrophicRegexSink at node
}
