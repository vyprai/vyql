module bindings.javascript.generated.mfb63017a;

binding browserlessWorkspaceFilePathTraversal642 {
  query pattern presenceNode where node.scope == "function" and node.context.name contains "<lambda>" and node.token contains "req.params[0]" and node.token contains "path.join(workspaceDir,file)" and node.token contains "rimraf(filePath" and not (node.token contains "filePath.includes(workspaceDir)")
  emit issue code.BrowserlessWorkspaceFilePathTraversal at node
}
