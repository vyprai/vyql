module bindings.javascript.generated.mfb63017a;

binding localFileUrlNavigation301 {
  query pattern presenceNode where node.scope == "function" and node.token contains "page.goto(request.url,requestOptions)" and not (node.token contains "request.url.trim().toLowerCase().substring(0,4)==='file'")
  emit issue code.LocalFileUrlNavigation at node
}
