module bindings.javascript.generated.mfb63017a;

binding unvalidatedDynamicRequirePathTraversal644 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "javascript" and node.context.identifier contains "widgetId" and node.context.literal contains "widget-${widgetId}.js" and node.context.callPath contains "path.join" and node.context.callPath contains "require" and not (node.context.callPath contains "resolveWidgetPath") and not (node.context.callPath contains "path.relative") and not (node.context.identifier contains "widgetIdPattern")
  emit issue code.UnvalidatedDynamicRequirePathTraversal at node
}
