module bindings.javascript.generated.mfb63017a;

binding vm2HostCallbackBridge266 {
  query pattern presenceNode where node.scope == "function" and node.token contains "new VM" and node.token contains "sandbox.templateFilters" and node.token contains "sandbox.templateGlobals" and node.token contains "vm.run" and not (node.token contains "isolated-vm") and not (node.token contains "Isolate")
  emit issue code.Vm2HostCallbackBridge at node
}
