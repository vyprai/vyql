module bindings.javascript.generated.mfb63017a;

binding clientLightboxOptionXss114 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "javascript" and node.context.callPath contains "setMessage" and node.context.identifier contains "html" and not (node.context.callPath contains "DOMPurify.sanitize") and not (node.context.callPath contains "sanitizeHtml") and not (node.context.callPath contains "sanitize")
  emit issue code.ClientLightboxOptionXss at node
}
