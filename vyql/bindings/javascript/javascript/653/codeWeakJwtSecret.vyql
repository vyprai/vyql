module bindings.javascript.generated.mfb63017a;

binding weakJwtSecret653 {
  query pattern presenceNode where node.kind == "call" and node.path ~= "jwt.verify" and containsAny(node.context.callArg, ["secret", "jwt-secret", "change-me", "changeme", "password"])
  emit issue code.WeakJwtSecret at node
}
