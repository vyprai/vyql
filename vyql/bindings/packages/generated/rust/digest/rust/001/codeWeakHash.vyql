module bindings.rust.generated.md509b086;

binding weakHash1 {
  requires {
    dependency("digest")
  }
  query pattern presenceNode where node.path ~= "digest.hash"
  emit issue code.WeakHash at node
}
