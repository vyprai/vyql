module bindings.php.generated.m9efedd99;

binding secretValue0 {
  requires {
    dependency("symfony/password-hasher")
  }
  query pattern callExpr where callee.method == "hash"
  emit source code.SecretValue at call.result
}

binding weakHash1 {
  requires {
    dependency("symfony/password-hasher")
  }
  query pattern presenceNode where node.path ~= "symfony.password.hasher.hash"
  emit issue code.WeakHash at node
}
