module bindings.python.generated.m21017d75;

binding secretComparisonReview257 {
  query pattern presenceNode where node.kind == "binop" and node.op contains "not in" and not (containsAny(node.context.scopeCall, [constant_time_compare, hmac.compare_digest, secrets.compare_digest, compare_digest])) and operand(node, where: operand.path ~= "get_request_basic_auth") and operand(node, where: operand.path ~= "basic_auth")
  emit issue code.SecretComparisonReview at node
}
