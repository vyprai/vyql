module bindings.python.generated.m21017d75;

binding hardcodedSecret107 {
  query pattern presenceNode where node.scope == "function" and node.token contains "app.config[\"SECRET_KEY\"]=\"" and not (node.token contains "os.environ") and not (node.token contains "os.getenv") and not (node.token contains "secrets.") and not (node.token contains "build_secret_key()")
  emit issue code.HardcodedSecret at node
}
