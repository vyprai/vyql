module bindings.python.generated.m21017d75;

binding jwtUnverified643 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "jwt.decode" and node.token contains "verify=False"
  emit issue code.JwtUnverified at node
}
