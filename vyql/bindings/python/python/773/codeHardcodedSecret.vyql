module bindings.python.generated.m21017d75;

binding hardcodedSecret773 {
  query pattern presenceNode where node.method == "connect" and node.token contains "password"
  emit issue code.HardcodedSecret at node
}
