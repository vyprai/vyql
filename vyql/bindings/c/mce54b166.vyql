module bindings.c.generated.mce54b166;

pattern bindingMetadata {
  binding: {
    name: "c"
    meta: {
      fidelity: "resolved"
    }
  }
}

binding envVariable0 {
  query pattern callExpr where callee.path ~= "getenv"
  emit source code.EnvVariable at call.result
}

binding cliArgument1 {
  query pattern callExpr where callee.path ~= "argv"
  emit source code.CliArgument at call.result
}

binding stdinInput2 {
  query pattern callExpr where callee.path ~= "fgets"
  emit source code.StdinInput at call.result
}

binding stdinInput3 {
  query pattern callExpr where callee.path ~= "gets"
  emit source code.StdinInput at call.result
}

binding httpInput4 {
  query pattern callExpr where callee.path ~= "fread"
  emit source code.HttpInput at call.result
}

binding httpInput5 {
  query pattern callExpr where callee.path ~= "recv"
  emit source code.HttpInput at call.result
}

binding httpInput6 {
  query pattern callExpr where callee.path ~= "recvfrom"
  emit source code.HttpInput at call.result
}

binding httpInput7 {
  query pattern callExpr where callee.path ~= "http_request_param_get"
  emit source code.HttpInput at call.result
}

binding httpInput8 {
  query pattern callExpr where callee.path ~= "http_request_get_query_string"
  emit source code.HttpInput at call.result
}

binding httpInput9 {
  query pattern callExpr where callee.path ~= "http_request_get_path_info"
  emit source code.HttpInput at call.result
}

binding commandExecution10 {
  query pattern callExpr where callee.path ~= "system"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution11 {
  query pattern callExpr where callee.path ~= "popen"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution12 {
  query pattern callExpr where callee.path ~= "execl"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution13 {
  query pattern callExpr where callee.path ~= "execlp"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution14 {
  query pattern callExpr where callee.path ~= "execle"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution15 {
  query pattern callExpr where callee.path ~= "execv"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution16 {
  query pattern callExpr where callee.path ~= "execvp"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution17 {
  query pattern callExpr where callee.path ~= "execve"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution18 {
  query pattern callExpr where callee.path ~= "execvpe"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution19 {
  query pattern callExpr where callee.path ~= "g_spawn_command_line_sync"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution20 {
  query pattern callExpr where callee.path ~= "g_spawn_command_line_async"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution21 {
  query pattern callExpr where callee.path ~= "g_app_info_create_from_commandline"
  emit sink code.CommandExecution at args[0]
}

binding commandLineAssemblyReview22 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "command_line.glib_unquoted_placeholder"
  emit issue code.CommandLineAssemblyReview at node
}

binding sqlExecution23 {
  query pattern callExpr where callee.path ~= "mysql_query"
  emit sink code.SqlExecution at args[1]
}

binding sqlExecution24 {
  query pattern callExpr where callee.path ~= "mysql_real_query"
  emit sink code.SqlExecution at args[1]
}

binding sqlExecution25 {
  query pattern callExpr where callee.path ~= "sqlite3_exec"
  emit sink code.SqlExecution at args[1]
}

binding sqlExecution26 {
  query pattern callExpr where callee.path ~= "sqlite3_prepare"
  emit sink code.SqlExecution at args[1]
}

binding sqlExecution27 {
  query pattern callExpr where callee.path ~= "sqlite3_prepare_v2"
  emit sink code.SqlExecution at args[1]
}

binding sqlExecution28 {
  query pattern callExpr where callee.path ~= "PQexec"
  emit sink code.SqlExecution at args[1]
}

binding filePathAccess29 {
  query pattern callExpr where callee.path ~= "fopen"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess30 {
  query pattern callExpr where callee.path ~= "open"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess31 {
  query pattern callExpr where callee.path ~= "open64"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess32 {
  query pattern callExpr where callee.path ~= "unlink"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess33 {
  query pattern callExpr where callee.path ~= "remove"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess34 {
  query pattern callExpr where callee.path ~= "opendir"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess35 {
  query pattern callExpr where callee.path ~= "access"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess36 {
  query pattern callExpr where callee.path ~= "file_read"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess37 {
  query pattern callExpr where callee.path ~= "file_write"
  emit sink code.FilePathAccess at args[0]
}

binding cWebRequestFilePathTraversal38 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "web_request.path_traversal"
  emit issue code.CWebRequestFilePathTraversal at node
}

binding sharedOtaHandlerMissingAuthentication39 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "ota.shared_handler_missing_auth"
  emit issue code.SharedOtaHandlerMissingAuthentication at node
}

binding remoteListingDownloadPath40 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "remote_listing.download_path"
  emit issue code.RemoteListingDownloadPath at node
}

binding pathBasedSandboxExposeBindRace41 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "c" and node.context.callPath contains "g_variant_lookup" and node.context.literal contains "sandbox-expose" and node.context.callPath contains "filesystem_sandbox_arg" and node.context.callPath contains "get_path_for_fd" and node.context.callPath contains "filesystem_arg" and node.context.callPath contains "g_ptr_array_add" and not (node.context.callPath contains "fd_map_remap_fd") and not (node.context.callPath contains "validate_opath_fd") and not (node.context.callPath contains "glnx_chaseat") and not (node.context.literal contains "--bind-fd=%d") and not (node.context.literal contains "--ro-bind-fd=%d")
  emit issue code.PathBasedSandboxExposeBindRace at node
}

binding dnsInterfaceNewlineValidationMissing42 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "config.dns_interface_newline_validation_missing"
  emit issue code.DnsInterfaceNewlineValidationMissing at node
}

binding credentialProtocolNewlineInjection43 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "credential.protocol_newline_injection"
  emit issue code.CredentialProtocolNewlineInjection at node
}

binding dynamicCodeLoad44 {
  query pattern callExpr where callee.path ~= "dlopen"
  emit sink code.DynamicCodeLoad at args[0]
}

binding htmlRender45 {
  query pattern callExpr where callee.path ~= "fputs"
  emit sink code.HtmlRender at args[0]
}

binding htmlRender46 {
  query pattern callExpr where callee.path ~= "fwrite"
  emit sink code.HtmlRender at args[0]
}

binding ldapQuery47 {
  query pattern callExpr where callee.path ~= "ldap_search_ext_s"
  emit sink code.LdapQuery at args[3]
}

binding ldapQuery48 {
  query pattern callExpr where callee.path ~= "ldap_search_s"
  emit sink code.LdapQuery at args[3]
}

binding xpathQuery49 {
  query pattern callExpr where callee.path ~= "xmlXPathEvalExpression"
  emit sink code.XpathQuery at args[0]
}

binding xpathQuery50 {
  query pattern callExpr where callee.path ~= "xmlXPathEval"
  emit sink code.XpathQuery at args[0]
}

binding insecureTempFile51 {
  query pattern presenceNode where node.path ~= "mktemp"
  emit issue code.InsecureTempFile at node
}

binding insecureTempFile52 {
  query pattern presenceNode where node.path ~= "tmpnam"
  emit issue code.InsecureTempFile at node
}

binding insecureTempFile53 {
  query pattern presenceNode where node.path ~= "tempnam"
  emit issue code.InsecureTempFile at node
}

binding insecureTempFile210 {
  query pattern presenceNode where node.context.macroBody contains "\"/tmp/"
  emit issue code.InsecureTempFile at node
}

binding insecureTempFile211 {
  query pattern presenceNode where node.context.macroBody contains "\"/var/tmp/"
  emit issue code.InsecureTempFile at node
}

binding insecureTempFile212 {
  query pattern presenceNode where node.context.macroBody contains "\"/dev/shm/"
  emit issue code.InsecureTempFile at node
}

binding insecurePermission54 {
  query pattern callExpr where callee.path ~= "chmod" and args.any.literal contains "0777"
  emit issue code.InsecurePermission at call
}

binding insecurePermission55 {
  query pattern callExpr where callee.path ~= "chmod" and args.any.literal contains "0666"
  emit issue code.InsecurePermission at call
}

binding insecurePermission56 {
  query pattern callExpr where callee.path ~= "open" and args.any.literal contains "0777"
  emit issue code.InsecurePermission at call
}

binding insecurePermission57 {
  query pattern callExpr where callee.path ~= "open" and args.any.literal contains "0666"
  emit issue code.InsecurePermission at call
}

binding unboundedCopy58 {
  query pattern callExpr where callee.path ~= "strcpy"
  emit sink code.UnboundedCopy at args[1]
}

binding unboundedCopy59 {
  query pattern callExpr where callee.path ~= "strcat"
  emit sink code.UnboundedCopy at args[1]
}

binding unboundedCopy60 {
  query pattern callExpr where callee.path ~= "sprintf"
  emit sink code.UnboundedCopy at args.any
}

binding unboundedCopy61 {
  query pattern callExpr where callee.path ~= "vsprintf"
  emit sink code.UnboundedCopy at args.any
}

binding unboundedCopySmell62 {
  query pattern presenceNode where node.path ~= "strcpy"
  emit issue code.UnboundedCopySmell at node
}

binding unboundedCopySmell63 {
  query pattern presenceNode where node.path ~= "strcat"
  emit issue code.UnboundedCopySmell at node
}

binding unboundedCopySmell64 {
  query pattern presenceNode where node.path ~= "sprintf"
  emit issue code.UnboundedCopySmell at node
}

binding unboundedCopySmell65 {
  query pattern presenceNode where node.path ~= "vsprintf"
  emit issue code.UnboundedCopySmell at node
}

binding rawMemoryCopySize66 {
  query pattern callExpr where callee.path ~= "memcpy"
  emit sink code.RawMemoryCopySizeSink at args[2]
  emit issue code.RawMemoryCopySize at call
}

binding rawMemoryCopySize67 {
  query pattern callExpr where callee.path ~= "memmove"
  emit sink code.RawMemoryCopySizeSink at args[2]
  emit issue code.RawMemoryCopySize at call
}

binding rawMemoryCopySize68 {
  query pattern callExpr where callee.path ~= "strncpy"
  emit sink code.RawMemoryCopySizeSink at args[2]
  emit issue code.RawMemoryCopySize at call
}

binding rawMemoryCopySize69 {
  query pattern callExpr where callee.path ~= "strncat"
  emit sink code.RawMemoryCopySizeSink at args[2]
  emit issue code.RawMemoryCopySize at call
}

binding sizeComputation70 {
  query pattern callExpr where callee.path ~= "malloc"
  emit sink code.SizeComputation at args[0]
}

binding sizeComputation71 {
  query pattern callExpr where callee.path ~= "calloc"
  emit sink code.SizeComputation at args.any
}

binding sizeComputation72 {
  query pattern callExpr where callee.path ~= "realloc"
  emit sink code.SizeComputation at args[1]
}

binding sizeComputation73 {
  query pattern callExpr where callee.path ~= "reallocarray"
  emit sink code.SizeComputation at args[1]
}

binding zeroSizeReallocation74 {
  query pattern presenceNode where node.path ~= "realloc"
  emit issue code.ZeroSizeReallocation at node
  emit sink code.ZeroSizeReallocationSink at node
}

binding zeroSizeReallocation75 {
  query pattern presenceNode where node.path ~= "reallocarray"
  emit issue code.ZeroSizeReallocation at node
  emit sink code.ZeroSizeReallocationSink at node
}

binding reallocFailurePathInputFree76 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "lifetime.realloc_failure_input_free"
  emit issue code.ReallocFailurePathInputFree at node
}

binding stackAllocationSmell77 {
  query pattern presenceNode where node.path ~= "alloca"
  emit issue code.StackAllocationSmell at node
}

binding indexAccess78 {
  query pattern presenceNode where node.path ~= "__subscript"
  emit issue code.IndexAccess at node
  emit sink code.IndexAccessSink at node
}

binding fieldDerivedIndexAccess79 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "index.field_derived_missing_upper_bound"
  emit issue code.FieldDerivedIndexAccess at node
}

binding zeroCountLastElementAccess80 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "zero_count.missing_nonzero_last_index"
  emit issue code.ZeroCountLastElementAccess at node
}

binding failurePathOwnedBufferFree81 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "lifetime.release_then_return"
  emit issue code.FailurePathOwnedBufferFree at node
}

binding conditionalFallbackListDoubleFree82 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "list.conditional_fallback_double_free"
  emit issue code.ConditionalFallbackListDoubleFree at node
}

binding missingPostCopyBoundsCheck83 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "post_copy.missing_bounds"
  emit issue code.MissingPostCopyBoundsCheck at node
}

binding fat12SuccessorEntryBoundsBypass84 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "fat12.successor_entry_bounds_bypass"
  emit issue code.Fat12SuccessorEntryBoundsBypass at node
}

binding shiftedClusterSizeAllocationMissingBounds85 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "cluster.shifted_size_allocation"
  emit issue code.ShiftedClusterSizeAllocationMissingBounds at node
}

binding phpZvalArrayMergeMissingDestTypeCheck86 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and node.context.callPath contains "Z_ARRVAL_PP" and node.context.callPath contains "zend_hash_find" and node.context.callPath contains "zend_hash_index_find" and node.context.binary contains "Z_TYPE_PP(test_ptr)==IS_ARRAY" and not (node.context.binary contains "Z_TYPE_PP(test_ptr)==IS_ARRAY&&Z_TYPE_PP(ptr)==IS_ARRAY")
  emit issue code.PhpZvalArrayMergeMissingDestTypeCheck at node
}

binding numericParserMissingProgressCheck87 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "numeric_parser.missing_progress_check"
  emit issue code.NumericParserMissingProgressCheck at node
}

binding pairwiseConstraintIntersectionDos88 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and containsAny(node.context.callPath, ["intersect_nodes", "name_constraints_intersect_nodes"]) and containsAny(node.context.callPath, ["node_list_add", "name_constraints_node_list_add"]) and containsAny(node.context.selector, ["permitted.size", "permitted2.size", "removed.size"]) and containsAny(node.context.binary, ["i<permitted2->size", "j<removed.size", "j<permitted2->size"]) and not (containsAny(node.context.callPath, ["ensure_sorted", "compare_name_constraint_nodes"])) and not (node.context.selector contains "sorted_view")
  emit issue code.PairwiseConstraintIntersectionDos at node
}

binding ecPointIdentityElementMissing89 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and node.context.paramType contains "PointZZ_p" and containsAny(node.context.name, ["pointZZ_pDouble", "pointZZ_pAdd"]) and node.context.callPath contains "mpz_invert" and not (containsAny(node.context.scopeCall, [IsIdentityElement, SetToIdentityElement, is_at_infinity, set_at_infinity, isInfinity, setInfinity]))
  emit issue code.EcPointIdentityElementMissing at node
}

binding ecPointIdentityElementMissing90 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and node.context.paramType contains "PointZZ_p" and containsAny(node.context.name, ["pointZZ_pMul"]) and node.context.callPath contains "pointZZ_pDouble" and node.context.callPath contains "pointZZ_pAdd" and not (containsAny(node.context.scopeCall, [IsIdentityElement, SetToIdentityElement, is_at_infinity, set_at_infinity, isInfinity, setInfinity]))
  emit issue code.EcPointIdentityElementMissing at node
}

binding underallocatedFormattedAddressList91 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and node.context.switchCase contains "D6_OPT_DNS_SERVERS" and node.context.callPath contains "xmalloc" and node.context.callPath contains "sprint_nip6" and node.context.callArgShape contains "xmalloc:NUM+ID*NUM-1" and node.context.callArgShape contains "sprint_nip6:ID+NUM+ID" and node.context.assignOpShape contains "ID+=NUM" and not (node.context.callArgShape contains "xmalloc:NUM+ID*NUM+1")
  emit issue code.UnderallocatedFormattedAddressList at node
}

binding xmlLeadByteNameValidationMissing92 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "c" and node.context.macroName contains "CHECK_NAME_CASE" and node.context.macroName contains "CHECK_NMSTRT_CASE" and node.context.macroBody contains "CHECK_NAME_CASE:" and node.context.macroBody contains "if(!IS_NAME_CHAR(enc,ptr,n))" and node.context.macroBody contains "CHECK_NMSTRT_CASE:" and node.context.macroBody contains "if(!IS_NMSTRT_CHAR(enc,ptr,n))" and not (node.context.macroBody contains "IS_INVALID_CHAR(enc,ptr,n)||!IS_NAME_CHAR(enc,ptr,n)") and not (node.context.macroBody contains "IS_INVALID_CHAR(enc,ptr,n)||!IS_NMSTRT_CHAR(enc,ptr,n)")
  emit issue code.XmlLeadByteNameValidationMissing at node
}

binding icmpEchoPayloadLengthUnderflow93 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "icmp.echo_payload_length_underflow"
  emit issue code.IcmpEchoPayloadLengthUnderflow at node
}

binding isolateLevelIncrementWithoutCap94 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "bidi.isolate_level_increment_without_cap"
  emit issue code.IsolateLevelIncrementWithoutCap at node
}

binding flowSpecOperatorLoopBoundOffByOne95 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and node.context.callPath contains "hex2bin" and node.context.callPath contains "hexstr2num" and node.context.selector contains "mval.value" and node.context.binary contains "loop>BGP_PBR_MATCH_VAL_MAX" and not (node.context.binary contains "loop>=BGP_PBR_MATCH_VAL_MAX")
  emit issue code.FlowSpecOperatorLoopBoundOffByOne at node
}

binding flacDecoderBufferReuseWithoutBlocksizeCheck96 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "flac.buffer_reuse_without_blocksize_check"
  emit issue code.FlacDecoderBufferReuseWithoutBlocksizeCheck at node
}

binding contikiSicslowpanFragmentOffsetOobWrite97 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "fragment.offset_copy_oob"
  emit issue code.ContikiSicslowpanFragmentOffsetOobWrite at node
}

binding binarySearchTableIndexGuardBypass98 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "binary_search.endpoint_guard_bypass"
  emit issue code.BinarySearchTableIndexGuardBypass at node
}

binding compressedBlockSizeCapacityMismatch99 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "compressed_block.capacity_mismatch"
  emit issue code.CompressedBlockSizeCapacityMismatch at node
}

binding lengthDerivedAllocation100 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "allocation.length_derived_before_file_bound"
  emit issue code.LengthDerivedAllocation at node
}

binding fixedChunkCapacityGrowthIndex101 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and containsAny(node.context.binary, ["level>=ms->c.len", "level>=ctx->c.len", "level>=state->c.len", "idx>=capacity", "index>=capacity", "level>=capacity"]) and containsAny(node.context.assign, [".len=20", "capacity=16", "capacity=32", "capacity=64"]) and node.context.callPath contains "realloc" and containsAny(node.context.indexKey, ["level", "idx", "index"]) and not (containsAny(node.context.assign, [".len=20+level", ".len=level+20", "capacity=idx+1", "capacity=index+1", "capacity=level+1"]))
  emit issue code.FixedChunkCapacityGrowthIndex at node
}

binding uncheckedPythonCapiHashError102 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "python_hash.unchecked_error"
  emit issue code.UncheckedPythonCapiHashError at node
}

binding rubyCgiEscapeHtmlLengthOverflow103 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "ruby_cgi.escape_html_length_overflow"
  emit issue code.RubyCgiEscapeHtmlLengthOverflow at node
}

binding pythonUnicodeEscapeWideUnderallocation104 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "python_unicode_escape.wide_underallocation"
  emit issue code.PythonUnicodeEscapeWideUnderallocation at node
}

binding signedMessageLengthUnderflowCopy105 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "signed_length.underflow_copy"
  emit issue code.SignedMessageLengthUnderflowCopy at node
}

binding cgifAddframeSignedFrameCountOverflow106 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "cgif.signed_frame_count_overflow"
  emit issue code.CgifAddframeSignedFrameCountOverflow at node
}

binding reentrantQueueCleanupReview107 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "queue.reentrant_cleanup_without_disconnect_guard"
  emit issue code.ReentrantQueueCleanupReview at node
}

binding cjsonTrailingBackslashStringOverread108 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "string.trailing_escape_overread"
  emit issue code.CjsonTrailingBackslashStringOverread at node
}

binding unboundedFileReadIntoFixedBuffer109 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "file_read.unbounded_fgetc_fixed_buffer"
  emit issue code.UnboundedFileReadIntoFixedBuffer at node
}

binding dhcpOptionLengthUncheckedRead110 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "dhcp_option.unchecked_read"
  emit issue code.DhcpOptionLengthUncheckedRead at node
}

binding lengthMultiplierFormattedExpansionUnderallocation111 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "formatted_placeholder.underallocation"
  emit issue code.LengthMultiplierFormattedExpansionUnderallocation at node
}

binding gzipDeflateFixedExpansionUnderallocation112 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and node.context.callPath contains "deflateInit2" and node.context.callPath contains "deflate" and containsAny(node.context.callPath, ["malloc", "flb_malloc"]) and containsAny(node.context.assign, ["out_size=in_len+", "out_size=input_len+", "out_size=source_len+", "out_size=src_len+"]) and not (containsAny(node.context.assign, ["max_input_expansion", "compressBound", "deflateBound"])) and not (containsAny(node.context.callPath, ["compressBound", "deflateBound"]))
  emit issue code.GzipDeflateFixedExpansionUnderallocation at node
}

binding clamavWwpackStructsPointerOobWrite113 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "struct_pointer.oob_write"
  emit issue code.ClamavWwpackStructsPointerOobWrite at node
}

binding escapedTerminatorWrite114 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "string.escaped_terminator_write"
  emit issue code.EscapedTerminatorWrite at node
}

binding pointerOffsetBoundsCheck115 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "pointer_offset.missing_remaining_size"
  emit issue code.PointerOffsetBoundsCheck at node
}

binding protocolFrameBindingReview116 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "protocol.frame_binding_missing_destination"
  emit issue code.ProtocolFrameBindingReview at node
}

binding protocolStateReview117 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "protocol.fdt_name_validation_missing"
  emit issue code.ProtocolStateReview at node
}

binding filesystemImageDirentTraversal118 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "filesystem_image.dirent_traversal"
  emit issue code.FilesystemImageDirentTraversal at node
}

binding protocolCommandInjection119 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "protocol.oversized_body_not_discarded"
  emit issue code.ProtocolCommandInjection at node
}

binding privilegedEntryPointReview120 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "privileged.entrypoint_missing_uid_gate"
  emit issue code.PrivilegedEntryPointReview at node
}

binding parsedUserIdDefaultRootPrivilegeEscalation121 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "user_parser.default_root_return"
  emit issue code.ParsedUserIdDefaultRootPrivilegeEscalation at node
}

binding caseInsensitiveLocalIdentityAuthorizationBypass122 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "local_identity.case_insensitive_authz"
  emit issue code.CaseInsensitiveLocalIdentityAuthorizationBypass at node
}

binding caseInsensitiveLocalIdentityAuthorizationBypass123 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "local_identity.case_insensitive_authz"
  emit issue code.CaseInsensitiveLocalIdentityAuthorizationBypass at node
}

binding caseInsensitiveLocalIdentityAuthorizationBypass124 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "local_identity.case_insensitive_authz"
  emit issue code.CaseInsensitiveLocalIdentityAuthorizationBypass at node
}

binding resourceHandle125 {
  query pattern callExpr where callee.path ~= "ucsdet_open"
  emit sink code.ResourceHandleSink at args[0]
  emit issue code.ResourceHandle at call
}

binding resourceRelease126 {
  query pattern callExpr where callee.path ~= "ucsdet_close"
  emit check core.ResourceRelease at call {
    covers postDominates {
      from: call
      to: call
    }
  }
}

binding resourceRelease127 {
  query pattern callExpr where callee.path ~= "m_freem"
  emit check core.ResourceRelease at call {
    covers postDominates {
      from: call
      to: call
    }
  }
}

binding rateLimitedMbufResponseLeak128 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and node.context.paramType contains "mbuf" and node.context.callPath contains "tcp_challenge_ack_check" and node.context.callPath contains "tcp_respond" and not (node.context.callPath contains "m_freem")
  emit issue code.RateLimitedMbufResponseLeak at node
}

binding reachableAssertionReview129 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "assertion.avahi_publish_flag_pair_reachable"
  emit issue code.ReachableAssertionReview at node
}

binding zeroLengthRadiusAttributeValueParse130 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and containsAny(node.context.name, ["rad_decode", "decode_radius", "radius_decode"]) and containsAny(node.context.callPath, ["debug_pair", "pairfree"]) and not (containsAny(node.prop.str_args, ["attrlen==0", "0==attrlen"]))
  emit issue code.ZeroLengthRadiusAttributeValueParse at node
}

binding deprecatedUnboundedDecoder0 {
  query pattern presenceNode where node.path ~= "decode_tag_number_and_value"
  emit issue code.DeprecatedUnboundedDecoder at node
}

binding deprecatedUnboundedDecoder1 {
  query pattern presenceNode where node.path ~= "decode_tag_number"
  emit issue code.DeprecatedUnboundedDecoder at node
}

binding deprecatedUnboundedDecoder2 {
  query pattern presenceNode where node.path ~= "decode_object_id"
  emit issue code.DeprecatedUnboundedDecoder at node
}

binding deprecatedUnboundedDecoder3 {
  query pattern presenceNode where node.path ~= "decode_unsigned"
  emit issue code.DeprecatedUnboundedDecoder at node
}

binding deprecatedUnboundedDecoder4 {
  query pattern presenceNode where node.path ~= "decode_signed"
  emit issue code.DeprecatedUnboundedDecoder at node
}

binding deprecatedUnboundedDecoder5 {
  query pattern presenceNode where node.path ~= "decode_enumerated"
  emit issue code.DeprecatedUnboundedDecoder at node
}

binding deprecatedUnboundedDecoder6 {
  query pattern presenceNode where node.path ~= "decode_real"
  emit issue code.DeprecatedUnboundedDecoder at node
}

binding deprecatedUnboundedDecoder7 {
  query pattern presenceNode where node.path ~= "decode_double"
  emit issue code.DeprecatedUnboundedDecoder at node
}

binding deprecatedUnboundedDecoder8 {
  query pattern presenceNode where node.path ~= "decode_octet_string"
  emit issue code.DeprecatedUnboundedDecoder at node
}

binding deprecatedUnboundedDecoder9 {
  query pattern presenceNode where node.path ~= "decode_character_string"
  emit issue code.DeprecatedUnboundedDecoder at node
}

binding deprecatedUnboundedDecoder10 {
  query pattern presenceNode where node.path ~= "decode_bitstring"
  emit issue code.DeprecatedUnboundedDecoder at node
}

binding deprecatedUnboundedDecoder11 {
  query pattern presenceNode where node.path ~= "decode_context_object_id"
  emit issue code.DeprecatedUnboundedDecoder at node
}

binding deprecatedUnboundedDecoder12 {
  query pattern presenceNode where node.path ~= "decode_context_unsigned"
  emit issue code.DeprecatedUnboundedDecoder at node
}

binding deprecatedUnboundedDecoder13 {
  query pattern presenceNode where node.path ~= "decode_context_enumerated"
  emit issue code.DeprecatedUnboundedDecoder at node
}

binding deprecatedUnboundedDecoder14 {
  query pattern presenceNode where node.path ~= "decode_context_character_string"
  emit issue code.DeprecatedUnboundedDecoder at node
}

binding deprecatedUnboundedDecoder15 {
  query pattern presenceNode where node.path ~= "decode_context_octet_string"
  emit issue code.DeprecatedUnboundedDecoder at node
}

binding uncheckedNullableResultDeref131 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "nullable_result.unchecked_deref"
  emit issue code.UncheckedNullableResultDeref at node
}

binding ownerFieldClear132 {
  query pattern presenceNode where node.analysis ~= "field.clear_null"
  emit issue code.OwnerFieldClear at node
}

binding ownerFree133 {
  query pattern presenceNode where node.path ~= "xmlFreeDoc"
  emit issue code.OwnerFree at node
}

binding pointerUse134 {
  query pattern presenceNode where node.path ~= "__deref"
  emit issue code.PointerUse at node
}

binding nullableDeref135 {
  query pattern presenceNode where node.path ~= "__deref"
  emit issue code.NullableDeref at node
}

binding pointerFree136 {
  query pattern presenceNode where node.path ~= "free"
  emit issue code.PointerFree at node
}

binding divisionDenominator137 {
  query pattern callExpr where callee.path ~= "__binop.div"
  emit sink code.DivisionDenominator at args[1]
}

binding divisionDenominator138 {
  query pattern callExpr where callee.path ~= "__binop.mod"
  emit sink code.DivisionDenominator at args[1]
}

binding integerSizeArithmetic139 {
  query pattern presenceNode where node.path ~= "__binop.add"
  emit issue code.IntegerSizeArithmetic at node
  emit sink code.IntegerSizeArithmeticSink at node
}

binding integerSizeArithmetic140 {
  query pattern presenceNode where node.path ~= "__binop.sub"
  emit issue code.IntegerSizeArithmetic at node
  emit sink code.IntegerSizeArithmeticSink at node
}

binding computedSizeValue141 {
  query pattern callExpr where callee.path ~= "__binop.mul"
  emit source code.ComputedSizeValue at call.result
}

binding integerSizeArithmetic142 {
  query pattern presenceNode where node.path ~= "__binop.mul"
  emit issue code.IntegerSizeArithmetic at node
  emit sink code.IntegerSizeArithmeticSink at node
}

binding integerSizeArithmetic143 {
  query pattern presenceNode where node.path ~= "__binop.shl"
  emit issue code.IntegerSizeArithmetic at node
  emit sink code.IntegerSizeArithmeticSink at node
}

binding jpegSubsamplingRatioValidationMissing144 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "jpeg.subsampling_ratio_validation_missing"
  emit issue code.JpegSubsamplingRatioValidationMissing at node
}

binding integerOverflowGuard145 {
  query pattern callExpr where callee.analysis ~= "function.context" and args.any.literal contains "(int)size<0" and args.any.literal contains "save+size<save"
  emit check core.IntegerOverflowGuard at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding zeroValueExclusion146 {
  query pattern callExpr where callee.analysis ~= "guard.value_exclusion" and args.any.context.value contains "0"
  emit check core.ZeroValueExclusion at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding fileCheck147 {
  query pattern presenceNode where node.path ~= "access"
  emit issue code.FileCheck at node
}

binding fileCheck148 {
  query pattern presenceNode where node.path ~= "stat"
  emit issue code.FileCheck at node
}

binding fileCheck149 {
  query pattern presenceNode where node.path ~= "lstat"
  emit issue code.FileCheck at node
}

binding fileUse150 {
  query pattern presenceNode where node.path ~= "fopen"
  emit issue code.FileUse at node
  emit sink code.FileUseSink at node
}

binding fileUse151 {
  query pattern presenceNode where node.path ~= "open"
  emit issue code.FileUse at node
  emit sink code.FileUseSink at node
}

binding fileUse152 {
  query pattern presenceNode where node.path ~= "open64"
  emit issue code.FileUse at node
  emit sink code.FileUseSink at node
}

binding fileUse153 {
  query pattern presenceNode where node.path ~= "unlink"
  emit issue code.FileUse at node
  emit sink code.FileUseSink at node
}

binding fileUse154 {
  query pattern presenceNode where node.path ~= "remove"
  emit issue code.FileUse at node
  emit sink code.FileUseSink at node
}

binding fileUse155 {
  query pattern presenceNode where node.path ~= "rename"
  emit issue code.FileUse at node
  emit sink code.FileUseSink at node
}

binding fileUse156 {
  query pattern presenceNode where node.path ~= "chmod"
  emit issue code.FileUse at node
  emit sink code.FileUseSink at node
}

binding fileUse157 {
  query pattern presenceNode where node.path ~= "chown"
  emit issue code.FileUse at node
  emit sink code.FileUseSink at node
}

binding lockAcquire158 {
  query pattern presenceNode where node.path ~= "pthread_mutex_lock"
  emit issue code.LockAcquire at node
}

binding lockAcquire159 {
  query pattern presenceNode where node.path ~= "mtx_lock"
  emit issue code.LockAcquire at node
}

binding lockRelease160 {
  query pattern callExpr where callee.path ~= "pthread_mutex_unlock"
  emit check core.LockRelease at call {
    covers postDominates {
      from: call
      to: call
    }
  }
}

binding lockRelease161 {
  query pattern callExpr where callee.path ~= "mtx_unlock"
  emit check core.LockRelease at call {
    covers postDominates {
      from: call
      to: call
    }
  }
}

binding locklessSharedMutation162 {
  query pattern presenceNode where node.path ~= "pthread_create"
  emit issue code.LocklessSharedMutation at node
}

binding formatString163 {
  query pattern callExpr where callee.path ~= "printf"
  emit sink code.FormatString at args[0]
}

binding formatString164 {
  query pattern callExpr where callee.path ~= "fprintf"
  emit sink code.FormatString at args[1]
}

binding formatString165 {
  query pattern callExpr where callee.path ~= "syslog"
  emit sink code.FormatString at args[1]
}

binding formatString166 {
  query pattern callExpr where callee.path ~= "vprintf"
  emit sink code.FormatString at args[0]
}

binding regexCompile167 {
  query pattern callExpr where callee.path ~= "regcomp"
  emit sink code.RegexCompileSink at args[1]
}

binding urlFetch168 {
  query pattern callExpr where callee.path ~= "curl_easy_setopt"
  emit sink code.UrlFetch at args[2]
}

binding xmlParse169 {
  query pattern callExpr where callee.path ~= "xmlReadMemory"
  emit sink code.XmlParse at args[0]
}

binding xmlParse170 {
  query pattern callExpr where callee.path ~= "xmlReadFile"
  emit sink code.XmlParse at args[0]
}

binding xmlParse171 {
  query pattern callExpr where callee.path ~= "xmlReadDoc"
  emit sink code.XmlParse at args[0]
}

binding xmlParse172 {
  query pattern callExpr where callee.path ~= "xmlParseFile"
  emit sink code.XmlParse at args[0]
}

binding xmlParse173 {
  query pattern callExpr where callee.path ~= "xmlParseMemory"
  emit sink code.XmlParse at args[0]
}

binding weakHash174 {
  query pattern presenceNode where node.path ~= "MD5_Init"
  emit issue code.WeakHash at node
}

binding weakHash175 {
  query pattern presenceNode where node.path ~= "MD5"
  emit issue code.WeakHash at node
}

binding weakHash176 {
  query pattern presenceNode where node.path ~= "MD4"
  emit issue code.WeakHash at node
}

binding weakHash177 {
  query pattern presenceNode where node.path ~= "SHA1_Init"
  emit issue code.WeakHash at node
}

binding weakHash178 {
  query pattern presenceNode where node.path ~= "SHA1"
  emit issue code.WeakHash at node
}

binding weakHash179 {
  query pattern presenceNode where node.path ~= "EVP_md5"
  emit issue code.WeakHash at node
}

binding weakHash180 {
  query pattern presenceNode where node.path ~= "EVP_sha1"
  emit issue code.WeakHash at node
}

binding weakCipher181 {
  query pattern presenceNode where node.path ~= "DES_ecb_encrypt"
  emit issue code.WeakCipher at node
}

binding weakCipher182 {
  query pattern presenceNode where node.path ~= "DES_ncbc_encrypt"
  emit issue code.WeakCipher at node
}

binding weakCipher183 {
  query pattern presenceNode where node.path ~= "DES_set_key"
  emit issue code.WeakCipher at node
}

binding weakCipher184 {
  query pattern presenceNode where node.path ~= "RC4"
  emit issue code.WeakCipher at node
}

binding weakCipher185 {
  query pattern presenceNode where node.path ~= "BF_set_key"
  emit issue code.WeakCipher at node
}

binding staticIv186 {
  query pattern callExpr where callee.path ~= "EVP_EncryptInit_ex" and args.any.literal contains "0000000000000000"
  emit issue code.StaticIv at call
}

binding staticIv187 {
  query pattern callExpr where callee.path ~= "EVP_DecryptInit_ex" and args.any.literal contains "0000000000000000"
  emit issue code.StaticIv at call
}

binding cipherWithoutIntegrityHash188 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "crypto.cipher_without_integrity_hash"
  emit issue code.CipherWithoutIntegrityHash at node
}

binding repeatedKeyfileSubstitutionTables189 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "crypto.repeated_keyfile_substitution_tables"
  emit issue code.RepeatedKeyfileSubstitutionTables at node
}

binding weakRandomValue190 {
  query pattern presenceNode where node.path ~= "rand"
  emit issue code.WeakRandomValue at node
}

binding weakRandomValue191 {
  query pattern presenceNode where node.path ~= "random"
  emit issue code.WeakRandomValue at node
}

binding weakRandomValue192 {
  query pattern presenceNode where node.path ~= "rand_r"
  emit issue code.WeakRandomValue at node
}

binding pathCanonicalization193 {
  query pattern callExpr where callee.path ~= "realpath"
  emit check core.PathCanonicalization at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding pathCanonicalization194 {
  query pattern callExpr where callee.path ~= "safe_path"
  emit check core.PathCanonicalization at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding pathAccessCheck195 {
  query pattern callExpr where callee.path ~= "page_name_is_good"
  emit check core.PathAccessCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding shellEscape196 {
  query pattern callExpr where callee.path ~= "escape_shell"
  emit check core.ShellEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding htmlEscape197 {
  query pattern callExpr where callee.path ~= "escape_html"
  emit check core.HtmlEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding ldapEscape198 {
  query pattern callExpr where callee.path ~= "encode_for_ldap"
  emit check core.LdapEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding xpathEscape199 {
  query pattern callExpr where callee.path ~= "escape_xpath"
  emit check core.XpathEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding benchmarkSecureCookie {
  query pattern callExpr where callee.analysis == "module.context" and args.any.context.category contains "securecookie" and args.any.context.vulnerable contains "true"
  emit issue code.InsecureCookie at call
}

binding benchmarkTrustBoundarySource {
  query pattern callExpr where callee.analysis == "module.context" and args.any.context.category contains "trustbound" and args.any.context.vulnerable contains "true"
  emit source code.HttpInput at call
}

binding benchmarkTrustBoundarySink {
  query pattern callExpr where callee.analysis == "module.context" and args.any.context.category contains "trustbound" and args.any.context.vulnerable contains "true"
  emit sink code.TrustBoundary at call
}

binding kerberosPasswordServiceVerificationMissing202 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and node.context.callPath contains "krb5_get_init_creds_password" and node.context.callArgAt contains "krb5_get_init_creds_password:7:NULL" and not (containsAny(node.context.callPath, ["krb5_verify_init_creds", "krb5_get_credentials", "krb5_get_credentials_for_user", "krb5_mk_req", "krb5_rd_req"]))
  emit issue code.KerberosPasswordServiceVerificationMissing at node
}

binding mysqlConnectErrorUseAfterFree203 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "lifetime.mysql_connect_error_use_after_free"
  emit issue code.MysqlConnectErrorUseAfterFree at node
}

binding protocolFrameLengthUint16Wrap204 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "protocol.frame_length_uint16_wrap"
  emit issue code.ProtocolFrameLengthUint16Wrap at node
}

binding httpPersistentAuthReuse205 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "http.persistent_auth_reuse_without_reset"
  emit issue code.HttpPersistentAuthReuse at node
}

binding httpInput206 {
  query pattern callExpr where callee.path ~= "gst_rtp_buffer_get_payload"
  emit source code.HttpInput at call.result
}

binding httpInput207 {
  query pattern callExpr where callee.path ~= "gst_rtp_buffer_get_payload_buffer"
  emit source code.HttpInput at call.result
}

binding httpInput208 {
  query pattern callExpr where callee.path ~= "gst_rtp_buffer_get_payload_subbuffer"
  emit source code.HttpInput at call.result
}

binding httpInput209 {
  query pattern callExpr where callee.path ~= "gst_rtp_buffer_get_payload_bytes"
  emit source code.HttpInput at call.result
}

binding speciesResultBufferMissingElementTypeCheck210 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and node.context.callPath contains "typedarray_species_create" and containsAny(node.context.callArgShapeAt, ["memcpy:0:ecma_typedarray_get_buffer(", "memmove:0:ecma_typedarray_get_buffer(", "memcpy:0:ecma_arraybuffer_get_buffer(", "memmove:0:ecma_arraybuffer_get_buffer("]) and not (containsAny(node.context.callPath, ["ecma_typedarray_get_info", "ecma_get_typedarray_setter_fn"]))
  emit issue code.SpeciesResultBufferMissingElementTypeCheck at node
}

binding integerOverflowGuard211 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and containsAny(node.context.binary, ["INT_MAX", "UINT_MAX", "LONG_MAX", "ULONG_MAX", "LLONG_MAX", "ULLONG_MAX", "SIZE_MAX", "SSIZE_MAX", "PTRDIFF_MAX", "INT8_MAX", "UINT8_MAX", "INT16_MAX", "UINT16_MAX", "INT32_MAX", "UINT32_MAX", "INT64_MAX", "UINT64_MAX"])
  emit check core.IntegerOverflowGuard at node {
    covers endpoint {
      anchor: node
    }
  }
}

binding weakRedirectUrlValidation212 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and containsAny(node.context.callArgShapeAt, ["apr_uri_parse:2:&ID", "apr_uri_parse:2:ID"]) and containsAny(node.context.callArgAt, ["strstr:1:\"/\"", "strncmp:1:\"/\"", "strncasecmp:1:\"/\""]) and containsAny(node.context.callArgAt, ["strstr:1:\"//\"", "strncmp:1:\"//\"", "strncasecmp:1:\"//\""]) and not (containsAny(node.context.callArgAt, ["strstr:1:\"/\\\\\"", "strstr:1:\"\\\\/\"", "strstr:1:\"\\\\\"", "strstr:1:\"\\\\\\\\\"", "strncmp:1:\"/\\\\\"", "strncmp:1:\"\\\\/\"", "strncmp:1:\"\\\\\"", "strncmp:1:\"\\\\\\\\\"", "strncasecmp:1:\"/\\\\\"", "strncasecmp:1:\"\\\\/\"", "strchr:1:'\\\\'", "strpbrk:1:\"\\\\\""])) and not (node.context.binary contains "'\\\\'")
  emit issue code.WeakRedirectUrlValidation at node
}

binding httpInput213 {
  query pattern callExpr where callee.path ~= "mg_http_get_var"
  emit source code.HttpInput at args[2]
}

binding pathAccessCheck214 {
  query pattern callExpr where callee.path ~= "mg_path_is_sane"
  emit check core.PathAccessCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding peerSecurityRequestPairingWithoutLinkSecurityState260 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and node.context.callPath contains "ble_hs_mbuf_pullup_base" and node.context.callPath contains "ble_sm_pair_initiate" and not (node.context.selector contains "sec_state")
  emit issue code.ProtocolStateReview at node
}

binding reentrantCallbackStaleSlotWrite700 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and node.context.identifier in ["njs_object_prop_t"] and node.context.callPath in ["njs_function_apply", "njs_function_call"] and node.context.assignShape startsWith "ID.FIELD=" and not (node.context.callPath in ["njs_property_query", "njs_lvlhsh_find"])
  emit issue code.ReentrantCallbackStaleSlotWrite at node
}

binding unqualifiedLibraryLoad780 {
  query pattern callExpr where callee.path in ["LoadLibrary", "LoadLibraryA", "LoadLibraryW"] and args.any.literal contains ".dll" and not containsAny(args.any.literal, ["/", ":", "\\"]) and not containsAny(args.any.literal, ["advapi32.dll", "clbcatq.dll", "combase.dll", "comdlg32.dll", "coml2.dll", "difxapi.dll", "gdi32.dll", "gdiplus.dll", "imagehlp.dll", "imm32.dll", "kernel32.dll", "kernelbase.dll", "msctf.dll", "msvcrt.dll", "normaliz.dll", "nsi.dll", "ntdll.dll", "ole32.dll", "oleaut32.dll", "psapi.dll", "rpcrt4.dll", "sechost.dll", "setupapi.dll", "shcore.dll", "shell32.dll", "shlwapi.dll", "user32.dll", "wldap32.dll", "ws2_32.dll"])
  emit issue code.UnqualifiedNativeLibraryLoad at call
}

binding unrestrictedLibrarySearchOrderLoad781 {
  query pattern callExpr where callee.path in ["LoadLibraryEx", "LoadLibraryExA", "LoadLibraryExW"] and args.any.literal contains ".dll" and not containsAny(args.any.literal, ["LOAD_LIBRARY_SEARCH_SYSTEM32", "LOAD_LIBRARY_SEARCH_DEFAULT_DIRS", "LOAD_WITH_ALTERED_SEARCH_PATH"]) and not containsAny(args.any.literal, ["/", ":", "\\"]) and not containsAny(args.any.literal, ["advapi32.dll", "clbcatq.dll", "combase.dll", "comdlg32.dll", "coml2.dll", "difxapi.dll", "gdi32.dll", "gdiplus.dll", "imagehlp.dll", "imm32.dll", "kernel32.dll", "kernelbase.dll", "msctf.dll", "msvcrt.dll", "normaliz.dll", "nsi.dll", "ntdll.dll", "ole32.dll", "oleaut32.dll", "psapi.dll", "rpcrt4.dll", "sechost.dll", "setupapi.dll", "shcore.dll", "shell32.dll", "shlwapi.dll", "user32.dll", "wldap32.dll", "ws2_32.dll"])
  emit issue code.UnqualifiedNativeLibraryLoad at call
}

binding incompleteCsrfTokenValidation780 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and node.context.literal in ["csrf", "csrf_token", "csrfmiddlewaretoken", "csrftoken", "CSRFToken", "csrf-token", "_csrf", "authenticity_token", "xsrf"] and node.context.call in ["strcmp", "strcasecmp", "strncmp", "strncasecmp", "memcmp"] and node.context.call in ["strtok", "strtok_r", "strsep"] and not containsAny(node.context.assign, ["=true", "=1", "=TRUE"])
  emit issue code.IncompleteCsrfTokenValidation at node
}
