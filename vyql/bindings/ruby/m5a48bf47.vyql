module bindings.ruby.generated.m5a48bf47;

pattern bindingMetadata {
  binding: {
    name: "ruby"
    meta: {
      fidelity: "resolved"
    }
  }
}

binding cliArgument0 {
  query pattern callExpr where callee.path ~= "ARGV"
  emit source code.CliArgument at call.result
}

binding stdinInput1 {
  query pattern callExpr where callee.path ~= "gets"
  emit source code.StdinInput at call.result
}

binding stdinInput2 {
  query pattern callExpr where callee.path ~= "STDIN"
  emit source code.StdinInput at call.result
}

binding httpInput3 {
  query pattern callExpr where callee.path ~= "params"
  emit source code.HttpInput at call.result
}

binding httpInput4 {
  query pattern callExpr where callee.path ~= "cookies"
  emit source code.HttpInput at call.result
}

binding httpInput5 {
  query pattern callExpr where callee.path ~= "session"
  emit source code.HttpInput at call.result
}

binding httpInput6 {
  query pattern callExpr where callee.path ~= "request.params"
  emit source code.HttpInput at call.result
}

binding httpInput7 {
  query pattern callExpr where callee.path ~= "request.body"
  emit source code.HttpInput at call.result
}

binding httpInput8 {
  query pattern callExpr where callee.path ~= "request.query_parameters"
  emit source code.HttpInput at call.result
}

binding httpInput9 {
  query pattern callExpr where callee.path ~= "request.request_parameters"
  emit source code.HttpInput at call.result
}

binding httpInput10 {
  query pattern callExpr where callee.path ~= "request.GET"
  emit source code.HttpInput at call.result
}

binding httpInput11 {
  query pattern callExpr where callee.path ~= "request.POST"
  emit source code.HttpInput at call.result
}

binding httpInput12 {
  query pattern callExpr where callee.path ~= "request.env"
  emit source code.HttpInput at call.result
}

binding httpInput13 {
  query pattern callExpr where callee.path ~= "request.headers"
  emit source code.HttpInput at call.result
}

binding httpInput14 {
  query pattern callExpr where callee.path ~= "request.path_parameters"
  emit source code.HttpInput at call.result
}

binding httpInput15 {
  query pattern callExpr where callee.path ~= "request.raw_post"
  emit source code.HttpInput at call.result
}

binding httpInput16 {
  query pattern callExpr where callee.analysis ~= "parameter.entry" and args.any.context.functionName contains "process" and args.any.context.paramName contains "target"
  emit source code.HttpInput at call.result
}

binding httpInput17 {
  query pattern callExpr where callee.path ~= "request.query_string"
  emit source code.HttpInput at call.result
}

binding databaseRead18 {
  query pattern callExpr where callee.method == "find_by"
  emit source code.DatabaseRead at call.result
}

binding databaseRead19 {
  query pattern callExpr where callee.method == "find_by_sql"
  emit source code.DatabaseRead at call.result
}

binding databaseRead20 {
  query pattern callExpr where callee.method == "where"
  emit source code.DatabaseRead at call.result
}

binding databaseRead21 {
  query pattern callExpr where callee.method == "pluck"
  emit source code.DatabaseRead at call.result
}

binding secretValue22 {
  query pattern callExpr where callee.path ~= "ENV"
  emit source code.SecretValue at call.result
}

binding secretValue23 {
  query pattern callExpr where callee.path ~= "Rails.application.secrets"
  emit source code.SecretValue at call.result
}

binding secretValue24 {
  query pattern callExpr where callee.path ~= "credentials"
  emit source code.SecretValue at call.result
}

binding hardcodedSecret25 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "ruby" and containsAny(node.context.assign, ["config.secret_token="]) and not (containsAny(node.context.assign, ["config.secret_token=ENV", "config.secret_token=Rails.application.credentials", "config.secret_token=credentials"])) and not (node.context.expr contains "Rails.env=='test'") and not (node.context.callPath contains "ENV.fetch") and not (node.context.callPath contains "ENV.[]") and not (node.context.callPath contains "SecureRandom.hex")
  emit issue code.HardcodedSecret at node
}

binding sqlExecution26 {
  query pattern callExpr where callee.method == "where"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution27 {
  query pattern callExpr where callee.method == "execute"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution28 {
  query pattern callExpr where callee.method == "find_by_sql"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution29 {
  query pattern callExpr where callee.method == "exec_query"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution30 {
  query pattern callExpr where callee.method == "select_all"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution31 {
  query pattern callExpr where callee.method == "select_rows"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution32 {
  query pattern callExpr where callee.method == "select_values"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution33 {
  query pattern callExpr where callee.method == "select_one"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution34 {
  query pattern callExpr where callee.method == "delete_all"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution35 {
  query pattern callExpr where callee.method == "update_all"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution36 {
  query pattern callExpr where callee.method == "pluck"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution37 {
  query pattern callExpr where callee.method == "count_by_sql"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution38 {
  query pattern callExpr where callee.method == "order"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution39 {
  query pattern callExpr where callee.method == "group"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution40 {
  query pattern callExpr where callee.method == "having"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution41 {
  query pattern callExpr where callee.method == "from"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution42 {
  query pattern callExpr where callee.method == "joins"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution43 {
  query pattern callExpr where callee.method == "exec_update"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution44 {
  query pattern callExpr where callee.method == "exec_delete"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution45 {
  query pattern callExpr where callee.method == "exec_insert"
  emit sink code.SqlExecution at args[0]
}

binding objectLookupByUserInput46 {
  query pattern callExpr where callee.method == "find"
  emit sink code.ObjectLookupByUserInput at args[0]
}

binding objectLookupByUserInput47 {
  query pattern callExpr where callee.method == "find_by_id"
  emit sink code.ObjectLookupByUserInput at args[0]
}

binding objectLookupByUserInput48 {
  query pattern callExpr where callee.method == "find_by!"
  emit sink code.ObjectLookupByUserInput at args[0]
}

binding stateChangingOp49 {
  query pattern callExpr where callee.method == "change_password"
  emit sink code.StateChangingOpSink at args[0]
}

binding stateChangingOp50 {
  query pattern callExpr where callee.method == "update_password"
  emit sink code.StateChangingOpSink at args[0]
}

binding stateChangingOp51 {
  query pattern callExpr where callee.method == "reset_password"
  emit sink code.StateChangingOpSink at args[0]
}

binding stateChangingOp52 {
  query pattern callExpr where callee.method == "change_email"
  emit sink code.StateChangingOpSink at args[0]
}

binding stateChangingOp53 {
  query pattern callExpr where callee.method == "transfer_funds"
  emit sink code.StateChangingOpSink at args[0]
}

binding stateChangingOp54 {
  query pattern callExpr where callee.method == "delete_account"
  emit sink code.StateChangingOpSink at args[0]
}

binding stateChangingOp55 {
  query pattern callExpr where callee.method == "grant_role"
  emit sink code.StateChangingOpSink at args[0]
}

binding stateChangingOp56 {
  query pattern callExpr where callee.method == "make_admin"
  emit sink code.StateChangingOpSink at args[0]
}

binding authenticationRequiredOp57 {
  query pattern presenceNode where node.path ~= "change_password"
  emit issue code.AuthenticationRequiredOp at node
}

binding authenticationRequiredOp58 {
  query pattern presenceNode where node.path ~= "update_password"
  emit issue code.AuthenticationRequiredOp at node
}

binding authenticationRequiredOp59 {
  query pattern presenceNode where node.path ~= "reset_password"
  emit issue code.AuthenticationRequiredOp at node
}

binding authenticationRequiredOp60 {
  query pattern presenceNode where node.path ~= "change_email"
  emit issue code.AuthenticationRequiredOp at node
}

binding authenticationRequiredOp61 {
  query pattern presenceNode where node.path ~= "transfer_funds"
  emit issue code.AuthenticationRequiredOp at node
}

binding authenticationRequiredOp62 {
  query pattern presenceNode where node.path ~= "delete_account"
  emit issue code.AuthenticationRequiredOp at node
}

binding authenticationRequiredOp63 {
  query pattern presenceNode where node.path ~= "grant_role"
  emit issue code.AuthenticationRequiredOp at node
}

binding authenticationRequiredOp64 {
  query pattern presenceNode where node.path ~= "make_admin"
  emit issue code.AuthenticationRequiredOp at node
}

binding csrfProtection65 {
  query pattern callExpr where callee.path ~= "protect_from_forgery"
  emit check core.CsrfProtection at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding csrfProtection66 {
  query pattern callExpr where callee.path ~= "verify_authenticity_token"
  emit check core.CsrfProtection at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding csrfProtection67 {
  query pattern callExpr where callee.path ~= "validate_csrf_token"
  emit check core.CsrfProtection at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding authenticationCheck68 {
  query pattern callExpr where callee.path ~= "authenticate_user!"
  emit check core.AuthenticationCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding authenticationCheck69 {
  query pattern callExpr where callee.path ~= "require_auth"
  emit check core.AuthenticationCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding authenticationCheck70 {
  query pattern callExpr where callee.path ~= "ensure_authenticated"
  emit check core.AuthenticationCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding authenticationCheck71 {
  query pattern callExpr where callee.path ~= "signed_in?"
  emit check core.AuthenticationCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding authorizationCheck72 {
  query pattern callExpr where callee.path ~= "action_auth_level"
  emit check core.AuthorizationCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding authorizationCheck73 {
  query pattern callExpr where callee.path ~= "authorize"
  emit check core.AuthorizationCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding authorizationCheck74 {
  query pattern callExpr where callee.path ~= "authorize!"
  emit check core.AuthorizationCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding ownershipCheck75 {
  query pattern callExpr where callee.path ~= "check_ownership"
  emit check core.OwnershipCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding ownershipCheck76 {
  query pattern callExpr where callee.path ~= "require_owner"
  emit check core.OwnershipCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding ownershipCheck77 {
  query pattern callExpr where callee.path ~= "owns_resource?"
  emit check core.OwnershipCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding sqlExecution78 {
  query pattern callExpr where callee.path ~= "DB.fetch"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution79 {
  query pattern callExpr where callee.path ~= "DB.run"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution80 {
  query pattern callExpr where callee.path ~= "DB.execute"
  emit sink code.SqlExecution at args[0]
}

binding commandExecution81 {
  query pattern callExpr where callee.path ~= "system"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution82 {
  query pattern callExpr where callee.path ~= "exec"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution83 {
  query pattern callExpr where callee.path ~= "spawn"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution84 {
  query pattern callExpr where callee.path ~= "Kernel.system"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution85 {
  query pattern callExpr where callee.path ~= "Kernel.exec"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution86 {
  query pattern callExpr where callee.path ~= "Kernel.spawn"
  emit sink code.CommandExecution at args[0]
}

binding rubyPipeOpenCommandExecution87 {
  query pattern callExpr where callee.path ~= "Kernel.open"
  emit sink code.RubyPipeOpenCommandExecution at args[0]
}

binding commandExecution88 {
  query pattern callExpr where callee.path ~= "IO.popen"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution89 {
  query pattern callExpr where callee.path ~= "IO.foreach"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution90 {
  query pattern callExpr where callee.path ~= "URI.open"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution91 {
  query pattern callExpr where callee.path ~= "Open3.capture2"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution92 {
  query pattern callExpr where callee.path ~= "Open3.capture2e"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution93 {
  query pattern callExpr where callee.path ~= "Open3.capture3"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution94 {
  query pattern callExpr where callee.path ~= "Open3.popen2"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution95 {
  query pattern callExpr where callee.path ~= "Open3.popen3"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution96 {
  query pattern callExpr where callee.path ~= "Open3.pipeline"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution97 {
  query pattern callExpr where callee.path ~= "Process.spawn"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution98 {
  query pattern callExpr where callee.path ~= "PTY.spawn"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution99 {
  query pattern callExpr where callee.path ~= "syscall"
  emit sink code.CommandExecution at args[0]
}

binding codeEval100 {
  query pattern callExpr where callee.path ~= "eval"
  emit sink code.CodeEval at args[0]
}

binding codeEval101 {
  query pattern callExpr where callee.path ~= "instance_eval"
  emit sink code.CodeEval at args[0]
}

binding codeEval102 {
  query pattern callExpr where callee.path ~= "class_eval"
  emit sink code.CodeEval at args[0]
}

binding codeEval103 {
  query pattern callExpr where callee.path ~= "module_eval"
  emit sink code.CodeEval at args[0]
}

binding codeEval104 {
  query pattern callExpr where callee.path ~= "instance_exec"
  emit sink code.CodeEval at args[0]
}

binding codeEval105 {
  query pattern callExpr where callee.path ~= "class_exec"
  emit sink code.CodeEval at args[0]
}

binding codeEval106 {
  query pattern callExpr where callee.path ~= "binding.eval"
  emit sink code.CodeEval at args[0]
}

binding deserialization107 {
  query pattern callExpr where callee.path ~= "Marshal.load"
  emit sink code.Deserialization at args[0]
}

binding deserialization108 {
  query pattern callExpr where callee.path ~= "Marshal.restore"
  emit sink code.Deserialization at args[0]
}

binding deserialization109 {
  query pattern callExpr where callee.path ~= "YAML.load"
  emit sink code.Deserialization at args[0]
}

binding deserialization110 {
  query pattern callExpr where callee.path ~= "YAML.load_stream"
  emit sink code.Deserialization at args[0]
}

binding deserialization111 {
  query pattern callExpr where callee.path ~= "Psych.load"
  emit sink code.Deserialization at args[0]
}

binding deserialization112 {
  query pattern callExpr where callee.path ~= "Oj.load"
  emit sink code.Deserialization at args[0]
}

binding deserialization113 {
  query pattern callExpr where callee.path ~= "JSON.load"
  emit sink code.Deserialization at args[0]
}

binding deserialization114 {
  query pattern callExpr where callee.path ~= "ActiveSupport.JSON.decode"
  emit sink code.Deserialization at args[0]
}

binding urlFetch115 {
  query pattern callExpr where callee.path ~= "Net.HTTP.get"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch116 {
  query pattern callExpr where callee.path ~= "Net.HTTP.get_response"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch117 {
  query pattern callExpr where callee.path ~= "Net.HTTP.start"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch118 {
  query pattern callExpr where callee.path ~= "Net.HTTP.post"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch119 {
  query pattern callExpr where callee.path ~= "HTTParty.get"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch120 {
  query pattern callExpr where callee.path ~= "HTTParty.post"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch121 {
  query pattern callExpr where callee.path ~= "RestClient.get"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch122 {
  query pattern callExpr where callee.path ~= "RestClient.post"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch123 {
  query pattern callExpr where callee.path ~= "open"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch124 {
  query pattern callExpr where callee.path ~= "URI.open"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch125 {
  query pattern callExpr where callee.path ~= "URI.parse"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch126 {
  query pattern callExpr where callee.path ~= "Faraday.get"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch127 {
  query pattern callExpr where callee.path ~= "Faraday.post"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch128 {
  query pattern callExpr where callee.path ~= "Excon.get"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch129 {
  query pattern callExpr where callee.path ~= "Typhoeus.get"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch130 {
  query pattern callExpr where callee.path ~= "HTTP.get"
  emit sink code.UrlFetch at args[0]
}

binding filePathAccess131 {
  query pattern callExpr where callee.path ~= "File.read"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess132 {
  query pattern callExpr where callee.path ~= "File.open"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess133 {
  query pattern callExpr where callee.path ~= "File.write"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess134 {
  query pattern callExpr where callee.path ~= "File.delete"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess135 {
  query pattern callExpr where callee.path ~= "File.new"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess136 {
  query pattern callExpr where callee.path ~= "IO.read"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess137 {
  query pattern callExpr where callee.path ~= "IO.write"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess138 {
  query pattern callExpr where callee.path ~= "FileUtils.cp"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess139 {
  query pattern callExpr where callee.path ~= "FileUtils.mv"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess140 {
  query pattern callExpr where callee.path ~= "FileUtils.rm"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess141 {
  query pattern callExpr where callee.path ~= "Dir.glob"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess142 {
  query pattern callExpr where callee.path ~= "Dir.entries"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess143 {
  query pattern callExpr where callee.path ~= "send_file"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess144 {
  query pattern callExpr where callee.path ~= "send_data"
  emit sink code.FilePathAccess at args[0]
}

binding dynamicCodeLoad145 {
  query pattern callExpr where callee.path ~= "require"
  emit sink code.DynamicCodeLoad at args[0]
}

binding dynamicCodeLoad146 {
  query pattern callExpr where callee.path ~= "load"
  emit sink code.DynamicCodeLoad at args[0]
}

binding unsafeUpload147 {
  query pattern callExpr where callee.path ~= "uploaded_file.save"
  emit sink code.UnsafeUpload at args[0]
}

binding unsafeUpload148 {
  query pattern callExpr where callee.path ~= "file.save"
  emit sink code.UnsafeUpload at args[0]
}

binding unsafeUpload149 {
  query pattern callExpr where callee.path ~= "File.write"
  emit sink code.UnsafeUpload at args[0]
}

binding archiveEntryWrite150 {
  query pattern callExpr where callee.method == "extract"
  emit sink code.ArchiveEntryWrite at args[0]
}

binding archiveEntryWrite151 {
  query pattern callExpr where callee.path ~= "Zip.File.extract"
  emit sink code.ArchiveEntryWrite at args[0]
}

binding htmlRender152 {
  query pattern callExpr where callee.path ~= "raw"
  emit sink code.HtmlRender at args[0]
}

binding htmlRender153 {
  query pattern callExpr where callee.path ~= "html_safe"
  emit sink code.HtmlRender at args[0]
}

binding htmlRender154 {
  query pattern callExpr where callee.path ~= "render"
  emit sink code.HtmlRender at args[0]
}

binding htmlRender155 {
  query pattern callExpr where callee.path ~= "content_tag"
  emit sink code.HtmlRender at args[0]
}

binding htmlRender156 {
  query pattern callExpr where callee.path == "concat"
  emit sink code.HtmlRender at args[0]
}

binding htmlRender157 {
  query pattern callExpr where callee.path ~= "safe_concat"
  emit sink code.HtmlRender at args[0]
}

binding erbHrefInterpolationHtmlEscapeMissing158 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "erb.unescaped_href_interpolation" and node.context.template == "erb" and node.context.attr == "href"
  emit issue code.ErbHrefInterpolationHtmlEscapeMissing at node
}

binding erbHrefUrlHtmlEscapeOnly159 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "erb.html_escaped_url_href" and node.context.template == "erb" and node.context.attr == "href" and node.context.erbValue == "url-like"
  emit issue code.ErbHrefUrlHtmlEscapeOnly at node
}

binding railsRawTranslationHtmlInterpolationXss160 {
  query pattern presenceNode where node.kind == "call" and node.path ~= "raw" and not (containsAny(node.context.scopeCall, [content_tag, "ERB::Util.html_escape", "ERB::Util.h", CGI.escapeHTML, Rack.Utils.escape_html])) and operand(node, where: operand.path ~= "t")
  emit issue code.RailsRawTranslationHtmlInterpolationXss at node
}

binding storedHtmlWrite161 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "ruby" and node.context.rubyReview == "stored_html_interpolation_missing_sanitize"
  emit issue code.StoredHtmlWrite at node
}

binding unparameterizedSqlQueryParser162 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "ruby" and node.context.rubyReview == "active_record_find_by_request_hash"
  emit issue code.UnparameterizedSqlQueryParser at node
}

binding chefUserManagementWithoutAdminGuard163 {
  query pattern presenceNode where node.scope == "class" and node.context.language == "ruby" and node.context.className == "Users" and node.context.rubyReview == "chef_user_management_missing_admin_guard"
  emit issue code.ChefUserManagementWithoutAdminGuard at node
}

binding persistentConnectionReuseAfterInterruptedResponse164 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "ruby" and node.context.functionName == "request" and node.context.rubyReview == "persistent_response_reuse_missing_guard"
  emit issue code.PersistentConnectionReuseAfterInterruptedResponse at node
}

binding unvalidatedCoordinateSqlInterpolation165 {
  query pattern presenceNode where node.scope == "function" and node.context.literal contains "BETWEEN" and node.context.language == "ruby" and containsAny(node.context.functionName, ["within_bounding_box", "bounding_box", "bbox"]) and containsAny(node.context.paramName, ["sw_lat", "southwest_lat", "south_lat", "min_lat", "lat_min"]) and containsAny(node.context.paramName, ["ne_lat", "northeast_lat", "north_lat", "max_lat", "lat_max"]) and containsAny(node.context.paramName, ["sw_lng", "sw_lon", "southwest_lng", "southwest_lon", "west_lng", "west_lon", "min_lng", "min_lon", "lng_min", "lon_min"]) and containsAny(node.context.paramName, ["ne_lng", "ne_lon", "northeast_lng", "northeast_lon", "east_lng", "east_lon", "max_lng", "max_lon", "lng_max", "lon_max"]) and not (containsAny(node.context.callPath, ["sw_lat.to_f", "southwest_lat.to_f", "south_lat.to_f", "min_lat.to_f", "lat_min.to_f"])) and not (containsAny(node.context.callPath, ["ne_lat.to_f", "northeast_lat.to_f", "north_lat.to_f", "max_lat.to_f", "lat_max.to_f"]))
  emit issue code.UnvalidatedCoordinateSqlInterpolation at node
}

binding unboundedNaturalLanguageScheduleParse166 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "ruby" and node.context.functionName == "parse" and node.context.callPath contains "s.strip" and node.context.callPath contains "Parser.parse" and node.context.callPath contains "opts.merge" and node.context.callPath contains "slots.to_crons" and not (node.context.callPath contains "s.length")
  emit issue code.UnboundedNaturalLanguageScheduleParse at node
}

binding templateRender167 {
  query pattern callExpr where callee.path ~= "ERB.new"
  emit sink code.TemplateRender at args[0]
}

binding templateRender168 {
  query pattern callExpr where callee.path ~= "Erubi.new"
  emit sink code.TemplateRender at args[0]
}

binding templateRender169 {
  query pattern callExpr where callee.path ~= "Erubis.new"
  emit sink code.TemplateRender at args[0]
}

binding templateRender170 {
  query pattern callExpr where callee.path ~= "Liquid.Template.parse"
  emit sink code.TemplateRender at args[0]
}

binding templateRender171 {
  query pattern callExpr where callee.path ~= "Tilt.new"
  emit sink code.TemplateRender at args[0]
}

binding redirectTarget172 {
  query pattern callExpr where callee.path ~= "redirect_to"
  emit sink code.RedirectTargetSink at args[0]
}

binding redirectTarget173 {
  query pattern callExpr where callee.path ~= "redirect"
  emit sink code.RedirectTargetSink at args[0]
}

binding clockworkWebJobCsrf174 {
  query pattern presenceNode where node.scope == "class" and node.context.language == "ruby" and node.context.className == "HomeController" and node.context.rubyReview == "clockwork_job_csrf_missing_forgery_protection"
  emit issue code.ClockworkWebJobCsrf at node
}

binding railsBasicAuthWeakCsrfStrategy175 {
  query pattern presenceNode where node.scope == "class" and node.context.language == "ruby" and node.context.classBase contains "ActionController.Base" and node.context.callPath contains "protect_from_forgery" and node.context.callPath contains "http_basic_authenticate_with" and not (node.context.callArg contains "protect_from_forgery:with=exception")
  emit issue code.RailsBasicAuthWeakCsrfStrategy at node
}

binding colorscoreImagemagickCommandInjection176 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "ruby" and node.context.functionName == "initialize" and node.context.rubyReview == "imagemagick_convert_backtick_unescaped_interpolation"
  emit issue code.ColorscoreImagemagickCommandInjection at node
}

binding windowsBacktickDiffCommandInjection177 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "ruby" and node.context.rubyReview == "windows_backtick_diff_command_unescaped"
  emit issue code.WindowsBacktickDiffCommandInjection at node
}

binding postEditReportPrivateContentLeak178 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "ruby" and node.context.functionName == "report_post_edits" and node.context.rubyReview == "post_edit_report_missing_private_filters"
  emit issue code.PostEditReportPrivateContentLeak at node
}

binding unescapedQueueQueryStringXss179 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "ruby" and node.context.functionName == "url_path" and node.context.rubyReview == "unescaped_query_string_join_interpolation"
  emit issue code.UnescapedQueueQueryStringXss at node
}

binding responseHeaderWrite180 {
  query pattern callExpr where callee.path ~= "response.set_header"
  emit sink code.ResponseHeaderWrite at args[1]
}

binding responseHeaderWrite181 {
  query pattern callExpr where callee.path ~= "headers.__setitem__"
  emit sink code.ResponseHeaderWrite at args[1]
}

binding logOutput182 {
  query pattern callExpr where callee.path ~= "Rails.logger.info"
  emit sink code.LogOutput at args.any
}

binding logOutput183 {
  query pattern callExpr where callee.path ~= "Rails.logger.warn"
  emit sink code.LogOutput at args.any
}

binding logOutput184 {
  query pattern callExpr where callee.path ~= "Rails.logger.error"
  emit sink code.LogOutput at args.any
}

binding logOutput185 {
  query pattern callExpr where callee.path ~= "logger.info"
  emit sink code.LogOutput at args.any
}

binding logOutput186 {
  query pattern callExpr where callee.path ~= "logger.warn"
  emit sink code.LogOutput at args.any
}

binding logOutput187 {
  query pattern callExpr where callee.path ~= "logger.error"
  emit sink code.LogOutput at args.any
}

binding logWrite188 {
  query pattern presenceNode where node.path ~= "Rails.logger.info"
  emit issue code.LogWrite at node
  emit sink code.LogWriteSink at node
}

binding logWrite189 {
  query pattern presenceNode where node.path ~= "Rails.logger.warn"
  emit issue code.LogWrite at node
  emit sink code.LogWriteSink at node
}

binding logWrite190 {
  query pattern presenceNode where node.path ~= "Rails.logger.error"
  emit issue code.LogWrite at node
  emit sink code.LogWriteSink at node
}

binding logWrite191 {
  query pattern presenceNode where node.path ~= "logger.info"
  emit issue code.LogWrite at node
  emit sink code.LogWriteSink at node
}

binding logWrite192 {
  query pattern presenceNode where node.path ~= "logger.warn"
  emit issue code.LogWrite at node
  emit sink code.LogWriteSink at node
}

binding logWrite193 {
  query pattern presenceNode where node.path ~= "logger.error"
  emit issue code.LogWrite at node
  emit sink code.LogWriteSink at node
}

binding csvCell194 {
  query pattern callExpr where callee.path ~= "csv.write_row"
  emit sink code.CsvCell at args.any
}

binding csvCell195 {
  query pattern callExpr where callee.path ~= "CSV.generate_line"
  emit sink code.CsvCell at args.any
}

binding csvCell196 {
  query pattern callExpr where callee.path ~= "CSV.open"
  emit sink code.CsvCell at args.any
}

binding htmlEscape197 {
  query pattern callExpr where callee.path ~= "CGI.escapeHTML"
  emit check core.HtmlEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding htmlEscape198 {
  query pattern callExpr where callee.path ~= "ERB.Util.html_escape"
  emit check core.HtmlEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding htmlEscape199 {
  query pattern callExpr where callee.path ~= "ERB.Util.h"
  emit check core.HtmlEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding htmlEscape200 {
  query pattern callExpr where callee.path ~= "Rack.Utils.escape_html"
  emit check core.HtmlEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding shellEscape201 {
  query pattern callExpr where callee.path ~= "Shellwords.escape"
  emit check core.ShellEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding shellEscape202 {
  query pattern callExpr where callee.path ~= "Shellwords.shelljoin"
  emit check core.ShellEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding regexCompile203 {
  query pattern callExpr where callee.path ~= "Regexp.new"
  emit sink code.RegexCompileSink at args[0]
}

binding regexCompile204 {
  query pattern callExpr where callee.path ~= "Regexp.compile"
  emit sink code.RegexCompileSink at args[0]
}

binding regexMatch205 {
  query pattern presenceNode where node.path ~= "__regex.match"
  emit issue code.RegexMatch at node
  emit sink code.RegexMatchSink at node
}

binding catastrophicRegex206 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "ruby" and containsAny(node.context.literal, ["(?:[^\\\"]|\\\\.)*?", "(?:[^\\\"]|\\\\.)*)"]) and not (containsAny(node.context.literal, ["(?:[^\\\"]|\\\\\")*?", "(?:[^\\\"]|\\\\\")*)"]))
  emit issue code.CatastrophicRegex at node
  emit sink code.CatastrophicRegexSink at node
}

binding catastrophicRegex207 {
  query pattern presenceNode where node.scope == "class" and node.context.language == "ruby" and containsAny(node.context.literal, ["\\A/([^/]+)/?([^/]+)?\\z", "^/([^/]+)/?([^/]+)?$"]) and containsAny(node.context.callPath, ["path.match", "path.match.to_a"]) and not (node.context.callPath contains "path.split") and not (containsAny(node.context.callPath, ["model_id.include", "model_id.include?"]))
  emit issue code.CatastrophicRegex at node
  emit sink code.CatastrophicRegexSink at node
}

binding rubyLineAnchorValidationBypass208 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "ruby" and node.context.functionName == "legal?" and node.context.rubyReview == "ruby_line_anchor_hex_validation"
  emit issue code.RubyLineAnchorValidationBypass at node
}

binding rubyFfiWindowsPosixLibrarySearchPath209 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "FFI.DynamicLibrary.open" and node.context.language == "ruby" and node.context.functionName contains "ffi_lib" and node.context.callPath contains "File.exist?" and node.context.literal contains "/usr/lib/" and node.context.literal contains "/usr/local/lib/" and not (node.context.callPath contains "FFI.Platform.windows?")
  emit issue code.RubyFfiWindowsPosixLibrarySearchPath at node
}

binding massAssign210 {
  query pattern callExpr where callee.method == "update_attributes"
  emit sink code.MassAssign at args[0]
}

binding massAssign211 {
  query pattern callExpr where callee.method == "assign_attributes"
  emit sink code.MassAssign at args[0]
}

binding weakCipher212 {
  query pattern presenceNode where node.path ~= "OpenSSL.Cipher.DES"
  emit issue code.WeakCipher at node
}

binding weakCipher213 {
  query pattern presenceNode where node.path ~= "OpenSSL.Cipher.RC4"
  emit issue code.WeakCipher at node
}

binding weakCipher214 {
  query pattern callExpr where callee.path ~= "OpenSSL.Cipher.new" and args.any.literal contains "des"
  emit issue code.WeakCipher at call
}

binding weakCipher215 {
  query pattern callExpr where callee.path ~= "OpenSSL.Cipher.new" and args.any.literal contains "rc4"
  emit issue code.WeakCipher at call
}

binding weakCipher216 {
  query pattern callExpr where callee.path ~= "OpenSSL.Cipher.new" and args.any.literal contains "rc2"
  emit issue code.WeakCipher at call
}

binding weakCipher217 {
  query pattern callExpr where callee.path ~= "OpenSSL.Cipher.new" and args.any.literal contains "bf"
  emit issue code.WeakCipher at call
}

binding weakCipher218 {
  query pattern callExpr where callee.path ~= "OpenSSL.Cipher.new" and args.any.literal contains "ecb"
  emit issue code.WeakCipher at call
}

binding staticIv219 {
  query pattern callExpr where callee.path ~= "OpenSSL.Cipher.new" and args.any.literal contains "0000000000000000"
  emit issue code.StaticIv at call
}

binding staticIv220 {
  query pattern callExpr where callee.path ~= "cipher.iv" and args.any.literal contains "0000000000000000"
  emit issue code.StaticIv at call
}

binding weakHash221 {
  query pattern presenceNode where node.path ~= "Digest.MD5"
  emit issue code.WeakHash at node
}

binding weakHash222 {
  query pattern presenceNode where node.path ~= "Digest.SHA1"
  emit issue code.WeakHash at node
}

binding weakHash223 {
  query pattern presenceNode where node.path ~= "OpenSSL.Digest.MD5"
  emit issue code.WeakHash at node
}

binding weakHash224 {
  query pattern presenceNode where node.path ~= "OpenSSL.Digest.SHA1"
  emit issue code.WeakHash at node
}

binding jwtUnverified225 {
  query pattern callExpr where callee.path ~= "JWT.decode" and args.any.literal contains "false"
  emit issue code.JwtUnverified at call
}

binding jwtUnverified226 {
  query pattern callExpr where callee.path ~= "JWT.decode" and args.any.literal contains "algorithm=none"
  emit issue code.JwtUnverified at call
}

binding jwtUnverified227 {
  query pattern callExpr where callee.path ~= "JWT.encode" and args.any.literal contains "algorithm=none"
  emit issue code.JwtUnverified at call
}

binding csrfDisabled228 {
  query pattern callExpr where callee.path ~= "skip_before_action" and args.any.literal contains "verify_authenticity_token"
  emit issue code.CsrfDisabled at call
}

binding certValidationDisabled229 {
  query pattern callExpr where callee.path ~= "verify_mode" and args.any.literal contains "VERIFY_NONE"
  emit issue code.CertValidationDisabled at call
}

binding certValidationDisabled230 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "ruby" and node.context.rubyReview == "tls_ca_file_without_verify_mode"
  emit issue code.CertValidationDisabled at node
}

binding certValidationDisabled231 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "ruby" and node.context.functionName contains "create_client_from_token" and node.context.selector contains "@opts" and node.context.field contains "verify_ssl=OpenSSL::SSL::VERIFY_NONE" and node.context.callPath contains "detect_version" and node.context.callPath contains "Kubeclient.Client.new"
  emit issue code.CertValidationDisabled at node
}

binding certValidationDisabled232 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "ruby" and node.context.callPath contains "Fog.Kubevirt.Compute.new" and node.context.field contains "kubevirt_verify_ssl=ca_cert.present?" and not (node.context.field contains "kubevirt_verify_ssl=true")
  emit issue code.CertValidationDisabled at node
}

binding insecurePermission233 {
  query pattern callExpr where callee.path ~= "File.chmod" and args.any.literal contains "0777"
  emit issue code.InsecurePermission at call
}

binding insecurePermission234 {
  query pattern callExpr where callee.path ~= "File.chmod" and args.any.literal contains "0666"
  emit issue code.InsecurePermission at call
}

binding networkFilteringDisabled235 {
  query pattern callExpr where callee.path ~= "command" and args.any.literal contains "bridge-nf-call-iptables" and args.any.literal contains "echo 0"
  emit issue code.NetworkFilteringDisabled at call
}

binding networkFilteringDisabled236 {
  query pattern callExpr where callee.path ~= "command" and args.any.literal contains "bridge-nf-call-ip6tables" and args.any.literal contains "echo 0"
  emit issue code.NetworkFilteringDisabled at call
}

binding networkFilteringDisabled237 {
  query pattern callExpr where callee.path ~= "command" and args.any.literal contains "bridge-nf-call-arptables" and args.any.literal contains "echo 0"
  emit issue code.NetworkFilteringDisabled at call
}

binding insecureTempFile238 {
  query pattern callExpr where callee.path ~= "File.open" and args.any.literal contains "/tmp/" and args.any.literal contains "w"
  emit issue code.InsecureTempFile at call
}

binding resourceHandle239 {
  query pattern presenceNode where node.path ~= "TCPSocket.open"
  emit issue code.ResourceHandle at node
  emit sink code.ResourceHandleSink at node
}

binding resourceHandle240 {
  query pattern presenceNode where node.path ~= "File.open"
  emit issue code.ResourceHandle at node
  emit sink code.ResourceHandleSink at node
}

binding resourceRelease241 {
  query pattern callExpr where callee.method == "close"
  emit check core.ResourceRelease at call {
    covers postDominates {
      from: call
      to: call
    }
  }
}

binding xpathQuery242 {
  query pattern callExpr where callee.method == "xpath"
  emit sink code.XpathQuery at args[0]
}

binding xpathQuery243 {
  query pattern callExpr where callee.method == "at_xpath"
  emit sink code.XpathQuery at args[0]
}

binding cleartextChannel244 {
  query pattern callExpr where callee.path ~= "HTTParty" and args.any.literal contains "http://" and not args.any.literal contains "127.0"
  emit issue code.CleartextChannel at call
}

binding cleartextChannel245 {
  query pattern callExpr where callee.path ~= "RestClient" and args.any.literal contains "http://" and not args.any.literal contains "127.0"
  emit issue code.CleartextChannel at call
}

binding cleartextChannel246 {
  query pattern callExpr where callee.path ~= "open" and args.any.literal contains "http://" and not args.any.literal contains "127.0"
  emit issue code.CleartextChannel at call
}

binding cleartextChannel247 {
  query pattern callExpr where callee.path ~= "URI.open" and args.any.literal contains "http://" and not args.any.literal contains "127.0"
  emit issue code.CleartextChannel at call
}

binding polymorphicTypeSelectionReview249 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "ruby" and node.context.rubyReview == "polymorphic_type_selected_from_params"
  emit issue code.PolymorphicTypeSelectionReview at node
}

binding reconfirmationDirtyTrackingRace250 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "ruby" and node.context.functionName == "postpone_email_change_until_confirmation_and_regenerate_confirmation_token" and node.context.rubyReview == "devise_reconfirmation_missing_dirty_tracking"
  emit issue code.ReconfirmationDirtyTrackingRace at node
}

binding ldapQuery251 {
  query pattern callExpr where callee.path ~= "search"
  emit sink code.LdapQuery at args[1]
}

binding ldapQuery252 {
  query pattern callExpr where callee.path ~= "Net::LDAP"
  emit sink code.LdapQuery at args[1]
}

binding ldapQuery253 {
  query pattern callExpr where callee.method == "search"
  emit sink code.LdapQuery at args[1]
}

binding weakRandomValue254 {
  query pattern presenceNode where node.path ~= "Random.rand"
  emit issue code.WeakRandomValue at node
}

binding weakRandomValue255 {
  query pattern presenceNode where node.path ~= "rand"
  emit issue code.WeakRandomValue at node
}

binding insecureCookie256 {
  query pattern callExpr where callee.path ~= "set_cookie" and args.any.literal contains "false"
  emit issue code.InsecureCookie at call
}

binding inputValidation257 {
  query pattern callExpr where callee.path ~= "validate_role"
  emit check core.InputValidation at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding ldapEscape258 {
  query pattern callExpr where callee.path ~= "ldap_escape"
  emit check core.LdapEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding xpathEscape259 {
  query pattern callExpr where callee.path ~= "xpath_escape"
  emit check core.XpathEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding pathCanonicalization260 {
  query pattern callExpr where callee.path ~= "File.basename"
  emit check core.PathCanonicalization at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding benchmarkLdapEscape {
  query pattern callExpr where callee.path ~= "Net.LDAP.Filter.escape"
  emit check core.LdapEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding benchmarkLdapSearch {
  query pattern callExpr where callee.path ~= "conn.search"
  emit sink code.LdapQuery at call
}

binding benchmarkValidateRole {
  query pattern callExpr where callee.method == "validate_role"
  emit check core.InputValidation at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding benchmarkSessionSetTrustBoundary {
  query pattern callExpr where callee.path ~= "session.set"
  emit sink code.TrustBoundary at args[1]
}

binding csvFormulaProneExportRow {
  query pattern presenceNode where node.scope == "function" and node.context.language == "ruby" and node.context.rubyReview == "csv_formula_prone_export_row"
  emit issue code.CsvFormulaProneExportRow at node
}

binding certValidationDisabled263 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "ruby" and node.context.call == "start_tls" and containsAny(node.context.callPath, ["EventMachine.connect", "EventMachine.bind_connect", "EventMachine::connect", "EM.connect", "EM.bind_connect", "EM::connect"]) and not containsAny(node.context.callPath, ["EventMachine.start_tls", "EventMachine::start_tls", "EM.start_tls", "EM::start_tls"]) and not containsAny(node.context.call, ["start_server", "start_unix_domain_server"]) and not containsAny(node.context.call, ["verify_certificate_identity", "add_cert", "ssl_verify_peer", "ssl_handshake_completed"]) and not (node.context.field contains "verify_peer=true")
  emit issue code.CertValidationDisabled at node
}

binding jwtUnverified292 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "ruby" and node.context.functionName contains "decode" and node.context.rubyReview == "jose_compact_decode_accepts_encrypted_serialization"
  emit issue code.JwtUnverified at node
}
