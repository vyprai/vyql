module bindings.ruby.generated.m5a48bf47;

binding unescapedQueueQueryStringXss179 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "ruby" and node.context.name contains "url_path" and node.token contains "?queues=#{@queues.join(\",\")}" and not (node.token contains "CGI.escape(@queues.join")
  emit issue code.UnescapedQueueQueryStringXss at node
}
