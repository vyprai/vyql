module bindings.ruby.generated.m5a48bf47;

binding erbHrefUrlHtmlEscapeOnly159 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "erb.html_escaped_url_href" and node.context.template == "erb" and node.context.attr == "href" and node.context.erbValue == "url-like"
  emit issue code.ErbHrefUrlHtmlEscapeOnly at node
}
