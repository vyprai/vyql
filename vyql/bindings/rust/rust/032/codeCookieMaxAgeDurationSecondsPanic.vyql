module bindings.rust.generated.mdc4f74cd;

binding cookieMaxAgeDurationSecondsPanic32 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "rust" and node.context.name contains "parse_inner" and node.token contains "\"max-age\",Some(v)" and node.token contains "v.parse()" and node.token contains "Duration::seconds(val)" and not (node.token contains "Duration::max_value().num_seconds()") and not (node.token contains "cmp::min")
  emit issue code.CookieMaxAgeDurationSecondsPanic at node
}
