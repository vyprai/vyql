module bindings.rust.generated.mdc4f74cd;

binding cryptoScalarRandomBitsByteLengthConfusion42 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "rust" and node.token contains ".bits().div_ceil(8)" and node.token contains "try_random_bits" and node.token contains "Scalar::from_uint" and node.token contains "ProjectivePoint::mul_by_generator" and not (node.token contains "try_generate_from_rng") and not (node.token contains "NonZeroScalar")
  emit issue code.CryptoScalarRandomBitsByteLengthConfusion at node
}
