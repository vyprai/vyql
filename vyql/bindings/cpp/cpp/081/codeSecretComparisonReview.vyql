module bindings.cpp.generated.m4e783366;

binding secretComparisonReview81 {
  query pattern presenceNode where node.kind == "binop" and node.op contains "==" and not (containsAny(node.context.scopeCall, [CRYPTO_memcmp, timingsafe_bcmp, constant_time])) and operand(node, where: operand.path ~= "DecodeBase64") and operand(node, where: containsAny(operand.identifier, [pass, password, secret, token, key, auth]))
  emit issue code.SecretComparisonReview at node
}
