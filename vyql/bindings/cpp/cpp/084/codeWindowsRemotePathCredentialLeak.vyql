module bindings.cpp.generated.m4e783366;

binding windowsRemotePathCredentialLeak84 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "cpp" and node.token contains "QFileInfo::exists(m_configFile)" and node.token contains "QSettingsconfig(m_configFile,QSettings::IniFormat)" and node.token contains "m_pWatchdog->setProcessConfig(command,elevate)" and not (node.token contains "m_configFile.startsWith(QStringLiteral(\"\\\\\\\\\"))") and not (node.token contains "m_configFile.startsWith(QStringLiteral(\"//\"))")
  emit issue code.WindowsRemotePathCredentialLeak at node
}
