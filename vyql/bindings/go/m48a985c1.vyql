module bindings.go.generated.m48a985c1;

pattern bindingMetadata {
  binding: {
    name: "go"
    meta: {
      fidelity: "resolved"
      match: "contains"
    }
  }
}

binding dB0 {
  query pattern callExpr where callee.path ~= "sql.Open"
  emit fact runtime.ReceiverType at call.result {
    about: sql.DB
  }
}

binding dB1 {
  query pattern callExpr where callee.path ~= "sql.OpenDB"
  emit fact runtime.ReceiverType at call.result {
    about: sql.DB
  }
}

binding dB2 {
  query pattern callExpr where callee.path ~= "sqlx.Open"
  emit fact runtime.ReceiverType at call.result {
    about: sqlx.DB
  }
}

binding dB3 {
  query pattern callExpr where callee.path ~= "sqlx.Connect"
  emit fact runtime.ReceiverType at call.result {
    about: sqlx.DB
  }
}

binding dB4 {
  query pattern callExpr where callee.path ~= "sqlx.MustConnect"
  emit fact runtime.ReceiverType at call.result {
    about: sqlx.DB
  }
}

binding dB5 {
  query pattern callExpr where callee.path ~= "gorm.Open"
  emit fact runtime.ReceiverType at call.result {
    about: gorm.DB
  }
}

binding client6 {
  query pattern callExpr where callee.path ~= "redis.NewClient"
  emit fact runtime.ReceiverType at call.result {
    about: redis.Client
  }
}

binding database7 {
  query pattern callExpr where callee.path ~= "mongo.Connect"
  emit fact runtime.ReceiverType at call.result {
    about: mongo.Database
  }
}

binding client8 {
  query pattern callExpr where callee.path ~= "graphql.NewClient"
  emit fact runtime.ReceiverType at call.result {
    about: graphql.Client
  }
}

binding conn9 {
  query pattern callExpr where callee.path ~= "ldap.DialURL"
  emit fact runtime.ReceiverType at call.result {
    about: ldap.Conn
  }
}

binding conn10 {
  query pattern callExpr where callee.path ~= "ldap.Dial"
  emit fact runtime.ReceiverType at call.result {
    about: ldap.Conn
  }
}

binding conn11 {
  query pattern callExpr where callee.path ~= "support.NewLDAP"
  emit fact runtime.ReceiverType at call.result {
    about: ldap.Conn
  }
}

binding doc12 {
  query pattern callExpr where callee.path ~= "support.NewXMLDoc"
  emit fact runtime.ReceiverType at call.result {
    about: xpath.Doc
  }
}

binding store13 {
  query pattern callExpr where callee.path ~= "support.NewSession"
  emit fact runtime.ReceiverType at call.result {
    about: session.Store
  }
}

binding writer14 {
  query pattern callExpr where callee.path ~= "csv.NewWriter"
  emit fact runtime.ReceiverType at call.result {
    about: csv.Writer
  }
}

binding httpInput15 {
  query pattern callExpr where callee.analysis ~= "parameter.entry" and args.any.context.paramType contains "http.Request"
  emit source code.HttpInput at call.result
}

binding cliArgument16 {
  query pattern callExpr where callee.path ~= "os.Args"
  emit source code.CliArgument at call.result
}

binding envVariable17 {
  query pattern callExpr where callee.path ~= "os.Getenv"
  emit source code.EnvVariable at call.result
}

binding secretValue18 {
  query pattern callExpr where callee.analysis ~= "function.context" and args.any.context.functionName contains "NewClient" and args.any.context.callPath contains "log.Infof" and args.any.context.literal contains "Loaded client config: %#v" and args.any.context.selector contains "cfg.Spec"
  emit source code.SecretValue at call.result
}

binding cliArgument19 {
  query pattern callExpr where callee.path ~= "flag.Arg"
  emit source code.CliArgument at call.result
}

binding cliArgument20 {
  query pattern callExpr where callee.path ~= "flag.Args"
  emit source code.CliArgument at call.result
}

binding httpInput21 {
  query pattern callExpr where callee.path ~= ".URL.Query"
  emit source code.HttpInput at call.result
}

binding httpInput22 {
  query pattern callExpr where callee.path ~= ".URL.RawQuery"
  emit source code.HttpInput at call.result
}

binding httpInput23 {
  query pattern callExpr where callee.path ~= ".URL.Path"
  emit source code.HttpInput at call.result
}

binding httpInput24 {
  query pattern callExpr where callee.path ~= "FormValue"
  emit source code.HttpInput at call.result
}

binding httpInput25 {
  query pattern callExpr where callee.path ~= "PostFormValue"
  emit source code.HttpInput at call.result
}

binding httpInput26 {
  query pattern callExpr where callee.path ~= "FormFile"
  emit source code.HttpInput at call.result
}

binding httpInput27 {
  query pattern callExpr where callee.path ~= ".PostForm"
  emit source code.HttpInput at call.result
}

binding httpInput28 {
  query pattern callExpr where callee.path ~= ".Form.Get"
  emit source code.HttpInput at call.result
}

binding httpInput29 {
  query pattern callExpr where callee.path ~= ".Header.Get"
  emit source code.HttpInput at call.result
}

binding httpInput30 {
  query pattern callExpr where callee.path ~= ".GetHeader"
  emit source code.HttpInput at call.result
}

binding httpInput31 {
  query pattern callExpr where callee.path ~= ".QueryParam"
  emit source code.HttpInput at call.result
}

binding httpInput32 {
  query pattern callExpr where callee.path ~= ".DefaultQuery"
  emit source code.HttpInput at call.result
}

binding httpInput33 {
  query pattern callExpr where callee.path ~= ".QueryArray"
  emit source code.HttpInput at call.result
}

binding httpInput34 {
  query pattern callExpr where callee.path ~= ".GetQuery"
  emit source code.HttpInput at call.result
}

binding httpInput35 {
  query pattern callExpr where callee.path ~= ".ShouldBind"
  emit source code.HttpInput at call.result
}

binding httpInput36 {
  query pattern callExpr where callee.path ~= ".ShouldBindJSON"
  emit source code.HttpInput at call.result
}

binding httpInput37 {
  query pattern callExpr where callee.path ~= ".ShouldBindQuery"
  emit source code.HttpInput at call.result
}

binding httpInput38 {
  query pattern callExpr where callee.path ~= ".ShouldBindUri"
  emit source code.HttpInput at call.result
}

binding httpInput39 {
  query pattern callExpr where callee.path ~= ".ShouldBindWith"
  emit source code.HttpInput at call.result
}

binding httpInput40 {
  query pattern callExpr where callee.path ~= ".ShouldBindBodyWith"
  emit source code.HttpInput at call.result
}

binding httpInput41 {
  query pattern callExpr where callee.path ~= ".BindJSON"
  emit source code.HttpInput at call.result
}

binding httpInput42 {
  query pattern callExpr where callee.path ~= ".BindQuery"
  emit source code.HttpInput at call.result
}

binding httpInput43 {
  query pattern callExpr where callee.path ~= ".BindUri"
  emit source code.HttpInput at call.result
}

binding httpInput44 {
  query pattern callExpr where callee.path ~= ".BindWith"
  emit source code.HttpInput at call.result
}

binding httpInput45 {
  query pattern callExpr where callee.path ~= ".URLParam"
  emit source code.HttpInput at call.result
}

binding httpInput46 {
  query pattern callExpr where callee.path ~= "mux.Vars"
  emit source code.HttpInput at call.result
}

binding httpInput47 {
  query pattern callExpr where callee.path ~= ".Cookie"
  emit source code.HttpInput at call.result
}

binding httpInput48 {
  query pattern callExpr where callee.path ~= ".Params"
  emit source code.HttpInput at call.result
}

binding httpInput49 {
  query pattern callExpr where callee.path ~= ".Param"
  emit source code.HttpInput at call.result
}

binding httpInput50 {
  query pattern callExpr where callee.path ~= "auth.ExtractIssuer"
  emit source code.HttpInput at call.result
}

binding httpInput51 {
  query pattern callExpr where callee.path ~= "apiauth.ExtractIssuer"
  emit source code.HttpInput at call.result
}

binding httpInput52 {
  query pattern callExpr where callee.path ~= "ExtractIssuer"
  emit source code.HttpInput at call.result
}

binding overbroadRolePermissionGrant53 {
  query pattern presenceNode where node.analysis == "go.overbroad_role_permission_grant"
  emit issue code.OverbroadRolePermissionGrant at node
}

binding tpmDiskKeyWeakPcrSealing54 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "go.tpm_weak_pcr_sealing"
  emit issue code.TpmDiskKeyWeakPcrSealing at node
}

binding shareInfoDirectDownloadTokenExposure55 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "go" and node.context.functionName contains "shareInfoHandler" and node.context.callPath contains "store.Share.GetByHash" and node.context.selector contains "commonShare.DownloadURL" and node.context.selector contains "shareLink.Token" and node.context.callArg contains "getShareURL:true" and node.context.callArg contains "getShareURL:shareLink.Token" and node.context.callPath contains "renderJSON"
  emit issue code.ShareInfoDirectDownloadTokenExposure at node
}

binding verboseHttpErrorObjectDisclosure56 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "go" and node.context.callPath contains "fmt.Errorf" and node.context.literal contains "%+v" and node.context.selector contains ".Request" and node.context.selector contains ".Body" and node.context.selector contains ".TLS"
  emit issue code.VerboseHttpErrorObjectDisclosure at node
}

binding bulkEmailRecipientDisclosure57 {
  query pattern presenceNode where node.scope == "function" and containsAny(node.context.callPath, ["SendIssueCommentMail", "SendIssueMentionMail", "SendMail", "SendEmail", "SendNotification", "SendNotificationMail", "SendNotificationEmail"]) and node.context.callPath contains "append" and node.context.selector contains ".Email" and containsAny(node.context.callArg, ["SendIssueCommentMail:tos", "SendIssueMentionMail:getUserEmailsByNames", "SendMail:tos", "SendMail:recipients", "SendEmail:recipients", "SendEmail:emails", "SendNotification:recipients", "SendNotificationMail:recipients", "SendNotificationEmail:recipients"]) and not (node.context.callArg contains "__object_literal") and not (containsAny(node.context.callPath, ["Bcc", "BlindCopy"]))
  emit issue code.BulkEmailRecipientDisclosure at node
}

binding unescapedMysqlDsnDatabaseName58 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "go" and node.context.callPath contains "sql.NewParserFromAlterStatement" and node.context.selector contains "migrationContext.DatabaseName" and not (node.context.callPath contains "url.QueryEscape")
  emit issue code.UnescapedMysqlDsnDatabaseName at node
}

binding sqlExecution59 {
  query pattern callExpr where callee.method == "Query" and callee.receiver.type in [sql.DB, sqlx.DB, gorm.DB]
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution60 {
  query pattern callExpr where callee.method == "QueryRow" and callee.receiver.type in [sql.DB, sqlx.DB, gorm.DB]
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution61 {
  query pattern callExpr where callee.method == "QueryContext" and callee.receiver.type in [sql.DB, sqlx.DB, gorm.DB]
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution62 {
  query pattern callExpr where callee.method == "QueryRowContext" and callee.receiver.type in [sql.DB, sqlx.DB, gorm.DB]
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution63 {
  query pattern callExpr where callee.method == "Exec" and callee.receiver.type in [sql.DB, sqlx.DB, gorm.DB]
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution64 {
  query pattern callExpr where callee.method == "ExecContext" and callee.receiver.type in [sql.DB, sqlx.DB, gorm.DB]
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution65 {
  query pattern callExpr where callee.method == "Prepare" and callee.receiver.type in [sql.DB, sqlx.DB, gorm.DB]
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution66 {
  query pattern callExpr where callee.method == "PrepareContext" and callee.receiver.type in [sql.DB, sqlx.DB, gorm.DB]
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution67 {
  query pattern callExpr where callee.path ~= "gorm.Raw"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution68 {
  query pattern callExpr where callee.path ~= "db.Raw"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution69 {
  query pattern callExpr where callee.method == "Raw"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution70 {
  query pattern callExpr where callee.path ~= "sqlx.Query"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution71 {
  query pattern callExpr where callee.path ~= "sqlx.MustExec"
  emit sink code.SqlExecution at args[0]
}

binding sqlExecution72 {
  query pattern callExpr where callee.path ~= "bun.Raw"
  emit sink code.SqlExecution at args[0]
}

binding commandExecution73 {
  query pattern callExpr where callee.path ~= "exec.Command"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution74 {
  query pattern callExpr where callee.path ~= "exec.CommandContext"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution75 {
  query pattern callExpr where callee.path ~= "os/exec.Command"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution76 {
  query pattern callExpr where callee.path ~= "exec.Command" and args.any.literal contains "-c"
  emit sink code.CommandExecution at args[2]
}

binding commandExecution77 {
  query pattern callExpr where callee.path ~= "exec.CommandContext" and args.any.literal contains "-c"
  emit sink code.CommandExecution at args[3]
}

binding commandExecution78 {
  query pattern callExpr where callee.path ~= "syscall.Exec"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution79 {
  query pattern callExpr where callee.path ~= "syscall.ForkExec"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution80 {
  query pattern callExpr where callee.path ~= "sh.Command"
  emit sink code.CommandExecution at args[0]
}

binding commandExecution81 {
  query pattern callExpr where callee.path ~= "channel.NewLocalChannel.Run"
  emit sink code.CommandExecution at args.any
}

binding urlFetch82 {
  query pattern callExpr where callee.path ~= "http.Get"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch83 {
  query pattern callExpr where callee.path ~= "http.Post"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch84 {
  query pattern callExpr where callee.path ~= "http.Head"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch85 {
  query pattern callExpr where callee.path ~= "http.PostForm"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch86 {
  query pattern callExpr where callee.path ~= "http.NewRequest"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch87 {
  query pattern callExpr where callee.path ~= "http.NewRequestWithContext"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch88 {
  query pattern callExpr where callee.path ~= "client.Get"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch89 {
  query pattern callExpr where callee.path ~= "client.Post"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch90 {
  query pattern callExpr where callee.path ~= "client.Do"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch91 {
  query pattern callExpr where callee.path ~= "client.Head"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch92 {
  query pattern callExpr where callee.path ~= "provider.Get" and args.any.literal contains "issuer"
  emit sink code.UrlFetch at args[1]
}

binding urlFetch93 {
  query pattern callExpr where callee.path ~= "oidc.NewProvider"
  emit sink code.UrlFetch at args[1]
}

binding urlFetch94 {
  query pattern callExpr where callee.path ~= "resty.R"
  emit sink code.UrlFetch at args[0]
}

binding urlFetch95 {
  query pattern callExpr where callee.path ~= "req.Get"
  emit sink code.UrlFetch at args[0]
}

binding filePathAccess96 {
  query pattern callExpr where callee.path ~= "os.Open"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess97 {
  query pattern callExpr where callee.path ~= "os.OpenFile"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess98 {
  query pattern callExpr where callee.path ~= "os.ReadFile"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess99 {
  query pattern callExpr where callee.path ~= "os.Create"
  emit sink code.FilePathAccess at args[0]
}

binding unsafeUpload100 {
  query pattern callExpr where callee.path ~= "os.Create"
  emit sink code.UnsafeUpload at args[0]
}

binding filePathAccess101 {
  query pattern callExpr where callee.path ~= "os.Remove"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess102 {
  query pattern callExpr where callee.path ~= "os.RemoveAll"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess103 {
  query pattern callExpr where callee.path ~= "ioutil.ReadFile"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess104 {
  query pattern callExpr where callee.path ~= "ioutil.WriteFile"
  emit sink code.FilePathAccess at args[0]
}

binding unsafeUpload105 {
  query pattern callExpr where callee.path ~= "os.WriteFile"
  emit sink code.UnsafeUpload at args[0]
}

binding unsafeUpload106 {
  query pattern callExpr where callee.path ~= "ioutil.WriteFile"
  emit sink code.UnsafeUpload at args[0]
}

binding filePathAccess107 {
  query pattern callExpr where callee.path ~= "http.ServeFile"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess108 {
  query pattern callExpr where callee.path ~= "os.Mkdir"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess109 {
  query pattern callExpr where callee.path ~= "os.MkdirAll"
  emit sink code.FilePathAccess at args[0]
}

binding filePathAccess110 {
  query pattern callExpr where callee.path ~= "filepath.Walk"
  emit sink code.FilePathAccess at args[0]
}

binding dynamicCodeLoad111 {
  query pattern callExpr where callee.path ~= "plugin.Open"
  emit sink code.DynamicCodeLoad at args[0]
}

binding archiveEntryWrite112 {
  query pattern callExpr where callee.path ~= "extractEntry"
  emit sink code.ArchiveEntryWrite at args[0]
}

binding archiveEntryWrite113 {
  query pattern callExpr where callee.path ~= "writeArchiveEntry"
  emit sink code.ArchiveEntryWrite at args[0]
}

binding ldapQuery114 {
  query pattern callExpr where callee.method == "Search" and callee.receiver.type == "ldap.Conn"
  emit sink code.LdapQuery at args[0]
}

binding ldapQuery115 {
  query pattern callExpr where callee.method == "SearchWithPaging" and callee.receiver.type == "ldap.Conn"
  emit sink code.LdapQuery at args[0]
}

binding xpathQuery116 {
  query pattern callExpr where callee.method == "XPath" and callee.receiver.type == "xpath.Doc"
  emit sink code.XpathQuery at args[0]
}

binding xpathQuery117 {
  query pattern callExpr where callee.method == "Find" and callee.receiver.type == "xpath.Doc"
  emit sink code.XpathQuery at args[0]
}

binding trustBoundary118 {
  query pattern callExpr where callee.method == "Set" and callee.receiver.type == "session.Store"
  emit sink code.TrustBoundary at args[1]
}

binding htmlRender119 {
  query pattern callExpr where callee.path ~= "template.HTML"
  emit sink code.HtmlRender at args[0]
}

binding htmlRender120 {
  query pattern callExpr where callee.path ~= "template.JS"
  emit sink code.HtmlRender at args[0]
}

binding htmlRender121 {
  query pattern callExpr where callee.path ~= "template.URL"
  emit sink code.HtmlRender at args[0]
}

binding htmlRender122 {
  query pattern callExpr where callee.analysis ~= "function.return" and args.any.literal contains "<html"
  emit sink code.HtmlRender at args[0]
}

binding htmlRender123 {
  query pattern callExpr where callee.analysis ~= "function.return" and args.any.literal contains "<body"
  emit sink code.HtmlRender at args[0]
}

binding htmlRender124 {
  query pattern callExpr where callee.analysis ~= "function.return" and args.any.literal contains "<a "
  emit sink code.HtmlRender at args[0]
}

binding gouniverseCmsAliasXss125 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "PageRenderHtmlByAlias" and node.context.paramType contains "http.Request" and node.context.callPath contains "cms.PageFindByAlias" and node.context.literal contains "Page with alias '" and not (node.context.scopeCall contains "ToHTML")
  emit issue code.GouniverseCmsAliasXss at node
}

binding documizeMarkdownRenderWithoutSanitizer126 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "Render" and node.context.callPath contains "blackfriday.Run" and node.context.call contains "string" and not (node.context.callPath contains "bluemonday.UGCPolicy") and not (node.context.scopeCall contains "SanitizeBytes")
  emit issue code.DocumizeMarkdownRenderWithoutSanitizer at node
}

binding htmlRender127 {
  query pattern callExpr where callee.path ~= "Writer.WriteString"
  emit sink code.HtmlRender at args[0]
}

binding htmlRender128 {
  query pattern callExpr where callee.path ~= "Writer.Write"
  emit sink code.HtmlRender at args[0]
}

binding redirectTarget129 {
  query pattern callExpr where callee.path ~= "http.Redirect"
  emit sink code.RedirectTargetSink at args[0]
}

binding redirectTarget130 {
  query pattern callExpr where callee.path ~= "c.Redirect"
  emit sink code.RedirectTargetSink at args[0]
}

binding redirectTarget131 {
  query pattern callExpr where callee.path ~= "ctx.Redirect"
  emit sink code.RedirectTargetSink at args[0]
}

binding objectLookupByUserInput132 {
  query pattern callExpr where callee.method == "FindByID"
  emit sink code.ObjectLookupByUserInput at args[0]
}

binding objectLookupByUserInput133 {
  query pattern callExpr where callee.method == "FindById"
  emit sink code.ObjectLookupByUserInput at args[0]
}

binding objectLookupByUserInput134 {
  query pattern callExpr where callee.method == "GetUser"
  emit sink code.ObjectLookupByUserInput at args[0]
}

binding objectLookupByUserInput135 {
  query pattern callExpr where callee.method == "GetAccount"
  emit sink code.ObjectLookupByUserInput at args[0]
}

binding objectLookupByUserInput136 {
  query pattern callExpr where callee.method == "GetOrder"
  emit sink code.ObjectLookupByUserInput at args[0]
}

binding authenticationRequiredOp137 {
  query pattern presenceNode where node.path ~= "ChangePassword"
  emit issue code.AuthenticationRequiredOp at node
}

binding authenticationRequiredOp138 {
  query pattern presenceNode where node.path ~= "UpdatePassword"
  emit issue code.AuthenticationRequiredOp at node
}

binding authenticationRequiredOp139 {
  query pattern presenceNode where node.path ~= "ResetPassword"
  emit issue code.AuthenticationRequiredOp at node
}

binding authenticationRequiredOp140 {
  query pattern presenceNode where node.path ~= "ChangeEmail"
  emit issue code.AuthenticationRequiredOp at node
}

binding authenticationRequiredOp141 {
  query pattern presenceNode where node.path ~= "TransferFunds"
  emit issue code.AuthenticationRequiredOp at node
}

binding authenticationRequiredOp142 {
  query pattern presenceNode where node.path ~= "DeleteAccount"
  emit issue code.AuthenticationRequiredOp at node
}

binding authenticationRequiredOp143 {
  query pattern presenceNode where node.path ~= "GrantRole"
  emit issue code.AuthenticationRequiredOp at node
}

binding authenticationRequiredOp144 {
  query pattern presenceNode where node.path ~= "MakeAdmin"
  emit issue code.AuthenticationRequiredOp at node
}

binding pathMatcherNormalizationOrderBypass145 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "Match" and node.context.callPath contains "url.PathUnescape" and node.context.selector contains "r.URL.Path" and node.context.callOrder contains "strings.TrimRight>path.Clean" and node.context.call contains "HasSuffix"
  emit issue code.PathMatcherNormalizationOrderBypass at node
}

binding unicodeNormalizationAfterFilterBypass146 {
  query pattern presenceNode where node.scope == "function" and containsAny(node.context.functionName, ["Preprocess", "preprocess", "Normalize", "normalize", "Canonical", "canonical", "Lookup", "lookup"]) and node.context.callPath contains "regexp.MustCompile.ReplaceAllString" and node.context.callPath contains "norm.NFKD.String" and node.context.callOrder contains "norm.NFKD.String>regexp.MustCompile.ReplaceAllString" and not (node.context.callOrder contains "regexp.MustCompile.ReplaceAllString>norm.NFKD.String") and containsAny(node.context.literal, ["-_ .", "[-_ .]", "-_."])
  emit issue code.UnicodeNormalizationAfterFilterBypass at node
}

binding leadingDotSegmentOnlyRouteFilter147 {
  query pattern presenceNode where node.kind == "any" and node.analysis == "function.context" and node.context.callPath contains "strings.HasPrefix" and node.context.literal contains "/." and node.context.selector contains "ctx.R.URL.Path" and not (node.context.callPath contains "strings.Contains")
  emit issue code.LeadingDotSegmentOnlyRouteFilter at node
}

binding authAttemptLimitReview148 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "SolveChallenge" and node.context.selector contains "challenge.tries" and node.context.selector contains "s.challenges"
  emit issue code.AuthAttemptLimitReview at node
}

binding emptyVerifierAcceptance149 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "Verify" and node.context.call contains "Get" and node.context.binary contains "v==answer" and not (node.context.binary contains "v!=\"\"")
  emit issue code.EmptyVerifierAcceptance at node
}

binding responseHeaderWrite150 {
  query pattern callExpr where callee.path ~= "Header.Set"
  emit sink code.ResponseHeaderWrite at args[1]
}

binding responseHeaderWrite151 {
  query pattern callExpr where callee.path ~= "Header.Add"
  emit sink code.ResponseHeaderWrite at args[1]
}

binding logOutput152 {
  query pattern callExpr where callee.path ~= "log.Print"
  emit sink code.LogOutput at args.any
}

binding logOutput153 {
  query pattern callExpr where callee.path ~= "log.Printf"
  emit sink code.LogOutput at args.any
}

binding logOutput154 {
  query pattern callExpr where callee.path ~= "log.Println"
  emit sink code.LogOutput at args.any
}

binding logWrite155 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "NewClient" and node.context.callPath contains "log.Infof" and node.context.literal contains "Loaded client config: %#v" and node.context.selector contains "cfg.Spec"
  emit issue code.LogWrite at node
  emit sink code.LogWriteSink at node
}

binding coderAgentManifestInfoLog156 {
  query pattern presenceNode where node.analysis == "go.coder_manifest_info_log"
  emit issue code.CoderAgentManifestInfoLog at node
}

binding kubernetesCurlAuthorizationHeaderLogLeak157 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "toCurl" and node.context.selector contains "r.RequestHeaders" and node.context.callPath contains "fmt.Sprintf" and node.context.literal contains "%s: %s" and not (node.context.scopeCall contains "maskValue")
  emit issue code.KubernetesCurlAuthorizationHeaderLogLeak at node
}

binding requestPathPercentSLogInjection158 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "fmt.Sprintf" and node.context.selector contains ".Path" and node.context.selector contains ".ErrorMessage" and node.context.literal contains "%s %s\n%s" and not (node.context.literal contains "%#v")
  emit issue code.RequestPathPercentSLogInjection at node
}

binding requestPluginNamePathTraversal159 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "ShouldBindJSON" and node.context.selector contains ".PlugName" and node.context.selector contains ".Snake" and containsAny(node.context.callPath, ["CreatePlug", "GeneratePlug", "CreatePlugin", "GeneratePlugin", "CreateModule", "GenerateModule"]) and not (containsAny(node.context.callPath, ["filepath.Base", "path.Base", "regexp.MatchString"])) and not (containsAny(node.context.callArg, ["strings.Contains:filepath.Separator", "strings.Contains:os.PathSeparator", "strings.Contains:/", "strings.Contains:\\"]))
  emit issue code.RequestPluginNamePathTraversal at node
}

binding textAccessLogDefaultFormatInjection160 {
  query pattern presenceNode where node.kind == "call" and node.path ~= "cfgcore.DataSource_InlineString" and operand(node, where: operand.identifier contains "EnvoyTextLogFormat")
  emit issue code.TextAccessLogDefaultFormatInjection at node
}

binding kubernetesNamespaceNotInEgressPolicy161 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "go" and node.context.callPath contains "netwv1.NewNetworkPolicy" and node.context.callPath contains "netwv1.NetworkPolicyEgressRuleArgs" and node.context.callPath contains "netwv1.NetworkPolicyPeerArgs" and node.context.callPath contains "metav1.LabelSelectorRequirementArgs" and node.context.literal contains "Egress" and node.context.literal contains "kubernetes.io/metadata.name" and node.context.literal contains "NotIn" and containsAny(node.context.callPath, ["Metadata.Name.Elem", "Namespace.Name.Elem"])
  emit issue code.KubernetesNamespaceNotInEgressPolicy at node
}

binding unescapedContentDispositionFilename162 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "Attachment" and node.context.callPath contains "filepath.Base" and node.context.callPath contains "ctx.Set" and node.context.callArg contains "ctx.Set:HeaderContentDisposition" and node.context.literal contains "attachment; filename=" and not (node.context.callPath contains "url.QueryEscape") and not (node.context.callPath contains "url.PathEscape")
  emit issue code.UnescapedContentDispositionFilename at node
}

binding logWrite163 {
  query pattern presenceNode where node.path ~= "log.Print"
  emit issue code.LogWrite at node
  emit sink code.LogWriteSink at node
}

binding logWrite164 {
  query pattern presenceNode where node.path ~= "log.Printf"
  emit issue code.LogWrite at node
  emit sink code.LogWriteSink at node
}

binding logWrite165 {
  query pattern presenceNode where node.path ~= "log.Println"
  emit issue code.LogWrite at node
  emit sink code.LogWriteSink at node
}

binding csvCell166 {
  query pattern callExpr where callee.path ~= "writer.Write"
  emit sink code.CsvCell at args.any.collection
}

binding csvCell167 {
  query pattern callExpr where callee.path ~= "csvWriter.Write"
  emit sink code.CsvCell at args.any.collection
}

binding csvCell168 {
  query pattern callExpr where callee.path ~= "csvw.Write"
  emit sink code.CsvCell at args.any.collection
}

binding htmlEscape169 {
  query pattern callExpr where callee.path ~= "html.EscapeString"
  emit check core.HtmlEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding htmlEscape170 {
  query pattern callExpr where callee.path ~= "template.HTMLEscapeString"
  emit check core.HtmlEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding safeResponseContentType171 {
  query pattern callExpr where callee.path ~= "ctx.Header" and args.any.literal contains "content-type" and args.any.literal contains "image/"
  emit check core.SafeResponseContentType at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding safeResponseContentType172 {
  query pattern callExpr where callee.path ~= "c.Header" and args.any.literal contains "content-type" and args.any.literal contains "image/"
  emit check core.SafeResponseContentType at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding safeResponseContentType173 {
  query pattern callExpr where callee.path ~= "Header.Set" and args.any.literal contains "content-type" and args.any.literal contains "image/"
  emit check core.SafeResponseContentType at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding safeResponseContentType174 {
  query pattern callExpr where callee.path ~= "Header.Add" and args.any.literal contains "content-type" and args.any.literal contains "image/"
  emit check core.SafeResponseContentType at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding pathCanonicalization175 {
  query pattern callExpr where callee.path ~= "filepath.Base"
  emit check core.PathCanonicalization at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding pathAccessCheck176 {
  query pattern callExpr where callee.path ~= "strings.Contains" and args.any.literal contains ".." and args.any.literal contains "\\"
  emit check core.PathAccessCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding pathAccessCheck177 {
  query pattern callExpr where callee.analysis ~= "function.context" and args.any.context.callPath contains "strings.Contains" and args.any.context.literal contains "bad path" and args.any.context.callPath contains "isSymbolLink" and args.any.context.callPath contains "ioutil.ReadFile"
  emit check core.PathAccessCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding pathAccessCheck178 {
  query pattern callExpr where callee.path ~= "regexp.MatchString" and args.any.literal contains "^/var/lib/casaos/\\d"
  emit check core.PathAccessCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding pathAccessCheck179 {
  query pattern callExpr where callee.path ~= "pathrs.Reopen"
  emit check core.PathAccessCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding pathAccessCheck180 {
  query pattern callExpr where callee.path ~= "procfs.OpenProcRoot"
  emit check core.PathAccessCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding pathAccessCheck181 {
  query pattern callExpr where callee.method == "OpenThreadSelf"
  emit check core.PathAccessCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding ldapEscape182 {
  query pattern callExpr where callee.path ~= "support.EscapeLDAP"
  emit check core.LdapEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding xpathEscape183 {
  query pattern callExpr where callee.path ~= "support.XPathLiteral"
  emit check core.XpathEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding inputValidation184 {
  query pattern callExpr where callee.path ~= "support.AllowRole"
  emit check core.InputValidation at call {
    covers path {
      from: call
      to: call
    }
  }
}

binding authenticationCheck185 {
  query pattern callExpr where callee.method == "RequireAuth"
  emit check core.AuthenticationCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding authenticationCheck186 {
  query pattern callExpr where callee.method == "EnsureAuthenticated"
  emit check core.AuthenticationCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding authenticationCheck187 {
  query pattern callExpr where callee.method == "IsAuthenticated"
  emit check core.AuthenticationCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding authenticationCheck188 {
  query pattern callExpr where callee.method == "Authenticate"
  emit check core.AuthenticationCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding ownershipCheck189 {
  query pattern callExpr where callee.method == "CheckOwnership"
  emit check core.OwnershipCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding ownershipCheck190 {
  query pattern callExpr where callee.method == "RequireOwner"
  emit check core.OwnershipCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding ownershipCheck191 {
  query pattern callExpr where callee.method == "IsOwner"
  emit check core.OwnershipCheck at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding allowlistValidation192 {
  query pattern callExpr where callee.path ~= "oidcvalidate.IsValidIssuer"
  emit check core.AllowlistValidation at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding allowlistValidation193 {
  query pattern callExpr where callee.path ~= "IsValidIssuer"
  emit check core.AllowlistValidation at call {
    covers endpoint {
      anchor: call
    }
  }
}

binding regexCompile194 {
  query pattern callExpr where callee.path ~= "regexp.Compile"
  emit sink code.RegexCompileSink at args[0]
}

binding regexCompile195 {
  query pattern callExpr where callee.path ~= "regexp.MustCompile"
  emit sink code.RegexCompileSink at args[0]
}

binding regexCompile196 {
  query pattern callExpr where callee.path ~= "regexp.MatchString"
  emit sink code.RegexCompileSink at args[0]
}

binding weakHash197 {
  query pattern presenceNode where node.path ~= "md5.New"
  emit issue code.WeakHash at node
}

binding weakHash198 {
  query pattern presenceNode where node.path ~= "sha1.New"
  emit issue code.WeakHash at node
}

binding weakHash199 {
  query pattern presenceNode where node.path ~= "md5.Sum"
  emit issue code.WeakHash at node
}

binding weakHash200 {
  query pattern presenceNode where node.path ~= "sha1.Sum"
  emit issue code.WeakHash at node
}

binding weakRandomValue201 {
  query pattern presenceNode where node.path ~= "rand.Intn"
  emit issue code.WeakRandomValue at node
}

binding weakRandomValue202 {
  query pattern presenceNode where node.path ~= "rand.Int31"
  emit issue code.WeakRandomValue at node
}

binding weakRandomValue203 {
  query pattern presenceNode where node.path ~= "rand.Int31n"
  emit issue code.WeakRandomValue at node
}

binding weakRandomValue204 {
  query pattern presenceNode where node.path ~= "rand.Int63"
  emit issue code.WeakRandomValue at node
}

binding weakRandomValue205 {
  query pattern presenceNode where node.path ~= "rand.Int63n"
  emit issue code.WeakRandomValue at node
}

binding weakRandomValue206 {
  query pattern presenceNode where node.path ~= "rand.Float64"
  emit issue code.WeakRandomValue at node
}

binding weakRandomValue207 {
  query pattern presenceNode where node.path ~= "rand.Float32"
  emit issue code.WeakRandomValue at node
}

binding weakRandomValue208 {
  query pattern presenceNode where node.path ~= "rand.Perm"
  emit issue code.WeakRandomValue at node
}

binding weakRandomValue209 {
  query pattern presenceNode where node.path ~= "rand.Shuffle"
  emit issue code.WeakRandomValue at node
}

binding weakRandomValue210 {
  query pattern presenceNode where node.path ~= "rand.NewSource"
  emit issue code.WeakRandomValue at node
}

binding cloudreveWeakSecretPrng211 {
  query pattern callExpr where callee.path == "util.RandStringRunes" and args.any.literal contains "32"
  emit issue code.CloudreveWeakSecretPrng at call
}

binding cloudreveWeakSecretPrng212 {
  query pattern callExpr where callee.path == "util.RandStringRunes" and args.any.literal contains "64"
  emit issue code.CloudreveWeakSecretPrng at call
}

binding cloudreveWeakSecretPrng213 {
  query pattern callExpr where callee.path == "util.RandStringRunes" and args.any.literal contains "128"
  emit issue code.CloudreveWeakSecretPrng at call
}

binding cloudreveWeakSecretPrng214 {
  query pattern callExpr where callee.path == "util.RandStringRunes" and args.any.literal contains "256"
  emit issue code.CloudreveWeakSecretPrng at call
}

binding constellationLuks2AttachedHeaderActivation215 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "initByDevicePath" and node.context.callPath contains "cryptsetup.Init" and not (node.context.callPath contains "detachHeader") and not (node.context.callPath contains "cryptsetup.InitDataDevice")
  emit issue code.ConstellationLuks2AttachedHeaderActivation at node
}

binding constellationLuks2AttachedHeaderActivation216 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "initByName" and node.context.callPath contains "cryptsetup.InitByName" and not (node.context.callPath contains "detachHeader") and not (node.context.callPath contains "cryptsetup.InitByNameAndHeader")
  emit issue code.ConstellationLuks2AttachedHeaderActivation at node
}

binding weakCipher217 {
  query pattern presenceNode where node.path ~= "des.NewCipher"
  emit issue code.WeakCipher at node
}

binding weakCipher218 {
  query pattern presenceNode where node.path ~= "des.NewTripleDESCipher"
  emit issue code.WeakCipher at node
}

binding weakCipher219 {
  query pattern presenceNode where node.path ~= "rc4.NewCipher"
  emit issue code.WeakCipher at node
}

binding staticIv220 {
  query pattern callExpr where callee.path ~= "cipher.NewCBCEncrypter" and args.any.literal contains "0000000000000000"
  emit issue code.StaticIv at call
}

binding staticIv221 {
  query pattern callExpr where callee.path ~= "cipher.NewCBCDecrypter" and args.any.literal contains "0000000000000000"
  emit issue code.StaticIv at call
}

binding insecurePermission222 {
  query pattern callExpr where callee.path ~= "os.Chmod" and args.any.literal contains "0777"
  emit issue code.InsecurePermission at call
}

binding insecurePermission223 {
  query pattern callExpr where callee.path ~= "os.Chmod" and args.any.literal contains "0666"
  emit issue code.InsecurePermission at call
}

binding insecurePermission224 {
  query pattern callExpr where callee.path ~= "os.MkdirAll" and args.any.literal contains "0777"
  emit issue code.InsecurePermission at call
}

binding insecurePermission225 {
  query pattern callExpr where callee.path ~= "os.WriteFile" and args.any.literal contains "0777"
  emit issue code.InsecurePermission at call
}

binding configExposureReview226 {
  query pattern callExpr where callee.path ~= "MarshalConfig" and args.any.literal contains "false"
  emit issue code.ConfigExposureReview at node
}

binding configExposureReview227 {
  query pattern callExpr where callee.path ~= "instance.MarshalConfig" and args.any.literal contains "false"
  emit issue code.ConfigExposureReview at node
}

binding configExposureReview228 {
  query pattern presenceNode where node.kind == "any" and node.analysis == "type.context" and node.context.field contains "Repositories" and node.context.field contains "Keyring" and not (node.context.method contains "MarshalYAML")
  emit issue code.ConfigExposureReview at node
}

binding inheritedProcessEnvironmentLeak229 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "unshare.Command" and node.context.selector contains "cmd.Env" and node.context.callPath contains "os.Environ" and node.context.literal contains "LOGLEVEL=%d"
  emit issue code.InheritedProcessEnvironmentLeak at node
}

binding containerdCriImageEnvAliasLeak230 {
  query pattern presenceNode where node.analysis == "go.containerd_cri_image_env_alias"
  emit issue code.ContainerdCriImageEnvAliasLeak at node
}

binding containerPrimaryGidMissingSupplementalGroup231 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "setupContainerUser" and node.context.callPath contains "specgen.SetProcessGID" and node.context.selector contains "sc.RunAsGroup" and node.context.callPath contains "specgen.AddProcessAdditionalGid" and not (node.context.callArg contains "specgen.AddProcessAdditionalGid:gid")
  emit issue code.ContainerPrimaryGidMissingSupplementalGroup at node
}

binding mountStubCleanupPathEscape232 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "os.Lstat" and node.context.callPath contains "filepath.Dir" and node.context.callPath contains "os.Remove" and node.context.callPath contains "os.Chtimes" and not (node.context.callPath contains "fs.RootPath")
  emit issue code.MountStubCleanupPathEscape at node
}

binding containersCommonFipsModeMountSymlink233 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "function.context" and node.context.functionName contains "addFIPSModeSubscription" and node.context.callPath contains "filepath.Join" and node.context.callPath contains "fileutils.Exists" and not (node.context.callPath contains "securejoin.SecureJoin")
  emit issue code.ContainersCommonFipsModeMountSymlink at node
}

binding dockerCliProgramDataPluginSearchPath234 {
  query pattern callExpr where callee.path == "filepath.Join" and args.any.literal contains "ProgramData" and args.any.literal contains "Docker" and args.any.literal contains "cli-plugins" and not args.any.literal contains "ProgramFiles"
  emit issue code.DockerCliProgramDataPluginSearchPath at node
}

binding sensitiveSettingsExposureReview235 {
  query pattern callExpr where callee.path ~= "settingspkg.Settings" and args.any.literal contains "PasswordPattern"
  emit issue code.SensitiveSettingsExposureReview at node
}

binding messageBusSecretConfigExposure236 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "bootstrapMessaging.SetOptionsAuthData" and node.context.callPath contains "messaging.NewMessageClient" and node.context.field contains "Optional" and node.context.selector contains "messageBusInfo.Optional" and not (node.context.selector contains "bootstrapMessaging.OptionsPasswordKey")
  emit issue code.MessageBusSecretConfigExposure at node
}

binding kubernetesTemplateSecretLookupExposure237 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "ctrlcfg.GetConfig" and node.context.callPath contains "kubernetes.NewForConfig" and node.context.callPath contains "clientset.CoreV1.Secrets.Get" and node.context.callArg contains "clientset.CoreV1.Secrets:namespace" and node.context.index contains "secret.Data" and node.context.index contains "secret.Data:keyName" and not (containsAny(node.context.scopeCall, [SelfSubjectAccessReview, SubjectAccessReview, CheckAccess, Authorize]))
  emit issue code.KubernetesTemplateSecretLookupExposure at node
}

binding protocolStateReview238 {
  query pattern presenceNode where node.scope == "function" and node.context.call contains "StopT3550" and not (node.context.selector contains "ue.T3550")
  emit issue code.ProtocolStateReview at node
}

binding protocolStateReview239 {
  query pattern presenceNode where node.scope == "function" and node.context.selector contains "PrevATXID" and node.context.call contains "GetLastIDByNodeID"
  emit issue code.ProtocolStateReview at node
}

binding protocolStateReview240 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "pc.c.WriteMsg" and node.context.callPath contains "pc.c.ReadMsg" and node.context.callPath contains "pc.c.Close" and node.context.selector contains "state.Req.Id" and node.context.selector contains "ret.Id" and not (node.context.selector contains "net.Error") and not (node.context.binary contains "proto==\"udp\"")
  emit issue code.ProtocolStateReview at node
}

binding requestScopePolicyReview241 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "ServeHTTP" and node.context.call contains "RequestInfoFrom" and node.context.call contains "Get" and node.context.call contains "serveResource" and not (node.context.selector contains "apiextensions.NamespaceScoped")
  emit issue code.RequestScopePolicyReview at node
}

binding externalResourceNamespaceControl242 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "newBuildPod" and node.context.callPath contains "build.BuilderPodNamespace"
  emit issue code.ExternalResourceNamespaceControl at node
}

binding externalResourceNamespaceControl243 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "deleteBuilderPod" and node.context.callPath contains "build.BuilderPodNamespace"
  emit issue code.ExternalResourceNamespaceControl at node
}

binding externalResourceNamespaceControl244 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "getBuilderPod" and node.context.callPath contains "build.BuilderPodNamespace"
  emit issue code.ExternalResourceNamespaceControl at node
}

binding tenantServiceAccountAdmissionBypass245 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "IsCapsuleUser" and node.context.call contains "NewUserGroupList" and node.context.literal contains "system:serviceaccounts:kube-system" and node.context.call contains "Find" and not (node.context.scopeCall contains "MatchingFieldsSelector") and not (node.context.literal contains ".status.namespaces")
  emit issue code.TenantServiceAccountAdmissionBypass at node
}

binding namespacedSubjectKeyCollision246 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "OwnerRoleBindingsIndexFunc" and node.context.selector contains "rb.Subjects" and node.context.selector contains "subject.Kind" and node.context.selector contains "subject.Name" and node.context.callPath contains "fmt.Sprintf" and not (node.context.selector contains "subject.Namespace") and not (node.context.callPath contains "strings.Join")
  emit issue code.NamespacedSubjectKeyCollision at node
}

binding hopByHopHeaderAuthBypass247 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "impersonateHandler" and node.context.callPath contains "hr.GetUserAndGroups" and node.context.callPath contains "request.Header.Set" and node.context.callPath contains "request.Header.Add" and node.context.literal contains "Authorization" and node.context.literal contains "Impersonate-User" and node.context.literal contains "Impersonate-Group" and not (node.context.callPath contains "n.removingHopByHopHeaders") and not (node.context.callPath contains "httpguts.HeaderValuesContainsToken")
  emit issue code.HopByHopHeaderAuthBypass at node
}

binding basicAuthStreamAuthBypass248 {
  query pattern callExpr where callee.path == "Options" and args.any.literal contains "BasicAuthEnabled" and args.any.literal contains "Creds" and not args.any.literal contains "AuthRequired"
  emit issue code.BasicAuthStreamAuthBypass at node
}

binding proxyClientIpSpoofingBypass249 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "g.management.GetProxy" and node.context.callPath contains "proxy.ServeHTTP" and not (node.context.callPath contains "rewriteRequestSourceIP")
  emit issue code.ProxyClientIpSpoofingBypass at node
}

binding unvalidatedRouteConfigKeyStoredXss250 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "mux.Vars" and node.context.callPath contains "meta.AddConnectionConfKey" and node.context.callPath contains "json.Unmarshal" and not (node.context.callPath contains "validate.ValidateID") and not (node.context.callPath contains "ValidateID")
  emit issue code.UnvalidatedRouteConfigKeyStoredXss at node
}

binding uploadFilePathTraversalWrite251 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "UploadFileSafe" and node.context.callPath contains "uploadFile" and not (node.context.callPath contains "strings.Contains")
  emit issue code.UploadFilePathTraversalWrite at node
}

binding attestationPredicateVerificationBypass252 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "policy.AttestationToPayloadJSON" and node.context.selector contains "c.PredicateType" and node.context.binary contains "len(payload)==0" and node.context.callPath contains "PrintVerification" and node.context.callArg contains "PrintVerification:verified" and not (node.context.callArg contains "PrintVerification:checked") and not (node.context.binary contains "len(checked)==0")
  emit issue code.AttestationPredicateVerificationBypass at node
}

binding admissionSubresourcePolicyReview253 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "shouldIgnore" and node.context.call contains "GetSubresource" and not (node.context.identifier contains "ignoredSubresources")
  emit issue code.AdmissionSubresourcePolicyReview at node
}

binding unboundedStateGrowthReview254 {
  query pattern presenceNode where node.scope == "function" and node.context.call contains "LoadOrStore" and node.context.literal contains "overflow"
  emit issue code.UnboundedStateGrowthReview at node
}

binding unboundedProtocolStateByIdentifier255 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "MessageReceived" and node.context.call contains "Wantlist" and node.context.call contains "splitWantsCancels" and node.context.call contains "Wants" and node.context.call contains "PushTasksTruncated" and not (node.context.scopeCall contains "ByteLen") and not (node.context.selector contains "mh.IDENTITY")
  emit issue code.UnboundedProtocolStateByIdentifier at node
}

binding blockingReadInAcceptLoop256 {
  query pattern presenceNode where node.scope == "function" and node.context.call contains "Accept" and node.context.call contains "ReadTCP" and node.context.call contains "serve"
  emit issue code.BlockingReadInAcceptLoop at node
}

binding underboundedProtocolCountLimit257 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "BtcDecode" and node.context.call contains "ReadVarInt" and node.context.selector contains "txin.Witness" and node.context.const contains "maxWitnessItemsPerInput=500000"
  emit issue code.UnderboundedProtocolCountLimit at node
}

binding integerSizeArithmetic258 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "readBytes" and node.context.call contains "ReadLong" and node.context.call contains "int" and node.context.call contains "make" and not (node.context.selector contains "math.MaxInt")
  emit issue code.IntegerSizeArithmetic at node
  emit issue code.SizeSensitiveIntegerArithmetic at node
  emit sink code.IntegerSizeArithmeticSink at node
}

binding integerSizeArithmetic259 {
  query pattern presenceNode where node.scope == "function" and node.context.call contains "ReadBlockHeader" and node.context.call contains "int" and node.context.call contains "getMaxSliceAllocSize"
  emit issue code.IntegerSizeArithmetic at node
  emit issue code.SizeSensitiveIntegerArithmetic at node
  emit sink code.IntegerSizeArithmeticSink at node
}

binding integerSizeArithmetic260 {
  query pattern presenceNode where node.scope == "function" and node.context.call contains "ReadBlockHeader" and node.context.call contains "int" and node.context.call contains "getMaxMapAllocSize"
  emit issue code.IntegerSizeArithmetic at node
  emit sink code.IntegerSizeArithmeticSink at node
}

binding integerSizeArithmetic261 {
  query pattern presenceNode where node.scope == "function" and node.context.call contains "SkipNBytes" and node.context.call contains "int" and not (node.context.scopeCall contains "SkipNBytesInt64") and not (node.context.functionName contains "SkipNBytesInt64")
  emit issue code.IntegerSizeArithmetic at node
  emit sink code.IntegerSizeArithmeticSink at node
}

binding integerSizeArithmetic262 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "readBlock" and node.context.call contains "ReadLong" and node.context.call contains "make" and not (node.context.selector contains "math.MaxInt")
  emit issue code.IntegerSizeArithmetic at node
  emit sink code.IntegerSizeArithmeticSink at node
}

binding chGoCompressedSizeUint32Overflow263 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "Compress" and node.context.callPath contains "binary.LittleEndian.PutUint32" and node.context.call contains "uint32" and not (node.context.selector contains "math.MaxUint32") and not (node.context.literal contains "compressed size overflows uint32")
  emit issue code.ChGoCompressedSizeUint32Overflow at node
}

binding offsetLengthBoundsOverflow264 {
  query pattern presenceNode where node.scope == "function" and node.context.call contains "int" and node.context.call contains "len" and node.context.callArg contains "int:header.StringOff+header.StringLen" and node.context.callArg contains "len:btf" and node.context.slice contains "btf:header.StringOff:" and node.context.slice contains "btf::header.StringOff+header.StringLen" and not (node.context.scopeCall contains "uint64") and not (node.context.callArg contains "uint64:header.StringOff")
  emit issue code.OffsetLengthBoundsOverflow at node
}

binding offsetLengthBoundsOverflow265 {
  query pattern presenceNode where node.scope == "function" and node.context.call contains "int" and node.context.call contains "len" and node.context.callArg contains "int:header.TypeOff+header.TypeLen" and node.context.callArg contains "len:btf" and node.context.slice contains "btf:header.TypeOff:" and node.context.slice contains "btf::header.TypeOff+header.TypeLen" and not (node.context.scopeCall contains "uint64") and not (node.context.callArg contains "uint64:header.TypeOff")
  emit issue code.OffsetLengthBoundsOverflow at node
}

binding lengthDerivedAllocation266 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "readNBytes" and node.context.call contains "make" and node.context.call contains "ReadByte" and not (node.context.selector contains "reader.data") and not (node.context.selector contains "reader.pos")
  emit issue code.LengthDerivedAllocation at node
}

binding lengthDerivedAllocation267 {
  query pattern presenceNode where node.scope == "function" and node.context.selector contains "r.ContentLength" and node.context.call contains "make" and node.context.callPath contains "io.ReadFull" and not (node.context.callPath contains "io.LimitReader") and not (node.context.callPath contains "http.MaxBytesReader")
  emit issue code.LengthDerivedAllocation at node
}

binding fieldDerivedIndexAccess268 {
  query pattern presenceNode where node.scope == "function" and node.context.call contains "ReadString" and node.context.call contains "BytesNtohs" and node.context.binary contains "len(buf)<=off" and not (node.context.binary contains "len(buf)<off+2")
  emit issue code.FieldDerivedIndexAccess at node
}

binding fieldDerivedIndexAccess269 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "readObject" and node.context.index contains "ber:offset" and node.context.binary contains "contentEnd>len(ber)" and node.context.literal contains "ber2der: BER tag length is more than available data" and not (node.context.binary contains "len(ber)<=offset")
  emit issue code.FieldDerivedIndexAccess at node
}

binding free5gcChfRechargeInfoSplitPanic270 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "RechargePut" and node.context.callPath contains "strings.Split" and node.context.literal contains "rechargingInfo" and node.context.index contains "ueIdRatingGroup:1" and not (node.context.scopeCall contains "len") and not (node.context.callPath contains "c.Query")
  emit issue code.Free5gcChfRechargeInfoSplitPanic at node
}

binding unboundedDelimiterSplitCount271 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "go" and node.context.callPath contains "strings.Split" and node.context.literal contains "." and node.context.callPath contains "len" and node.context.index contains "parts:1" and node.context.callPath contains "base64.RawURLEncoding.DecodeString" and not (node.context.callPath contains "strings.Count") and not (node.context.callPath contains "strings.SplitN")
  emit issue code.UnboundedDelimiterSplitCount at node
}

binding nilMapMergeWritePanic272 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "go" and node.context.selector contains "options.Helm.Values" and node.context.selector contains "options.Helm.ValuesFrom" and node.context.assign contains "values=options.Helm.Values.Data" and node.context.callPath contains "mergeValues" and not (node.context.binary contains "values==nil") and not (node.context.assign contains "values=map[string]interface{}{}") and not (node.context.callPath contains "make")
  emit issue code.NilMapMergeWritePanic at node
}

binding manualZipEntryFileWrite273 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "Decode" and node.context.callPath contains "zip.NewReader" and node.context.callPath contains "filepath.Join" and node.context.selector contains "f.Name" and node.context.callPath contains "copy" and not (node.context.callPath contains "sanitizeArchivePath") and not (node.context.callPath contains "strings.HasPrefix")
  emit issue code.ManualZipEntryFileWrite at node
}

binding manualZipEntryFileWrite274 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "UnzipDirectory" and node.context.callPath contains "zip.OpenReader" and node.context.callPath contains "filepath.Join" and node.context.selector contains "f.Name" and node.context.callPath contains "os.OpenFile" and node.context.callArg contains "os.OpenFile:filePath" and node.context.selector contains "os.O_TRUNC" and not (node.context.callPath contains "os.Stat") and not (node.context.callPath contains "GetInput")
  emit issue code.ManualZipEntryFileWrite at node
}

binding hardlinkedFileOverwriteWithoutClobber275 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "filepath.Abs" and node.context.callPath contains "os.Stat" and node.context.callPath contains "os.Create" and containsAny(node.context.identifier, ["filename", "filePath", "filepath", "path"]) and not (node.context.callPath contains "os.Lstat") and not (node.context.callPath contains "os.Remove") and not (node.context.callPath contains "os.OpenFile") and not (node.context.selector contains "os.O_EXCL")
  emit issue code.HardlinkedFileOverwriteWithoutClobber at node
}

binding ciliumBroadTcFilterCleanup276 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "getTCFilters" and node.context.selector contains "bpfFilter.Name" and node.context.callPath contains "strings.Contains" and node.context.literal contains "cilium" and not (node.context.literal contains "cil_")
  emit issue code.CiliumBroadTcFilterCleanup at node
}

binding ciliumCliEtcdInitMountMismatch277 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "generateDeployment" and node.context.literal contains "etcd-init" and node.context.field contains "MountPath=etcd-data-dir" and node.context.field contains "Args"
  emit issue code.CiliumCliEtcdInitMountMismatch at node
}

binding charmRouteFileStorePathAccess278 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "handlePostFile" and node.context.callPath contains "pattern.Path" and node.context.callPath contains "s.cfg.FileStore.Put" and not (node.context.callPath contains "filepath.Clean")
  emit issue code.CharmRouteFileStorePathAccess at node
}

binding charmRouteFileStorePathAccess279 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "handleGetFile" and node.context.callPath contains "pattern.Path" and node.context.callPath contains "s.cfg.FileStore.Get" and not (node.context.callPath contains "filepath.Clean")
  emit issue code.CharmRouteFileStorePathAccess at node
}

binding charmRouteFileStorePathAccess280 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "handleDeleteFile" and node.context.callPath contains "pattern.Path" and node.context.callPath contains "s.cfg.FileStore.Delete" and not (node.context.callPath contains "filepath.Clean")
  emit issue code.CharmRouteFileStorePathAccess at node
}

binding boundRequestPathDeleteTraversal281 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "Delete" and node.context.callPath contains "self.Validate" and node.context.selector contains "params.Path" and node.context.callPath contains "GetSaveRealPath" and node.context.callPath contains "os.Remove" and not (node.context.callPath contains "filepath.IsLocal") and not (node.context.callPath contains "filepath.Clean")
  emit issue code.BoundRequestPathDeleteTraversal at node
}

binding underwidthFormattedIndex282 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "fmt.Sprintf" and node.context.literal contains "%08b" and node.context.index contains "capabilities:8" and not (node.context.literal contains "%016b")
  emit issue code.UnderwidthFormattedIndex at node
}

binding unsafeBuildClientCommandAssembly283 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "BuildClient" and node.context.literal contains "GO_ENABLED=1 GOOS=%s GOARCH=amd64 go build" and node.context.selector contains "input.Filename" and node.context.call contains "handleFilename" and node.context.callPath contains "fmt.Sprintf" and node.context.callPath contains "exec.Command" and not (node.context.callPath contains "utils.NormalizeString") and not (node.context.callPath contains "utils.StringIsNumber") and not (node.context.callPath contains "utils.IsValidIPAddress")
  emit issue code.UnsafeBuildClientCommandAssembly at node
}

binding unsignedIndexConversionGuardBypass284 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "checkTopicRegister" and node.context.selector contains "data.Idx" and node.context.selector contains "data.Topics" and node.context.call contains "int" and node.context.call contains "len" and node.context.binary contains "data.Idx<0" and not (node.context.binary contains "int(data.Idx)<0")
  emit issue code.UnsignedIndexConversionGuardBypass at node
}

binding countDerivedElementAccess285 {
  query pattern callExpr where callee.method == "BranchPageElement" and args.any.literal contains "Count"
  emit sink code.CountDerivedElementAccessSink at args[0]
  emit issue code.CountDerivedElementAccess at call
}

binding memoryBoundsCheck286 {
  query pattern presenceNode where node.kind == "binop" and node.path ~= "__binop.ne" and node.arg contains "Count" and node.arg contains "0"
  emit check core.MemoryBoundsCheck at node {
    covers endpoint {
      anchor: node
    }
  }
}

binding unqualifiedDatabaseRoutineReview287 {
  query pattern presenceNode where node.scope == "function" and node.context.literal contains "SELECT 'aurora_global_db_status'::regproc" and not (node.context.literal contains "pg_catalog.aurora_global_db_status")
  emit issue code.UnqualifiedDatabaseRoutineReview at node
}

binding unqualifiedDatabaseRoutineReview288 {
  query pattern presenceNode where node.scope == "function" and node.context.literal contains "SELECT 'aurora_global_db_instance_status'::regproc" and not (node.context.literal contains "pg_catalog.aurora_global_db_instance_status")
  emit issue code.UnqualifiedDatabaseRoutineReview at node
}

binding unqualifiedDatabaseRoutineReview289 {
  query pattern presenceNode where node.scope == "function" and node.context.literal contains "SELECT count(1) FROM aurora_global_db_status()" and not (node.context.literal contains "pg_catalog.aurora_global_db_status")
  emit issue code.UnqualifiedDatabaseRoutineReview at node
}

binding unqualifiedDatabaseRoutineReview290 {
  query pattern presenceNode where node.scope == "function" and node.context.literal contains "FROM aurora_global_db_instance_status()" and not (node.context.literal contains "pg_catalog.aurora_global_db_instance_status")
  emit issue code.UnqualifiedDatabaseRoutineReview at node
}

binding unparameterizedSqlQueryParser291 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "goqu.L" and node.context.callArg contains "goqu.L:project"
  emit issue code.UnparameterizedSqlQueryParser at node
}

binding unparameterizedSqlQueryParser292 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "goqu.L" and node.context.callArg contains "goqu.L:group"
  emit issue code.UnparameterizedSqlQueryParser at node
}

binding unparameterizedSqlQueryParser293 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "impl.dbConnection.Query" and node.context.literal contains "SELECT " and node.context.literal contains " and " and node.context.literal contains "='" and not (node.context.identifier contains "queryParams")
  emit issue code.UnparameterizedSqlQueryParser at node
}

binding commandOptionInjection294 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "NodePublishVolume" and node.context.call contains "GetVolumeContext" and node.context.call contains "append" and node.context.call contains "Mount" and not (node.context.scopeCall contains "ParseIP")
  emit issue code.CommandOptionInjection at node
  emit sink code.CommandOptionInjectionSink at node
}

binding commandAllowlistBeforeParsing295 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "go" and node.context.callPath contains "strings.Split" and node.context.callPath contains "d.user.CanExecute" and node.context.callPath contains "runner.ParseCommand" and node.context.callPath contains "exec.Command" and node.context.callBefore contains "d.user.CanExecute>runner.ParseCommand"
  emit issue code.CommandAllowlistBeforeParsing at node
}

binding apiScanFoundActionCommandExecution296 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "postScanHandler" and node.context.callPath contains "c.Bind" and node.context.selector contains "rq.Options" and node.context.callPath contains "ScanFromAPI" and not (node.context.callPath contains "sanitizeAPIScanOptions")
  emit issue code.ApiScanFoundActionCommandExecution at node
}

binding specialFileAccessReview297 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "os.OpenFile" and node.context.literal contains "/proc/self/attr/exec" and not (node.context.scopeCall contains "OpenProcRoot") and not (node.context.scopeCall contains "OpenThreadSelf") and not (node.context.callPath contains "pathrs.Reopen")
  emit issue code.SpecialFileAccessReview at node
}

binding secretComparisonReview298 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "ParseAndValidate" and node.context.selector contains "r.Header.Get" and node.context.call contains "string" and not (node.context.scopeCall contains "ConstantTimeCompare")
  emit issue code.SecretComparisonReview at node
}

binding secretComparisonReview299 {
  query pattern presenceNode where node.scope == "function" and node.context.call contains "BasicAuth" and node.context.binary contains "username==wrapper.username" and node.context.binary contains "password==wrapper.password" and not (node.context.scopeCall contains "ConstantTimeCompare")
  emit issue code.SecretComparisonReview at node
}

binding secretComparisonReview300 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "scrypt.Key" and node.context.callPath contains "bytes.Equal" and node.context.callArg contains "bytes.Equal:source_master_key" and node.context.callArg contains "bytes.Equal:target_master_key" and not (node.context.scopeCall contains "ConstantTimeCompare") and not (node.context.callPath contains "hmac.Equal") and not (node.context.callPath contains "subtle.ConstantTimeCompare")
  emit issue code.SecretComparisonReview at node
}

binding secretComparisonReview301 {
  query pattern presenceNode where node.kind == "binop" and node.op in ["==", "!="] and not (containsAny(node.context.scopeCall, [ConstantTimeCompare, hmac.Equal, subtle.ConstantTimeCompare, CompareHashAndPassword])) and operand(node, where: containsAny(operand.key, [".Password", ".password", ".Secret", ".secret", ".Token", ".token", ".APIKey", ".apiKey", ".AccessKey", ".accessKey"])) and operand(node, where: containsAny(operand.key, [".Password", ".password", ".Secret", ".secret", ".Token", ".token", ".APIKey", ".apiKey", ".AccessKey", ".accessKey"]))
  emit issue code.SecretComparisonReview at node
}

binding fastPasswordHash302 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "GenerateUsers" and node.context.selector contains "user.Password" and node.context.call contains "sha256sum" and not (node.context.call contains "GenerateFromPassword") and not (node.context.identifier contains "bcrypt") and not (node.context.identifier contains "argon2") and not (node.context.identifier contains "scrypt")
  emit issue code.FastPasswordHash at node
}

binding fastPasswordHash303 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "FindByPassword" and node.context.selector contains "user.Password" and node.context.call contains "sha256sum" and not (node.context.call contains "CompareHashAndPassword") and not (node.context.identifier contains "bcrypt") and not (node.context.identifier contains "argon2") and not (node.context.identifier contains "scrypt")
  emit issue code.FastPasswordHash at node
}

binding insecurePermission304 {
  query pattern callExpr where callee.path ~= "os.WriteFile" and args.any.literal contains "0666"
  emit issue code.InsecurePermission at call
}

binding insecurePermission305 {
  query pattern callExpr where callee.path ~= "ioutil.WriteFile" and args.any.literal contains "0777"
  emit issue code.InsecurePermission at call
}

binding insecurePermission306 {
  query pattern callExpr where callee.path ~= "ioutil.WriteFile" and args.any.literal contains "0666"
  emit issue code.InsecurePermission at call
}

binding insecureTempFile307 {
  query pattern callExpr where callee.path ~= "os.Create" and args.any.literal contains "/tmp/"
  emit issue code.InsecureTempFile at call
}

binding certValidationDisabled308 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "go.tls_insecure_skip_verify"
  emit issue code.CertValidationDisabled at node
}

binding jwtUnverified309 {
  query pattern presenceNode where node.path ~= "ParseUnverified"
  emit issue code.JwtUnverified at node
}

binding csrfDisabled310 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "CheckOrigin" and node.context.paramType contains "http.Request" and node.context.return contains "true" and not (node.context.callPath contains "r.Header.Get") and not (node.context.selector contains "r.Host")
  emit issue code.CsrfDisabled at node
}

binding dtlsEpochZeroApplicationData311 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "handleIncomingPacket" and node.context.callPath contains "h.Unmarshal" and node.context.selector contains "h.epoch" and node.context.callPath contains "c.state.cipherSuite.decrypt" and node.context.callPath contains "r.Unmarshal" and node.context.selector contains "c.decrypted" and not (node.context.binary contains "h.epoch==0") and not (node.context.binary contains "0==h.epoch")
  emit issue code.DtlsEpochZeroApplicationData at node
}

binding grpcGatewayCommonNameAuthenticationBypass312 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "peer.FromContext" and node.context.selector contains "tlsInfo.State.VerifiedChains" and node.context.selector contains "chains.Subject.CommonName" and not (node.context.literal contains "grpcgateway-accept")
  emit issue code.GrpcGatewayCommonNameAuthenticationBypass at node
}

binding unverifiedFederationMissingEvents313 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "missingResp.Events.UntrustedEvents" and node.context.callPath contains "t.cacheAndReturn" and not (node.context.callPath contains "ev.VerifyEventSignatures")
  emit issue code.UnverifiedFederationMissingEvents at node
}

binding samlXmlRoundtripValidationBypass314 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "HandlePOST" and node.context.callPath contains "base64.StdEncoding.DecodeString" and node.context.callPath contains "verifyResponseSig" and node.context.callPath contains "xml.Unmarshal" and node.context.callPath contains "p.validateSubject" and not (node.context.callPath contains "xrv.Validate")
  emit issue code.SamlXmlRoundtripValidationBypass at node
}

binding samlAssertionWrappingBypass315 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "go" and node.context.callPath contains "etreeutils.NSDetatch" and node.context.callPath contains "v.context.Validate" and node.context.callPath contains "elt.RemoveChild" and node.context.callPath contains "elt.AddChild" and not (node.context.callPath contains "elt.ChildElements") and not (node.context.callPath contains "child.NamespaceURI") and not (node.context.varName contains "errMultipleAssertions")
  emit issue code.SamlAssertionWrappingBypass at node
}

binding resourceHandle316 {
  query pattern presenceNode where node.path ~= "net.Dial"
  emit issue code.ResourceHandle at node
  emit sink code.ResourceHandleSink at node
}

binding resourceHandle317 {
  query pattern presenceNode where node.path ~= "net.DialTimeout"
  emit issue code.ResourceHandle at node
  emit sink code.ResourceHandleSink at node
}

binding resourceHandle318 {
  query pattern presenceNode where node.path ~= "os.Open"
  emit issue code.ResourceHandle at node
  emit sink code.ResourceHandleSink at node
}

binding resourceHandle319 {
  query pattern presenceNode where node.path ~= "os.OpenFile"
  emit issue code.ResourceHandle at node
  emit sink code.ResourceHandleSink at node
}

binding resourceHandle320 {
  query pattern presenceNode where node.path ~= "os.Create"
  emit issue code.ResourceHandle at node
  emit sink code.ResourceHandleSink at node
}

binding resourceRelease321 {
  query pattern callExpr where callee.method == "Close"
  emit check core.ResourceRelease at call {
    covers postDominates {
      from: call
      to: call
    }
  }
}

binding permissiveCors322 {
  query pattern callExpr where callee.path ~= "Header.Set" and args.any.literal contains "Access-Control-Allow-Origin" and args.any.literal contains "*"
  emit issue code.PermissiveCors at call
}

binding permissiveCors323 {
  query pattern callExpr where callee.path ~= "Header.Add" and args.any.literal contains "Access-Control-Allow-Origin" and args.any.literal contains "*"
  emit issue code.PermissiveCors at call
}

binding corsWildcardPrefixTruncation324 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "parseWildcardRules" and node.context.selector contains "c.AllowOrigins" and node.context.selector contains "c.AllowWildcard" and node.context.callPath contains "strings.Index" and node.context.callPath contains "append" and node.context.slice contains "o::i-1"
  emit issue code.CorsWildcardPrefixTruncation at node
}

binding missingFrameProtection325 {
  query pattern callExpr where callee.path ~= "Header.Set" and args.any.literal contains "X-Frame-Options" and args.any.literal contains "ALLOWALL"
  emit issue code.MissingFrameProtection at call
}

binding missingFrameProtection326 {
  query pattern callExpr where callee.path ~= "Header.Add" and args.any.literal contains "X-Frame-Options" and args.any.literal contains "ALLOWALL"
  emit issue code.MissingFrameProtection at call
}

binding insecureCookie327 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "go.insecure_cookie_httponly_false"
  emit issue code.InsecureCookie at node
}

binding insecureCookie328 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "go.insecure_cookie_secure_false"
  emit issue code.InsecureCookie at node
}

binding insecureCookie329 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "go.insecure_cookie_setcookie_secure_false"
  emit issue code.InsecureCookie at node
}

binding cleartextChannel330 {
  query pattern callExpr where callee.path ~= "http.Get" and args.any.literal contains "http://" and not args.any.literal contains "127.0"
  emit issue code.CleartextChannel at call
}

binding cleartextChannel331 {
  query pattern callExpr where callee.path ~= "http.Post" and args.any.literal contains "http://" and not args.any.literal contains "127.0"
  emit issue code.CleartextChannel at call
}

binding cleartextChannel332 {
  query pattern callExpr where callee.path ~= "http.NewRequest" and args.any.literal contains "http://" and not args.any.literal contains "127.0"
  emit issue code.CleartextChannel at call
}

binding lockAcquire333 {
  query pattern presenceNode where node.path ~= "Lock"
  emit issue code.LockAcquire at node
}

binding lockAcquire334 {
  query pattern presenceNode where node.path ~= "RLock"
  emit issue code.LockAcquire at node
}

binding recursiveJsonMarshalUnderLock335 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "MarshalJSON" and node.context.callPath contains "ps.mtx.Lock" and node.context.callPath contains "cmtjson.Marshal" and node.context.selector contains "ps" and not (node.context.identifier contains "jsonPeerState") and not (node.context.callPath contains "ps.Count") and not (node.context.callPath contains "ps.Total")
  emit issue code.RecursiveJsonMarshalUnderLock at node
}

binding locklessSharedMutation336 {
  query pattern presenceNode where node.kind == "any" and node.analysis == "global.mutation" and node.context.field contains "ctx"
  emit issue code.LocklessSharedMutation at node
}

binding locklessSharedMutation337 {
  query pattern presenceNode where node.kind == "any" and node.analysis == "global.mutation" and node.context.field contains "roundTripper"
  emit issue code.LocklessSharedMutation at node
}

binding locklessSharedMutation338 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "ReplaceIndividualSubcription" and node.context.call contains "GetSubscription" and node.context.index contains "bsfContext.BsfSelf.Subscriptions:subId" and node.context.call contains "Status" and node.context.selector contains "http.StatusNoContent" and not (node.context.selector contains "http.StatusNotFound")
  emit issue code.LocklessSharedMutation at node
}

binding powerShellCommandStringWrapperEnv339 {
  query pattern presenceNode where node.analysis == "go.powershell_command_string_wrapper_env"
  emit issue code.CommandStringWrapperExecution at node
}

binding lockRelease339 {
  query pattern callExpr where callee.method == "Unlock"
  emit check core.LockRelease at call {
    covers postDominates {
      from: call
      to: call
    }
  }
}

binding lockRelease340 {
  query pattern callExpr where callee.method == "RUnlock"
  emit check core.LockRelease at call {
    covers postDominates {
      from: call
      to: call
    }
  }
}

binding publicUserListRouteMissingAuthentication343 {
  query pattern presenceNode where node.analysis == "go.public_user_list_route_missing_auth"
  emit issue code.PublicUserListRouteMissingAuthentication at node
}

binding goStaticPosixCleanWindowsTraversal344 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "url.PathUnescape" and node.context.callPath contains "filepath.Join" and node.context.callPath contains "path.Clean" and containsAny(node.context.callPath, ["os.Stat", "File"]) and not (node.context.callPath contains "filepath.Clean")
  emit issue code.GoStaticPosixCleanWindowsTraversal at node
}

binding tlsWeakClientAuth345 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "tls.RequestClientCert"
  emit issue code.CertValidationDisabled at node
}

binding tlsWeakClientAuth346 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "tls.RequireAnyClientCert"
  emit issue code.CertValidationDisabled at node
}

binding tlsWeakClientAuth347 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "tls.VerifyClientCertIfGiven"
  emit issue code.CertValidationDisabled at node
}
