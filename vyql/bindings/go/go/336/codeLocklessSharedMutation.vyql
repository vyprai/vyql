module bindings.go.generated.m48a985c1;

binding locklessSharedMutation336 {
  query pattern presenceNode where node.kind == "any" and node.analysis == "global.mutation" and node.context.field contains "ctx"
  emit issue code.LocklessSharedMutation at node
}
