module bindings.go.generated.m48a985c1;

binding fastPasswordHash303 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "FindByPassword" and node.context.selector contains "user.Password" and node.context.call contains "sha256sum" and not (node.context.call contains "CompareHashAndPassword") and not (node.context.identifier contains "bcrypt") and not (node.context.identifier contains "argon2") and not (node.context.identifier contains "scrypt")
  emit issue code.FastPasswordHash at node
}
