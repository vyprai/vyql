module bindings.go.generated.m48a985c1;

binding locklessSharedMutation338 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "ReplaceIndividualSubcription" and node.context.call contains "GetSubscription" and node.context.index contains "bsfContext.BsfSelf.Subscriptions:subId" and node.context.call contains "Status" and node.context.selector contains "http.StatusNoContent" and not (node.context.selector contains "http.StatusNotFound")
  emit issue code.LocklessSharedMutation at node
}
