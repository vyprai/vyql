module bindings.go.generated.m48a985c1;

binding dockerCliProgramDataPluginSearchPath234 {
  query pattern presenceNode where node.kind == "any" and node.path == "filepath.Join" and node.token contains "ProgramData" and node.token contains "Docker" and node.token contains "cli-plugins" and not (node.token contains "ProgramFiles")
  emit issue code.DockerCliProgramDataPluginSearchPath at node
}
