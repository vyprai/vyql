module bindings.go.generated.m48a985c1;

binding unparameterizedSqlQueryParser293 {
  query pattern presenceNode where node.scope == "function" and node.context.callPath contains "impl.dbConnection.Query" and node.context.literal contains "SELECT " and node.context.literal contains " and " and node.token contains "='" and not (node.token contains "queryParams")
  emit issue code.UnparameterizedSqlQueryParser at node
}
