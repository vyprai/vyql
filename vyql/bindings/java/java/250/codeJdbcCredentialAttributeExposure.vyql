module bindings.java.generated.m420f6b67;

binding jdbcCredentialAttributeExposure250 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "connect" and node.context.call contains "tokenizeDatabase" and node.context.call contains "tokenizeURLProperties" and node.context.call contains "NetConnection" and not (node.context.scopeCall contains "appendDatabaseAttributes")
  emit issue code.JdbcCredentialAttributeExposure at node
}
