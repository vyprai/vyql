module vypr.smartcontract;

rule Reentrancy {
  meta {
    cwe: [CWE_841]
    id: "VYQL-SC-004"
    severity: high
  }
  query concept as first where first.concept == code.ExternalCall reaches concept as second where second.concept == code.StateWrite select second
}
