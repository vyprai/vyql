module vypr.identity;

rule ToxicWorkloadExposure {
  meta {
    id: "VYQL-IDN-004"
    severity: critical
  }
  fact identity.WorkloadIdentity as w
  where reach(cloud.Internet, w.workload) and grant(w, identity.AdminPrivilege)
}
