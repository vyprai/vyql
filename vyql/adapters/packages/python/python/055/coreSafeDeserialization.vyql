module bindings.python.migration.m3d9ccbad;

binding safeDeserialization55 {
  requires {
    dependency("pyyaml")
  }
  query pattern callExpr where callee.path ~= "yaml.safe_load"
  emit check core.SafeDeserialization at call {
    covers path {
      from: call
      to: call
    }
  }
}
