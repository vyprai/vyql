module bindings.python.migration.m3d9ccbad;

binding unboundedResourceOperation71 {
  requires {
    dependency("zlib")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "zlib.decompress"
  emit issue code.UnboundedResourceOperation at node
}
