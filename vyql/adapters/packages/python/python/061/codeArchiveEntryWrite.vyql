module bindings.python.migration.m3d9ccbad;

binding archiveEntryWrite61 {
  requires {
    dependency("zipfile")
  }
  query pattern callExpr where callee.path ~= "zipfile.ZipFile.extract"
  emit sink code.ArchiveEntryWrite at args[0]
}
