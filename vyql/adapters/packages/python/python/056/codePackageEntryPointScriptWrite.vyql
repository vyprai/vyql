module bindings.python.generated.m3d9ccbad;

binding packageEntryPointScriptWrite56 {
  requires {
    dependency("pip")
  }
  query pattern presenceNode where node.scope == "function" and node.token contains "PipScriptMaker" and node.token contains "super().make" and node.token contains "_raise_for_invalid_entrypoint(specification)" and not (node.token contains "self.target_dir")
  emit issue code.PackageEntryPointScriptWrite at node
}
