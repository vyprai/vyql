module bindings.python.migration.m3d9ccbad;

binding pathCanonicalization78 {
  requires {
    dependency("werkzeug")
  }
  query pattern callExpr where callee.path ~= "werkzeug.utils.safe_join"
  emit check core.PathCanonicalization at call {
    covers path {
      from: call
      to: call
    }
  }
}
