module bindings.ruby.migration.m5895bccf;

binding pathCanonicalization20 {
  requires {
    dependency("ruby")
  }
  query pattern callExpr where callee.path ~= "File.expand_path"
  emit check core.PathCanonicalization at call {
    covers path {
      from: call
      to: call
    }
  }
}
