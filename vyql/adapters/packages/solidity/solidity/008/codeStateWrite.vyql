module bindings.solidity.migration.m72929a80;

binding stateWrite8 {
  requires {
    dependency("solidity")
  }
  query pattern presenceNode where node.path ~= "state_write"
  emit issue code.StateWrite at node
}
