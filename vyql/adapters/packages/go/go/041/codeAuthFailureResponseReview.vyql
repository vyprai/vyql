module bindings.go.migration.m39efcd29;

binding authFailureResponseReview41 {
  requires {
    dependency("github.com/authelia/authelia")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "ctx.Error" and node.token contains "Unable to parse target URL"
  emit issue code.AuthFailureResponseReview at node
}
