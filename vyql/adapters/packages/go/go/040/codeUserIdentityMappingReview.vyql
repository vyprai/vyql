module bindings.go.migration.m39efcd29;

binding userIdentityMappingReview40 {
  requires {
    dependency("github.com/ubuntu/authd")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "function_name:userPreCheck" and node.token contains "call_path:json.Unmarshal" and node.token contains "call_path:s.userManager.GenerateUID" and node.token contains "call_path:s.userManager.GenerateGID" and not (node.token contains "call_path:s.userManager.RegisterUserPreAuth")
  emit issue code.UserIdentityMappingReview at node
}
