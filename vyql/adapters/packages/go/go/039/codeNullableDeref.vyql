module bindings.go.migration.m39efcd29;

binding nullableDeref39 {
  requires {
    dependency("github.com/free5gc/ausf/internal/context")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "function_name:UeAuthPostRequestProcedure" and node.token contains "selector:updateAuthenticationInfo.ResynchronizationInfo" and node.token contains "call:GetSupiFromSuciSupiMap" and node.token contains "call:GetAusfUeContext" and node.token contains "selector:ausfCurrentContext.Rand" and not (node.scopeCall.any contains "CheckIfSuciSupiPairExists") and not (node.scopeCall.any contains "CheckIfAusfUeContextExists")
  emit issue code.NullableDeref at node
}
