module bindings.perl.migration.ma164dcec;

binding archiveEntryWrite20 {
  requires {
    dependency("Archive::Extract")
  }
  query pattern callExpr where callee.path ~= "Archive.Extract.extract"
  emit sink code.ArchiveEntryWrite at args.any
}
