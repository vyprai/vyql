module bindings.rust.migration.m222602a5;

binding shellEscape39 {
  requires {
    dependency("shell-escape")
  }
  query pattern callExpr where callee.path ~= "shell_escape.escape"
  emit check core.ShellEscape at call {
    covers path {
      from: call
      to: call
    }
  }
}
