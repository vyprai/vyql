module bindings.java.migration.m9ef49b0f;

binding unescapedExceptionMessageMarkup49 {
  requires {
    dependency("org.kohsuke.stapler")
  }
  query pattern presenceNode where node.scope == "function" and node.token contains "call_path:Pattern.compile" and node.token contains "call_path:FormValidation.errorWithMarkup" and node.token contains "call:getMessage" and not (node.token contains "call_path:Util.escape")
  emit issue code.UnescapedExceptionMessageMarkup at node
}
