module bindings.groovy.migration.m8aff9f26;

binding weakHash0 {
  requires {
    dependency("com.squareup.okio:okio")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "com.squareup.okio.okio.hash"
  emit issue code.WeakHash at node
}
