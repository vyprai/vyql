module bindings.groovy.migration.m3d3e6904;

binding redirectTarget2 {
  requires {
    dependency("org.seleniumhq.selenium:selenium-ie-driver")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "withInitialBrowserUrl"
  emit issue code.RedirectTarget at node
  emit sink code.RedirectTargetSink at node
}
