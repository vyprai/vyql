import code.{Deserialization, FilePathAccess, NetworkData, UrlFetch}
adapter groovy {
  package "com.typesafe:config" {
    sink method "parse" -> code.Deserialization
    sink method "load" -> code.Deserialization
  }
}
