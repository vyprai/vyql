module bindings.groovy.migration.mcc3d1d4f;

binding rawMemoryCopySize0 {
  requires {
    dependency("net.java.dev.jna:jna")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "read"
  emit issue code.RawMemoryCopySize at node
  emit sink code.RawMemoryCopySizeSink at node
}
