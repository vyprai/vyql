module bindings.groovy.migration.m018359cb;

binding regexCompile0 {
  requires {
    dependency("oro:oro")
  }
  query pattern callExpr where callee.method == "compile"
  emit issue code.RegexCompile at call
  emit sink code.RegexCompileSink at call
}
