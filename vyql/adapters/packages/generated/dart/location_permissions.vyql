import code.{CommandExecution, FilePathAccess, RedirectTarget}

adapter dart {
  package "location_permissions" {
    flag code.RedirectTarget on any {
      method "openAppSettings"
    }
    control method "requestPermissions" -> core.AuthorizationCheck
  }
}
