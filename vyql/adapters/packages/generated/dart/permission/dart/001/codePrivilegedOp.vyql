module bindings.dart.migration.m98c0c7f4;

binding privilegedOp1 {
  requires {
    dependency("permission")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "requestSinglePermission"
  emit issue code.PrivilegedOp at node
  emit sink code.PrivilegedOpSink at node
}
