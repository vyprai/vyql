module bindings.dart.migration.m56895ad0;

binding sessionId0 {
  requires {
    dependency("firebase_analytics_web")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "getSessionId"
  emit issue code.SessionId at node
  emit sink code.SessionIdSink at node
}
