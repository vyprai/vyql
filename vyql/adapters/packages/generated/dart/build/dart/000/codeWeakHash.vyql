module bindings.dart.migration.m60609d5f;

binding weakHash0 {
  requires {
    dependency("build")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "build.hash"
  emit issue code.WeakHash at node
}
