module bindings.dart.migration.md1712474;

binding deserialization1 {
  requires {
    dependency("build_config")
  }
  query pattern callExpr where callee.path ~= "BuildConfig.fromBuildConfigDir"
  emit sink code.Deserialization at args[0]
}
