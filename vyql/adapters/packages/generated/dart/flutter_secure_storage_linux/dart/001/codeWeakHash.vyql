module bindings.dart.migration.m1346cefe;

binding weakHash1 {
  requires {
    dependency("flutter_secure_storage_linux")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "flutter_secure_storage_linux.hash"
  emit issue code.WeakHash at node
}
