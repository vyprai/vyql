module bindings.dart.migration.m7fc7337d;

binding privilegedOp11 {
  requires {
    dependency("bitsdojo_window_linux")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "maximizeWindow"
  emit issue code.PrivilegedOp at node
  emit sink code.PrivilegedOpSink at node
}
