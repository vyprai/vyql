import code.{RedirectTarget}

adapter dart {
  package "map_launcher" {
    flag code.RedirectTarget on any {
      method "showMarker"
    }
    flag code.RedirectTarget on any {
      method "showDirections"
    }
  }
}
