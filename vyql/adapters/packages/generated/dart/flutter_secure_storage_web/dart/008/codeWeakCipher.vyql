module bindings.dart.migration.m90979562;

binding weakCipher8 {
  requires {
    dependency("flutter_secure_storage_web")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "_crypto"
  emit issue code.WeakCipher at node
}
