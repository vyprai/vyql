module bindings.dart.migration.me0c7bf86;

binding redirectTarget2 {
  requires {
    dependency("flutter_modular")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "setInitialRoute"
  emit issue code.RedirectTarget at node
  emit sink code.RedirectTargetSink at node
}
