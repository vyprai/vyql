module bindings.dart.migration.me014437f;

binding weakHash2 {
  requires {
    dependency("awesome_notifications")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "rstr_md5"
  emit issue code.WeakHash at node
}
