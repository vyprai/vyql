import code.{Deserialization}
import core.{AuthenticationCheck}

adapter scala {
  package "org.apache.hadoop:hadoop-auth" {
    sink method "AuthToken.parse" -> code.Deserialization
    control method "Authenticator.authenticate" -> core.AuthenticationCheck
  }
}
