module bindings.scala.migration.mf9b36ce7;

binding weakRandomValue5 {
  requires {
    dependency("org.apache.activemq:activemq-spring")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "setSecureRandomAlgorithm"
  emit issue code.WeakRandomValue at node
}
