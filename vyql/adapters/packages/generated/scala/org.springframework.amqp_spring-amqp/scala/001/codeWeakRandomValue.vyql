module bindings.scala.migration.mb8442c3c;

binding weakRandomValue1 {
  requires {
    dependency("org.springframework.amqp:spring-amqp")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "random"
  emit issue code.WeakRandomValue at node
}
