import code.{RegexCompile}
adapter cpp {
  package "ctre" {
    flag code.RegexCompile on any {
      method "exec"
    }
    flag code.RegexCompile on any {
      method "match"
    }
    flag code.RegexCompile on any {
      method "search"
    }
  }
}
