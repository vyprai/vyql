module bindings.cpp.migration.md92f6a5c;

binding weakRandomValue0 {
  requires {
    dependency("boost-random")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "random_device"
  emit issue code.WeakRandomValue at node
}
