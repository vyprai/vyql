module bindings.cpp.migration.mc96d565b;

binding weakRandomValue0 {
  requires {
    dependency("kuku")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "MakeRandomItem"
  emit issue code.WeakRandomValue at node
}
