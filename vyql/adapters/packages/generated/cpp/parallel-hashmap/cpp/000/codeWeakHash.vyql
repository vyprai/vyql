module bindings.cpp.migration.made3f8eb;

binding weakHash0 {
  requires {
    dependency("parallel-hashmap")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "parallel.hashmap.hash"
  emit issue code.WeakHash at node
}
