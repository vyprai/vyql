module bindings.cpp.migration.m9cb55992;

binding regexCompile0 {
  requires {
    dependency("tre")
  }
  query pattern presenceNode where node.path ~= "tre.compile"
  emit issue code.RegexCompile at node
  emit sink code.RegexCompileSink at node
}
