module bindings.rust.migration.m5589dbe9;

binding weakRandomValue0 {
  requires {
    dependency("oorandom")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "rand_u32"
  emit issue code.WeakRandomValue at node
}
