module bindings.rust.migration.me5130b49;

binding weakRandomValue2 {
  requires {
    dependency("rand_hc")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "try_fill_bytes"
  emit issue code.WeakRandomValue at node
}
