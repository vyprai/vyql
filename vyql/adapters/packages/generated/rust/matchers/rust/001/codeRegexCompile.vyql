module bindings.rust.migration.mc6514917;

binding regexCompile1 {
  requires {
    dependency("matchers")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "new_anchored"
  emit issue code.RegexCompile at node
  emit sink code.RegexCompileSink at node
}
