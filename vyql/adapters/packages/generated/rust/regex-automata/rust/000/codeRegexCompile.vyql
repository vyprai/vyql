module bindings.rust.migration.m89a9404e;

binding regexCompile0 {
  requires {
    dependency("regex-automata")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "search"
  emit issue code.RegexCompile at node
  emit sink code.RegexCompileSink at node
}
