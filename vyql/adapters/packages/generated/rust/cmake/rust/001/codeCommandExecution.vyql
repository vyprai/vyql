module bindings.rust.migration.ma2f92ddf;

binding commandExecution1 {
  requires {
    dependency("cmake")
  }
  query pattern callExpr where callee.method == "cmake_configure_command"
  emit sink code.CommandExecution at args[0]
}
