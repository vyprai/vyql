module bindings.rust.migration.m92c6dca7;

binding weakRandomValue1 {
  requires {
    dependency("crypto-bigint")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "try_random"
  emit issue code.WeakRandomValue at node
}
