import code.{RegexCompile}

adapter rust {
  package "proptest" {
    flag code.RegexCompile on any {
      method "string_regex"
    }
    flag code.RegexCompile on any {
      method "string_regex_parsed"
    }
  }
}
