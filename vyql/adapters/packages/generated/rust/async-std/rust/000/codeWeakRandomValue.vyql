module bindings.rust.migration.mccc94b36;

binding weakRandomValue0 {
  requires {
    dependency("async-std")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "async.std.random"
  emit issue code.WeakRandomValue at node
}
