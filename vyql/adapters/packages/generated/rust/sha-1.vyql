import code.{WeakHash}
adapter rust {
  package "sha-1" {
    flag code.WeakHash on any {
      method "update_blocks"
    }
    flag code.WeakHash on any {
      method "finalize_fixed_core"
    }
  }
}
