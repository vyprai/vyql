module bindings.rust.migration.m943bf737;

binding weakRandomValue3 {
  requires {
    dependency("getrandom")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "u64"
  emit issue code.WeakRandomValue at node
}
