import code.{WeakRandomValue}
adapter rust {
  package "crypto-bigint" {
    flag code.WeakRandomValue on any {
      method "random"
    }
    flag code.WeakRandomValue on any {
      method "try_random"
    }
  }
}
