module bindings.rust.migration.ma5bae377;

binding weakHash1 {
  requires {
    dependency("hmac")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "finalize_fixed_core"
  emit issue code.WeakHash at node
}
