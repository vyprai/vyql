module bindings.rust.migration.mac1e0436;

binding regexCompile0 {
  requires {
    dependency("utf8-ranges")
  }
  query pattern callExpr where callee.method == "matches"
  emit issue code.RegexCompile at call
  emit sink code.RegexCompileSink at call
}
