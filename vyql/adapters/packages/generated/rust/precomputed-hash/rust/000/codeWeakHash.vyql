module bindings.rust.migration.m8e5dfe4d;

binding weakHash0 {
  requires {
    dependency("precomputed-hash")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "precomputed_hash"
  emit issue code.WeakHash at node
}
