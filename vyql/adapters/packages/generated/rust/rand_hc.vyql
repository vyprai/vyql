import code.{WeakRandomValue}
adapter rust {
  package "rand_hc" {
    flag code.WeakRandomValue on any {
      method "try_next_u32"
    }
    flag code.WeakRandomValue on any {
      method "try_next_u64"
    }
    flag code.WeakRandomValue on any {
      method "try_fill_bytes"
    }
  }
}
