module bindings.rust.migration.mb1ee4775;

binding weakHash0 {
  requires {
    dependency("polyval")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "finalize"
  emit issue code.WeakHash at node
}
