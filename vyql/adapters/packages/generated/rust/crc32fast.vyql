import code.{WeakHash}

adapter rust {
  package "crc32fast" {
    flag code.WeakHash on any {
      method "hash"
    }
    flag code.WeakHash on any {
      method "update"
    }
    flag code.WeakHash on any {
      method "finalize"
    }
  }
}
