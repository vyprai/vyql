module bindings.rust.migration.m1bbcfdab;

binding weakRandomValue0 {
  requires {
    dependency("aead")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "encrypt_in_place"
  emit issue code.WeakRandomValue at node
}
