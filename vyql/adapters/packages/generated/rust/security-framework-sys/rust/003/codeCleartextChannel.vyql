module bindings.rust.migration.m137cc19f;

binding cleartextChannel3 {
  requires {
    dependency("security-framework-sys")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "SSLCreateContext"
  emit issue code.CleartextChannel at node
}
