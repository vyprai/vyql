module bindings.rust.migration.m36d8a95d;

binding weakRandomValue0 {
  requires {
    dependency("fastrand")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "gen_u32"
  emit issue code.WeakRandomValue at node
}
