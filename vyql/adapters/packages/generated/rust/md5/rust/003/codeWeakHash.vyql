module bindings.rust.migration.m820bc722;

binding weakHash3 {
  requires {
    dependency("md5")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "compute"
  emit issue code.WeakHash at node
}
