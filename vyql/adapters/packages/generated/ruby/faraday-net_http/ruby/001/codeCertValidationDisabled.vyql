module bindings.ruby.migration.m096e689f;

binding certValidationDisabled1 {
  requires {
    dependency("faraday-net_http")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "configure_ssl"
  emit issue code.CertValidationDisabled at node
}
