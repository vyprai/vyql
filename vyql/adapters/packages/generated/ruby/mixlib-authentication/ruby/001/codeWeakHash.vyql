module bindings.ruby.migration.mddd10154;

binding weakHash1 {
  requires {
    dependency("mixlib-authentication")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "Mixlib::Authentication::Digester.hash_string"
  emit issue code.WeakHash at node
}
