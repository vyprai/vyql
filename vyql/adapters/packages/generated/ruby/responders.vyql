import code.{HtmlRender, RedirectTarget}

adapter ruby {
  package "responders" {
    sink method "respond_with" -> code.HtmlRender
    flag code.RedirectTarget on any {
      method "respond_with"
    }
  }
}
