import code.{RegexCompile}
adapter ruby {
  package "strscan" {
    flag code.RegexCompile on any {
      method "scan"
    }
    flag code.RegexCompile on any {
      method "scan_until"
    }
    flag code.RegexCompile on any {
      method "search_full"
    }
  }
}
