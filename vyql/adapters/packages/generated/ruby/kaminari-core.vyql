import code.{HtmlRender, RedirectTarget}

adapter ruby {
  package "kaminari-core" {
    flag code.RedirectTarget on any {
      method "page_url_for"
    }
    sink method "page_url_for" -> code.HtmlRender
  }
}
