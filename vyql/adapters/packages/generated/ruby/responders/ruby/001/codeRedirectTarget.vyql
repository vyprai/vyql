module bindings.ruby.migration.m8ad8dde5;

binding redirectTarget1 {
  requires {
    dependency("responders")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "respond_with"
  emit issue code.RedirectTarget at node
  emit sink code.RedirectTargetSink at node
}
