module bindings.swift.migration.m0b81ba3c;

binding weakRandomValue0 {
  requires {
    dependency("github.com/leif-ibsen/BigInt")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "randomLessThan"
  emit issue code.WeakRandomValue at node
}
