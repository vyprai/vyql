module bindings.kotlin.migration.mec43e687;

binding redirectTarget0 {
  requires {
    dependency("com.google.android:support-v4")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "startActivity"
  emit issue code.RedirectTarget at node
  emit sink code.RedirectTargetSink at node
}
