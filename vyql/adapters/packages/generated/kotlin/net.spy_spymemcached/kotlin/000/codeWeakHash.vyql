module bindings.kotlin.migration.mda0f55e2;

binding weakHash0 {
  requires {
    dependency("net.spy:spymemcached")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "net.spy.spymemcached.hash"
  emit issue code.WeakHash at node
}
