module bindings.kotlin.migration.ma8d8feef;

binding redirectTarget19 {
  requires {
    dependency("javax.servlet:servlet-api")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "sendRedirect"
  emit issue code.RedirectTarget at node
  emit sink code.RedirectTargetSink at node
}
