module bindings.kotlin.migration.m8e564aef;

binding redirectTarget4 {
  requires {
    dependency("com.lowagie:itext")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "Chunk.setAnchor"
  emit issue code.RedirectTarget at node
  emit sink code.RedirectTargetSink at node
}
