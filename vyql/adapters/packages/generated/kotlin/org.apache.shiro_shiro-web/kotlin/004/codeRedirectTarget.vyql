module bindings.kotlin.migration.m36422801;

binding redirectTarget4 {
  requires {
    dependency("org.apache.shiro:shiro-web")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "issueRedirect"
  emit issue code.RedirectTarget at node
  emit sink code.RedirectTargetSink at node
}
