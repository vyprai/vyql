module bindings.kotlin.migration.meafcdfaf;

binding weakHash1 {
  requires {
    dependency("com.sun.jersey:jersey-core")
  }
  query pattern presenceNode where node.path ~= "com.sun.jersey.jersey.core.hash"
  emit issue code.WeakHash at node
}
