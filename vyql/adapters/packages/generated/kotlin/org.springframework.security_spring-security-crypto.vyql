import code.{WeakHash}

adapter kotlin {
  package "org.springframework.security:spring-security-crypto" {
    flag code.WeakHash on any {
      method "Md4PasswordEncoder.encode"
    }
    flag code.WeakHash on any {
      method "Md4PasswordEncoder.matches"
    }
  }
}
