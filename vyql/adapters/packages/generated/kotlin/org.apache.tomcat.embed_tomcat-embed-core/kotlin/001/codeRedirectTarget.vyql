module bindings.kotlin.migration.m29054f43;

binding redirectTarget1 {
  requires {
    dependency("org.apache.tomcat.embed:tomcat-embed-core")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "dispatch"
  emit issue code.RedirectTarget at node
  emit sink code.RedirectTargetSink at node
}
