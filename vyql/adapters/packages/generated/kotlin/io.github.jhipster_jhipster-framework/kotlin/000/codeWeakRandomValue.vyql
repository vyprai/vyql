module bindings.kotlin.migration.mfcb30ccc;

binding weakRandomValue0 {
  requires {
    dependency("io.github.jhipster:jhipster-framework")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "generateRandomAlphanumericString"
  emit issue code.WeakRandomValue at node
}
