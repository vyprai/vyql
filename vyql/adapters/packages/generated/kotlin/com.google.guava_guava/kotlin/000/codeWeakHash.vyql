module bindings.kotlin.migration.m4452564c;

binding weakHash0 {
  requires {
    dependency("com.google.guava:guava")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "com.google.guava.guava.hash"
  emit issue code.WeakHash at node
}
