module bindings.kotlin.migration.mf0957eb3;

binding privilegedOp0 {
  requires {
    dependency("org.apache.curator:curator-framework")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "getACL"
  emit issue code.PrivilegedOp at node
  emit sink code.PrivilegedOpSink at node
}
