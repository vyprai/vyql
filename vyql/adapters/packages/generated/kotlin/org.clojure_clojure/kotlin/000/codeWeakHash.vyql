module bindings.kotlin.migration.m2de7372a;

binding weakHash0 {
  requires {
    dependency("org.clojure:clojure")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "org.clojure.clojure.hash"
  emit issue code.WeakHash at node
}
