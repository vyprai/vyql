import code.{FilePathAccess, RegexCompile}

adapter python {
  package "cfgv" {
    source method "validate" -> core.UserControlledData
    sink method "load_from_filename" -> code.FilePathAccess
    flag code.RegexCompile on any {
      method "check_regex"
    }
  }
}
