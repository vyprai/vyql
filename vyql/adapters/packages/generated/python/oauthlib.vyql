import code.{SecretValue, WeakRandomValue}

adapter python {
  package "oauthlib" {
    flag code.WeakRandomValue on any {
      method "generate_token"
    }
    source method "generate_signed_token" -> code.SecretValue
    control method "verify_signed_token" -> core.AuthenticationCheck
  }
}
