module bindings.python.migration.m8bff7fd6;

binding regexCompile2 {
  requires {
    dependency("cfgv")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "check_regex"
  emit issue code.RegexCompile at node
  emit sink code.RegexCompileSink at node
}
