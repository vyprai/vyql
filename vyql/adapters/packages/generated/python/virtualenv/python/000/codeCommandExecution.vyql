module bindings.python.migration.m553261a8;

binding commandExecution0 {
  requires {
    dependency("virtualenv")
  }
  query pattern callExpr where callee.method == "run_with_catch"
  emit sink code.CommandExecution at args[0]
}
