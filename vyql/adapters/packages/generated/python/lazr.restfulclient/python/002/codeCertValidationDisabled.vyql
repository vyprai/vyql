module bindings.python.migration.m94c8b013;

binding certValidationDisabled2 {
  requires {
    dependency("lazr.restfulclient")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "ssl_certificate_validation_disabled"
  emit issue code.CertValidationDisabled at node
}
