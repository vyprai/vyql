module bindings.python.migration.m441b537e;

binding deserialization1 {
  requires {
    dependency("scikit-learn")
  }
  query pattern callExpr where callee.path ~= "sklearn.externals._arff.loads"
  emit sink code.Deserialization at args[0]
}
