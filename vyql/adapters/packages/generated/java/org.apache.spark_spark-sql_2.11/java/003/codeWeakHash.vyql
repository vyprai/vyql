module bindings.java.migration.m34ad357f;

binding weakHash3 {
  requires {
    dependency("org.apache.spark:spark-sql_2.11")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "org.apache.spark.spark.sql_2.11.hash"
  emit issue code.WeakHash at node
}
