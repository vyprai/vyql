module bindings.java.migration.m6daec676;

binding regexCompile4 {
  requires {
    dependency("com.sun.jersey:jersey-core")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "UriPattern.compile"
  emit issue code.RegexCompile at node
  emit sink code.RegexCompileSink at node
}
