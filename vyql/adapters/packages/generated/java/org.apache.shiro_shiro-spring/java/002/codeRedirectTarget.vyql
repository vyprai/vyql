module bindings.java.migration.m758d4a69;

binding redirectTarget2 {
  requires {
    dependency("org.apache.shiro:shiro-spring")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "setUnauthorizedUrl"
  emit issue code.RedirectTarget at node
  emit sink code.RedirectTargetSink at node
}
