module bindings.java.migration.mf27d1696;

binding redirectTarget3 {
  requires {
    dependency("io.springfox:springfox-boot-starter")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "setBaseUrl"
  emit issue code.RedirectTarget at node
  emit sink code.RedirectTargetSink at node
}
