module bindings.java.migration.m97f81708;

binding redirectTarget5 {
  requires {
    dependency("org.apache.tomcat:tomcat-jsp-api")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "forward"
  emit issue code.RedirectTarget at node
  emit sink code.RedirectTargetSink at node
}
