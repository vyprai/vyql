module bindings.java.migration.med1ac799;

binding weakCipher1 {
  requires {
    dependency("org.jasypt:jasypt")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "decrypt"
  emit issue code.WeakCipher at node
}
