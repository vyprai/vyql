module bindings.java.migration.me48c3402;

binding weakCipher4 {
  requires {
    dependency("org.springframework.security:spring-security-crypto")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "TextEncryptor.encrypt"
  emit issue code.WeakCipher at node
}
