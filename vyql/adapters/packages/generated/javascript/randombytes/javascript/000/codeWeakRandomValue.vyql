module bindings.javascript.migration.mb5fca82f;

binding weakRandomValue0 {
  requires {
    dependency("randombytes")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "randomBytes"
  emit issue code.WeakRandomValue at node
}
