module bindings.javascript.migration.m067028b6;

binding regexCompile2 {
  requires {
    dependency("extglob")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "matcher"
  emit issue code.RegexCompile at node
  emit sink code.RegexCompileSink at node
}
