import code.{FilePathAccess, Deserialization}
import core.{UserControlledData}

adapter javascript {
  package "ignore-walk" {
    sink method "addIgnoreFile" -> code.FilePathAccess
    sink method "onReadIgnoreFile" -> code.Deserialization
  }
}
