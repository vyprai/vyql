import code.{RegexCompile}

adapter javascript {
  package "@babel/helper-regex" {
    flag code.RegexCompile on any {
      method "is"
    }
    flag code.RegexCompile on any {
      method "pullFlag"
    }
  }
}
