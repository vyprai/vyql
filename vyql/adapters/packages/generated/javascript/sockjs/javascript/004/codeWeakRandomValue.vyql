module bindings.javascript.migration.m77236a9a;

binding weakRandomValue4 {
  requires {
    dependency("sockjs")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "random32"
  emit issue code.WeakRandomValue at node
}
