module bindings.javascript.migration.md3d2ac70;

binding weakRandomValue0 {
  requires {
    dependency("unique-string")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "uniqueString"
  emit issue code.WeakRandomValue at node
}
