module bindings.javascript.migration.m82a6f82d;

binding regexCompile0 {
  requires {
    dependency("glob-to-regexp")
  }
  query pattern callExpr where callee.method == "assertMatch"
  emit issue code.RegexCompile at call
  emit sink code.RegexCompileSink at call
}
