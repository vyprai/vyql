module bindings.javascript.migration.mfde5fd96;

binding weakRandomValue0 {
  requires {
    dependency("nanoid")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "non-secure.nanoid"
  emit issue code.WeakRandomValue at node
}
