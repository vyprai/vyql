import code.{RegexCompile}

adapter javascript {
  package "emoji-regex" {
    flag code.RegexCompile on any {
      method "module.exports"
    }
    flag code.RegexCompile on any {
      method "default"
    }
    flag code.RegexCompile on any {
      method "emojiRegex"
    }
  }
}
