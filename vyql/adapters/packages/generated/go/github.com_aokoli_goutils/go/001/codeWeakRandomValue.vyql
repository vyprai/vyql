module bindings.go.migration.m9e95a54a;

binding weakRandomValue1 {
  requires {
    dependency("github.com/aokoli/goutils")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "RandomAscii"
  emit issue code.WeakRandomValue at node
}
