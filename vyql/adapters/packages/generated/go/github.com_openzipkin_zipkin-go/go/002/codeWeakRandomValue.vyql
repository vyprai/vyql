module bindings.go.migration.m74bd0025;

binding weakRandomValue2 {
  requires {
    dependency("github.com/openzipkin/zipkin-go")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "NewRandomTimestamped"
  emit issue code.WeakRandomValue at node
}
