module bindings.go.migration.m5030e2b8;

binding weakRandomValue0 {
  requires {
    dependency("github.com/d2g/hardwareaddr")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "GenerateEUI48"
  emit issue code.WeakRandomValue at node
}
