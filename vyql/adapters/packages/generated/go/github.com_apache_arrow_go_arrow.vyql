import code.{WeakHash}

adapter go {
  package "github.com/apache/arrow/go/arrow" {
    flag code.WeakHash on any {
      method "HashType"
    }
    flag code.WeakHash on any {
      method "Hash"
    }
  }
}
