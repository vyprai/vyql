module bindings.go.migration.m18f7025b;

binding weakRandomValue0 {
  requires {
    dependency("github.com/sean-/seed")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "Init"
  emit issue code.WeakRandomValue at node
}
