import code.{WeakRandomValue}
adapter go {
  package "github.com/sean-/seed" {
    flag code.WeakRandomValue on any {
      method "Init"
    }
    flag code.WeakRandomValue on any {
      method "MustInit"
    }
  }
}
