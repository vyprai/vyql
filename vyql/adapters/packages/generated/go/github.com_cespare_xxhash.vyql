import code.{WeakHash}

adapter go {
  package "github.com/cespare/xxhash" {
    flag code.WeakHash on any {
      method "New"
    }
    flag code.WeakHash on any {
      method "Sum64"
    }
    flag code.WeakHash on any {
      method "Sum64String"
    }
  }
}
