module bindings.go.migration.mec084e86;

binding weakRandomValue4 {
  requires {
    dependency("github.com/Masterminds/goutils")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "RandomAlphaNumeric"
  emit issue code.WeakRandomValue at node
}
