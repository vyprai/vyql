module bindings.go.migration.m22ff4284;

binding weakHash0 {
  requires {
    dependency("github.com/containerd/cgroups")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "containerd.cgroups.hash"
  emit issue code.WeakHash at node
}
