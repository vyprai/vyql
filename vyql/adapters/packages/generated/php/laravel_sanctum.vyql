import code.{SecretValue}
import core.{AuthorizationCheck}

adapter php {
  package "laravel/sanctum" {
    source method "createToken" -> code.SecretValue
    source method "currentAccessToken" -> code.SecretValue
    control method "tokenCan" -> core.AuthorizationCheck
  }
}
