module bindings.php.migration.m9600bd31;

binding weakRandomValue0 {
  requires {
    dependency("jms/security-extra-bundle")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "JMS\\SecurityExtraBundle\\Command\\InitSecureRandomCommand::execute"
  emit issue code.WeakRandomValue at node
}
