import code.{RegexCompile}
adapter php {
  package "beberlei/assert" {
    flag code.RegexCompile on any {
      method "regex"
    }
    flag code.RegexCompile on any {
      method "notRegex"
    }
  }
}
