module bindings.php.migration.mce78a0f3;

binding commandExecution0 {
  requires {
    dependency("symplify/easy-coding-standard")
  }
  query pattern callExpr where callee.method == "exec"
  emit sink code.CommandExecution at args[0]
}
