module bindings.php.migration.m7cea5654;

binding weakRandomValue1 {
  requires {
    dependency("zendframework/zend-math")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "ZendMathRand::getBoolean"
  emit issue code.WeakRandomValue at node
}
