module bindings.php.migration.me7b7042d;

binding redirectTarget3 {
  requires {
    dependency("laravel/jetstream")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "redirectPath"
  emit issue code.RedirectTarget at node
  emit sink code.RedirectTargetSink at node
}
