module bindings.php.migration.m9542f977;

binding weakRandomValue0 {
  requires {
    dependency("laravel/helpers")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "str_random"
  emit issue code.WeakRandomValue at node
}
