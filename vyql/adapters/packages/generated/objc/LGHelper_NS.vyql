import code.{WeakHash, WeakCipher}

adapter objc {
  package "LGHelper+NS" {
    flag code.WeakHash on any {
      method "md5Hash"
    }
    flag code.WeakHash on any {
      method "sha1Hash"
    }
    flag code.WeakCipher on any {
      method "xorCryptedWithKey"
    }
  }
}
