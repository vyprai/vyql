module bindings.objc.migration.md74ce000;

binding weakHash2 {
  requires {
    dependency("GRDB.swift")
  }
  query pattern presenceNode where node.path ~= "grdb.swift.hash"
  emit issue code.WeakHash at node
}
