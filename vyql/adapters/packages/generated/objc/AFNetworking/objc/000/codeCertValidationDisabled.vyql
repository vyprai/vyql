module bindings.objc.migration.mfa27ee45;

binding certValidationDisabled0 {
  requires {
    dependency("AFNetworking")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "evaluateServerTrust:forDomain:"
  emit issue code.CertValidationDisabled at node
}
