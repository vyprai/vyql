module bindings.objc.migration.m61061ea8;

binding weakHash1 {
  requires {
    dependency("CHCSVParser")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "chcsvparser.hash"
  emit issue code.WeakHash at node
}
