module bindings.objc.migration.ma606e784;

binding redirectTarget5 {
  requires {
    dependency("SlateURLProtocol")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "connection:willSendRequest:redirectResponse:"
  emit issue code.RedirectTarget at node
  emit sink code.RedirectTargetSink at node
}
