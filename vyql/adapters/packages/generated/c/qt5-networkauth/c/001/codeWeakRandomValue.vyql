module bindings.c.migration.m9c49fefa;

binding weakRandomValue1 {
  requires {
    dependency("qt5-networkauth")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "QAbstractOAuth2Private::generateRandomState"
  emit issue code.WeakRandomValue at node
}
