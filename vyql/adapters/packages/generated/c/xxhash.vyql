import code.{WeakHash}
adapter c {
  package "xxhash" {
    flag code.WeakHash on any {
      method "XXH32"
    }
    flag code.WeakHash on any {
      method "XXH64"
    }
    flag code.WeakHash on any {
      method "XXH3_64bits"
    }
    flag code.WeakHash on any {
      method "XXH3_128bits"
    }
  }
}
