module bindings.c.migration.mc72f33f0;

binding weakRandomValue0 {
  requires {
    dependency("speex")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "speex_rand"
  emit issue code.WeakRandomValue at node
}
