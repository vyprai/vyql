import code.{Deserialization, WeakHash}

adapter c {
  package "tsl-sparse-map" {
    sink method "deserialize" -> code.Deserialization
    flag code.WeakHash on any {
      method "hash_key"
    }
  }
}
