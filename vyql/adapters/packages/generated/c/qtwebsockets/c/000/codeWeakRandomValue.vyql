module bindings.c.migration.m3ac2633c;

binding weakRandomValue0 {
  requires {
    dependency("qtwebsockets")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "QMaskGenerator::generateMask"
  emit issue code.WeakRandomValue at node
}
