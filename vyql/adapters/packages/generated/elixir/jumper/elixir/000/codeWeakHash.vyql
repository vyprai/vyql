module bindings.elixir.migration.ma56c0836;

binding weakHash0 {
  requires {
    dependency("jumper")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "slot"
  emit issue code.WeakHash at node
}
