module bindings.elixir.migration.m1febe749;

binding weakHash0 {
  requires {
    dependency("siphash")
  }
  query pattern presenceNode where node.path ~= "siphash.hash"
  emit issue code.WeakHash at node
}
