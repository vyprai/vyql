module bindings.elixir.migration.macc32cec;

binding weakHash0 {
  requires {
    dependency("surface")
  }
  query pattern presenceNode where node.path ~= "surface.hash"
  emit issue code.WeakHash at node
}
