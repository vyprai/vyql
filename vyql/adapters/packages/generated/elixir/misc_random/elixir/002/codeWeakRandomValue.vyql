module bindings.elixir.migration.m89dc0457;

binding weakRandomValue2 {
  requires {
    dependency("misc_random")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "Misc.Random.string"
  emit issue code.WeakRandomValue at node
}
