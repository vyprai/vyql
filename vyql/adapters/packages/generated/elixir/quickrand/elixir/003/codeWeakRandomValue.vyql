module bindings.elixir.migration.mb6e60529;

binding weakRandomValue3 {
  requires {
    dependency("quickrand")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "mwc256_128"
  emit issue code.WeakRandomValue at node
}
