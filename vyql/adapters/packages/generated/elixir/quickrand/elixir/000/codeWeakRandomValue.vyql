module bindings.elixir.migration.mb6e60529;

binding weakRandomValue0 {
  requires {
    dependency("quickrand")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "lcg35x_32"
  emit issue code.WeakRandomValue at node
}
