module bindings.elixir.migration.ma8d7fc75;

binding sqlExecution3 {
  requires {
    dependency("ash_postgres")
  }
  query pattern callExpr where callee.method == "run_query_with_lateral_join"
  emit sink code.SqlExecution at args[0]
}
