import code.{RedirectTarget}
import core.{PathCanonicalization}

adapter perl {
  package "OX" {
    flag code.RedirectTarget on any {
      method "uri_for"
    }
    control method "canonicalize_path" -> core.PathCanonicalization
  }
}
