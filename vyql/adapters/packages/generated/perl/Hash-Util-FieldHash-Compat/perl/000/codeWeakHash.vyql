module bindings.perl.migration.m51c5629c;

binding weakHash0 {
  requires {
    dependency("Hash-Util-FieldHash-Compat")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "fieldhash"
  emit issue code.WeakHash at node
}
