import code.{WeakHash}
import core.{AuthenticationCheck}

adapter perl {
  package "Nitesi" {
    control method "Nitesi::Account::Password::check_password" -> core.AuthenticationCheck
    flag code.WeakHash on any {
      method "Nitesi::Account::Password::password"
    }
  }
}
