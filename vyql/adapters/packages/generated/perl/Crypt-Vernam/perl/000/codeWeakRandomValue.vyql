module bindings.perl.migration.m6d7dec09;

binding weakRandomValue0 {
  requires {
    dependency("Crypt-Vernam")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "_get_key_mod26"
  emit issue code.WeakRandomValue at node
}
