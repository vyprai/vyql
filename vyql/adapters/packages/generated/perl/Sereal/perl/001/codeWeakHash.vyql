module bindings.perl.migration.m18b29b45;

binding weakHash1 {
  requires {
    dependency("Sereal")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "sereal.hash"
  emit issue code.WeakHash at node
}
