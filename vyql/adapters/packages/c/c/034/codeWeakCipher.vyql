module bindings.c.migration.m2816daee;

binding weakCipher34 {
  requires {
    dependency("openssl")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "EVP_des_ede3_cbc"
  emit issue code.WeakCipher at node
}
