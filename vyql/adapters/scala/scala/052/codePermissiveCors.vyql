module bindings.scala.migration.m06d0685b;

binding permissiveCors52 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "setHeader" and node.token contains "Access-Control-Allow-Origin" and node.token contains "*"
  emit issue code.PermissiveCors at node
}
