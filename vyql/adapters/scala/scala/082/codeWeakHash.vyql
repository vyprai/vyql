module bindings.scala.migration.m06d0685b;

binding weakHash82 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "MessageDigest.getInstance" and node.token contains "SHA-1"
  emit issue code.WeakHash at node
}
