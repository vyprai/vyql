module bindings.scala.migration.m06d0685b;

binding incompleteCsrfTokenValidation41 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path == "analysis.condition.if" and node.token contains "csrf" and node.token contains "==" and node.token contains "submitted" and node.token contains "cookie" and not (node.token contains ".isEmpty") and not (node.token contains ".nonEmpty") and not (node.token contains "!=null") and not (node.token contains "!=''") and not (node.token contains "!=\"\"")
  emit issue code.IncompleteCsrfTokenValidation at node
}
