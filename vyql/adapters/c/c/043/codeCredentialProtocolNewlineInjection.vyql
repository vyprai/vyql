module bindings.c.migration.mce54b166;

binding credentialProtocolNewlineInjection43 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=c" and node.token contains "name=credential_write_item" and node.token contains "call_path:fprintf" and node.token contains "literal:%s=%s" and not (node.token contains "call_path:strchr")
  emit issue code.CredentialProtocolNewlineInjection at node
}
