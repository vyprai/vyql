module bindings.c.migration.mce54b166;

binding credentialProtocolNewlineInjection43 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and node.context.name contains "credential_write_item" and node.context.callPath contains "fprintf" and node.context.literal contains "%s=%s" and not (node.context.callPath contains "strchr")
  emit issue code.CredentialProtocolNewlineInjection at node
}
