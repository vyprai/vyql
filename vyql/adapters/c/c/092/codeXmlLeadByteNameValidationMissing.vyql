module bindings.c.migration.mce54b166;

binding xmlLeadByteNameValidationMissing92 {
  query pattern presenceNode where node.scope == "module" and node.token contains "lang=c" and node.token contains "macro_name:CHECK_NAME_CASE" and node.token contains "macro_name:CHECK_NMSTRT_CASE" and node.token contains "macro_body:CHECK_NAME_CASE:" and node.token contains "macro_body:if(!IS_NAME_CHAR(enc,ptr,n))" and node.token contains "macro_body:CHECK_NMSTRT_CASE:" and node.token contains "macro_body:if(!IS_NMSTRT_CHAR(enc,ptr,n))" and not (node.token contains "macro_body:IS_INVALID_CHAR(enc,ptr,n)||!IS_NAME_CHAR(enc,ptr,n)") and not (node.token contains "macro_body:IS_INVALID_CHAR(enc,ptr,n)||!IS_NMSTRT_CHAR(enc,ptr,n)")
  emit issue code.XmlLeadByteNameValidationMissing at node
}
