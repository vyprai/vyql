module bindings.c.migration.mce54b166;

binding uncheckedNullableResultDeref131 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=c" and node.token contains "px->ft=imcb_file_send_start" and node.token contains "px->ft->data=px" and not (node.token contains "if(!px->ft)")
  emit issue code.UncheckedNullableResultDeref at node
}
