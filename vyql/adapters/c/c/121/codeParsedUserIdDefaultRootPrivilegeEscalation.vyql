module bindings.c.migration.mce54b166;

binding parsedUserIdDefaultRootPrivilegeEscalation121 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=c" and node.token contains "strtoll(userspec,&endptr,10)" and node.token contains "if(*endptr=='\\0')returnu" and node.token contains "u->gid=(int)l" and not (node.token contains "u->uid=(int)l;if(*endptr=='\\0')returnu")
  emit issue code.ParsedUserIdDefaultRootPrivilegeEscalation at node
}
