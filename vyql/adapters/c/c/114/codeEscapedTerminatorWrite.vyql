module bindings.c.generated.mce54b166;

binding escapedTerminatorWrite114 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and node.token contains "unescapeChar(*readPtr++);}*writePtr++=c;"
  emit issue code.EscapedTerminatorWrite at node
}
