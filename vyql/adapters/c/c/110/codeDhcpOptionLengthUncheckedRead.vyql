module bindings.c.migration.mce54b166;

binding dhcpOptionLengthUncheckedRead110 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and node.context.name contains "parse_options" and node.token contains "u8_t*end=optptr+len;" and node.token contains "while(optptr<end)" and node.token contains "type=*(optptr+2);" and node.token contains "memcmp((char*)&client.addr,(char*)optptr+2,4)==0" and node.token contains "optptr+=optptr[1]+2;" and not (node.token contains "optptr+1>=end") and not (node.token contains "optptr+2+opt_len>end") and not (node.token contains "opt_len>=4")
  emit issue code.DhcpOptionLengthUncheckedRead at node
}
