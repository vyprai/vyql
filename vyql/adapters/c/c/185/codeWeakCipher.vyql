module bindings.c.migration.mce54b166;

binding weakCipher185 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "BF_set_key"
  emit issue code.WeakCipher at node
}
