module bindings.c.migration.mce54b166;

binding zeroCountLastElementAccess80 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "analysis.zero_count.last_index" and node.token contains "index_kind=field_minus_one" and node.token contains "guard=missing_nonzero"
  emit issue code.ZeroCountLastElementAccess at node
}
