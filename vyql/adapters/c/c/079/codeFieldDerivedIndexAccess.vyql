module bindings.c.generated.mce54b166;

binding fieldDerivedIndexAccess79 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "index.access" and node.token contains "index_kind=field_derived" and node.token contains "guard=missing_upper_bound"
  emit issue code.FieldDerivedIndexAccess at node
}
