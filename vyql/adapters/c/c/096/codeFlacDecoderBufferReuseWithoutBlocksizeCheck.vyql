module bindings.c.migration.mce54b166;

binding flacDecoderBufferReuseWithoutBlocksizeCheck96 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "c" and node.context.name contains "flac_buffer_copy" and node.context.callPath contains "memcpy" and node.context.selector contains "frame.header.blocksize" and node.context.attrPath contains "pflac.wbuffer" and node.context.attrPath contains "pflac.rbuffer" and node.context.binary contains "frame->header.blocksize>FLAC__MAX_BLOCK_SIZE" and not (node.context.attrPath contains "pflac.wbuffer_size") and not (node.context.binary contains "frame->header.blocksize>pflac->wbuffer_size")
  emit issue code.FlacDecoderBufferReuseWithoutBlocksizeCheck at node
}
