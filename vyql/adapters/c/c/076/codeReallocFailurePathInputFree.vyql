module bindings.c.generated.mce54b166;

binding reallocFailurePathInputFree76 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "lifetime.realloc_failure_input_free" and node.token contains "allocator=realloc" and node.token contains "release=free" and node.token contains "return=null"
  emit issue code.ReallocFailurePathInputFree at node
}
