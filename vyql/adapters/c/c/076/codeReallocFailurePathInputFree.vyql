module bindings.c.migration.mce54b166;

binding reallocFailurePathInputFree76 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "analysis.lifetime.realloc_failure_input_free" and node.token contains "allocator=realloc" and node.token contains "release=free" and node.token contains "return=null"
  emit issue code.ReallocFailurePathInputFree at node
}
