module bindings.c.migration.mce54b166;

binding dnsInterfaceNewlineValidationMissing42 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=c" and node.token contains "name=initConfig" and node.token contains "literal:dns.interface" and node.token contains "attr_path:conf.dns.interface.c" and node.token contains "attr_path:conf.dns.interface.t" and node.token contains "assign:conf.dns.interface.c=validate_stub" and not (containsAny(node.token, ["assign:conf.dns.interface.c=validate_str_no_newline"]))
  emit issue code.DnsInterfaceNewlineValidationMissing at node
}
