module bindings.c.migration.mce54b166;

binding lengthMultiplierFormattedExpansionUnderallocation111 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=c" and node.token contains "newSV(strlen(" and node.token contains ")*3)" and node.token contains "sprintf(" and node.token contains "%d" and not (node.token contains "*6+16") and not (node.token contains "snprintf(")
  emit issue code.LengthMultiplierFormattedExpansionUnderallocation at node
}
