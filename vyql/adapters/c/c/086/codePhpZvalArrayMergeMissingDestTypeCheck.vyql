module bindings.c.migration.mce54b166;

binding phpZvalArrayMergeMissingDestTypeCheck86 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=c" and node.token contains "call_path:Z_ARRVAL_PP" and node.token contains "call_path:zend_hash_find" and node.token contains "call_path:zend_hash_index_find" and node.token contains "binary:Z_TYPE_PP(test_ptr)==IS_ARRAY" and not (node.token contains "binary:Z_TYPE_PP(test_ptr)==IS_ARRAY&&Z_TYPE_PP(ptr)==IS_ARRAY")
  emit issue code.PhpZvalArrayMergeMissingDestTypeCheck at node
}
