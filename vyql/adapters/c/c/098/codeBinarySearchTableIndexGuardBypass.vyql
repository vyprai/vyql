module bindings.c.migration.mce54b166;

binding binarySearchTableIndexGuardBypass98 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=c" and node.token contains "name=X86_insn_reg_intel" and node.token contains "insn_regs_intel_sorted[mid].insn<id" and node.token contains "first=mid+1" and node.token contains "last=mid-1" and node.token contains "mid=(first+last)/2" and not (node.token contains "insn_regs_intel_sorted[0].insn>id") and not (node.token contains "insn_regs_intel_sorted[last].insn<id")
  emit issue code.BinarySearchTableIndexGuardBypass at node
}
