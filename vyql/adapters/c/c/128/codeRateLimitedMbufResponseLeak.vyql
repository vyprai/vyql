module bindings.c.migration.mce54b166;

binding rateLimitedMbufResponseLeak128 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=c" and node.token contains "param_type:mbuf" and node.token contains "call_path:tcp_challenge_ack_check" and node.token contains "call_path:tcp_respond" and not (node.token contains "call_path:m_freem")
  emit issue code.RateLimitedMbufResponseLeak at node
}
