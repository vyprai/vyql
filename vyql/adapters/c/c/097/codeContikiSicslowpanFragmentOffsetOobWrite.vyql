module bindings.c.migration.mce54b166;

binding contikiSicslowpanFragmentOffsetOobWrite97 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=c" and node.token contains "name=copy_frags2uip" and node.token contains "memcpy((uint8_t*)UIP_IP_BUF+(uint16_t)(frag_buf[i].offset<<3),(uint8_t*)frag_buf[i].data,frag_buf[i].len)" and not (node.token contains "(frag_buf[i].offset<<3)+frag_buf[i].len>sizeof(uip_buf)") and not (node.token contains "frag_info[context].len>sizeof(uip_buf)")
  emit issue code.ContikiSicslowpanFragmentOffsetOobWrite at node
}
