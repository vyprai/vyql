module bindings.elixir.migration.m728b8a72;

binding weakHash57 {
  query pattern presenceNode where node.kind == "any" and node.path ~= ":crypto.hash" and node.token contains ":md5"
  emit issue code.WeakHash at node
}
