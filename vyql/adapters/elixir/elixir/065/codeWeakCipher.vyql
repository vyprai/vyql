module bindings.elixir.migration.m728b8a72;

binding weakCipher65 {
  query pattern presenceNode where node.kind == "any" and node.path ~= ":crypto.block_encrypt" and node.token contains ":blowfish"
  emit issue code.WeakCipher at node
}
