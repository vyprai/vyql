module bindings.elixir.migration.m728b8a72;

binding insecureCookie69 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "put_resp_cookie" and node.token contains "false"
  emit issue code.InsecureCookie at node
}
