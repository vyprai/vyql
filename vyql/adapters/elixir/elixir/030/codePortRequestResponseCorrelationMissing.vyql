module bindings.elixir.migration.m728b8a72;

binding portRequestResponseCorrelationMissing30 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=elixir" and node.token contains "call_path:Port.command" and node.token contains "call_path:Jason.encode!" and node.token contains "call_path:get_response" and not (node.token contains "uid_counter") and not (node.token contains "expected_uid") and not (node.token contains "extract_uid")
  emit issue code.PortRequestResponseCorrelationMissing at node
}
