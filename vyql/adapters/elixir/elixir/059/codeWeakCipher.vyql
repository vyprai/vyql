module bindings.elixir.migration.m728b8a72;

binding weakCipher59 {
  query pattern presenceNode where node.kind == "any" and node.path ~= ":crypto.crypto_one_time" and node.token contains ":des"
  emit issue code.WeakCipher at node
}
