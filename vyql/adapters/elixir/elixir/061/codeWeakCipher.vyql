module bindings.elixir.migration.m728b8a72;

binding weakCipher61 {
  query pattern presenceNode where node.kind == "any" and node.path ~= ":crypto.crypto_one_time" and node.token contains ":blowfish"
  emit issue code.WeakCipher at node
}
