module bindings.cpp.migration.m4e783366;

binding secretComparisonReview81 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "binop" and node.op contains "==" and not (containsAny(node.scopeCall.any, [CRYPTO_memcmp, timingsafe_bcmp, constant_time])) and operand(node, where: operand.path ~= "DecodeBase64") and operand(node, where: containsAny(operand.identifier, [pass, password, secret, token, key, auth]))
  emit issue code.SecretComparisonReview at node
}
