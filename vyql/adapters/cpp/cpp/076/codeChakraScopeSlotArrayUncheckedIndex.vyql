module bindings.cpp.migration.m4e783366;

binding chakraScopeSlotArrayUncheckedIndex76 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=cpp" and node.token contains "propertyIdsForScopeSlotArray[sym->GetScopeSlot()]=sym->EnsurePosition(funcInfo)" and node.token contains "propertyIdsForScopeSlotArray[funcInfo->thisScopeSlot]=Js::PropertyIds::_lexicalThisSlotSymbol" and node.token contains "propertyIdsForScopeSlotArray[funcInfo->superCtorScopeSlot]=Js::PropertyIds::_superCtorReferenceSymbol" and not (node.token contains "setPropertyIdForScopeSlotArray(sym->GetScopeSlot(),sym->EnsurePosition(funcInfo))") and not (node.token contains "slot<0||(uint)slot>=scopeSlotCount") and not (node.token contains "Js::Throw::FatalInternalError()")
  emit issue code.ChakraScopeSlotArrayUncheckedIndex at node
}
