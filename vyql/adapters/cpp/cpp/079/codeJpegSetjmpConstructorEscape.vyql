module bindings.cpp.migration.m4e783366;

binding jpegSetjmpConstructorEscape79 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "cpp" and node.token contains "jpeg_std_error" and node.token contains "jerr_.pub.error_exit=my_error_exit" and node.token contains "setjmp(jerr_.setjmp_buffer)" and node.token contains "jpeg_create_decompress" and not (node.token contains "std::memcpy(jerr_.setjmp_buffer,setjmp_buffer") and not (node.token contains "DIP__DECLARE_JPEG_EXIT")
  emit issue code.JpegSetjmpConstructorEscape at node
}
