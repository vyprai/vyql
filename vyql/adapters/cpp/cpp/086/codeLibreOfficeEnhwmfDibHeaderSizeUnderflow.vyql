module bindings.cpp.migration.m4e783366;

binding libreOfficeEnhwmfDibHeaderSizeUnderflow86 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=cpp" and node.token contains "EMR_ALPHABLEND" and node.token contains "getDIBV5HeaderSize" and node.token contains "cbBmiSrc" and node.token contains "nDeltaToDIB5HeaderSize" and node.token contains "nTargetSize" and node.token contains "newchar" and node.token contains "ReadBytes" and not (node.token contains "cbBmiSrc>nHeaderSize") and not (node.token contains "bSafeRead=false")
  emit issue code.LibreOfficeEnhwmfDibHeaderSizeUnderflow at node
}
