module bindings.cpp.migration.m4e783366;

binding fieldDerivedIndexAccess61 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "analysis.index.access" and node.token contains "index_kind=field_derived" and node.token contains "guard=missing_upper_bound"
  emit issue code.FieldDerivedIndexAccess at node
}
