module bindings.cpp.migration.m4e783366;

binding fieldDerivedIndexAccess61 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "analysis.index.access" and node.token contains "index_kind=field_derived" and node.token contains "guard=missing_upper_bound"
  emit issue code.FieldDerivedIndexAccess at node
}
