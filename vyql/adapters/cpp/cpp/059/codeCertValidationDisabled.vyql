module bindings.cpp.migration.m4e783366;

binding certValidationDisabled59 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "setsslverify" and node.token contains "false"
  emit issue code.CertValidationDisabled at node
}
