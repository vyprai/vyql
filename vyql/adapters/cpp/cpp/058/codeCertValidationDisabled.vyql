module bindings.cpp.migration.m4e783366;

binding certValidationDisabled58 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "curl_easy_setopt" and node.token contains "CURLOPT_SSL_VERIFYPEER" and node.token contains "0"
  emit issue code.CertValidationDisabled at node
}
