module bindings.cpp.migration.m4e783366;

binding cimgPnmDimensionProductOverflow83 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=cpp" and node.token contains "cimg::fsize(filename)" and node.token contains "W*H*D>siz" and not (node.token contains "(cimg_int64)W*H*D>siz")
  emit issue code.CimgPnmDimensionProductOverflow at node
}
