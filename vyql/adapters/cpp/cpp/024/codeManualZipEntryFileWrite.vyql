module bindings.cpp.migration.m4e783366;

binding manualZipEntryFileWrite24 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=cpp" and node.token contains "call_path:mapProperties.value" and node.token contains "identifier:XBinary::FPART_PROP_ORIGINALNAME" and node.token contains "call_path:QDir.separator" and node.token contains "call_path:file.setFileName" and node.token contains "call_path:file.open" and node.token contains "identifier:QIODevice::ReadWrite" and not (node.token contains "call_path:QDir.cleanPath") and not (node.token contains "call_path:startsWith")
  emit issue code.ManualZipEntryFileWrite at node
}
