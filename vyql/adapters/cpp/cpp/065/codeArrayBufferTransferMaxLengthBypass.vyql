module bindings.cpp.migration.m4e783366;

binding arrayBufferTransferMaxLengthBypass65 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=cpp" and node.token contains "newByteLength=argv[0].toIndex(state)" and node.token contains "maxLength=obj->maxByteLength()" and node.token contains "allocateArrayBuffer(state,state.context()->globalObject()->arrayBuffer(),newByteLength,maxLength)" and not (node.token contains "newByteLength>maxLength.value()") and not (node.token contains "throwTypeErrorIfDetached(state);Optional")
  emit issue code.ArrayBufferTransferMaxLengthBypass at node
}
