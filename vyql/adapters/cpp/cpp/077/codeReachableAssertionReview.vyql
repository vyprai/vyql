module bindings.cpp.migration.m4e783366;

binding reachableAssertionReview77 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "cpp" and node.token contains "visitBrOn(&curr)" and node.token contains "validateTypeAnnotation(in,curr.ref)" and node.token contains "curr.desc" and not (node.token contains "curr.ref->type.isRef") and not (node.token contains "curr.desc->type.isRef")
  emit issue code.ReachableAssertionReview at node
}
