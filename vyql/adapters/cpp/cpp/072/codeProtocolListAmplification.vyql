module bindings.cpp.migration.m4e783366;

binding protocolListAmplification72 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=cpp" and node.token contains "inlineCompositeListElementCount" and node.token contains "wordsPerElement" and node.token contains "ListReader" and not (node.token contains "amplifiedRead")
  emit issue code.ProtocolListAmplification at node
}
