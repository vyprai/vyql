module bindings.cpp.migration.m4e783366;

binding escapedTerminatorWrite63 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=cpp" and node.token contains "unescapeChar(*readPtr++);}*writePtr++=c;"
  emit issue code.EscapedTerminatorWrite at node
}
