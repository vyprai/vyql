module bindings.cpp.migration.m4e783366;

binding escapedTerminatorWrite63 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=cpp" and node.token contains "unescapeChar(*readPtr++);}*writePtr++=c;"
  emit issue code.EscapedTerminatorWrite at node
}
