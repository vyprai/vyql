module bindings.cpp.migration.m4e783366;

binding jpegSetjmpConstructorEscape80 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=cpp" and node.token contains "jpeg_std_error" and node.token contains "jerr_.pub.error_exit=my_error_exit" and node.token contains "setjmp(jerr_.setjmp_buffer)" and node.token contains "jpeg_create_compress" and not (node.token contains "std::memcpy(jerr_.setjmp_buffer,setjmp_buffer") and not (node.token contains "DIP__DECLARE_JPEG_EXIT")
  emit issue code.JpegSetjmpConstructorEscape at node
}
