module bindings.cpp.migration.m4e783366;

binding weakCipher147 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "RC4"
  emit issue code.WeakCipher at node
}
