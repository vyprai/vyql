module bindings.cpp.migration.m4e783366;

binding pointerAddOverflowAllocation68 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "binop" and node.op contains "+" and node.flowTo.op contains ">" and not (node.flowTo.op contains "<") and operand(node, where: operand.path ~= "alignPointer")
  emit issue code.PointerAddOverflowAllocation at node
}
