module bindings.cpp.migration.m4e783366;

binding randomSpreadModuloZeroDenominator69 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=cpp" and node.token contains "call_path:randomish" and node.token contains "call_path:count" and containsAny(node.token, ["binary:%", "binary:__binop.mod"]) and not (containsAny(node.token, ["call_path:milliseconds.zero", "call_path:seconds.zero", "call_path:minutes.zero", "call_path:hours.zero"]))
  emit issue code.RandomSpreadModuloZeroDenominator at node
}
