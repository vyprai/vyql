module bindings.cpp.migration.m4e783366;

binding pointerOffsetBoundsCheck67 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=cpp" and node.token contains "start+len+1>end" and node.token contains "start+=len" and not (node.token contains "buff_size") and not (node.token contains "end-start-1")
  emit issue code.PointerOffsetBoundsCheck at node
}
