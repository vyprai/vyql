module bindings.cpp.migration.m4e783366;

binding sniffCsvExternalAccessBypass85 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=cpp" and node.token contains "name=CSVSniffBind" and node.token contains "result->path=input.inputs[0].ToString();" and node.token contains "result->options.FromNamedParameters(input.named_parameters,context);" and not (node.token contains "enable_external_access")
  emit issue code.SniffCsvExternalAccessBypass at node
}
