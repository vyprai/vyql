module bindings.cpp.migration.m4e783366;

binding certValidationDisabled55 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "setPeerVerifyMode" and node.token contains "QSslSocket::VerifyNone"
  emit issue code.CertValidationDisabled at node
}
