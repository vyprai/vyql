module bindings.cpp.migration.m4e783366;

binding cryptoImproperBlinding82 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=cpp" and node.token contains "r.Randomize" and node.token contains "MultiplicativeInverse(r)" and node.token contains "Jacobi" and node.token contains "modn.Square(r)" and node.token contains "modn.Multiply(y,rInv)" and not (node.token contains "r=modn.Square(r);rInv=modn.MultiplicativeInverse(r)")
  emit issue code.CryptoImproperBlinding at node
}
