module bindings.lua.migration.ma99d12f7;

binding resourceHandle79 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "socket.tcp"
  emit issue code.ResourceHandle at node
  emit sink code.ResourceHandleSink at node
}
