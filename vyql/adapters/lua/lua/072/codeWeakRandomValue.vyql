module bindings.lua.migration.ma99d12f7;

binding weakRandomValue72 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "math.random"
  emit issue code.WeakRandomValue at node
}
