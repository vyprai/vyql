module bindings.solidity.migration.m88e544d8;

binding rsaPkcs1DigestSuffixOnlyVerification14 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=solidity" and node.token contains "RSAVerify.rsarecover(modulus,exponent,sig)" and node.token contains "sha256(data)==result.readBytes32(result.length-32)" and not (node.token contains "RSAPKCS1Verify.verifySHA256") and not (node.token contains "result[0]!=0x00||result[1]!=0x01") and not (node.token contains "DigestInfo")
  emit issue code.RsaPkcs1DigestSuffixOnlyVerification at node
}
