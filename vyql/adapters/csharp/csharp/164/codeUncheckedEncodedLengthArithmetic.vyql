module bindings.csharp.migration.m1caa8bf2;

binding uncheckedEncodedLengthArithmetic164 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=csharp" and node.token contains "currentLength+=count" and not (node.token contains "checked(stackFrame.currentLength+count)")
  emit issue code.UncheckedEncodedLengthArithmetic at node
}
