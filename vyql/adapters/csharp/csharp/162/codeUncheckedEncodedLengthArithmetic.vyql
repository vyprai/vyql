module bindings.csharp.migration.m1caa8bf2;

binding uncheckedEncodedLengthArithmetic162 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=csharp" and node.token contains "input.SkipBytes(count*sizeof(" and not (node.token contains "checked(count*sizeof(")
  emit issue code.UncheckedEncodedLengthArithmetic at node
}
