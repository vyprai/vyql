module bindings.csharp.migration.m1caa8bf2;

binding jwtUnverified155 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "TokenValidationParameters" and node.token contains "RequireSignedTokens=false"
  emit issue code.JwtUnverified at node
}
