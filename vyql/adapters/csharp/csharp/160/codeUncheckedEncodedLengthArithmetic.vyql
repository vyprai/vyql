module bindings.csharp.migration.m1caa8bf2;

binding uncheckedEncodedLengthArithmetic160 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=csharp" and node.token contains "ReadVarUInt32" and node.token contains "(int)input.ReadVarUInt32()" and not (node.token contains "checked")
  emit issue code.UncheckedEncodedLengthArithmetic at node
}
