module bindings.csharp.migration.m1caa8bf2;

binding uncheckedEncodedLengthArithmetic163 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=csharp" and node.token contains "name=SkipBytes" and node.token contains "position+=count" and not (node.token contains "checked(position+count)")
  emit issue code.UncheckedEncodedLengthArithmetic at node
}
