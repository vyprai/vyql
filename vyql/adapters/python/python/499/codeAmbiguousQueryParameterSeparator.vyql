module bindings.python.migration.m21017d75;

binding ambiguousQueryParameterSeparator499 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "name=_parse_qsl" and node.token contains "qs.replace(';','&').split('&')"
  emit issue code.AmbiguousQueryParameterSeparator at node
}
