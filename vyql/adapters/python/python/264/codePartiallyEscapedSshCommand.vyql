module bindings.python.migration.m21017d75;

binding partiallyEscapedSshCommand264 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "name=run_ssh_command_with_credentials" and node.token contains "escaped_password" and node.token contains "escaped_command" and node.token contains "{username}@{host}" and node.token contains "-p {port}" and node.token contains "run_command(ssh_command)" and not (node.token contains "quoted_username") and not (node.token contains "quoted_host") and not (node.token contains "port=int(port)")
  emit issue code.PartiallyEscapedSshCommand at node
}
