module bindings.python.migration.m21017d75;

binding unsafeYamlLoad291 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "call" and node.path == "yaml.load" and operand(node, where: operand.path == "yaml.UnsafeLoader")
  emit issue code.UnsafeYamlLoad at node
}
