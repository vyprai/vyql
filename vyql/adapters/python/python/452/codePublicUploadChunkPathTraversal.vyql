module bindings.python.migration.m21017d75;

binding publicUploadChunkPathTraversal452 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "python" and containsAny(node.token, ["name=Chunk", "name=chunk"]) and containsAny(node.token, ["literal:/api/uploadChunked/public", "literal:/uploadChunked/public"]) and node.context.callPath contains "request.form.get" and node.context.literal contains "checkSum" and node.context.callPath contains "os.path.join" and node.context.callPath contains "open" and not (node.context.callPath contains "re.sub") and not (node.context.callPath contains "os.path.realpath.startswith")
  emit issue code.PublicUploadChunkPathTraversal at node
}
