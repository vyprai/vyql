module bindings.python.migration.m21017d75;

binding cobblerDjangoStateChangeGetCsrf615 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "name=generic_delete" and node.token contains "remote.remove_item" and not (node.token contains "decorator_path:require_POST")
  emit issue code.CobblerDjangoStateChangeGetCsrf at node
}
