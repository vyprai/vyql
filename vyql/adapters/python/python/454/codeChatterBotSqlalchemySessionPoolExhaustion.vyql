module bindings.python.migration.m21017d75;

binding chatterBotSqlalchemySessionPoolExhaustion454 {
  query pattern presenceNode where node.scope == "function" and node.token contains "class_name=SQLStorageAdapter" and node.token contains "name=__init__" and node.token contains "create_engine(self.database_uri)" and node.token contains "sessionmaker(bind=self.engine,expire_on_commit=True)" and not (node.token contains "scoped_session") and not (node.token contains "pool_timeout")
  emit issue code.ChatterBotSqlalchemySessionPoolExhaustion at node
}
