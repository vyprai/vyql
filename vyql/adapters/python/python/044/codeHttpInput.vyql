module bindings.python.generated.m21017d75;

binding httpInput44 {
  query pattern callExpr where callee.analysis ~= "function.context.source" and args.any.context.name contains "get_filename_for_event_id" and args.any.literal contains "event_id" and args.any.literal contains "os.path.join" and args.any.literal contains "INGEST_STORE_BASE_DIR" and not args.any.literal contains "uuid.UUID(event_id).hex"
  emit source code.HttpInput at call.result
}
