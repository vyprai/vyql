module bindings.python.migration.m21017d75;

binding certValidationDisabled640 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "use_ssl=False" and node.token contains "Tls(" and node.token contains "validate=ssl.CERT_REQUIRED" and node.token contains "Server("
  emit issue code.CertValidationDisabled at node
}
