module bindings.python.migration.m21017d75;

binding protocolStateReview501 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "_original_transport" and node.token contains "_reader._transport" and node.token contains "_writer._transport" and not (node.token contains "_reader._buffer.clear")
  emit issue code.ProtocolStateReview at node
}
