module bindings.python.migration.m21017d75;

binding requestPathPercentSLogInjection209 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=python" and containsAny(node.token, ["call_path:LOG.debug", "call_path:logger.debug", "call_path:logging.debug", "call_path:log.debug"]) and node.token contains "attr_path:request.path" and containsAny(node.token, ["call_arg:LOG.debug:Request to '%s'", "call_arg:logger.debug:request path %s", "call_arg:logging.debug:path %s", "call_arg:log.debug:path %s"]) and not (containsAny(node.token, ["call_arg:LOG.debug:Request to '%r'", "call_arg:logger.debug:request path %r", "call_arg:logging.debug:path %r", "call_arg:log.debug:path %r"])) and not (containsAny(node.token, ["call_path:repr", "call_path:escape", "call_path:sanitize"]))
  emit issue code.RequestPathPercentSLogInjection at node
}
