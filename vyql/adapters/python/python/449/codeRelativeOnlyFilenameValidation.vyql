module bindings.python.migration.m21017d75;

binding relativeOnlyFilenameValidation449 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "'../'in" and node.token contains "re.search" and node.token contains "A-Za-z0-9" and node.token contains "returnTrue" and node.token contains "returnFalse" and not (node.token contains "startswith('/')") and not (node.token contains "os.path.isabs") and not (node.token contains "is_absolute()")
  emit issue code.RelativeOnlyFilenameValidation at node
}
