module bindings.python.migration.m21017d75;

binding incompleteTemplateValidation491 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "Formatter().parse" and node.token contains "." and node.token contains "format_spec" and not (node.token contains "{") and not (node.token contains "}")
  emit issue code.IncompleteTemplateValidation at node
}
