module bindings.python.migration.m21017d75;

binding copypartyCprStaticPathTraversal451 {
  query pattern presenceNode where node.scope == "function" and node.token contains "class_name=HttpCli" and node.context.name contains "handle_get" and node.token contains "self.vpath.startswith(\".cpr\")" and node.token contains "static_path=os.path.join(self.E.mod,\"web/\",self.vpath[5:])" and node.token contains "self.tx_file(static_path)" and not (node.token contains "absreal(os.path.join(path_base,self.vpath[5:]))") and not (node.token contains "static_path.startswith(path_base)")
  emit issue code.CopypartyCprStaticPathTraversal at node
}
