module bindings.python.migration.m21017d75;

binding copypartyCprStaticPathTraversal451 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "class_name=HttpCli" and node.token contains "name=handle_get" and node.token contains "self.vpath.startswith(\".cpr\")" and node.token contains "static_path=os.path.join(self.E.mod,\"web/\",self.vpath[5:])" and node.token contains "self.tx_file(static_path)" and not (node.token contains "absreal(os.path.join(path_base,self.vpath[5:]))") and not (node.token contains "static_path.startswith(path_base)")
  emit issue code.CopypartyCprStaticPathTraversal at node
}
