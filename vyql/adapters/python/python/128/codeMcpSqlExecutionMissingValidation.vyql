module bindings.python.migration.m21017d75;

binding mcpSqlExecutionMissingValidation128 {
  query pattern presenceNode where node.scope == "function" and node.token contains "name=execute_sql_for_mcp" and node.token contains "QueryRequest(" and node.token contains "sql=sql" and node.token contains "self.execute_query(query_request,auth_context)" and not (node.token contains "validate_sql_security") and not (node.token contains "DorisSecurityManager")
  emit issue code.McpSqlExecutionMissingValidation at node
}
