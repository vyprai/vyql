module bindings.python.migration.m21017d75;

binding weakJwtSecret646 {
  query pattern presenceNode where node.kind == "call" and node.path ~= "jwt.decode" and containsAny(node.context.callArg, ["secret", "jwt-secret", "change-me", "changeme", "password"])
  emit issue code.WeakJwtSecret at node
}
