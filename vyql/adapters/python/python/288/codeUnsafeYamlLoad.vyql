module bindings.python.migration.m21017d75;

binding unsafeYamlLoad288 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "call" and node.path == "yaml.unsafe_load_all"
  emit issue code.UnsafeYamlLoad at node
}
