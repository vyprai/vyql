module bindings.python.migration.m21017d75;

binding logWrite213 {
  query pattern presenceNode where node.path ~= "logging.debug"
  emit issue code.LogWrite at node
  emit sink code.LogWriteSink at node
}
