module bindings.python.migration.m21017d75;

binding xmlParserCreate325 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "lxml.etree.XMLParser" and not (containsAny(node.token, ["call_arg:lxml.etree.XMLParser:resolve_entities=False", "call_arg:lxml.etree.XMLParser:no_network=True"]))
  emit issue code.XmlParserCreate at node
}
