module bindings.python.migration.m21017d75;

binding weakRedirectUrlValidation252 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=python" and containsAny(node.token, ["function_name:validate_redirect", "function_name:is_safe_url", "function_name:safe_redirect"]) and node.token contains "call_path:urlsplit" and node.token contains "attr_path:request.host_url" and node.token contains "call_path:current_app.config.get" and containsAny(node.token, ["binary:__binop.not in"]) and containsAny(node.token, ["binary:__binop.ne"]) and not (containsAny(node.token, ["call_path:ord", "call_path:url.startswith", "call_path:url.replace", "call_path:quote", "call_path:urlquote"])) and not (containsAny(node.token, ["binary:__binop.le"]))
  emit issue code.WeakRedirectUrlValidation at node
}
