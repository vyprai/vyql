module bindings.python.migration.m21017d75;

binding pickleUnsafeImportBlocklistGap312 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "unsafe_imports" and containsAny(node.token, ["literal:runpy", "literal:cProfile", "literal:marshal", "literal:types", "literal:subprocess", "literal:os"]) and not (node.context.literal contains "ctypes")
  emit issue code.PickleUnsafeImportBlocklistGap at node
}
