module bindings.python.migration.m21017d75;

binding unparameterizedSqlQueryParser496 {
  query pattern presenceNode where node.scope == "function" and node.token contains "name=parse" and node.token contains "query_expr.parseString" and node.token contains "repr(" and node.token contains ".replace(" and node.token contains "__default_field__" and node.token contains "=ANY("
  emit issue code.UnparameterizedSqlQueryParser at node
}
