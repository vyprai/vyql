module bindings.python.migration.m21017d75;

binding weakJwtSecret645 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "call" and node.path ~= "jwt.encode" and containsAny(node.token, ["call_arg:secret", "call_arg:jwt-secret", "call_arg:change-me", "call_arg:changeme", "call_arg:password"])
  emit issue code.WeakJwtSecret at node
}
