module bindings.python.migration.m21017d75;

binding htmlSanitizerLogicReview441 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "class_bases=HTMLSerializer" and node.token contains "name=serialize" and not (node.token contains "escape_rcdata=True")
  emit issue code.HtmlSanitizerLogicReview at node
}
