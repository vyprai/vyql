module bindings.python.migration.m21017d75;

binding unparameterizedSqlQueryParser494 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "name=parse" and node.token contains "query_expr.parseString" and node.token contains "repr(" and node.token contains ".replace(" and node.token contains "__default_field__" and node.token contains "ILIKE"
  emit issue code.UnparameterizedSqlQueryParser at node
}
