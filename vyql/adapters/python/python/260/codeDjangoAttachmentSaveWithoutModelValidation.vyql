module bindings.python.migration.m21017d75;

binding djangoAttachmentSaveWithoutModelValidation260 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "name=process_attachments" and node.token contains "attached_files" and node.token contains "FollowUpAttachment(" and node.token contains "file=attached" and node.token contains "filename=filename" and node.token contains "att.save()" and not (node.token contains "att.full_clean()") and not (node.token contains "full_clean()")
  emit issue code.DjangoAttachmentSaveWithoutModelValidation at node
}
