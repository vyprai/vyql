module bindings.python.migration.m21017d75;

binding unvalidatedSqlIdentifierInterpolation497 {
  query pattern presenceNode where node.scope == "function" and node.context.name contains "update_profile" and node.context.literal contains "UPDATE users SET" and node.context.subscript contains "field[0]" and node.context.callPath contains "nonquery" and not (node.context.expr contains "field[0]in[\"firstname\",\"lastname\"]")
  emit issue code.UnvalidatedSqlIdentifierInterpolation at node
}
