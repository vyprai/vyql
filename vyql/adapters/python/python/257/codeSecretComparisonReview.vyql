module bindings.python.migration.m21017d75;

binding secretComparisonReview257 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "binop" and node.op contains "not in" and not (containsAny(node.scopeCall.any, [constant_time_compare, hmac.compare_digest, secrets.compare_digest, compare_digest])) and operand(node, where: operand.path ~= "get_request_basic_auth") and operand(node, where: operand.path ~= "basic_auth")
  emit issue code.SecretComparisonReview at node
}
