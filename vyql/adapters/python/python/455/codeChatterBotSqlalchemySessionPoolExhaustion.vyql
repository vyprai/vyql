module bindings.python.migration.m21017d75;

binding chatterBotSqlalchemySessionPoolExhaustion455 {
  query pattern presenceNode where node.scope == "function" and node.token contains "class_name=SQLStorageAdapter" and node.context.name contains "filter" and node.token contains "session=self.Session()" and node.token contains "yieldself.model_to_object(statement)" and not (node.token contains "finally")
  emit issue code.ChatterBotSqlalchemySessionPoolExhaustion at node
}
