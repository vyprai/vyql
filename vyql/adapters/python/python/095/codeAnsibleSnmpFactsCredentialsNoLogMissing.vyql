module bindings.python.generated.m21017d75;

binding ansibleSnmpFactsCredentialsNoLogMissing95 {
  query pattern presenceNode where node.scope == "function" and node.context.name contains "main" and node.token contains "AnsibleModule" and node.token contains "authkey=dict(type='str')" and node.token contains "privkey=dict(type='str')" and node.token contains "cmdgen.UsmUserData" and not (node.token contains "authkey=dict(type='str',no_log=True)") and not (node.token contains "privkey=dict(type='str',no_log=True)")
  emit issue code.AnsibleSnmpFactsCredentialsNoLogMissing at node
}
