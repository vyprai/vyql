module bindings.python.migration.m21017d75;

binding unparameterizedSqlQueryParser495 {
  query pattern presenceNode where node.scope == "function" and node.context.name contains "parse" and node.token contains "query_expr.parseString" and node.token contains "repr(" and node.token contains ".replace(" and node.token contains "__default_field__" and node.token contains "::jsonb"
  emit issue code.UnparameterizedSqlQueryParser at node
}
