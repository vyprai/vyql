module bindings.python.migration.m21017d75;

binding commandExecution239 {
  query pattern callExpr where callee.path ~= "subprocess.check_call" and args.any.literal contains "-c"
  emit sink code.CommandExecution at args[0].collection[2]
}
