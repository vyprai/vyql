module bindings.python.migration.m21017d75;

binding cloudStackBaremetalMacCommandInjection265 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "name=notify_provisioning_done" and node.token contains "/baremetal/provisiondone/<mac>" and node.token contains "server.notify_provisioning_done(mac)" and not (node.token contains "is_a_mac(mac)")
  emit issue code.CloudStackBaremetalMacCommandInjection at node
}
