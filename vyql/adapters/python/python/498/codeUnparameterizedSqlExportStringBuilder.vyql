module bindings.python.migration.m21017d75;

binding unparameterizedSqlExportStringBuilder498 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=python" and node.token contains "call_path:cur.execute" and containsAny(node.token, ["identifier:creation_list", "identifier:values_list", "identifier:insert_list", "identifier:insert_query"]) and containsAny(node.token, ["identifier:plugin", "identifier:table", "identifier:col_names", "identifier:columns"]) and not (containsAny(node.token, ["call_path:sql.Identifier", "call_path:sql.Literal", "call_path:sql.Placeholder"])) and not (containsAny(node.token, ["call_path:cur.executemany", "call_path:execute_values", "call_path:execute_batch"]))
  emit issue code.UnparameterizedSqlExportStringBuilder at node
}
