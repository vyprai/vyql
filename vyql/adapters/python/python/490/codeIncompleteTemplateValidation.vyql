module bindings.python.migration.m21017d75;

binding incompleteTemplateValidation490 {
  query pattern presenceNode where node.scope == "function" and node.token contains "Formatter().parse" and node.token contains "[" and not (node.token contains "format_spec")
  emit issue code.IncompleteTemplateValidation at node
}
