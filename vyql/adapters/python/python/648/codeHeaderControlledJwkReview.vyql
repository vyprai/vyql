module bindings.python.migration.m21017d75;

binding headerControlledJwkReview648 {
  query pattern presenceNode where node.scope == "function" and node.context.name contains "prepare_key" and node.token contains "key is None and \"jwk\" in header" and node.token contains "key = header[\"jwk\"]" and node.token contains "prepare_key(key)"
  emit issue code.HeaderControlledJwkReview at node
}
