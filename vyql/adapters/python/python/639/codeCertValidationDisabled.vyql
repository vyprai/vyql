module bindings.python.migration.m21017d75;

binding certValidationDisabled639 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "httpx" and node.token contains "verify=False"
  emit issue code.CertValidationDisabled at node
}
