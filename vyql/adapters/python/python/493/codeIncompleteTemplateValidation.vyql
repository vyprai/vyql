module bindings.python.migration.m21017d75;

binding incompleteTemplateValidation493 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "name=get_jinja_env" and node.token contains "Environment(extensions=" and node.token contains "filters.update(get_jinja_filters())" and node.token contains "returnjinja_env" and not (node.token contains "SandboxedEnvironment") and not (node.token contains "ImmutableSandboxedEnvironment")
  emit issue code.IncompleteTemplateValidation at node
}
