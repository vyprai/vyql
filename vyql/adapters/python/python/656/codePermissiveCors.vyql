module bindings.python.migration.m21017d75;

binding permissiveCors656 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "CORS" and node.token contains "origins=*"
  emit issue code.PermissiveCors at node
}
