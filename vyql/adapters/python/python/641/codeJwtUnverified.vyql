module bindings.python.migration.m21017d75;

binding jwtUnverified641 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "jwt.decode" and node.token contains "algorithms=none"
  emit issue code.JwtUnverified at node
}
