module bindings.python.migration.m21017d75;

binding collectiveContactWidgetTitleXss458 {
  query pattern presenceNode where node.scope == "function" and node.token contains "name=title" and node.token contains "class_name=TermViewlet" and node.token contains "term-contact" and node.token contains "objpath" and node.token contains "self.title" and not (node.token contains "quote=True") and not (node.token contains "html.escape") and not (node.token contains "markupsafe.escape")
  emit issue code.CollectiveContactWidgetTitleXss at node
}
