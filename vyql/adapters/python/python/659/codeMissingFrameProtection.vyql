module bindings.python.migration.m21017d75;

binding missingFrameProtection659 {
  query pattern callExpr where callee.path ~= "headers.setdefault" and args.any.literal contains "X-Frame-Options" and args.any.literal contains "ALLOWALL"
  emit issue code.MissingFrameProtection at call
}
