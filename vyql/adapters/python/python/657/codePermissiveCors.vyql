module bindings.python.generated.m21017d75;

binding permissiveCors657 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "cross_origin" and node.token contains "origins=*"
  emit issue code.PermissiveCors at node
}
