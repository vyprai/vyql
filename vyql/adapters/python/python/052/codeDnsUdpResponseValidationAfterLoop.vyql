module bindings.python.migration.m21017d75;

binding dnsUdpResponseValidationAfterLoop52 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "name=udp" and node.token contains ".recvfrom" and node.token contains "dns.message.from_wire" and node.token contains "q.is_response" and node.token contains "from_address==destination" and node.token contains "break" and not (node.token contains "ignore_errors") and not (node.token contains "exceptdns.message.Truncated")
  emit issue code.DnsUdpResponseValidationAfterLoop at node
}
