module bindings.python.migration.m21017d75;

binding unsafeYamlLoad290 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "call" and node.path == "yaml.load_all" and operand(node, where: operand.path == "yaml.Loader")
  emit issue code.UnsafeYamlLoad at node
}
