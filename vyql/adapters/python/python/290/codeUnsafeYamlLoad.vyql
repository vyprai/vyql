module bindings.python.generated.m21017d75;

binding unsafeYamlLoad290 {
  query pattern presenceNode where node.kind == "call" and node.path == "yaml.load_all" and operand(node, where: operand.path == "yaml.Loader")
  emit issue code.UnsafeYamlLoad at node
}
