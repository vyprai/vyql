module bindings.python.migration.m21017d75;

binding emmettUnhandledCookieParseException51 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "name=cookies" and node.token contains "forcookieinself.headers.get(\"cookie\",\"\").split(\";\"):" and node.token contains "cookies.load(cookie)" and not (node.token contains "exceptException") and not (node.token contains "continue")
  emit issue code.EmmettUnhandledCookieParseException at node
}
