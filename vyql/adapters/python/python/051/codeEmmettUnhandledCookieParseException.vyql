module bindings.python.migration.m21017d75;

binding emmettUnhandledCookieParseException51 {
  query pattern presenceNode where node.scope == "function" and node.context.name contains "cookies" and node.token contains "forcookieinself.headers.get(\"cookie\",\"\").split(\";\"):" and node.token contains "cookies.load(cookie)" and not (node.token contains "exceptException") and not (node.token contains "continue")
  emit issue code.EmmettUnhandledCookieParseException at node
}
