module bindings.python.migration.m21017d75;

binding unvalidatedChoiceControlField500 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "name=set_transaction_type" and node.token contains "ifargs.transaction_type:" and node.token contains "args.transaction_type=\"selling\"" and node.token contains "args.transaction_type=\"buying\"" and not (node.token contains "args.transaction_typein[\"buying\",\"selling\"]")
  emit issue code.UnvalidatedChoiceControlField at node
}
