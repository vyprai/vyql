module bindings.python.migration.m21017d75;

binding accessPolicyReview164 {
  query pattern presenceNode where node.scope == "function" and node.context.name contains "create_shelf" and node.token contains "ub.Shelf()" and node.token contains "create_edit_shelf" and node.token contains "page=\"shelfcreate\"" and not (node.token contains "role_edit_shelfs")
  emit issue code.AccessPolicyReview at node
}
