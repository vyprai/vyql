module bindings.python.migration.m21017d75;

binding requestSelectedDeserializationFormat311 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "name=api_endpoint" and node.token contains "request.headers.get" and node.token contains "Content-Type" and node.token contains "ALL_SERDE.get" and node.token contains "from_http_request" and not (node.token contains "name=api_endpoint_wrapper") and not (node.token contains "UNSUPPORTED_MEDIA_TYPE")
  emit issue code.RequestSelectedDeserializationFormat at node
}
