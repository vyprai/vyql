module bindings.python.migration.m21017d75;

binding ownedResourceDeleteMissingOwnershipCheck527 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "python" and node.context.name contains "delete" and containsAny(node.token, ["call_path:get_image_meta_or_404", "call_path:get_resource", "call_path:get_object", "call_path:get_by_id", "call_path:find_by_id"]) and containsAny(node.token, ["call_path:schedule_delete_from_backend", "call_path:delete_image_metadata", "call_path:delete_resource", "call_path:delete_object"]) and containsAny(node.token, ["selector:req.context.read_only", "selector:request.context.read_only"]) and containsAny(node.token, ["subscript:image['protected']", "subscript:resource['protected']", "subscript:object['protected']"]) and not (containsAny(node.token, ["selector:req.context.is_admin", "selector:request.context.is_admin", "selector:req.context.owner", "selector:request.context.owner", "selector:current_user.id"])) and not (containsAny(node.token, ["subscript:image['owner']", "subscript:resource['owner']", "subscript:object['owner']"])) and not (containsAny(node.token, ["call_path:check_ownership", "call_path:require_owner", "call_path:is_owner", "call_path:checkPermission", "call_path:check_permission"]))
  emit issue code.OwnedResourceDeleteMissingOwnershipCheck at node
}
