module bindings.python.migration.m21017d75;

binding cinderUnsafeMinidomXmlDeserializer316 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "class_bases=wsgi.MetadataXMLDeserializer" and node.token contains "name=default" and node.token contains "minidom.parseString(string)" and not (node.token contains "safe_minidom_parse_string(string)")
  emit issue code.CinderUnsafeMinidomXmlDeserializer at node
}
