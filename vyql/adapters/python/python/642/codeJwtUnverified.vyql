module bindings.python.migration.m21017d75;

binding jwtUnverified642 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "jwt.decode" and node.token contains "verify_signature=False"
  emit issue code.JwtUnverified at node
}
