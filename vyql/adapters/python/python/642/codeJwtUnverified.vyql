module bindings.python.migration.m21017d75;

binding jwtUnverified642 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "jwt.decode" and node.token contains "verify_signature=False"
  emit issue code.JwtUnverified at node
}
