module bindings.groovy.migration.m6eabb149;

binding csrfDisabled67 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "csrf.disable"
  emit issue code.CsrfDisabled at node
}
