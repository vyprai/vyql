module bindings.java.migration.m420f6b67;

binding unsafeYamlConstructor139 {
  requires {
    dependency("org.yaml.snakeyaml")
  }
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "class" and node.token contains "class_base:Constructor"
  emit issue code.UnsafeYamlConstructor at node
}
