module bindings.java.migration.m420f6b67;

binding unparameterizedSqlQueryParser124 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path == "analysis.function.return" and node.token contains "function_name:toString" and node.token contains "::box"
  emit issue code.UnparameterizedSqlQueryParser at node
}
