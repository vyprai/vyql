module bindings.java.migration.m420f6b67;

binding integerSizeArithmetic57 {
  query pattern presenceNode where node.scope == "function" and node.token contains "function_name:crypt_raw" and node.token contains "expr:1<<log_rounds" and not (node.scopeCall.any contains "roundsForLogRounds")
  emit issue code.IntegerSizeArithmetic at node
  emit sink code.IntegerSizeArithmeticSink at node
}
