module bindings.java.migration.m420f6b67;

binding integerSizeArithmetic57 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "function_name:crypt_raw" and node.token contains "expr:1<<log_rounds" and not (node.scopeCall.any contains "roundsForLogRounds")
  emit issue code.IntegerSizeArithmetic at node
  emit sink code.IntegerSizeArithmeticSink at node
}
