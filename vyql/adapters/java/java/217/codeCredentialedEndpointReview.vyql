module bindings.java.migration.m420f6b67;

binding credentialedEndpointReview217 {
  query pattern presenceNode where node.scope == "function" and node.token contains "function_name:doFill" and node.token contains "call:getBzmUtils" and not (node.scopeCall.any contains "checkPermission")
  emit issue code.CredentialedEndpointReview at node
}
