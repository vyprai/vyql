module bindings.java.migration.m420f6b67;

binding svgOnclickWindowOpenLinkXss86 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "identifier:link" and node.token contains "window.open" and node.token contains "cursor: pointer" and node.token contains "call:escapeHtml" and node.token contains "call:replace" and node.token contains "call:getBytes" and not (node.scopeCall.any contains "getProtocol") and not (node.token contains "&quot;")
  emit issue code.SvgOnclickWindowOpenLinkXss at node
}
