module bindings.java.migration.m420f6b67;

binding premasterSecretLengthReview63 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "safeDecryptPreMasterSecret" and node.context.call contains "doFinal" and node.context.call contains "createSecret" and node.token contains "M[0]" and not (node.context.expr contains "m.length==48")
  emit issue code.PremasterSecretLengthReview at node
}
