module bindings.java.migration.m420f6b67;

binding unvalidatedModelSelection61 {
  query pattern presenceNode where node.scope == "function" and node.token contains "function_name:addResults" and node.token contains "call_path:request.getParameter" and node.token contains "literal:classifier" and node.token contains "call_path:response.addHeader" and node.token contains "call_path:ners.get.classifyToString" and not (node.token contains "identifier:nerModel")
  emit issue code.UnvalidatedModelSelection at node
}
