module bindings.java.migration.m420f6b67;

binding publicKeyHashBindingReview72 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "correctlySpends" and node.context.call contains "fromPublicOnly" and node.context.call contains "verify" and node.token contains "hashFor" and not (node.scopeCall.any contains "sha256hash160")
  emit issue code.PublicKeyHashBindingReview at node
}
