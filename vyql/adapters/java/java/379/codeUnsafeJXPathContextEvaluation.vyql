module bindings.java.migration.m420f6b67;

binding unsafeJXPathContextEvaluation379 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and containsAny(node.token, ["call_path:JXPathContext.newContext", "call_path:JXPathContextFactory.newInstance.newContext"]) and containsAny(node.token, ["call_path:getValue", "call_path:selectNodes", "call_path:iterate"]) and not (node.token contains "call_path:JXPathUtils.newSafeContext") and not (node.token contains "call_path:setFunctions")
  emit issue code.UnsafeJXPathContextEvaluation at node
}
