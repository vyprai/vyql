module bindings.java.migration.m420f6b67;

binding sessionStoredRedirectTarget75 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "determineTargetUrl" and node.token contains "SimpleUrlAuthenticationSuccessHandler" and node.token contains "session.getAttribute" and node.token contains "targetUrlSessionAttribute" and node.token contains "targetUrl.substring" and not (node.token contains "Util.isSafeToRedirectTo")
  emit issue code.SessionStoredRedirectTarget at node
}
