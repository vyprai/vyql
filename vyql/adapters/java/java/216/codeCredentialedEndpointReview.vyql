module bindings.java.migration.m420f6b67;

binding credentialedEndpointReview216 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "doTest" and node.context.call contains "setCredentialsId" and node.context.call contains "accurevLoginFromGlobalConfig" and not (node.scopeCall.any contains "hasPermission") and not (node.scopeCall.any contains "checkPermission")
  emit issue code.CredentialedEndpointReview at node
}
