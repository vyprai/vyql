module bindings.java.migration.m420f6b67;

binding missingSessionRotationAfterAuthentication69 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "call:setAuthentication" and node.token contains "call:getSession" and not (node.scopeCall.any contains "invalidate")
  emit issue code.MissingSessionRotationAfterAuthentication at node
}
