module bindings.java.migration.m420f6b67;

binding integerSizeArithmetic56 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "expr:offset+length>b.length" and node.token contains "call_path:write" and not (node.token contains "expr:offset>b.length-length")
  emit issue code.IntegerSizeArithmetic at node
  emit sink code.IntegerSizeArithmeticSink at node
}
