module bindings.java.migration.m420f6b67;

binding frameworkRequestBeforeSessionValidation60 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "function_name:doFilter" and node.token contains "call_path:Meteor.build" and node.token contains "call_path:httpReq.getParameter" and node.token contains "call_path:pushContext.getSessionManager.getPushSession" and node.token contains "call_path:httpResp.sendError" and node.token contains "call:RequestImpl" and node.token contains "call_order:Meteor.build>Collections.emptyList" and node.token contains "call_order:Collections.emptyList>httpReq.getParameter" and not (node.token contains "call_order:httpResp.setContentType>Meteor.build")
  emit issue code.FrameworkRequestBeforeSessionValidation at node
}
