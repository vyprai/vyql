module bindings.java.migration.m420f6b67;

binding insecurePermission326 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "createTempFile" and node.context.callPath contains "File.createTempFile" and not (node.context.callPath contains "setReadable") and not (node.context.callPath contains "Files.setPosixFilePermissions")
  emit issue code.InsecurePermission at node
}
