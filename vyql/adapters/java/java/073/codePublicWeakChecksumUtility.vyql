module bindings.java.migration.m420f6b67;

binding publicWeakChecksumUtility73 {
  query pattern presenceNode where node.scope == "class" and node.token contains "class_name:Checksum" and node.token contains "class_modifier:public" and node.token contains "literal:SHA-1" and node.token contains "literal:CRC32" and node.token contains "call_path:MessageDigest.getInstance" and node.token contains "call_path:res.setHash" and node.token contains "call_path:res.setCrc"
  emit issue code.PublicWeakChecksumUtility at node
}
