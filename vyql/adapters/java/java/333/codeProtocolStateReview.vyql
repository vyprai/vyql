module bindings.java.migration.m420f6b67;

binding protocolStateReview333 {
  query pattern presenceNode where node.scope == "function" and node.token contains "call:getType" and node.token contains "selector:FEDERATION_DOWNSTREAM_CONNECT" and node.token contains "call:deploy" and not (node.scopeCall.any contains "getSubject")
  emit issue code.ProtocolStateReview at node
}
