module bindings.java.migration.m420f6b67;

binding protocolStateReview333 {
  query pattern presenceNode where node.scope == "function" and node.context.call contains "getType" and node.context.selector contains "FEDERATION_DOWNSTREAM_CONNECT" and node.context.call contains "deploy" and not (node.scopeCall.any contains "getSubject")
  emit issue code.ProtocolStateReview at node
}
