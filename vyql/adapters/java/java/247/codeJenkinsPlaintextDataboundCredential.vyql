module bindings.java.migration.m420f6b67;

binding jenkinsPlaintextDataboundCredential247 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "@DataBoundConstructor" and node.token contains "Stringpassword" and node.token contains "this.password=password;" and not (node.token contains "Secret.fromString(password)") and not (node.token contains "Secretpassword")
  emit issue code.JenkinsPlaintextDataboundCredential at node
}
