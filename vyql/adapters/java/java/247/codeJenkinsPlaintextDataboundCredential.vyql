module bindings.java.generated.m420f6b67;

binding jenkinsPlaintextDataboundCredential247 {
  query pattern presenceNode where node.scope == "function" and node.token contains "@DataBoundConstructor" and node.token contains "Stringpassword" and node.token contains "this.password=password;" and not (node.token contains "Secret.fromString(password)") and not (node.token contains "Secretpassword")
  emit issue code.JenkinsPlaintextDataboundCredential at node
}
