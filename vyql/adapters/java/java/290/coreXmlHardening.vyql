module bindings.java.migration.m420f6b67;

binding xmlHardening290 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "setAttribute" and node.token contains "ACCESS_EXTERNAL_SCHEMA"
  emit check core.XmlHardening at node {
    advisory: true
  }
}
