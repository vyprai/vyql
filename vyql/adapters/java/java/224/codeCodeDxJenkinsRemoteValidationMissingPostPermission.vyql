module bindings.java.migration.m420f6b67;

binding codeDxJenkinsRemoteValidationMissingPostPermission224 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "function_name:doCheckSelfSignedCertificateFingerprint" and node.token contains "call:buildClient" and node.token contains "call:getProjects" and not (node.token contains "annotation:POST") and not (node.scopeCall.any contains "checkPermissionForRemoteRequests")
  emit issue code.CodeDxJenkinsRemoteValidationMissingPostPermission at node
}
