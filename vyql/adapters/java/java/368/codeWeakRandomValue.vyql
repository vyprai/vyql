module bindings.java.migration.m420f6b67;

binding weakRandomValue368 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "Random" and not (node.scopeCall.any contains "SecureRandom.getInstance")
  emit issue code.WeakRandomValue at node
}
