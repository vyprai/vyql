module bindings.java.migration.m420f6b67;

binding jdbcCredentialAttributeExposure250 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "function_name:connect" and node.token contains "call:tokenizeDatabase" and node.token contains "call:tokenizeURLProperties" and node.token contains "call:NetConnection" and not (node.scopeCall.any contains "appendDatabaseAttributes")
  emit issue code.JdbcCredentialAttributeExposure at node
}
