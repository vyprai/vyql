module bindings.java.migration.m420f6b67;

binding jenkinsDescriptorPropertyFileValidationMissingPost233 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "class_base:ParameterDescriptor" and containsAny(node.token, ["function_name:doCheckPropertyKey", "function_name:doCheckDefaultPropertyFile", "function_name:doCheckDefaultPropertyKey"]) and node.token contains "identifier:QueryParameter" and node.token contains "call_path:doCheckPropertyFile" and not (node.token contains "annotation:POST") and not (node.token contains "annotation:RequirePOST")
  emit issue code.JenkinsDescriptorPropertyFileValidationMissingPost at node
}
