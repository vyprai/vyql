module bindings.java.migration.m420f6b67;

binding requestHostUrlValidationBypass66 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "call:getServerName" and node.token contains "call:getHost" and node.token contains "call:equals" and not (node.scopeCall.any contains "contains")
  emit issue code.RequestHostUrlValidationBypass at node
}
