module bindings.sca.migration.mc40a837e;

binding reachableSymbol11 {
  query pattern presenceNode where node.kind == "any" and node.path == "analysis.sca.package" and node.token contains "reachable=true"
  emit issue sbom.ReachableSymbol at node
}
