module bindings.sca.generated.mc40a837e;

binding vulnerableDependency0 {
  query pattern presenceNode where node.kind == "any" and node.analysis == "sca.package" and node.token contains "status=vulnerable"
  emit issue sbom.VulnerableDependency at node
}
