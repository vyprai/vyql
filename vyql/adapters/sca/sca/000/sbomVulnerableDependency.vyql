module bindings.sca.migration.mc40a837e;

binding vulnerableDependency0 {
  query pattern presenceNode where node.kind == "any" and node.path == "analysis.sca.package" and node.token contains "status=vulnerable"
  emit issue sbom.VulnerableDependency at node
}
