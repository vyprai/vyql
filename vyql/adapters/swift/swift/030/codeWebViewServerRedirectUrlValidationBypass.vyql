module bindings.swift.migration.m388f23d0;

binding webViewServerRedirectUrlValidationBypass30 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "module" and node.token contains "lang=swift" and node.token contains "call_path:RequestHandler.handle" and node.token contains "call_path:decisionHandler" and node.token contains "selector:navigationAction.request" and not (node.token contains "param_label:didReceiveServerRedirectForProvisionalNavigation")
  emit issue code.WebViewServerRedirectUrlValidationBypass at node
}
