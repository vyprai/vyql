module bindings.swift.migration.m388f23d0;

binding clearanceKitPolicyApplyBeforeAdapterStart49 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "module" and node.token contains "lang=swift" and node.token contains "server.applyPolicyToFilter()server.applyAllowlistToFilter()server.applyJailRulesToFilter()" and node.token contains "adapter.start(initialRules:initialRules,onXProtectChanged:{server.handleXProtectChange()})" and not (node.token contains "adapter.start(initialRules:initialRules,onXProtectChanged:{server.handleXProtectChange()})server.applyPolicyToFilter()")
  emit issue code.ClearanceKitPolicyApplyBeforeAdapterStart at node
}
