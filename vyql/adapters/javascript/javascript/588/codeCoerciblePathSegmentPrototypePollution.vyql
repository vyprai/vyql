module bindings.javascript.migration.mfb63017a;

binding coerciblePathSegmentPrototypePollution588 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "path.length-1" and node.token contains "temp=path[i]" and node.token contains "temp===\"constructor\"||temp===\"__proto__\"||temp===\"prototype\"" and node.token contains "obj[temp]===void0" and node.token contains "obj=obj[temp]" and node.token contains "obj[temp]=value" and not (node.token contains "temp.constructor!==String&&temp.constructor!==Number") and not (node.token contains "typeof temp") and not (node.token contains "String(temp)")
  emit issue code.CoerciblePathSegmentPrototypePollution at node
}
