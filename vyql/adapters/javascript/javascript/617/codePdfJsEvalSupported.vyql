module bindings.javascript.migration.mfb63017a;

binding pdfJsEvalSupported617 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "__object_literal" and node.token contains "isEvalSupported=true"
  emit issue code.PdfJsEvalSupported at node
  emit source code.PdfJsEvalSupportedSource at node
}
