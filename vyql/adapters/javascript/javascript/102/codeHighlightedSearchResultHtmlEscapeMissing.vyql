module bindings.javascript.migration.mfb63017a;

binding highlightedSearchResultHtmlEscapeMissing102 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path == "analysis.function.context" and node.token contains "lang=javascript" and node.token contains "name=search" and node.token contains "call_path:ignoreDiacriticalMarks" and node.token contains "literal:search-keyword" and node.token contains "identifier:matchingPost" and not (node.token contains "call_path:escapeHtml")
  emit issue code.HighlightedSearchResultHtmlEscapeMissing at node
}
