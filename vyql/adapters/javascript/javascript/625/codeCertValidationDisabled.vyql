module bindings.javascript.generated.mfb63017a;

binding certValidationDisabled625 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "tls.connect" and node.token contains "rejectUnauthorized=false"
  emit issue code.CertValidationDisabled at node
}
