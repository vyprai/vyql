module bindings.javascript.migration.mfb63017a;

binding chartkickRecursiveExtendPrototypePollution578 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "name=extend" and node.token contains "for(keyinsource)" and node.token contains "target[key]=source[key]" and node.token contains "extend(target[key],source[key])" and not (node.token contains "key===\"__proto__\"")
  emit issue code.ChartkickRecursiveExtendPrototypePollution at node
}
