module bindings.javascript.migration.mfb63017a;

binding gitPackSignatureSearchParsingBypass254 {
  query pattern presenceNode where node.scope == "function" and containsAny(node.token, ["call_path:?.lastIndexOf", "call_path:req.body.lastIndexOf"]) and node.context.literal contains "PACK" and containsAny(node.token, ["call_path:?.slice", "call_path:req.body.slice"]) and node.context.callPath contains "getPackMeta" and not (node.context.identifier contains "packDataOffset") and not (node.context.literal contains "PACK_SIGNATURE")
  emit issue code.GitPackSignatureSearchParsingBypass at node
}
