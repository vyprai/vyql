module bindings.javascript.migration.mfb63017a;

binding unboundedResourceOperation627 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "responseBuffer.push(chunk)" and node.token contains "Buffer.concat(responseBuffer).length>config.maxContentLength" and not (node.token contains "stream.destroy()")
  emit issue code.UnboundedResourceOperation at node
}
