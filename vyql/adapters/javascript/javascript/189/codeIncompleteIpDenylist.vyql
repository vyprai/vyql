module bindings.javascript.migration.mfb63017a;

binding incompleteIpDenylist189 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "name=isValidPublicIPv4Address" and node.token contains "call_path:parseInt" and node.token contains "expr:first===10" and node.token contains "expr:first===127" and node.token contains "expr:first===169&&second===254" and node.token contains "expr:first===172&&second>=16&&second<=31" and node.token contains "expr:first===192&&second===168"
  emit issue code.IncompleteIpDenylist at node
}
