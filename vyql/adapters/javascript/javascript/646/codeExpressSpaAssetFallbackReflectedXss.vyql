module bindings.javascript.migration.mfb63017a;

binding expressSpaAssetFallbackReflectedXss646 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "module" and node.token contains "lang=javascript" and node.token contains "call_path:express.static" and node.token contains "literal:/*" and containsAny(node.token, ["literal:./build/index.html", "literal:./src/client/index.html"]) and not (node.token contains "literal:/js/*") and not (node.token contains "literal:/images/*") and not (node.token contains "literal:/styles/*")
  emit issue code.ExpressSpaAssetFallbackReflectedXss at node
}
