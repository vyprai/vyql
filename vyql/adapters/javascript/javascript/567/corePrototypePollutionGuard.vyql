module bindings.javascript.migration.mfb63017a;

binding prototypePollutionGuard567 {
  query pattern callExpr where callee.path ~= "analysis.function.context" and args.any.literal contains "Object.prototype.hasOwnProperty.call(pointer,key)"
  emit check core.PrototypePollutionGuard at call {
    covers path {
      from: call
      to: call
    }
  }
}
