module bindings.javascript.migration.mfb63017a;

binding prototypePollutionGuard567 {
  query pattern callExpr where callee.analysis ~= "function.context" and args.any.literal contains "Object.prototype.hasOwnProperty.call(pointer,key)"
  emit check core.PrototypePollutionGuard at call {
    covers endpoint {
      anchor: call
    }
  }
}
