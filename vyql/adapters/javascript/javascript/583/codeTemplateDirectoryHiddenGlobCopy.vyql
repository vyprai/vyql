module bindings.javascript.migration.mfb63017a;

binding templateDirectoryHiddenGlobCopy583 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "javascript" and node.context.name contains "downloadTemplate" and node.context.callPath contains "fs.statSync" and node.context.callPath contains "d.copy" and not (node.context.prop contains "hide=false")
  emit issue code.TemplateDirectoryHiddenGlobCopy at node
}
