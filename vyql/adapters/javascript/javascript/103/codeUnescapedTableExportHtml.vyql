module bindings.javascript.migration.mfb63017a;

binding unescapedTableExportHtml103 {
  query pattern presenceNode where node.scope == "module" and node.token contains "lang=javascript" and node.token contains "prop:onCellHtmlData" and node.token contains "identifier:htmlData" and node.token contains "call_path:tableExport" and node.token contains "prop:escape=false" and not (node.token contains "prop:escape=true") and not (node.token contains "call_path:cell.text")
  emit issue code.UnescapedTableExportHtml at node
}
