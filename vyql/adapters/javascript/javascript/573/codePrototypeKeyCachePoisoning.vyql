module bindings.javascript.migration.mfb63017a;

binding prototypeKeyCachePoisoning573 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "name=cachedPathRelative" and node.token contains "cache[from]" and node.token contains "cache[from][to]" and node.token contains "relative.call(path,from,to)" and node.token contains "cache[from]=cache[from]||{}" and not (node.token contains "Object.create(null)")
  emit issue code.PrototypeKeyCachePoisoning at node
}
