module bindings.javascript.migration.mfb63017a;

binding swaggerParserExternalRefDereference302 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "call_path:yaml.safeLoad" and node.token contains "call_path:swaggerParser.validate" and node.token contains "identifier:loaded" and not (node.token contains "prop:external=false")
  emit issue code.SwaggerParserExternalRefDereference at node
}
