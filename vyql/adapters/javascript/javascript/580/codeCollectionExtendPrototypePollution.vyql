module bindings.javascript.migration.mfb63017a;

binding collectionExtendPrototypePollution580 {
  query pattern presenceNode where node.scope == "module" and node.token contains "lang=javascript" and node.token contains "_core.Collection.prototype.extend" and node.token contains "forEach((el,key)=>" and node.token contains "_link.byLink" and node.token contains "setVal(data,key,val)" and not (node.token contains "key==='__proto__'")
  emit issue code.CollectionExtendPrototypePollution at node
}
