module bindings.javascript.generated.mfb63017a;

binding clientLightboxOptionXss114 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "javascript" and node.token contains "setHTMLContent" and node.token contains "setMessage(html)" and not (node.token contains "DOMPurify") and not (node.token contains "sanitize(html)") and not (node.token contains "sanitizeHtml(html)")
  emit issue code.ClientLightboxOptionXss at node
}
