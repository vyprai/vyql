module bindings.javascript.migration.mfb63017a;

binding authenticodeSignaturePathBindingBypass244 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "name=verifySignature" and node.token contains "Get-AuthenticodeSignature" and node.token contains "LiteralPath" and node.token contains "call_path:parseOut" and node.token contains "selector:data.SignerCertificate.Subject" and node.token contains "publisherNames" and not (node.token contains "selector:data.Path") and not (node.token contains "call_path:path.normalize")
  emit issue code.AuthenticodeSignaturePathBindingBypass at node
}
