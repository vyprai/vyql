module bindings.javascript.migration.mfb63017a;

binding authenticodeSignaturePathBindingBypass244 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "javascript" and node.context.name contains "verifySignature" and node.token contains "Get-AuthenticodeSignature" and node.token contains "LiteralPath" and node.context.callPath contains "parseOut" and node.context.selector contains "data.SignerCertificate.Subject" and node.token contains "publisherNames" and not (node.context.selector contains "data.Path") and not (node.context.callPath contains "path.normalize")
  emit issue code.AuthenticodeSignaturePathBindingBypass at node
}
