module bindings.javascript.migration.mfb63017a;

binding electronNodeIntegration208 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "BrowserWindow" and node.token contains "contextIsolation=false"
  emit issue code.ElectronNodeIntegration at node
}
