module bindings.javascript.migration.mfb63017a;

binding unescapedLinterIssueMessageXss122 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "name=renderIssue" and node.token contains "issueTemplate({" and node.token contains "message:message.message" and not (node.token contains "message:_.escape(message.message)")
  emit issue code.UnescapedLinterIssueMessageXss at node
}
