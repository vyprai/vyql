module bindings.javascript.migration.mfb63017a;

binding certValidationDisabled623 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "https.get" and node.token contains "rejectUnauthorized=false"
  emit issue code.CertValidationDisabled at node
}
