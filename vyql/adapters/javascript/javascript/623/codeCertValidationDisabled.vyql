module bindings.javascript.migration.mfb63017a;

binding certValidationDisabled623 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "https.get" and node.token contains "rejectUnauthorized=false"
  emit issue code.CertValidationDisabled at node
}
