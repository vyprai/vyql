module bindings.javascript.migration.mfb63017a;

binding localRequestBypassReview182 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "isLocalRequest" and node.token contains "isLocal||awaithasValidToken" and not (node.token contains "hasValidCliToken")
  emit issue code.LocalRequestBypassReview at node
}
