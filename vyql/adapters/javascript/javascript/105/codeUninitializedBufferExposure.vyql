module bindings.javascript.migration.mfb63017a;

binding uninitializedBufferExposure105 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "javascript" and node.context.selector contains "name.encode" and node.context.callPath contains "Buffer.allocUnsafe" and node.context.callPath contains "name.encodingLength" and node.context.callPath contains "str.replace" and node.context.selector contains "name.encodingLength" and node.context.callPath contains "Buffer.byteLength" and not (node.context.callArg contains "Buffer.byteLength:n.replace")
  emit issue code.UninitializedBufferExposure at node
}
