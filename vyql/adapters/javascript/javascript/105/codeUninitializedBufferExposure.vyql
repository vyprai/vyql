module bindings.javascript.migration.mfb63017a;

binding uninitializedBufferExposure105 {
  query pattern presenceNode where node.scope == "module" and node.token contains "lang=javascript" and node.token contains "selector:name.encode" and node.token contains "call_path:Buffer.allocUnsafe" and node.token contains "call_path:name.encodingLength" and node.token contains "call_path:str.replace" and node.token contains "selector:name.encodingLength" and node.token contains "call_path:Buffer.byteLength" and not (node.token contains "call_arg:Buffer.byteLength:n.replace")
  emit issue code.UninitializedBufferExposure at node
}
