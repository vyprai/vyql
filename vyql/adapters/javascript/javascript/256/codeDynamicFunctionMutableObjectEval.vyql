module bindings.javascript.migration.mfb63017a;

binding dynamicFunctionMutableObjectEval256 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "newFunction('node'" and node.token contains "props(expr,'node.')" and not (node.token contains "Object.freeze(node)")
  emit issue code.DynamicFunctionMutableObjectEval at node
}
