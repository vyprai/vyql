module bindings.javascript.migration.mfb63017a;

binding responseParserFunctionEval257 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "call_path:Function" and node.token contains "call_path:parsedResponse.push" and containsAny(node.token, ["literal:return ", "literal:\"use strict\"; return "]) and node.token contains "identifier:data" and not (node.token contains "call_path:JSON.parse")
  emit issue code.ResponseParserFunctionEval at node
}
