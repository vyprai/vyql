module bindings.javascript.migration.mfb63017a;

binding urlPathParameterCaseNormalizationBypass587 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "name=getUrlParams" and node.token contains "call_path:nodeUrl.parse" and node.token contains "call_path:parsed.pathname.split" and node.token contains "call_path:_.tail" and node.token contains "index:parts.__subscript" and node.token contains "call_path:__js_dynamic_property_write" and not (node.token contains "call_path:parts[].toLowerCase")
  emit issue code.UrlPathParameterCaseNormalizationBypass at node
}
