module bindings.javascript.migration.mfb63017a;

binding urlPathParameterCaseNormalizationBypass587 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "javascript" and node.context.name contains "getUrlParams" and node.context.callPath contains "nodeUrl.parse" and node.context.callPath contains "parsed.pathname.split" and node.context.callPath contains "_.tail" and node.context.index contains "parts.__subscript" and node.context.callPath contains "__js_dynamic_property_write" and not (node.context.callPath contains "parts[].toLowerCase")
  emit issue code.UrlPathParameterCaseNormalizationBypass at node
}
