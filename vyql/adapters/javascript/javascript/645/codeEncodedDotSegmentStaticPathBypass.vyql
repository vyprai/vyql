module bindings.javascript.migration.mfb63017a;

binding encodedDotSegmentStaticPathBypass645 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "selector:req.isStaticFile" and node.token contains "selector:req.uri.pathname" and node.token contains "call_path:req.uri.pathname.indexOf" and node.token contains "literal:./" and node.token contains "call_path:req.$total_file" and not (node.token contains "literal:%2e") and not (node.token contains "literal:%2E") and not (node.token contains "call_path:decodeURIComponent") and not (node.token contains "call_path:decodeURI")
  emit issue code.EncodedDotSegmentStaticPathBypass at node
}
