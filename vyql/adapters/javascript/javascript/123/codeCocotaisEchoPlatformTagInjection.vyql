module bindings.javascript.migration.mfb63017a;

binding cocotaisEchoPlatformTagInjection123 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "module" and node.token contains "lang=javascript" and node.token contains "builtin:echo" and node.token contains "\"/echo\"" and node.token contains "_msgs.join(\" \")" and node.token contains "event.reply(content)"
  emit issue code.CocotaisEchoPlatformTagInjection at node
}
