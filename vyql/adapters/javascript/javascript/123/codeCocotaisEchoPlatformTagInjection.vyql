module bindings.javascript.migration.mfb63017a;

binding cocotaisEchoPlatformTagInjection123 {
  query pattern presenceNode where node.scope == "module" and node.token contains "lang=javascript" and node.token contains "builtin:echo" and node.token contains "\"/echo\"" and node.token contains "_msgs.join(\" \")" and node.token contains "event.reply(content)"
  emit issue code.CocotaisEchoPlatformTagInjection at node
}
