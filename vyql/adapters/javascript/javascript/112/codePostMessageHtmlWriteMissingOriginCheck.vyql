module bindings.javascript.migration.mfb63017a;

binding postMessageHtmlWriteMissingOriginCheck112 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "selector:message.data" and containsAny(node.token, ["call_path:document.write", "call_path:parseFromString"]) and node.token contains "selector:innerHTML" and not (containsAny(node.token, ["selector:message.origin", "selector:event.origin", "selector:.origin"])) and not (containsAny(node.token, ["literal:origin", "literal:Origin"]))
  emit issue code.PostMessageHtmlWriteMissingOriginCheck at node
}
