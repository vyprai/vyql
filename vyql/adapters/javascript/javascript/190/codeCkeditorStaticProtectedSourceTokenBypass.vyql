module bindings.javascript.migration.mfb63017a;

binding ckeditorStaticProtectedSourceTokenBypass190 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "name=protectSource" and node.token contains "protectedHtml" and node.token contains "cke_tempcomment" and node.token contains "/cke_temp(comment)?/" and node.token contains "'<!--{cke_temp}'" and not (node.token contains "randomNumber") and not (node.token contains "cke_temp_")
  emit issue code.CkeditorStaticProtectedSourceTokenBypass at node
}
