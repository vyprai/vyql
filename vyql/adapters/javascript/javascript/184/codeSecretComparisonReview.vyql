module bindings.javascript.migration.mfb63017a;

binding secretComparisonReview184 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "javascript" and node.context.callPath contains "secure-compare" and node.context.callPath contains "keys.findIndex"
  emit issue code.SecretComparisonReview at node
}
