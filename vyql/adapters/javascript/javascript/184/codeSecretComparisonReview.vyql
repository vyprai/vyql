module bindings.javascript.migration.mfb63017a;

binding secretComparisonReview184 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "call_path:secure-compare" and node.token contains "call_path:keys.findIndex"
  emit issue code.SecretComparisonReview at node
}
