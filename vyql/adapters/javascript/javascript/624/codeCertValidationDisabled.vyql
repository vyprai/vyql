module bindings.javascript.migration.mfb63017a;

binding certValidationDisabled624 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "https.Agent" and node.token contains "rejectUnauthorized=false"
  emit issue code.CertValidationDisabled at node
}
