module bindings.javascript.migration.mfb63017a;

binding catastrophicRegex544 {
  query pattern presenceNode where node.scope == "module" and node.token contains "lang=javascript" and node.token contains "constcommentre=/\\/\\*[^*]*\\*+([^/*][^*]*\\*+)*\\//g" and not (node.token contains "[^]*?(?:\\*\\/|$)")
  emit issue code.CatastrophicRegex at node
  emit sink code.CatastrophicRegexSink at node
}
