module bindings.javascript.migration.mfb63017a;

binding methodGatedRedirectValidationBypass187 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=javascript" and containsAny(node.token, ["selector:ctx.request.method", "selector:ctx.request?.method"]) and node.token contains "literal:POST" and node.token contains "identifier:callbackURL" and node.token contains "identifier:redirectURL" and node.token contains "selector:body.redirectTo" and node.token contains "identifier:trustedOrigins" and node.token contains "call_path:validateURL"
  emit issue code.MethodGatedRedirectValidationBypass at node
}
