module bindings.javascript.migration.mfb63017a;

binding runtimeSettingsNestedCredentialsExposure620 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "javascript" and node.context.callPath contains "apiApp.get" and node.context.literal contains "/api/settings" and node.context.callPath contains "JSON.parse" and node.context.callPath contains "JSON.stringify" and node.context.selector contains "runtime.settings" and node.context.selector contains "tosend.smtp.password" and node.context.selector contains "settings.daqstore" and node.context.callPath contains "res.json" and not (node.context.selector contains "tosend.daqstore.credentials")
  emit issue code.RuntimeSettingsNestedCredentialsExposure at node
}
