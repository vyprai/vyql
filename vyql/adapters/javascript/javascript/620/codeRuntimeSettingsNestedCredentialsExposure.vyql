module bindings.javascript.migration.mfb63017a;

binding runtimeSettingsNestedCredentialsExposure620 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "module" and node.token contains "lang=javascript" and node.token contains "call_path:apiApp.get" and node.token contains "literal:/api/settings" and node.token contains "call_path:JSON.parse" and node.token contains "call_path:JSON.stringify" and node.token contains "selector:runtime.settings" and node.token contains "selector:tosend.smtp.password" and node.token contains "selector:settings.daqstore" and node.token contains "call_path:res.json" and not (node.token contains "selector:tosend.daqstore.credentials")
  emit issue code.RuntimeSettingsNestedCredentialsExposure at node
}
