module bindings.javascript.migration.mfb63017a;

binding unboundedDelimiterSplitCount628 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "body.split('&').length" and node.token contains "len>limit?undefined:len-1" and not (node.token contains "indexOf('&'")
  emit issue code.UnboundedDelimiterSplitCount at node
}
