module bindings.javascript.migration.mfb63017a;

binding browserPostExportMissingOriginGuard167 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "javascript" and node.context.name contains "<lambda>" and node.context.callPath contains "app.post" and node.context.selector contains "req.body.path" and node.context.callPath contains "exportAll" and not (node.context.selector contains "req.headers.origin") and not (containsAny(node.context.scopeCall, [verifyCsrfToken, validateCsrfToken, checkCsrf, csrf]))
  emit issue code.BrowserPostExportMissingOriginGuard at node
}
