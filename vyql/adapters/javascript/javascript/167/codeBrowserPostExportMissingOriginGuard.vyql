module bindings.javascript.migration.mfb63017a;

binding browserPostExportMissingOriginGuard167 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "name=<lambda>" and node.token contains "call_path:app.post" and node.token contains "selector:req.body.path" and node.token contains "call_path:exportAll" and not (node.token contains "selector:req.headers.origin") and not (containsAny(node.scopeCall.any, [verifyCsrfToken, validateCsrfToken, checkCsrf, csrf]))
  emit issue code.BrowserPostExportMissingOriginGuard at node
}
