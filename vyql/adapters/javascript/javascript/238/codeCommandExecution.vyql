module bindings.javascript.migration.mfb63017a;

binding commandExecution238 {
  query pattern callExpr where callee.path ~= "spawn" and args.any.literal contains "shell" and args.any.literal contains "true"
  emit sink code.CommandExecution at args[1].collection
}
