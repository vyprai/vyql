module bindings.javascript.generated.mfb63017a;

binding defaultExternalRelaySecretExposure618 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "javascript" and node.context.name contains "buildRelayUrl" and node.context.literal contains "herokuapp.com" and node.context.callPath contains "encodeURIComponent" and containsAny(node.context.identifier, ["token", "gurl"]) and not (node.context.identifier contains "configuredRelayHost")
  emit issue code.DefaultExternalRelaySecretExposure at node
}
