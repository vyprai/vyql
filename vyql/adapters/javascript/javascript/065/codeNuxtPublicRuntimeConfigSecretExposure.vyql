module bindings.javascript.migration.mfb63017a;

binding nuxtPublicRuntimeConfigSecretExposure65 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "module" and node.token contains "call_path:defineNuxtModule" and node.token contains "literal:public_runtime_secret_config"
  emit issue code.NuxtPublicRuntimeConfigSecretExposure at node
}
