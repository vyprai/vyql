module bindings.javascript.migration.mfb63017a;

binding compassCsfleEjsonParserMissingStrictMode267 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "name=textToEncryptedFieldConfig" and node.token contains "parseFilter(text)" and not (node.token contains "parseShellStringToEJSON") and not (node.token contains "ParseMode.Strict")
  emit issue code.CompassCsfleEjsonParserMissingStrictMode at node
}
