module bindings.javascript.migration.mfb63017a;

binding compassCsfleEjsonParserMissingStrictMode267 {
  query pattern presenceNode where node.scope == "function" and node.context.name contains "textToEncryptedFieldConfig" and node.token contains "parseFilter(text)" and not (node.token contains "parseShellStringToEJSON") and not (node.token contains "ParseMode.Strict")
  emit issue code.CompassCsfleEjsonParserMissingStrictMode at node
}
