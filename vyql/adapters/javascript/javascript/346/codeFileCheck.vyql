module bindings.javascript.migration.mfb63017a;

binding fileCheck346 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "call_path:fs.lstat" and node.token contains "call_path:fs.chown" and not (node.token contains "call_path:fs.lchown")
  emit issue code.FileCheck at node
}
