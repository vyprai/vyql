module bindings.javascript.migration.mfb63017a;

binding electronNodeIntegration207 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "BrowserWindow" and node.token contains "nodeIntegration=true"
  emit issue code.ElectronNodeIntegration at node
}
