module bindings.javascript.migration.mfb63017a;

binding unfilteredAgentCommandTag269 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "name=parseCustomTags" and node.token contains "identifier:customTagNames" and node.token contains "literal:dyad-execute-sql" and node.token contains "literal:dyad-codebase-context" and node.token contains "identifier:tagPattern" and not (node.token contains "literal:dyad-command")
  emit issue code.UnfilteredAgentCommandTag at node
}
