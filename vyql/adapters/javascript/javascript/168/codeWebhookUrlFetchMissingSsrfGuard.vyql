module bindings.javascript.migration.mfb63017a;

binding webhookUrlFetchMissingSsrfGuard168 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=javascript" and containsAny(node.token, ["name=callWebhook", "name=sendWebhook", "name=deliverWebhook", "name=postWebhook"]) and containsAny(node.token, ["call_path:axios.post", "call_path:axios.request", "call_path:request.post", "call_path:undici.fetch", "call_path:fetch"]) and containsAny(node.token, ["selector:webhookUrl.url", "selector:webhook.url", "selector:callbackUrl", "selector:callback.url"]) and not (node.token contains "identifier:secureDispatcher") and not (containsAny(node.scopeCall.any, [safeFetch, fetchWithSsrfGuard, validateUrl, assertPublicUrl]))
  emit issue code.WebhookUrlFetchMissingSsrfGuard at node
}
