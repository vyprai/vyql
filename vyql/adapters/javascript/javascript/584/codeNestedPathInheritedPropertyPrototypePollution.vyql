module bindings.javascript.migration.mfb63017a;

binding nestedPathInheritedPropertyPrototypePollution584 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "currentKeyPath" and node.token contains "remainingKeyPath" and node.token contains "obj[currentKeyPath]" and node.token contains "obj[currentKeyPath]={}" and node.token contains "setByKeyPath(innerObj,remainingKeyPath,value)" and not (node.token contains "hasOwn(obj,currentKeyPath)") and not (node.token contains "Object.prototype.hasOwnProperty.call(obj,currentKeyPath)")
  emit issue code.NestedPathInheritedPropertyPrototypePollution at node
}
