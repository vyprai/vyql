module bindings.javascript.migration.mfb63017a;

binding dropdownEntryLabelInnerHtmlXss108 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "name=addDropDownEntry" and node.token contains "selector:li.innerHTML" and node.token contains "literal:dropdown-item" and node.token contains "identifier:label" and not (node.token contains "call_arg:sanitizeHTML:label") and not (node.token contains "selector:textContent")
  emit issue code.DropdownEntryLabelInnerHtmlXss at node
}
