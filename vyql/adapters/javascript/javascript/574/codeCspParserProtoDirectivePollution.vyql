module bindings.javascript.migration.mfb63017a;

binding cspParserProtoDirectivePollution574 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "module" and node.token contains "lang=javascript" and node.token contains "constresult:PolicyResult={}" and node.token contains "directiveKey===\"__proto__\"" and node.token contains "Object.defineProperty(result,directiveKey" and node.token contains "value:directiveValue" and not (node.token contains "newMap()") and not (node.token contains "result.set(directiveKey,directiveValue)")
  emit issue code.CspParserProtoDirectivePollution at node
}
