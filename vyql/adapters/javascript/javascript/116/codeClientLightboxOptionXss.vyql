module bindings.javascript.migration.mfb63017a;

binding clientLightboxOptionXss116 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "javascript" and node.token contains "setSlideshowContent" and node.token contains "options.slides.forEach" and node.token contains "data-elementor-slideshow-video" and node.token contains "slide.image" and not (node.token contains "isValidUrl") and not (node.token contains "startsWith(protocol)")
  emit issue code.ClientLightboxOptionXss at node
}
