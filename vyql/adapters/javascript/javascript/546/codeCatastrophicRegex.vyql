module bindings.javascript.migration.mfb63017a;

binding catastrophicRegex546 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.method == "match" and node.token contains "^(\\s|\\/\\*.*?\\*\\/)*[\\[\\(\\w]" and not (node.token contains "([^*]|\\*(?!\\/))*?")
  emit issue code.CatastrophicRegex at node
  emit sink code.CatastrophicRegexSink at node
}
