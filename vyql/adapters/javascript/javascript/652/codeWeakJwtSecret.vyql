module bindings.javascript.migration.mfb63017a;

binding weakJwtSecret652 {
  query pattern presenceNode where node.kind == "call" and node.path ~= "jwt.sign" and containsAny(node.context.callArg, ["secret", "jwt-secret", "change-me", "changeme", "password"])
  emit issue code.WeakJwtSecret at node
}
