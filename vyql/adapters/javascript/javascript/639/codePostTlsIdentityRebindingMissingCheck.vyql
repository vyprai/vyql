module bindings.javascript.migration.mfb63017a;

binding postTlsIdentityRebindingMissingCheck639 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "call_path:?.sendPacket" and node.token contains "call_path:?.readPacket" and node.token contains "selector:?.identity.body.protocolVersion" and node.token contains "selector:?.identity.body.deviceId" and node.token contains "assign:?.identity=awaitthis.readPacket()" and not (containsAny(node.token, ["binary:this.identity.body.protocolVersion!==identity.body.protocolVersion", "binary:this.identity.body.deviceId!==identity.body.deviceId", "binary:this.identity.body.protocolVersion!=identity.body.protocolVersion", "binary:this.identity.body.deviceId!=identity.body.deviceId"]))
  emit issue code.PostTlsIdentityRebindingMissingCheck at node
}
