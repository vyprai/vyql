module bindings.javascript.migration.mfb63017a;

binding jwtUnverified650 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "jwt.verify" and node.token contains "algorithms=none"
  emit issue code.JwtUnverified at node
}
