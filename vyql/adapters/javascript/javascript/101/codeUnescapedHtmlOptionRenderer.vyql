module bindings.javascript.migration.mfb63017a;

binding unescapedHtmlOptionRenderer101 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=javascript" and node.token contains "call_path:getAttribute" and node.token contains "literal:data-content" and node.token contains "selector:optionContent" and node.token contains "selector:innerHTML" and node.token contains "call_path:appendChild" and not (node.token contains "call_path:sanitizeHtml") and not (node.token contains "call_path:DOMPurify") and not (node.token contains "literal:sanitize-html")
  emit issue code.UnescapedHtmlOptionRenderer at node
}
