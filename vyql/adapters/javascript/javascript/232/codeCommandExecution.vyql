module bindings.javascript.migration.mfb63017a;

binding commandExecution232 {
  query pattern callExpr where callee.path ~= "execFile" and args.any.literal contains "powershell"
  emit sink code.CommandExecution at args[1].collection
}
