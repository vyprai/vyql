module bindings.javascript.migration.mfb63017a;

binding clientLightboxOptionXss115 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "module" and node.token contains "lang=javascript" and node.token contains "setVideoContent" and node.token contains "options.videoParams" and node.token contains "$('<video>',videoParams)" and not (node.token contains "startsWith('on')") and not (node.token contains "startsWith(\"on\")") and not (node.token contains "deletevideoParams[key]")
  emit issue code.ClientLightboxOptionXss at node
}
