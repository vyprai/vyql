module bindings.javascript.migration.mfb63017a;

binding jobOutputEventUpdateAuthorizationBypass118 {
  query pattern presenceNode where node.scope == "function" and node.token contains "literal:Got job update from child" and node.token contains "literal:update_event" and node.token contains "assign:job[]=data[key]" and node.token contains "subscript:data[key]" and not (node.token contains "literal:allow_event_updates_from_jobs") and not (node.token contains "selector:data.update_event")
  emit issue code.JobOutputEventUpdateAuthorizationBypass at node
}
