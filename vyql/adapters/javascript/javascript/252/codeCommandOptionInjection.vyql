module bindings.javascript.migration.mfb63017a;

binding commandOptionInjection252 {
  query pattern presenceNode where node.scope == "function" and node.token contains "name=clone" and node.token contains "call_path:?.gitExec" and containsAny(node.token, ["literal:clone${repository}", "literal:clone${remote}", "literal:clone${url}"]) and not (containsAny(node.token, ["literal:--${repository}", "literal:--${remote}", "literal:--${url}"]))
  emit issue code.CommandOptionInjection at node
  emit sink code.CommandOptionInjectionSink at node
}
