module bindings.ruby.migration.m5a48bf47;

binding erbHrefInterpolationHtmlEscapeMissing158 {
  query pattern presenceNode where node.kind == "any" and node.analysis ~= "erb.unescaped_href_interpolation" and node.token contains "template=erb" and node.token contains "attr=href"
  emit issue code.ErbHrefInterpolationHtmlEscapeMissing at node
}
