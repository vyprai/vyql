module bindings.ruby.migration.m5a48bf47;

binding erbHrefInterpolationHtmlEscapeMissing158 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "analysis.erb.unescaped_href_interpolation" and node.token contains "template=erb" and node.token contains "attr=href"
  emit issue code.ErbHrefInterpolationHtmlEscapeMissing at node
}
