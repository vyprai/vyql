module bindings.ruby.migration.m5a48bf47;

binding postEditReportPrivateContentLeak178 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=ruby" and node.token contains "name=report_post_edits" and node.token contains "FROM post_revisions pr" and node.token contains "JOIN posts p" and node.token contains "report.data << revision" and not (node.token contains "Archetype.private_message") and not (node.token contains "secure_category")
  emit issue code.PostEditReportPrivateContentLeak at node
}
