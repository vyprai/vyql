module bindings.ruby.generated.m5a48bf47;

binding jwtUnverified227 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "JWT.encode" and node.token contains "algorithm=none"
  emit issue code.JwtUnverified at node
}
