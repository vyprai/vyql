module bindings.ruby.migration.m5a48bf47;

binding authenticationRequiredOp63 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "grant_role"
  emit issue code.AuthenticationRequiredOp at node
}
