module bindings.ruby.migration.m5a48bf47;

binding windowsBacktickDiffCommandInjection177 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=ruby" and node.token contains "WINDOWS" and node.token contains "sprintf" and node.token contains "diff_options.join(' ')" and node.token contains "@paths.map" and node.token contains "cmd" and not (node.token contains "Open3.capture3") and not (node.token contains "Shellwords") and not (node.token contains "shellescape")
  emit issue code.WindowsBacktickDiffCommandInjection at node
}
