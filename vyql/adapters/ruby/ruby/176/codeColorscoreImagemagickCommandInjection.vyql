module bindings.ruby.migration.m5a48bf47;

binding colorscoreImagemagickCommandInjection176 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=ruby" and node.token contains "name=initialize" and node.token contains "`convert #{image_path}" and node.token contains "-colors #{colors}" and node.token contains "-depth #{depth}" and not (node.token contains "shellescape") and not (node.token contains "colors.to_i") and not (node.token contains "depth.to_i")
  emit issue code.ColorscoreImagemagickCommandInjection at node
}
