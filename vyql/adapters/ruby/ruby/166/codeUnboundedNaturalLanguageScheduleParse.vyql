module bindings.ruby.migration.m5a48bf47;

binding unboundedNaturalLanguageScheduleParse166 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=ruby" and node.token == "function_name:parse" and node.token contains "call_path:s.strip" and node.token contains "call_path:Parser.parse" and node.token contains "call_path:opts.merge" and node.token contains "call_path:slots.to_crons" and not (node.token contains "call_path:s.length")
  emit issue code.UnboundedNaturalLanguageScheduleParse at node
}
