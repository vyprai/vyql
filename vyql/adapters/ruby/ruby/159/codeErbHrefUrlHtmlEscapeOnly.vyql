module bindings.ruby.migration.m5a48bf47;

binding erbHrefUrlHtmlEscapeOnly159 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "analysis.erb.html_escaped_url_href" and node.token contains "template=erb" and node.token contains "attr=href" and node.token contains "value=url-like"
  emit issue code.ErbHrefUrlHtmlEscapeOnly at node
}
