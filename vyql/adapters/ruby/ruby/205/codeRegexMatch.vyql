module bindings.ruby.migration.m5a48bf47;

binding regexMatch205 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "__regex.match"
  emit issue code.RegexMatch at node
  emit sink code.RegexMatchSink at node
}
