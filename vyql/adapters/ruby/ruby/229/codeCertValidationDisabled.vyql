module bindings.ruby.migration.m5a48bf47;

binding certValidationDisabled229 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "verify_mode" and node.token contains "VERIFY_NONE"
  emit issue code.CertValidationDisabled at node
}
