module bindings.ruby.migration.m5a48bf47;

binding clockworkWebJobCsrf174 {
  query pattern presenceNode where node.scope == "class" and node.token contains "lang=ruby" and node.token contains "name=HomeController" and node.token contains "params[:job]" and node.token contains "params[:enable]" and node.token contains "ClockworkWeb.enable(job)" and node.token contains "ClockworkWeb.disable(job)" and not (node.token contains "protect_from_forgery")
  emit issue code.ClockworkWebJobCsrf at node
}
