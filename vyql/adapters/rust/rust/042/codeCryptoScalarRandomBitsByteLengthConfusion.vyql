module bindings.rust.migration.mdc4f74cd;

binding cryptoScalarRandomBitsByteLengthConfusion42 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=rust" and node.token contains ".bits().div_ceil(8)" and node.token contains "try_random_bits" and node.token contains "Scalar::from_uint" and node.token contains "ProjectivePoint::mul_by_generator" and not (node.token contains "try_generate_from_rng") and not (node.token contains "NonZeroScalar")
  emit issue code.CryptoScalarRandomBitsByteLengthConfusion at node
}
