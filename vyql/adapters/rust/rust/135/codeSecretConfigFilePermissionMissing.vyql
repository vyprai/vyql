module bindings.rust.migration.mdc4f74cd;

binding secretConfigFilePermissionMissing135 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "rust" and node.token contains "config_dest.push(\"ignition\");" and node.token contains "config_dest.push(\"config.ign\");" and node.token contains "create_dir_all(&config_dest)" and node.token contains "OpenOptions::new()" and node.token contains ".create_new(true)" and node.token contains "copy(&mutconfig_in,&mutconfig_out)" and not (node.token contains "set_permissions") and not (node.token contains "Permissions::from_mode") and not (node.token contains ".chmod(") and not (node.token contains ".set_mode(")
  emit issue code.SecretConfigFilePermissionMissing at node
}
