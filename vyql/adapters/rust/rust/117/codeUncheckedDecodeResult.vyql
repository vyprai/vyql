module bindings.rust.migration.mdc4f74cd;

binding uncheckedDecodeResult117 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path == "b62_decode.expect"
  emit issue code.UncheckedDecodeResult at node
}
