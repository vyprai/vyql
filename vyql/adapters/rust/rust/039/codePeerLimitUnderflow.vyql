module bindings.rust.migration.mdc4f74cd;

binding peerLimitUnderflow39 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=rust" and node.token contains "name=poll" and node.token contains "DiscoveryMessage::Handshake" and node.token contains "self.peer_list_limit = Some(limit);" and node.token contains "self.peer_list_limit.unwrap() as usize - 1" and node.token contains "self.get_peer_contacts(" and not (node.token contains ".saturating_sub(1)") and not (node.token contains ".min(self.config.update_limit)")
  emit issue code.PeerLimitUnderflow at node
}
