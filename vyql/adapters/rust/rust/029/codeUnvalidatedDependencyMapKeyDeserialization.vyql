module bindings.rust.migration.mdc4f74cd;

binding unvalidatedDependencyMapKeyDeserialization29 {
  query pattern presenceNode where node.kind == "any" and node.path == "analysis.rust.serde_field" and node.token contains "derive:Deserialize" and node.token contains "semantic:dependency_map" and containsAny(node.context.field, ["depend", "package", "plugin"]) and containsAny(node.token, ["field_type:Map", "field_type:Dependencies", "field_type:Packages", "field_type:Plugins"]) and not (node.token contains "serde_attr:deserialize_with")
  emit issue code.UnvalidatedDependencyMapKeyDeserialization at node
}
