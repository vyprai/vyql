module bindings.rust.migration.mdc4f74cd;

binding callFutureRawWeakStateRef33 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=rust" and node.token contains "name=poll" and node.token contains "letstate_ptr=Weak::into_raw(Arc::downgrade(&self_ref.state));letmutstate=self_ref.state.write().unwrap();" and node.token contains "state.waker.is_none()" and node.token contains "ic0::call_new"
  emit issue code.CallFutureRawWeakStateRef at node
}
