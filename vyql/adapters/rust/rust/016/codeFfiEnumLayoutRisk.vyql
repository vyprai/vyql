module bindings.rust.migration.mdc4f74cd;

binding ffiEnumLayoutRisk16 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path == "analysis.rust.enum" and node.token contains "kind:enum" and node.token contains "repr:C"
  emit issue code.FfiEnumLayoutRisk at node
}
