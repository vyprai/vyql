module bindings.rust.migration.mdc4f74cd;

binding ffiEnumLayoutRisk16 {
  query pattern presenceNode where node.kind == "any" and node.path == "analysis.rust.enum" and node.token contains "kind:enum" and node.token contains "repr:C"
  emit issue code.FfiEnumLayoutRisk at node
}
