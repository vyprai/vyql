module bindings.rust.migration.mdc4f74cd;

binding windowsReservedDeviceNameBypass34 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=rust" and node.token contains "path.file_stem()" and node.token contains "stemstr.to_uppercase().as_str()" and node.token contains "\"COM0\"" and node.token contains "\"COM9\"" and node.token contains "manually::open" and not (node.token contains "\"COM¹\"")
  emit issue code.WindowsReservedDeviceNameBypass at node
}
