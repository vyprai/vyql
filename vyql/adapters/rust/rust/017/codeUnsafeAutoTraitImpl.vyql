module bindings.rust.migration.mdc4f74cd;

binding unsafeAutoTraitImpl17 {
  query pattern presenceNode where node.kind == "any" and node.analysis == "rust.unsafe_impl" and node.token contains "trait:Send" and not (node.token contains "bound:Send")
  emit issue code.UnsafeAutoTraitImpl at node
}
