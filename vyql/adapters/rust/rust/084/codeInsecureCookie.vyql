module bindings.rust.migration.mdc4f74cd;

binding insecureCookie84 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "set_http_only" and node.token contains "false"
  emit issue code.InsecureCookie at node
}
