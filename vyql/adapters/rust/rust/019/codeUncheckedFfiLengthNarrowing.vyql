module bindings.rust.migration.mdc4f74cd;

binding uncheckedFfiLengthNarrowing19 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=rust" and node.token contains "avail_in=input.len()asc_uint" and node.token contains "avail_out=output.len()asc_uint" and node.token contains "BZ2_bz" and not (node.token contains ".min(c_uint::MAXasusize)asc_uint")
  emit issue code.UncheckedFfiLengthNarrowing at node
}
