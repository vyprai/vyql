module bindings.rust.migration.mdc4f74cd;

binding unboundedResourceOperation25 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=rust" and node.token contains ".next().await" and not (node.token contains "timeout")
  emit issue code.UnboundedResourceOperation at node
}
