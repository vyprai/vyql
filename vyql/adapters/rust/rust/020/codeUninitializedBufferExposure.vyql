module bindings.rust.migration.mdc4f74cd;

binding uninitializedBufferExposure20 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=rust" and node.token contains "with_capacity" and node.token contains "as_mut_ptr" and node.token contains "from_raw_parts_mut" and node.token contains "read_exact" and node.token contains "set_len"
  emit issue code.UninitializedBufferExposure at node
}
