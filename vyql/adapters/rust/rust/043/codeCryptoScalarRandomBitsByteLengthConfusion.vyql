module bindings.rust.migration.mdc4f74cd;

binding cryptoScalarRandomBitsByteLengthConfusion43 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=rust" and node.token contains "name=next_k" and node.token contains "try_random_bits(rng,bit_length)" and node.token contains "k<*" and node.token contains "::ORDER" and node.token contains "is_zero" and not (node.token contains "try_generate_from_rng") and not (node.token contains "NonZeroScalar")
  emit issue code.CryptoScalarRandomBitsByteLengthConfusion at node
}
