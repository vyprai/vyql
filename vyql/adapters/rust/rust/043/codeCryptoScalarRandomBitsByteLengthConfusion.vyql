module bindings.rust.migration.mdc4f74cd;

binding cryptoScalarRandomBitsByteLengthConfusion43 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=rust" and node.token contains "name=next_k" and node.token contains "try_random_bits(rng,bit_length)" and node.token contains "k<*" and node.token contains "::ORDER" and node.token contains "is_zero" and not (node.token contains "try_generate_from_rng") and not (node.token contains "NonZeroScalar")
  emit issue code.CryptoScalarRandomBitsByteLengthConfusion at node
}
