module bindings.dart.migration.m1c398214;

binding authenticationRequiredOp53 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "resetPassword"
  emit issue code.AuthenticationRequiredOp at node
}
