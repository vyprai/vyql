module bindings.dart.migration.m1c398214;

binding weakCipher74 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "BlockCipher" and node.token contains "DES"
  emit issue code.WeakCipher at node
}
