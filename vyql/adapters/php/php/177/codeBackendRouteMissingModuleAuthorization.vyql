module bindings.php.migration.mfbe84119;

binding backendRouteMissingModuleAuthorization177 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "php" and node.token contains "DashboardAjaxController" and node.token contains "/dashboard/" and node.token contains "methods" and not (node.token contains "inheritAccessFromModule")
  emit issue code.BackendRouteMissingModuleAuthorization at node
}
