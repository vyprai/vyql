module bindings.php.migration.mfbe84119;

binding svgNestedResourceValidationBypass188 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=resolve_url" and node.token contains "Helpers::dompdf_getimagesize" and node.token contains "\"svg\"" and node.token contains "file_put_contents($resolved_url,$image)" and not (node.token contains "$type===\"svg\"") and not (node.token contains "xml_set_element_handler") and not (node.token contains "self::resolve_url($url")
  emit issue code.SvgNestedResourceValidationBypass at node
}
