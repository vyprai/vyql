module bindings.php.migration.mfbe84119;

binding cockpitAuthArrayNosqlInjection121 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=check" and node.token contains "$this->param('auth')" and node.token contains "$this->module('cockpit')->authenticate($data)" and not (node.token contains "is_string($data['user'])") and not (node.token contains "is_string($data['password'])")
  emit issue code.CockpitAuthArrayNosqlInjection at node
}
