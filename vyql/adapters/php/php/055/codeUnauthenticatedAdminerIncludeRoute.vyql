module bindings.php.migration.mfbe84119;

binding unauthenticatedAdminerIncludeRoute55 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=index" and node.token contains "annotation:Route" and node.token contains "annotation_arg:adminer" and node.token contains "annotation_arg:auth_required'=>false" and node.token contains "call_path:chdir" and node.token contains "call_path:include" and node.token contains "call_arg:../Adminer/Adminer.php" and not (node.token contains "subscript:$_SESSION['frosh_adminer_authenticated']")
  emit issue code.UnauthenticatedAdminerIncludeRoute at node
}
