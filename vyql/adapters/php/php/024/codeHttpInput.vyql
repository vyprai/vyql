module bindings.php.migration.mfbe84119;

binding httpInput24 {
  query pattern callExpr where callee.path ~= "$_SERVER" and args.any.literal contains "REMOTE_PORT"
  emit source code.HttpInput at call.result
}
