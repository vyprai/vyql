module bindings.php.migration.mfbe84119;

binding persistentModelTitleStoredXss95 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=onSaveElement" and node.token contains "$model->title=$title" and node.token contains "$snapshot['title']=Html::encode($title)" and node.token contains "$model->snapshot=$this->afterSnapshot" and not (node.token contains "$model->title=Html::encode($title)")
  emit issue code.PersistentModelTitleStoredXss at node
}
