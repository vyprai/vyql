module bindings.php.migration.mfbe84119;

binding persistentModelTitleStoredXss95 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.context.name contains "onSaveElement" and node.token contains "$model->title=$title" and node.token contains "$snapshot['title']=Html::encode($title)" and node.token contains "$model->snapshot=$this->afterSnapshot" and not (node.token contains "$model->title=Html::encode($title)")
  emit issue code.PersistentModelTitleStoredXss at node
}
