module bindings.php.migration.mfbe84119;

binding absolutePathDisclosure391 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.token contains "getAbsolutePath" and node.token contains "touch(" and not (node.token contains "@touch(")
  emit issue code.AbsolutePathDisclosure at node
}
