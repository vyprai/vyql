module bindings.php.migration.mfbe84119;

binding shareLinkDeletionMissingAuthGuard58 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=deleteShareLink" and node.token contains "subscript:$_POST" and node.token contains "call_path:FileModel.deleteShareLink" and not (node.token contains "call_path:$this.checkCsrf") and not (node.token contains "call_path:$this.requireAuth")
  emit issue code.ShareLinkDeletionMissingAuthGuard at node
}
