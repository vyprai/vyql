module bindings.php.migration.mfbe84119;

binding shareLinkDeletionMissingAuthGuard58 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=deleteShareLink" and node.token contains "subscript:$_POST" and node.token contains "call_path:FileModel.deleteShareLink" and not (node.token contains "call_path:$this.checkCsrf") and not (node.token contains "call_path:$this.requireAuth")
  emit issue code.ShareLinkDeletionMissingAuthGuard at node
}
