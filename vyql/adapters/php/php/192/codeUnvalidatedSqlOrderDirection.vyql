module bindings.php.migration.mfbe84119;

binding unvalidatedSqlOrderDirection192 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=processOrderBy" and node.token contains "ORDERBY" and node.token contains "$orderBy['direction']" and not (node.token contains "QUERY_ORDER_DESC") and not (node.token contains "QUERY_ORDER_ASC")
  emit issue code.UnvalidatedSqlOrderDirection at node
}
