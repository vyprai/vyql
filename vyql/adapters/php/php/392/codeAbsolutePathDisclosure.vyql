module bindings.php.migration.mfbe84119;

binding absolutePathDisclosure392 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.token contains "getAbsolutePath" and node.token contains "unlink(" and not (node.token contains "@unlink(")
  emit issue code.AbsolutePathDisclosure at node
}
