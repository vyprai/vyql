module bindings.php.migration.mfbe84119;

binding civiCrmScientificNotationNumericValidationBypass201 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=numeric" and node.token contains "returnpreg_match" and not (node.token contains "!is_numeric($value)")
  emit issue code.CiviCrmScientificNotationNumericValidationBypass at node
}
