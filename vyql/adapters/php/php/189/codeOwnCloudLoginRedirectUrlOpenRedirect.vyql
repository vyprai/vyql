module bindings.php.migration.mfbe84119;

binding ownCloudLoginRedirectUrlOpenRedirect189 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.context.name contains "redirectToDefaultPage" and node.token contains "isset($_REQUEST['redirect_url'])" and node.token contains "header('Location:/'.htmlentities($_REQUEST['redirect_url']))" and not (node.token contains "substr($_REQUEST['redirect_url'],0,strlen(OC::$WEBROOT))==OC::$WEBROOT")
  emit issue code.OwnCloudLoginRedirectUrlOpenRedirect at node
}
