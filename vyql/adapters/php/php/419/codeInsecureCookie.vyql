module bindings.php.migration.mfbe84119;

binding insecureCookie419 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "setrawcookie" and node.token contains "httponly=false"
  emit issue code.InsecureCookie at node
}
