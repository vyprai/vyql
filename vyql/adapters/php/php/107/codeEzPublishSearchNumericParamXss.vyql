module bindings.php.migration.mfbe84119;

binding ezPublishSearchNumericParamXss107 {
  query pattern presenceNode where node.scope == "module" and node.token contains "lang=php" and node.token contains "call_path:eZSearch.search" and node.token contains "call_path:$tpl.setVariable" and node.token contains "assign_call:$searchTimestamp:$http.variable" and node.token contains "assign_literal:$searchTimestamp:SearchTimestamp" and node.token contains "call_arg:eZSearch.search:array" and node.token contains "call_arg:$tpl.setVariable:$searchTimestamp" and not (node.token contains "cast_call_literal:int:$http.variable:SearchTimestamp")
  emit issue code.EzPublishSearchNumericParamXss at node
}
