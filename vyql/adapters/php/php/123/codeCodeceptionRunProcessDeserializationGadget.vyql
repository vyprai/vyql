module bindings.php.generated.mfbe84119;

binding codeceptionRunProcessDeserializationGadget123 {
  query pattern presenceNode where node.scope == "class" and node.context.language == "php" and node.context.name contains "RunProcess" and node.token contains "Process::fromShellCommandline($command" and node.token contains "function__destruct()" and node.token contains "$this->stopProcess()" and not (node.token contains "function__sleep()") and not (node.token contains "function__wakeup()")
  emit issue code.CodeceptionRunProcessDeserializationGadget at node
}
