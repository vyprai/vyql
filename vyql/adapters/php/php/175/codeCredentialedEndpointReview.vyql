module bindings.php.migration.mfbe84119;

binding credentialedEndpointReview175 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.token contains "getRequiredBodyParam" and node.token contains "loadBucketList" and node.token contains "asJson" and not (node.token contains "requireAdmin")
  emit issue code.CredentialedEndpointReview at node
}
