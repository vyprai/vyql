module bindings.php.migration.mfbe84119;

binding civiCrmScientificNotationNumericValidationBypass200 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.context.name contains "positiveInteger" and node.token contains "is_int($value)" and node.token contains "is_numeric($value)&&preg_match" and not (node.token contains "!is_numeric($value)")
  emit issue code.CiviCrmScientificNotationNumericValidationBypass at node
}
