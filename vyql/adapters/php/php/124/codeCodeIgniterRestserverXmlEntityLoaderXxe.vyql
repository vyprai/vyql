module bindings.php.migration.mfbe84119;

binding codeIgniterRestserverXmlEntityLoaderXxe124 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=__construct" and node.token contains "_detect_input_format" and node.token contains "$this->format->factory" and node.token contains "->to_array" and not (node.token contains "libxml_disable_entity_loader")
  emit issue code.CodeIgniterRestserverXmlEntityLoaderXxe at node
}
