module bindings.php.migration.mfbe84119;

binding absolutePathDisclosure389 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "getAbsolutePath" and node.token contains "rename(" and not (node.token contains "@rename(")
  emit issue code.AbsolutePathDisclosure at node
}
