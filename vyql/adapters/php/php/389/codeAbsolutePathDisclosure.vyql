module bindings.php.migration.mfbe84119;

binding absolutePathDisclosure389 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=php" and node.token contains "getAbsolutePath" and node.token contains "rename(" and not (node.token contains "@rename(")
  emit issue code.AbsolutePathDisclosure at node
}
