module bindings.php.migration.mfbe84119;

binding ezPublishSearchNumericParamXss104 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "php" and node.context.callPath contains "eZSearch.search" and node.context.callPath contains "$tpl.setVariable" and node.token contains "assign_call:$pageLimit:$http.variable" and node.token contains "assign_literal:$pageLimit:BrowsePageLimit" and node.context.callArg contains "eZSearch.search:array" and node.context.callArg contains "$tpl.setVariable:$pageLimit" and not (node.token contains "cast_call_literal:int:$http.variable:BrowsePageLimit")
  emit issue code.EzPublishSearchNumericParamXss at node
}
