module bindings.php.migration.mfbe84119;

binding contaoAjaxToggleSubpaletteSqlIdentifierInjection172 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "toggleSubpalette" and node.token contains "$this->Database->prepare(\"UPDATE\"" and node.token contains "\"SET\".$this->Input->post('field')" and node.token contains "WHEREid=?" and not (node.token contains "__selector__") and not (node.token contains "hasAccess($dc->table.'::'.$this->Input->post('field'),'alexf')")
  emit issue code.ContaoAjaxToggleSubpaletteSqlIdentifierInjection at node
}
