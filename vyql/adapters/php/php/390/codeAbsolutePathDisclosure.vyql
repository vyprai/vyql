module bindings.php.migration.mfbe84119;

binding absolutePathDisclosure390 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.token contains "getAbsolutePath" and node.token contains "copy(" and not (node.token contains "@copy(")
  emit issue code.AbsolutePathDisclosure at node
}
