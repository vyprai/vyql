module bindings.php.migration.mfbe84119;

binding missingHttpMethodAssertion365 {
  query pattern presenceNode where node.scope == "class" and node.token contains "Action" and node.token contains "deleteBy" and node.token contains "redirect" and not (node.token contains "assertAllowedHttpMethod")
  emit issue code.MissingHttpMethodAssertion at node
}
