module bindings.php.generated.mfbe84119;

binding commandOptionInjection132 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.token contains "escapeshellarg($keyId)" and node.token contains "$operation='--utf8-strings--list-secret-keys'.escapeshellarg($keyId)" and not (node.token contains "$operation='--utf8-strings--list-secret-keys--'.escapeshellarg($keyId)")
  emit issue code.CommandOptionInjection at node
  emit sink code.CommandOptionInjectionSink at node
}
