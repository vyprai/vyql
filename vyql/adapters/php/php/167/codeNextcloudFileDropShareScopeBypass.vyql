module bindings.php.migration.mfbe84119;

binding nextcloudFileDropShareScopeBypass167 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.context.name contains "addMetadataFileDrop" and node.token contains "$this->getOwnerId($shareToken)" and node.token contains "$this->rootFolder->getUserFolder($ownerId)" and node.token contains "$userFolder->getById($id)" and node.token contains "$this->metaDataStorage->updateMetaDataIntoIntermediateFile($ownerId,$id" and not (node.token contains "getFileDropOwnerId") and not (node.token contains "getFirstNodeById") and not (node.token contains "$share->getNode()")
  emit issue code.NextcloudFileDropShareScopeBypass at node
}
