module bindings.php.migration.mfbe84119;

binding unparameterizedSqlQueryParser191 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "toLegacyOrderBy" and node.token contains "toLegacyOrderWay" and node.token contains "orderBy" and not (node.token contains "isOrderBy") and not (node.token contains "isOrderWay")
  emit issue code.UnparameterizedSqlQueryParser at node
}
