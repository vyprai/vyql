module bindings.php.migration.mfbe84119;

binding tempPathJoinBeforeSeparatorCheck81 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.context.name contains "zipdl" and node.token contains "assign_call_method:getTempPath" and node.token contains "assign_literal:DIRECTORY_SEPARATOR" and node.token contains "global_subscript_write=true" and node.token contains "$GLOBALS['elFinderTempFiles'][$path]=true;" and not (node.token contains "strpos($file,DIRECTORY_SEPARATOR)")
  emit issue code.TempPathJoinBeforeSeparatorCheck at node
}
