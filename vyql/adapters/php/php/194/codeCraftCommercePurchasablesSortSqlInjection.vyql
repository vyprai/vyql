module bindings.php.migration.mfbe84119;

binding craftCommercePurchasablesSortSqlInjection194 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=actionPurchasablesTable" and node.token contains "$sort=$this->request->getParam('sort')" and node.token contains "[$column,$direction]=explode('|',$sort)" and node.token contains "$sqlQuery->orderBy([$column=>$direction=='asc'?SORT_ASC:SORT_DESC])" and not (node.token contains "$column=null")
  emit issue code.CraftCommercePurchasablesSortSqlInjection at node
}
