module bindings.php.generated.mfbe84119;

binding craftCommercePurchasablesSortSqlInjection194 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.context.name contains "actionPurchasablesTable" and node.token contains "$sort=$this->request->getParam('sort')" and node.token contains "[$column,$direction]=explode('|',$sort)" and node.token contains "$sqlQuery->orderBy([$column=>$direction=='asc'?SORT_ASC:SORT_DESC])" and not (node.token contains "$column=null")
  emit issue code.CraftCommercePurchasablesSortSqlInjection at node
}
