module bindings.php.migration.mfbe84119;

binding commonMarkPreserveEntitiesUrlAttributeXss115 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=render" and node.token contains "RegexHelper::isLinkPotentiallyUnsafe($inline->getUrl())" and node.token contains "Xml::escape($inline->getUrl(),true)" and not (node.token contains "Xml::escape($inline->getUrl())")
  emit issue code.CommonMarkPreserveEntitiesUrlAttributeXss at node
}
