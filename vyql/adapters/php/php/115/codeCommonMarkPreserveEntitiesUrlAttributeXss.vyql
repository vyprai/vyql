module bindings.php.migration.mfbe84119;

binding commonMarkPreserveEntitiesUrlAttributeXss115 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=render" and node.token contains "RegexHelper::isLinkPotentiallyUnsafe($inline->getUrl())" and node.token contains "Xml::escape($inline->getUrl(),true)" and not (node.token contains "Xml::escape($inline->getUrl())")
  emit issue code.CommonMarkPreserveEntitiesUrlAttributeXss at node
}
