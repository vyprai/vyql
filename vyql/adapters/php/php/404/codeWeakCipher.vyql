module bindings.php.migration.mfbe84119;

binding weakCipher404 {
  query pattern callExpr where callee.path ~= "openssl_encrypt" and args.any.literal contains "des"
  emit issue code.WeakCipher at call
}
