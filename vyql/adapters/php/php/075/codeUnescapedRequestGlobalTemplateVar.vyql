module bindings.php.generated.mfbe84119;

binding unescapedRequestGlobalTemplateVar75 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "php" and node.token contains "$GLOBALS[" and node.token contains "$_GET[" and node.token contains "$this->_tplVars=$this->_globalVar" and not (node.token contains "htmlspecialchars($_GET") and not (node.token contains "htmlentities($_GET") and not (node.token contains "esc_html($_GET") and not (node.token contains "strip_tags($_GET")
  emit issue code.UnescapedRequestGlobalTemplateVar at node
}
