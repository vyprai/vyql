module bindings.php.migration.mfbe84119;

binding unrestrictedVariableVariableAssignment440 {
  query pattern presenceNode where node.scope == "module" and node.token contains "lang=php" and node.token contains "foreach" and node.token contains "$_GET" and node.token contains "$$" and not (node.token contains "preg_match") and not (node.token contains "in_array") and not (node.token contains "array_key_exists")
  emit issue code.UnrestrictedVariableVariableAssignment at node
}
