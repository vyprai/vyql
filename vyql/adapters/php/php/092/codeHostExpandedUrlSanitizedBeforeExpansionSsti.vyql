module bindings.php.migration.mfbe84119;

binding hostExpandedUrlSanitizedBeforeExpansionSsti92 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=safeCanonicalUrl" and node.token contains "$url=Craft::$app->getRequest()->getPathInfo()" and node.token contains "$url=DynamicMetaHelper::sanitizeUrl($url)" and node.token contains "returnUrlHelper::absoluteUrlWithProtocol($url)" and not (node.token contains "returnDynamicMetaHelper::sanitizeUrl(UrlHelper::absoluteUrlWithProtocol($url))")
  emit issue code.HostExpandedUrlSanitizedBeforeExpansionSsti at node
}
