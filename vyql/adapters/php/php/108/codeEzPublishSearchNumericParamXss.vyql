module bindings.php.migration.mfbe84119;

binding ezPublishSearchNumericParamXss108 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "module" and node.token contains "lang=php" and node.token contains "call_path:eZSearch.search" and node.token contains "call_path:$tpl.setVariable" and node.token contains "assign_call:$subTreeList:$http.variable" and node.token contains "assign_literal:$subTreeList:SubTreeArray" and node.token contains "call_arg:eZSearch.search:array" and node.token contains "call_arg:$tpl.setVariable:$subTreeArray" and node.token contains "subscript:$subTreeArray[]" and not (node.token contains "call_arg:is_numeric:$subTreeItem")
  emit issue code.EzPublishSearchNumericParamXss at node
}
