module bindings.php.migration.mfbe84119;

binding prestaShopContactformMessageEmailXss109 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=sendMessage" and node.token contains "Tools::nl2br" and node.token contains "Tools::stripslashes" and node.token contains "Mail::Send" and not (node.token contains "htmlentitiesUTF8")
  emit issue code.PrestaShopContactformMessageEmailXss at node
}
