module bindings.php.migration.mfbe84119;

binding craftAnonymousTemplateRenderActionXss90 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "class" and node.token contains "lang=php" and node.token contains "name=TemplatesController" and node.token contains "public$allowAnonymous=true" and node.token contains "functionactionRender($template,array$variables=array())" and node.token contains "doesTemplateExist($template)" and node.token contains "$this->renderTemplate($template,$variables)" and not (node.token contains "getActionSegments") and not (node.token contains "HttpException(403)")
  emit issue code.CraftAnonymousTemplateRenderActionXss at node
}
