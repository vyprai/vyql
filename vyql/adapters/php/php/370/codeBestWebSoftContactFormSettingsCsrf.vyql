module bindings.php.migration.mfbe84119;

binding bestWebSoftContactFormSettingsCsrf370 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.context.name contains "cntctfrm_settings_page" and node.token contains "cntctfrm_form_submit" and node.token contains "$cntctfrm_options_submit" and node.token contains "update_option" and not (node.token contains "check_admin_referer") and not (node.token contains "wp_nonce_field")
  emit issue code.BestWebSoftContactFormSettingsCsrf at node
}
