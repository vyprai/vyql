module bindings.php.migration.mfbe84119;

binding permissiveCors401 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "header" and node.token contains "Access-Control-Allow-Origin: *"
  emit issue code.PermissiveCors at node
}
