module bindings.php.migration.mfbe84119;

binding incompleteArchiveFilenameValidation181 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.token contains "_maliciousFilename" and node.token contains "strpos(" and node.token contains "/../" and node.token contains "../" and not (node.token contains "phar://")
  emit issue code.IncompleteArchiveFilenameValidation at node
}
