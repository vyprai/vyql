module bindings.php.migration.mfbe84119;

binding installerHiddenFieldXss143 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=show_next_step" and node.token contains "$uchidden" and node.token contains "<input type=" and node.token contains "hidden" and node.token contains "uchidden" and not (node.token contains "dhtmlspecialchars($uchidden)") and not (node.token contains "htmlspecialchars($uchidden") and not (node.token contains "htmlentities($uchidden")
  emit issue code.InstallerHiddenFieldXss at node
}
