module bindings.php.migration.mfbe84119;

binding passwordRecoveryLinkHostControl305 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=toMail" and node.token contains "call_path:MailMessage" and node.token contains "call_path:?.line.action" and node.token contains "call_path:route" and node.token contains "literal:password.reset" and node.token contains "selector:$this.token" and not (node.token contains "call_path:url") and not (node.token contains "call_path:config") and not (node.token contains "literal:app.url")
  emit issue code.PasswordRecoveryLinkHostControl at node
}
