module bindings.php.migration.mfbe84119;

binding passwordRecoveryLinkHostControl305 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.context.name contains "toMail" and node.context.callPath contains "MailMessage" and node.context.callPath contains "?.line.action" and node.context.callPath contains "route" and node.context.literal contains "password.reset" and node.context.selector contains "$this.token" and not (node.context.callPath contains "url") and not (node.context.callPath contains "config") and not (node.context.literal contains "app.url")
  emit issue code.PasswordRecoveryLinkHostControl at node
}
