module bindings.php.migration.mfbe84119;

binding escapedSqlValueFunctionCallAllowed147 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=ConditionExpr" and node.token contains "call_path:is_array" and node.token contains "call_path:array_keys" and node.token contains "index:$Value.__subscript" and node.token contains "call_path:stripos" and node.token contains "call_path:sprintf" and not (node.token contains "call_path:Gdn_UserException")
  emit issue code.EscapedSqlValueFunctionCallAllowed at node
}
