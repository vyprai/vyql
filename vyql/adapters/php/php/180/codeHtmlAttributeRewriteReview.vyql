module bindings.php.generated.mfbe84119;

binding htmlAttributeRewriteReview180 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.token contains "autoptimize_filter_imgopt_lazyload_placeholder" and node.token contains "preg_replace" and node.token contains "$placeholder" and node.token contains "data-src" and node.token contains "data-srcset"
  emit issue code.HtmlAttributeRewriteReview at node
}
