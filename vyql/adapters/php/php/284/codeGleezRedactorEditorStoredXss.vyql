module bindings.php.migration.mfbe84119;

binding gleezRedactorEditorStoredXss284 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path == "self.codes" and node.token contains ".redactor({" and node.token contains "autoresize: false"
  emit issue code.GleezRedactorEditorStoredXss at node
}
