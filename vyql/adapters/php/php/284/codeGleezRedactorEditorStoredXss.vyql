module bindings.php.migration.mfbe84119;

binding gleezRedactorEditorStoredXss284 {
  query pattern presenceNode where node.kind == "any" and node.path == "self.codes" and node.token contains ".redactor({" and node.token contains "autoresize: false"
  emit issue code.GleezRedactorEditorStoredXss at node
}
