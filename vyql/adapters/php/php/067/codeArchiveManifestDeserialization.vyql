module bindings.php.migration.mfbe84119;

binding archiveManifestDeserialization67 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "Tar" and node.token contains "extract(" and node.token contains "manifest" and node.token contains "file_get_contents" and node.token contains "unserialize" and not (node.token contains "allowed_classes")
  emit issue code.ArchiveManifestDeserialization at node
}
