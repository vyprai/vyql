module bindings.php.migration.mfbe84119;

binding unfilteredSearchWhereColumns198 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=scopeSearch" and node.token contains "array_intersect(array_keys($search),$this->searchable)" and node.token contains "return$query->where($search);" and not (node.token contains "array_intersect_key($search,array_flip($this->searchable))") and not (node.token contains "where($allowed_search)")
  emit issue code.UnfilteredSearchWhereColumns at node
}
