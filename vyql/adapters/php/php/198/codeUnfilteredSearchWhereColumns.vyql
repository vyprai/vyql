module bindings.php.migration.mfbe84119;

binding unfilteredSearchWhereColumns198 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.context.name contains "scopeSearch" and node.token contains "array_intersect(array_keys($search),$this->searchable)" and node.token contains "return$query->where($search);" and not (node.token contains "array_intersect_key($search,array_flip($this->searchable))") and not (node.token contains "where($allowed_search)")
  emit issue code.UnfilteredSearchWhereColumns at node
}
