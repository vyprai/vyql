module bindings.php.migration.mfbe84119;

binding workflowPolicyReview151 {
  query pattern presenceNode where node.kind == "any" and node.path == "__object_literal" and node.token contains "order.customerid" and node.token contains "order.product.attribute.id" and not (node.token contains "order.statuspayment")
  emit issue code.WorkflowPolicyReview at node
}
