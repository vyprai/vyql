module bindings.php.migration.mfbe84119;

binding freePbxContactManagerGroupLinkXss110 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=ajaxHandler" and node.token contains "$final[$i]['actions']" and node.token contains "group='.$_REQUEST['group'].'" and not (node.token contains "(int)$_REQUEST['group']")
  emit issue code.FreePbxContactManagerGroupLinkXss at node
}
