module bindings.php.migration.mfbe84119;

binding freePbxContactManagerGroupLinkXss110 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=ajaxHandler" and node.token contains "$final[$i]['actions']" and node.token contains "group='.$_REQUEST['group'].'" and not (node.token contains "(int)$_REQUEST['group']")
  emit issue code.FreePbxContactManagerGroupLinkXss at node
}
