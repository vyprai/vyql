module bindings.php.migration.mfbe84119;

binding oveleonCookiebarLocaleReflectedXss111 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=blockAction" and node.token contains "System::loadLanguageFile('tl_cookiebar',$locale)" and node.token contains "$objTemplate->language=$locale" and node.token contains "return$objTemplate->getResponse();" and not (node.token contains "Validator::isLocale($locale)")
  emit issue code.OveleonCookiebarLocaleReflectedXss at node
}
