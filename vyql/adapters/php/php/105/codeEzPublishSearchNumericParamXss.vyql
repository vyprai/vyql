module bindings.php.migration.mfbe84119;

binding ezPublishSearchNumericParamXss105 {
  query pattern presenceNode where node.scope == "module" and node.token contains "lang=php" and node.token contains "call_path:eZSearch.search" and node.token contains "call_path:$tpl.setVariable" and node.token contains "assign_call:$searchPageLimit:$http.variable" and node.token contains "assign_literal:$searchPageLimit:SearchPageLimit" and node.token contains "call_arg:$tpl.setVariable:$searchPageLimit" and not (node.token contains "cast_call_literal:int:$http.variable:SearchPageLimit")
  emit issue code.EzPublishSearchNumericParamXss at node
}
