module bindings.php.migration.mfbe84119;

binding phpWindowOpenerCallbackNameXss77 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "php" and node.context.callPath contains "echo" and containsAny(node.token, ["literal:window.opener.", "literal:typeofwindow.opener."]) and containsAny(node.token, ["assign:$func=$_GET", "assign:$func=@$_GET", "assign:$func=(isset($_GET['func']))?$_GET['func']"]) and not (node.context.assign contains "$func=var_out")
  emit issue code.PhpWindowOpenerCallbackNameXss at node
}
