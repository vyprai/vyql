module bindings.php.migration.mfbe84119;

binding phpWindowOpenerCallbackNameXss77 {
  query pattern presenceNode where node.scope == "module" and node.token contains "lang=php" and node.token contains "call_path:echo" and containsAny(node.token, ["literal:window.opener.", "literal:typeofwindow.opener."]) and containsAny(node.token, ["assign:$func=$_GET", "assign:$func=@$_GET", "assign:$func=(isset($_GET['func']))?$_GET['func']"]) and not (node.token contains "assign:$func=var_out")
  emit issue code.PhpWindowOpenerCallbackNameXss at node
}
