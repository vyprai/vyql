module bindings.php.migration.mfbe84119;

binding sensitiveModelJsonSerializationExposure94 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "class" and node.token contains "lang=php" and node.token contains "name=UpdateUserController" and node.token contains "CurrentUserSerializer::class" and node.token contains "call_path:$request.getQueryParams" and node.token contains "call_path:$request.getAttribute" and node.token contains "call_path:EditUser" and not (node.token contains "call_path:$this.serializer")
  emit issue code.SensitiveModelJsonSerializationExposure at node
}
