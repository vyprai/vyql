module bindings.php.migration.mfbe84119;

binding rawDatatablesAttributeXss145 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=php" and node.token contains "call_path:Datatables.of" and node.token contains ".rawColumns" and node.token contains "data-title" and node.token contains "attr_path:$tasks.title" and not (node.token contains "htmlspecialchars") and not (node.token contains "htmlentities") and not (node.token contains "e($tasks->title")
  emit issue code.RawDatatablesAttributeXss at node
}
