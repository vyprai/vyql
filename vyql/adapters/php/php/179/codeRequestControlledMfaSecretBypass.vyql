module bindings.php.migration.mfbe84119;

binding requestControlledMfaSecretBypass179 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=getAuthService" and node.token contains "MfaHelper" and node.token contains "$this->Session->get('mfa_secret')" and node.token contains "$this->Request->request->get('mfa_secret')" and not (node.token contains "newMfaHelper($this->Session->get('mfa_secret'))")
  emit issue code.RequestControlledMfaSecretBypass at node
}
