module bindings.php.migration.mfbe84119;

binding certValidationDisabled430 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "curl_setopt_array" and node.token contains "CURLOPT_SSL_VERIFYPEER=false"
  emit issue code.CertValidationDisabled at node
}
