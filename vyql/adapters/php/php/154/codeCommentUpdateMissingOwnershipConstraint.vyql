module bindings.php.migration.mfbe84119;

binding commentUpdateMissingOwnershipConstraint154 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.context.name contains "updateComment" and node.token contains "comment_comment" and node.token contains "comment_id" and node.token contains "getDb()->update" and not (node.token contains "comment_author_id") and not (node.token contains "USERID")
  emit issue code.CommentUpdateMissingOwnershipConstraint at node
}
