module bindings.php.migration.mfbe84119;

binding unsignedStorageDownloadRoute51 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "php" and node.context.callPath contains "Route.get" and node.context.callPath contains "Route.get.where" and node.context.callArg contains "Route.get.where:.*" and node.context.callPath contains "Storage.disk.path" and node.context.callPath contains "response.download" and node.context.callPath contains "response.download.deleteFileAfterSend" and containsAny(node.token, ["literal:{path}", "literal:{file}", "literal:{filename}"]) and not (node.context.callPath contains "Route.get.middleware") and not (containsAny(node.token, ["literal:signed", "literal:auth", "literal:can:"]))
  emit issue code.UnsignedStorageDownloadRoute at node
}
