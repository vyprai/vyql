module bindings.php.generated.mfbe84119;

binding phpNegativeOffsetStringSearchWarningDisclosure88 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.token contains "$_REQUEST" and node.token contains "strrpos($key,'_id',-3)" and not (node.token contains "strlen($key)>=3") and not (node.token contains "strlen($key)>2")
  emit issue code.PhpNegativeOffsetStringSearchWarningDisclosure at node
}
