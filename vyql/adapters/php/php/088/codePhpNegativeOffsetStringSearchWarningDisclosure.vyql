module bindings.php.migration.mfbe84119;

binding phpNegativeOffsetStringSearchWarningDisclosure88 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "$_REQUEST" and node.token contains "strrpos($key,'_id',-3)" and not (node.token contains "strlen($key)>=3") and not (node.token contains "strlen($key)>2")
  emit issue code.PhpNegativeOffsetStringSearchWarningDisclosure at node
}
