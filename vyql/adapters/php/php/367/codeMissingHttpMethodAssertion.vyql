module bindings.php.generated.mfbe84119;

binding missingHttpMethodAssertion367 {
  query pattern presenceNode where node.scope == "class" and node.token contains "Action" and node.token contains "PasswordReset::class" and node.token contains "->initiateReset" and not (node.token contains "$this->assertAllowedHttpMethod")
  emit issue code.MissingHttpMethodAssertion at node
}
