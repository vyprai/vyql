module bindings.php.migration.mfbe84119;

binding unrestrictedRequestParamDeserialization65 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "module" and node.token contains "lang=php" and node.token contains "param('params'" and node.token contains "unserialize($params" and not (node.token contains "substr") and not (node.token contains "a:")
  emit issue code.UnrestrictedRequestParamDeserialization at node
}
