module bindings.php.migration.mfbe84119;

binding stateChangingOp374 {
  query pattern callExpr where callee.path ~= "queryDB" and args.any.literal contains "replace into"
  emit sink code.StateChangingOpSink at args[0]
}
