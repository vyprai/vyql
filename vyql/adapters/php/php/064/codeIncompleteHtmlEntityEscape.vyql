module bindings.php.migration.mfbe84119;

binding incompleteHtmlEntityEscape64 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.token contains "$_POST" and node.token contains ".=strip_tags(" and not (node.token contains "htmlspecialchars(strip_tags(") and not (node.token contains "htmlentities(strip_tags(") and not (node.token contains "esc_html(strip_tags(")
  emit issue code.IncompleteHtmlEntityEscape at node
}
