module bindings.php.migration.mfbe84119;

binding incompleteHtmlEntityEscape64 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "$_POST" and node.token contains ".=strip_tags(" and not (node.token contains "htmlspecialchars(strip_tags(") and not (node.token contains "htmlentities(strip_tags(") and not (node.token contains "esc_html(strip_tags(")
  emit issue code.IncompleteHtmlEntityEscape at node
}
