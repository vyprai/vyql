module bindings.php.migration.mfbe84119;

binding csvFormulaProneExportRow325 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "getForCsvExport" and node.token contains "self::COLUMNS" and node.token contains "$row[self::COLUMNS][]=" and node.token contains "$rows[]=$row" and not (node.token contains "escapeCsvRecord") and not (node.token contains "esc_csv")
  emit issue code.CsvFormulaProneExportRow at node
}
