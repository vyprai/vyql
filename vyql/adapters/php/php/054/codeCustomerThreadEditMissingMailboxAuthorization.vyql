module bindings.php.migration.mfbe84119;

binding customerThreadEditMissingMailboxAuthorization54 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=edit" and node.token contains "param_type:User" and node.token contains "param_type:Thread" and node.token contains "attr_path:$thread.created_by_customer_id" and node.token contains "attr_path:$thread.type" and node.token contains "call_path:in_array" and node.token contains "call_arg:Thread::TYPE_CUSTOMER" and node.token contains "returntrue;" and not (node.token contains "call_path:userHasAccessToMailbox")
  emit issue code.CustomerThreadEditMissingMailboxAuthorization at node
}
