module bindings.php.migration.mfbe84119;

binding templatePathComponentTraversal139 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.token contains "templates/{$type}/{$language}" and node.token contains "{$entityType}" and node.token contains "{$name}.tpl" and not (node.token contains "basename($entityType)")
  emit issue code.TemplatePathComponentTraversal at node
}
