module bindings.php.migration.mfbe84119;

binding preservedRequestHiddenInputValueXss144 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=modifyForm" and node.token contains "attr_path:$this.parentPlugin.piVars" and node.token contains "assign_literal:$hiddenFields[]:<inputtype=\"hidden\"name=" and node.token contains "assign_literal:$hiddenFields[]:value=" and node.token contains "subscript:$markers['hidden_parameter_fields']" and node.token contains "call:implode" and not (containsAny(node.scopeCall.any, [cleanFormValue, htmlspecialchars, htmlentities, esc_attr, esc_html, strip_tags, removeXSS, filter_var]))
  emit issue code.PreservedRequestHiddenInputValueXss at node
}
