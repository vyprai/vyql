module bindings.php.migration.mfbe84119;

binding preservedRequestHiddenInputValueXss144 {
  query pattern presenceNode where node.scope == "function" and node.context.language == "php" and node.context.name contains "modifyForm" and node.context.attrPath contains "$this.parentPlugin.piVars" and node.token contains "assign_literal:$hiddenFields[]:<inputtype=\"hidden\"name=" and node.token contains "assign_literal:$hiddenFields[]:value=" and node.context.subscript contains "$markers['hidden_parameter_fields']" and node.context.call contains "implode" and not (containsAny(node.scopeCall.any, [cleanFormValue, htmlspecialchars, htmlentities, esc_attr, esc_html, strip_tags, removeXSS, filter_var]))
  emit issue code.PreservedRequestHiddenInputValueXss at node
}
