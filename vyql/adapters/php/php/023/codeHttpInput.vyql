module bindings.php.migration.mfbe84119;

binding httpInput23 {
  query pattern callExpr where callee.path ~= "$_SERVER" and args.any.literal contains "REMOTE_ADDR"
  emit source code.HttpInput at call.result
}
