module bindings.php.migration.mfbe84119;

binding cockpitLimeRedirectStopContinuation122 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=stop" and node.token contains "$this->request->stopped=true" and not (node.token contains "app:request:stop")
  emit issue code.CockpitLimeRedirectStopContinuation at node
}
