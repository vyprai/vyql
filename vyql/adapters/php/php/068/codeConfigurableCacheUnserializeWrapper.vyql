module bindings.php.migration.mfbe84119;

binding configurableCacheUnserializeWrapper68 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=unserialize" and node.token contains "param_type:string" and node.token contains "call_path:is_numeric" and node.token contains "call_path:$unserialize"
  emit issue code.ConfigurableCacheUnserializeWrapper at node
}
