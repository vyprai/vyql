module bindings.php.migration.mfbe84119;

binding configurableCacheUnserializeWrapper68 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=php" and node.token contains "name=unserialize" and node.token contains "param_type:string" and node.token contains "call_path:is_numeric" and node.token contains "call_path:$unserialize"
  emit issue code.ConfigurableCacheUnserializeWrapper at node
}
