module bindings.php.migration.mfbe84119;

binding requestBackedFooterScriptXss277 {
  query pattern presenceNode where node.scope == "module" and node.context.language == "php" and containsAny(node.context.subscript, ["$_REQUEST[\"footerScripts\"]", "$_REQUEST['footerScripts']"]) and containsAny(node.context.callPath, ["print", "echo"]) and not (containsAny(node.context.subscript, ["$GLOBALS[\"footerScripts\"]", "$GLOBALS['footerScripts']"]))
  emit issue code.RequestBackedFooterScriptXss at node
}
