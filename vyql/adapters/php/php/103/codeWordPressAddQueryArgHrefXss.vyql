module bindings.php.migration.mfbe84119;

binding wordPressAddQueryArgHrefXss103 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=php" and node.token contains "sprintf(" and node.token contains "<ahref=\"%s\"" and node.token contains "add_query_arg(" and not (node.token contains "esc_url(add_query_arg(") and not (node.token contains "esc_attr(add_query_arg(") and not (node.token contains "htmlspecialchars(add_query_arg(")
  emit issue code.WordPressAddQueryArgHrefXss at node
}
