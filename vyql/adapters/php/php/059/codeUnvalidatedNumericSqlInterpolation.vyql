module bindings.php.migration.mfbe84119;

binding unvalidatedNumericSqlInterpolation59 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "class" and node.token contains "lang=php" and node.token contains "getMessageParam('iid'" and node.token contains "item_id=$this->iid" and node.token contains "db_query(" and not (node.token contains "is_numeric($this->iid)")
  emit issue code.UnvalidatedNumericSqlInterpolation at node
}
