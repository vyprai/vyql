module bindings.kotlin.migration.m304d1620;

binding xmlHardening92 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "setProperty" and node.token contains "ACCESS_EXTERNAL_DTD"
  emit check core.XmlHardening at node {
    advisory: true
  }
}
