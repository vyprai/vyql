module bindings.kotlin.migration.m304d1620;

binding insecurePermission115 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "Files.setPosixFilePermissions" and node.token contains "OTHERS_WRITE"
  emit issue code.InsecurePermission at node
}
