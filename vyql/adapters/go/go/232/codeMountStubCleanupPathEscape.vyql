module bindings.go.migration.m48a985c1;

binding mountStubCleanupPathEscape232 {
  query pattern presenceNode where node.scope == "function" and node.token contains "call_path:os.Lstat" and node.token contains "call_path:filepath.Dir" and node.token contains "call_path:os.Remove" and node.token contains "call_path:os.Chtimes" and not (node.token contains "call_path:fs.RootPath")
  emit issue code.MountStubCleanupPathEscape at node
}
