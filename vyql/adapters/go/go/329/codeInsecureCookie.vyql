module bindings.go.migration.m48a985c1;

binding insecureCookie329 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "ctx.SetCookie" and node.token contains "GenerateToken" and node.token contains "opt.CookieHttpOnly" and node.token contains "false" and not (node.token contains "opt.Secure")
  emit issue code.InsecureCookie at node
}
