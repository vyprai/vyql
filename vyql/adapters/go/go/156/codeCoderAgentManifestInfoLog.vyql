module bindings.go.migration.m48a985c1;

binding coderAgentManifestInfoLog156 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path == "a.logger.Info" and node.token contains "fetched manifest" and node.token contains "slog.F" and node.token contains "manifest"
  emit issue code.CoderAgentManifestInfoLog at node
}
