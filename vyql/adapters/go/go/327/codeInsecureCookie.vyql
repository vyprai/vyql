module bindings.go.migration.m48a985c1;

binding insecureCookie327 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "http.Cookie" and node.token contains "HttpOnly=false"
  emit issue code.InsecureCookie at node
}
