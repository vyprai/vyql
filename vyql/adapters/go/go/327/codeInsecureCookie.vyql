module bindings.go.migration.m48a985c1;

binding insecureCookie327 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "http.Cookie" and node.token contains "HttpOnly=false"
  emit issue code.InsecureCookie at node
}
