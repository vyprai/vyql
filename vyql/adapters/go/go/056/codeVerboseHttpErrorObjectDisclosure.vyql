module bindings.go.migration.m48a985c1;

binding verboseHttpErrorObjectDisclosure56 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=go" and node.token contains "call_path:fmt.Errorf" and node.token contains "literal:%+v" and node.token contains "selector:.Request" and node.token contains "selector:.Body" and node.token contains "selector:.TLS"
  emit issue code.VerboseHttpErrorObjectDisclosure at node
}
