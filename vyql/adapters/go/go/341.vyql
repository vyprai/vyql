module bindings.go.migration.m48a985c1;

binding binding341 {
  query pattern callExpr where callee.method == "ReplaceAllString" and call.filter.global == true
  emit check threat.CharFilter at call {
    covers path {
      from: call
      to: call
    }
  }
}
