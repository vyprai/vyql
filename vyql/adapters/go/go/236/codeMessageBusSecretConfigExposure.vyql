module bindings.go.migration.m48a985c1;

binding messageBusSecretConfigExposure236 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "call_path:bootstrapMessaging.SetOptionsAuthData" and node.token contains "call_path:messaging.NewMessageClient" and node.token contains "field:Optional" and node.token contains "selector:messageBusInfo.Optional" and not (node.token contains "selector:bootstrapMessaging.OptionsPasswordKey")
  emit issue code.MessageBusSecretConfigExposure at node
}
