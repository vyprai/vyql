module bindings.go.migration.m48a985c1;

binding leadingDotSegmentOnlyRouteFilter147 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path == "analysis.function.context" and node.token contains "call_path:strings.HasPrefix" and node.token contains "literal:/." and node.token contains "selector:ctx.R.URL.Path" and not (node.token contains "call_path:strings.Contains")
  emit issue code.LeadingDotSegmentOnlyRouteFilter at node
}
