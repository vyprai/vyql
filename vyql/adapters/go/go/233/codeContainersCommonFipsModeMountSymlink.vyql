module bindings.go.migration.m48a985c1;

binding containersCommonFipsModeMountSymlink233 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "analysis.function.context" and node.token contains "function_name:addFIPSModeSubscription" and node.token contains "call_path:filepath.Join" and node.token contains "call_path:fileutils.Exists" and not (node.token contains "call_path:securejoin.SecureJoin")
  emit issue code.ContainersCommonFipsModeMountSymlink at node
}
