module bindings.go.migration.m48a985c1;

binding proxyClientIpSpoofingBypass249 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "call_path:g.management.GetProxy" and node.token contains "call_path:proxy.ServeHTTP" and not (node.token contains "call_path:rewriteRequestSourceIP")
  emit issue code.ProxyClientIpSpoofingBypass at node
}
