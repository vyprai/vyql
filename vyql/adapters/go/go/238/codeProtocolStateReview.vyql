module bindings.go.migration.m48a985c1;

binding protocolStateReview238 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "call:StopT3550" and not (node.token contains "selector:ue.T3550")
  emit issue code.ProtocolStateReview at node
}
