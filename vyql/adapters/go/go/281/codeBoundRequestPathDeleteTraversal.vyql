module bindings.go.migration.m48a985c1;

binding boundRequestPathDeleteTraversal281 {
  query pattern presenceNode where node.scope == "function" and node.token contains "function_name:Delete" and node.token contains "call_path:self.Validate" and node.token contains "selector:params.Path" and node.token contains "call_path:GetSaveRealPath" and node.token contains "call_path:os.Remove" and not (node.token contains "call_path:filepath.IsLocal") and not (node.token contains "call_path:filepath.Clean")
  emit issue code.BoundRequestPathDeleteTraversal at node
}
