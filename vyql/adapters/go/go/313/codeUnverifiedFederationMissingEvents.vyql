module bindings.go.migration.m48a985c1;

binding unverifiedFederationMissingEvents313 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "call_path:missingResp.Events.UntrustedEvents" and node.token contains "call_path:t.cacheAndReturn" and not (node.token contains "call_path:ev.VerifyEventSignatures")
  emit issue code.UnverifiedFederationMissingEvents at node
}
