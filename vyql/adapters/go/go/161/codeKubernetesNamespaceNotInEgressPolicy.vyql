module bindings.go.migration.m48a985c1;

binding kubernetesNamespaceNotInEgressPolicy161 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=go" and node.token contains "call_path:netwv1.NewNetworkPolicy" and node.token contains "call_path:netwv1.NetworkPolicyEgressRuleArgs" and node.token contains "call_path:netwv1.NetworkPolicyPeerArgs" and node.token contains "call_path:metav1.LabelSelectorRequirementArgs" and node.token contains "literal:Egress" and node.token contains "literal:kubernetes.io/metadata.name" and node.token contains "literal:NotIn" and containsAny(node.token, ["call_path:Metadata.Name.Elem", "call_path:Namespace.Name.Elem"])
  emit issue code.KubernetesNamespaceNotInEgressPolicy at node
}
