module bindings.go.migration.m48a985c1;

binding underwidthFormattedIndex282 {
  query pattern presenceNode where node.scope == "function" and node.token contains "call_path:fmt.Sprintf" and node.token contains "literal:%08b" and node.token contains "index:capabilities:8" and not (node.token contains "literal:%016b")
  emit issue code.UnderwidthFormattedIndex at node
}
