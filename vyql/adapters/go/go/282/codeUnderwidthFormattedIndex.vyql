module bindings.go.migration.m48a985c1;

binding underwidthFormattedIndex282 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "call_path:fmt.Sprintf" and node.token contains "literal:%08b" and node.token contains "index:capabilities:8" and not (node.token contains "literal:%016b")
  emit issue code.UnderwidthFormattedIndex at node
}
