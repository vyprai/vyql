module bindings.go.migration.m48a985c1;

binding certValidationDisabled308 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "tls.Config" and node.token contains "InsecureSkipVerify=true"
  emit issue code.CertValidationDisabled at node
}
