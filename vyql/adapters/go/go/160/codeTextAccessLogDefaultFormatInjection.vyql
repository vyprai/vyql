module bindings.go.migration.m48a985c1;

binding textAccessLogDefaultFormatInjection160 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "call" and node.path ~= "cfgcore.DataSource_InlineString" and operand(node, where: operand.identifier contains "EnvoyTextLogFormat")
  emit issue code.TextAccessLogDefaultFormatInjection at node
}
