module bindings.go.migration.m48a985c1;

binding shareInfoDirectDownloadTokenExposure55 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "lang=go" and node.token contains "function_name:shareInfoHandler" and node.token contains "call_path:store.Share.GetByHash" and node.token contains "selector:commonShare.DownloadURL" and node.token contains "selector:shareLink.Token" and node.token contains "call_arg:getShareURL:true" and node.token contains "call_arg:getShareURL:shareLink.Token" and node.token contains "call_path:renderJSON"
  emit issue code.ShareInfoDirectDownloadTokenExposure at node
}
