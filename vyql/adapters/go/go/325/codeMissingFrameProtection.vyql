module bindings.go.migration.m48a985c1;

binding missingFrameProtection325 {
  query pattern callExpr where callee.path ~= "Header.Set" and args.any.literal contains "X-Frame-Options" and args.any.literal contains "ALLOWALL"
  emit issue code.MissingFrameProtection at call
}
