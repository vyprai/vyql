module bindings.go.migration.m48a985c1;

binding unqualifiedDatabaseRoutineReview289 {
  query pattern presenceNode where node.scope == "function" and node.context.literal contains "SELECT count(1) FROM aurora_global_db_status()" and not (node.token contains "pg_catalog.aurora_global_db_status")
  emit issue code.UnqualifiedDatabaseRoutineReview at node
}
