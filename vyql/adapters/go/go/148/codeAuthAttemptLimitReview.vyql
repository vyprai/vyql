module bindings.go.migration.m48a985c1;

binding authAttemptLimitReview148 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "SolveChallenge" and node.context.selector contains "challenge.tries" and node.context.selector contains "s.challenges"
  emit issue code.AuthAttemptLimitReview at node
}
