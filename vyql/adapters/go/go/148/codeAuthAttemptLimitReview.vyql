module bindings.go.migration.m48a985c1;

binding authAttemptLimitReview148 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "function_name:SolveChallenge" and node.token contains "selector:challenge.tries" and node.token contains "selector:s.challenges"
  emit issue code.AuthAttemptLimitReview at node
}
