module bindings.go.migration.m48a985c1;

binding authAttemptLimitReview148 {
  query pattern presenceNode where node.scope == "function" and node.token contains "function_name:SolveChallenge" and node.token contains "selector:challenge.tries" and node.token contains "selector:s.challenges"
  emit issue code.AuthAttemptLimitReview at node
}
