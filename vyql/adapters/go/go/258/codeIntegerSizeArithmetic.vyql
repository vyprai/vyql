module bindings.go.migration.m48a985c1;

binding integerSizeArithmetic258 {
  query pattern presenceNode where node.scope == "function" and node.token contains "function_name:readBytes" and node.token contains "call:ReadLong" and node.token contains "call:int" and node.token contains "call:make" and not (node.token contains "selector:math.MaxInt")
  emit issue code.IntegerSizeArithmetic at node
  emit sink code.IntegerSizeArithmeticSink at node
}
