module bindings.go.migration.m48a985c1;

binding commandAllowlistBeforeParsing295 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=go" and node.token contains "call_path:strings.Split" and node.token contains "call_path:d.user.CanExecute" and node.token contains "call_path:runner.ParseCommand" and node.token contains "call_path:exec.Command" and node.token contains "call_before:d.user.CanExecute>runner.ParseCommand"
  emit issue code.CommandAllowlistBeforeParsing at node
}
