module bindings.go.migration.m48a985c1;

binding unparameterizedSqlQueryParser293 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "call_path:impl.dbConnection.Query" and node.token contains "literal:SELECT " and node.token contains "literal: and " and node.token contains "='" and not (node.token contains "queryParams")
  emit issue code.UnparameterizedSqlQueryParser at node
}
