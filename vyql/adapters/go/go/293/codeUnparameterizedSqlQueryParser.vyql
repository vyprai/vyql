module bindings.go.migration.m48a985c1;

binding unparameterizedSqlQueryParser293 {
  query pattern presenceNode where node.scope == "function" and node.token contains "call_path:impl.dbConnection.Query" and node.token contains "literal:SELECT " and node.token contains "literal: and " and node.token contains "='" and not (node.token contains "queryParams")
  emit issue code.UnparameterizedSqlQueryParser at node
}
