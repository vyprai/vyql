module bindings.go.migration.m48a985c1;

binding unparameterizedSqlQueryParser292 {
  query pattern presenceNode where node.kind == "any" and node.path == "goqu.L" and node.token contains "group"
  emit issue code.UnparameterizedSqlQueryParser at node
}
