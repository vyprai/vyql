module bindings.go.migration.m48a985c1;

binding chGoCompressedSizeUint32Overflow263 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "function_name:Compress" and node.token contains "call_path:binary.LittleEndian.PutUint32" and node.token contains "call:uint32" and not (node.token contains "selector:math.MaxUint32") and not (node.token contains "literal:compressed size overflows uint32")
  emit issue code.ChGoCompressedSizeUint32Overflow at node
}
