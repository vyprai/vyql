module bindings.go.migration.m48a985c1;

binding apiScanFoundActionCommandExecution296 {
  query pattern presenceNode where node.scope == "function" and node.token contains "function_name:postScanHandler" and node.token contains "call_path:c.Bind" and node.token contains "selector:rq.Options" and node.token contains "call_path:ScanFromAPI" and not (node.token contains "call_path:sanitizeAPIScanOptions")
  emit issue code.ApiScanFoundActionCommandExecution at node
}
