module bindings.go.migration.m48a985c1;

binding charmRouteFileStorePathAccess278 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "function_name:handlePostFile" and node.token contains "call_path:pattern.Path" and node.token contains "call_path:s.cfg.FileStore.Put" and not (node.token contains "call_path:filepath.Clean")
  emit issue code.CharmRouteFileStorePathAccess at node
}
