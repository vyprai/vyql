module bindings.go.migration.m48a985c1;

binding integerSizeArithmetic262 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "function_name:readBlock" and node.token contains "call:ReadLong" and node.token contains "call:make" and not (node.token contains "selector:math.MaxInt")
  emit issue code.IntegerSizeArithmetic at node
  emit sink code.IntegerSizeArithmeticSink at node
}
