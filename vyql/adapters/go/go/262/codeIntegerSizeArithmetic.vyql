module bindings.go.generated.m48a985c1;

binding integerSizeArithmetic262 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "readBlock" and node.context.call contains "ReadLong" and node.context.call contains "make" and not (node.context.selector contains "math.MaxInt")
  emit issue code.IntegerSizeArithmetic at node
  emit sink code.IntegerSizeArithmeticSink at node
}
