module bindings.go.migration.m48a985c1;

binding lengthDerivedAllocation266 {
  query pattern presenceNode where node.scope == "function" and node.token contains "function_name:readNBytes" and node.token contains "call:make" and node.token contains "call:ReadByte" and not (node.token contains "selector:reader.data") and not (node.token contains "selector:reader.pos")
  emit issue code.LengthDerivedAllocation at node
}
