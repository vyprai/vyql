module bindings.go.migration.m48a985c1;

binding containerdCriImageEnvAliasLeak230 {
  query pattern presenceNode where node.scope == "function" and node.token contains "assign:env=imageConfig.Env" and node.token contains "call_path:config.GetEnvs" and node.token contains "call_path:oci.WithEnv" and not (node.token contains "append_copy:imageConfig.Env")
  emit issue code.ContainerdCriImageEnvAliasLeak at node
}
