module bindings.go.migration.m48a985c1;

binding containerdCriImageEnvAliasLeak230 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "assign:env=imageConfig.Env" and node.token contains "call_path:config.GetEnvs" and node.token contains "call_path:oci.WithEnv" and not (node.token contains "append_copy:imageConfig.Env")
  emit issue code.ContainerdCriImageEnvAliasLeak at node
}
