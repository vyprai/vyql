module bindings.go.migration.m48a985c1;

binding constellationLuks2AttachedHeaderActivation216 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "function_name:initByName" and node.token contains "call_path:cryptsetup.InitByName" and not (node.token contains "call_path:detachHeader") and not (node.token contains "call_path:cryptsetup.InitByNameAndHeader")
  emit issue code.ConstellationLuks2AttachedHeaderActivation at node
}
