module bindings.go.migration.m48a985c1;

binding insecureCookie328 {
  query pattern presenceNode where node.kind == "any" and node.path ~= "http.Cookie" and node.token contains "Secure=false"
  emit issue code.InsecureCookie at node
}
