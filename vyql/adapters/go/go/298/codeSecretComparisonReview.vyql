module bindings.go.migration.m48a985c1;

binding secretComparisonReview298 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "function_name:ParseAndValidate" and node.token contains "selector:r.Header.Get" and node.token contains "call:string" and not (node.scopeCall.any contains "ConstantTimeCompare")
  emit issue code.SecretComparisonReview at node
}
