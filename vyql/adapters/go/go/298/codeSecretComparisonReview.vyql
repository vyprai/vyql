module bindings.go.migration.m48a985c1;

binding secretComparisonReview298 {
  query pattern presenceNode where node.scope == "function" and node.token contains "function_name:ParseAndValidate" and node.token contains "selector:r.Header.Get" and node.token contains "call:string" and not (node.scopeCall.any contains "ConstantTimeCompare")
  emit issue code.SecretComparisonReview at node
}
