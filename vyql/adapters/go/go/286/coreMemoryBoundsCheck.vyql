module bindings.go.migration.m48a985c1;

binding memoryBoundsCheck286 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "__binop.ne" and node.token contains "Count" and node.token contains "0"
  emit check core.MemoryBoundsCheck at node {
    advisory: true
  }
}
