module bindings.go.migration.m48a985c1;

binding ciliumBroadTcFilterCleanup276 {
  query pattern presenceNode where node.scope == "function" and node.token contains "function_name:getTCFilters" and node.token contains "selector:bpfFilter.Name" and node.token contains "call_path:strings.Contains" and node.token contains "literal:cilium" and not (node.token contains "literal:cil_")
  emit issue code.CiliumBroadTcFilterCleanup at node
}
