module bindings.go.migration.m48a985c1;

binding ciliumBroadTcFilterCleanup276 {
  query pattern presenceNode where node.scope == "function" and node.context.functionName contains "getTCFilters" and node.context.selector contains "bpfFilter.Name" and node.context.callPath contains "strings.Contains" and node.context.literal contains "cilium" and not (node.context.literal contains "cil_")
  emit issue code.CiliumBroadTcFilterCleanup at node
}
