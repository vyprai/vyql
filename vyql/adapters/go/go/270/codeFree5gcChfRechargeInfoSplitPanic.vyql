module bindings.go.migration.m48a985c1;

binding free5gcChfRechargeInfoSplitPanic270 {
  query pattern presenceNode where node.scope == "function" and node.token contains "function_name:RechargePut" and node.token contains "call_path:strings.Split" and node.token contains "literal:rechargingInfo" and node.token contains "index:ueIdRatingGroup:1" and not (node.scopeCall.any contains "len") and not (node.token contains "call_path:c.Query")
  emit issue code.Free5gcChfRechargeInfoSplitPanic at node
}
