module bindings.go.migration.m48a985c1;

binding unvalidatedRouteConfigKeyStoredXss250 {
  query pattern presenceNode where node.scope == "function" and node.token contains "call_path:mux.Vars" and node.token contains "call_path:meta.AddConnectionConfKey" and node.token contains "call_path:json.Unmarshal" and not (node.token contains "call_path:validate.ValidateID") and not (node.token contains "call_path:ValidateID")
  emit issue code.UnvalidatedRouteConfigKeyStoredXss at node
}
