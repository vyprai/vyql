module bindings.go.migration.m48a985c1;

binding gouniverseCmsAliasXss125 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "function_name:PageRenderHtmlByAlias" and node.token contains "param_type:http.Request" and node.token contains "call_path:cms.PageFindByAlias" and node.token contains "literal:Page with alias '" and not (node.scopeCall.any contains "ToHTML")
  emit issue code.GouniverseCmsAliasXss at node
}
