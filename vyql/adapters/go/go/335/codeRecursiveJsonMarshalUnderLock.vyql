module bindings.go.migration.m48a985c1;

binding recursiveJsonMarshalUnderLock335 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "function_name:MarshalJSON" and node.token contains "call_path:ps.mtx.Lock" and node.token contains "call_path:cmtjson.Marshal" and node.token contains "selector:ps" and not (node.token contains "jsonPeerState") and not (node.token contains "call_path:ps.Count") and not (node.token contains "call_path:ps.Total")
  emit issue code.RecursiveJsonMarshalUnderLock at node
}
