module bindings.go.migration.m48a985c1;

binding lengthDerivedAllocation267 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "selector:r.ContentLength" and node.token contains "call:make" and node.token contains "call_path:io.ReadFull" and not (node.token contains "call_path:io.LimitReader") and not (node.token contains "call_path:http.MaxBytesReader")
  emit issue code.LengthDerivedAllocation at node
}
