module bindings.go.migration.m48a985c1;

binding locklessSharedMutation336 {
  query pattern presenceNode where node.kind == "any" and node.path == "analysis.global.mutation" and node.token contains "field:ctx"
  emit issue code.LocklessSharedMutation at node
}
