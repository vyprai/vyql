module bindings.go.migration.m48a985c1;

binding blockingReadInAcceptLoop256 {
  query pattern presenceNode where node.scope == "function" and node.token contains "call:Accept" and node.token contains "call:ReadTCP" and node.token contains "call:serve"
  emit issue code.BlockingReadInAcceptLoop at node
}
