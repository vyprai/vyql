module bindings.go.migration.m48a985c1;

binding trustBoundary118 {
  query pattern callExpr where callee.method == "Set" and callee.receiver.type == "session.Store"
  emit sink code.TrustBoundary at args[1]
}
