module bindings.go.migration.m48a985c1;

binding unparameterizedSqlQueryParser291 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path == "goqu.L" and node.token contains "project"
  emit issue code.UnparameterizedSqlQueryParser at node
}
