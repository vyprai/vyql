module bindings.go.migration.m48a985c1;

binding offsetLengthBoundsOverflow265 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "call:int" and node.token contains "call:len" and node.token contains "call_arg:int:header.TypeOff+header.TypeLen" and node.token contains "call_arg:len:btf" and node.token contains "slice:btf:header.TypeOff:" and node.token contains "slice:btf::header.TypeOff+header.TypeLen" and not (node.scopeCall.any contains "uint64") and not (node.token contains "call_arg:uint64:header.TypeOff")
  emit issue code.OffsetLengthBoundsOverflow at node
}
