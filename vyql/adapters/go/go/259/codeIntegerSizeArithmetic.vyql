module bindings.go.migration.m48a985c1;

binding integerSizeArithmetic259 {
  query pattern presenceNode where node.scope == "function" and node.token contains "call:ReadBlockHeader" and node.token contains "call:int" and node.token contains "call:getMaxSliceAllocSize"
  emit issue code.IntegerSizeArithmetic at node
  emit sink code.IntegerSizeArithmeticSink at node
}
