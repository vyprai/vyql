module bindings.go.migration.m48a985c1;

binding kubernetesCurlAuthorizationHeaderLogLeak157 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "function_name:toCurl" and node.token contains "selector:r.RequestHeaders" and node.token contains "call_path:fmt.Sprintf" and node.token contains "literal:%s: %s" and not (node.scopeCall.any contains "maskValue")
  emit issue code.KubernetesCurlAuthorizationHeaderLogLeak at node
}
