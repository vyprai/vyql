module bindings.go.migration.m48a985c1;

binding safeResponseContentType171 {
  query pattern callExpr where callee.path ~= "ctx.Header" and args.any.literal contains "content-type" and args.any.literal contains "image/"
  emit check core.SafeResponseContentType at call {
    covers endpoint {
      anchor: call
    }
  }
}
