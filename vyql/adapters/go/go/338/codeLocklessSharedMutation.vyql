module bindings.go.migration.m48a985c1;

binding locklessSharedMutation338 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "function_name:ReplaceIndividualSubcription" and node.token contains "call:GetSubscription" and node.token contains "index:bsfContext.BsfSelf.Subscriptions:subId" and node.token contains "call:Status" and node.token contains "selector:http.StatusNoContent" and not (node.token contains "selector:http.StatusNotFound")
  emit issue code.LocklessSharedMutation at node
}
