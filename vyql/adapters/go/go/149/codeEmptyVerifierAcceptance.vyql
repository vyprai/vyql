module bindings.go.migration.m48a985c1;

binding emptyVerifierAcceptance149 {
  query pattern presenceNode where node.scope == "function" and node.token contains "function_name:Verify" and node.token contains "call:Get" and node.token contains "binary:v==answer" and not (node.token contains "binary:v!=\"\"")
  emit issue code.EmptyVerifierAcceptance at node
}
