module bindings.go.migration.m48a985c1;

binding unboundedDelimiterSplitCount271 {
  query pattern presenceNode where node.scope == "function" and node.token contains "lang=go" and node.token contains "call_path:strings.Split" and node.token contains "literal:." and node.token contains "call_path:len" and node.token contains "index:parts:1" and node.token contains "call_path:base64.RawURLEncoding.DecodeString" and not (node.token contains "call_path:strings.Count") and not (node.token contains "call_path:strings.SplitN")
  emit issue code.UnboundedDelimiterSplitCount at node
}
