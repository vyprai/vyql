module bindings.go.migration.m48a985c1;

binding documizeMarkdownRenderWithoutSanitizer126 {
  query pattern presenceNode where node.scope == "function" and node.token contains "function_name:Render" and node.token contains "call_path:blackfriday.Run" and node.token contains "call:string" and not (node.token contains "call_path:bluemonday.UGCPolicy") and not (node.scopeCall.any contains "SanitizeBytes")
  emit issue code.DocumizeMarkdownRenderWithoutSanitizer at node
}
