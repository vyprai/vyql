module bindings.go.migration.m48a985c1;

binding fieldDerivedIndexAccess268 {
  query pattern presenceNode where node.scope == "function" and node.token contains "call:ReadString" and node.token contains "call:BytesNtohs" and node.token contains "binary:len(buf)<=off" and not (node.token contains "binary:len(buf)<off+2")
  emit issue code.FieldDerivedIndexAccess at node
}
