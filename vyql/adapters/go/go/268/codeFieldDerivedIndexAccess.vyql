module bindings.go.migration.m48a985c1;

binding fieldDerivedIndexAccess268 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "call:ReadString" and node.token contains "call:BytesNtohs" and node.token contains "binary:len(buf)<=off" and not (node.token contains "binary:len(buf)<off+2")
  emit issue code.FieldDerivedIndexAccess at node
}
