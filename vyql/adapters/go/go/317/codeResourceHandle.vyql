module bindings.go.migration.m48a985c1;

binding resourceHandle317 {
  query pattern presenceNode where node.path ~= "net.DialTimeout"
  emit issue code.ResourceHandle at node
  emit sink code.ResourceHandleSink at node
}
