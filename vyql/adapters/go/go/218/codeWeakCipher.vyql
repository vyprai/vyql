module bindings.go.migration.m48a985c1;

binding weakCipher218 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.kind == "any" and node.path ~= "des.NewTripleDESCipher"
  emit issue code.WeakCipher at node
}
