module bindings.go.migration.m48a985c1;

binding namespacedSubjectKeyCollision246 {
  query pattern presenceNode where node.scope == "function" and node.token contains "function_name:OwnerRoleBindingsIndexFunc" and node.token contains "selector:rb.Subjects" and node.token contains "selector:subject.Kind" and node.token contains "selector:subject.Name" and node.token contains "call_path:fmt.Sprintf" and not (node.token contains "selector:subject.Namespace") and not (node.token contains "call_path:strings.Join")
  emit issue code.NamespacedSubjectKeyCollision at node
}
