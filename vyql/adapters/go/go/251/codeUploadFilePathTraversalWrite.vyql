module bindings.go.migration.m48a985c1;

binding uploadFilePathTraversalWrite251 {
  unstable: {
    owner: "migration"
    reason: "v1 flag predicates depend on legacy token/path matching not yet promoted to stable v2 facts"
  }
  query unstable.legacyFlag as node where node.scope == "function" and node.token contains "function_name:UploadFileSafe" and node.token contains "call_path:uploadFile" and not (node.token contains "call_path:strings.Contains")
  emit issue code.UploadFilePathTraversalWrite at node
}
