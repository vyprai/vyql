module bindings.auditreview.migration.mbf0d19ec;

binding sensitiveOperation13 {
  query pattern presenceNode where node.kind == "any" and node.method == "update" and node.token contains "password"
  emit issue code.SensitiveOperation at node
}
